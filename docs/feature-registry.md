# Feature Number Registry

Date: 2026-06-22

The canonical feature number registry now lives in
`docs/audio-capture-backlog.md`.

Keep all capture, recording, upload, transcription, and media-revision feature
number allocations in that file so there is one source of truth. This file is
kept only as a compatibility pointer for older links and must not grow a
separate allocation table.

Compatibility note: feature `038-apple-voice-processing-spike` now has the
recorded outcome `defer_to_webrtc_aec3`; see `docs/audio-capture-backlog.md`
for the canonical registry entry and next-step state.
