# v2026.07.24.6 — надёжный жизненный цикл новых итогов

## Что изменилось

- Варианты итогов привязаны к неизменяемой версии расшифровки и результата.
- Текущие итоги остаются опубликованными до явного принятия нового варианта.
- Владелец видит безопасный предпросмотр, источник и понятные действия для
  принятия, отклонения, повтора и обновления.
- Повторные запросы, сбои Temporal/провайдера, устаревшие источники и истёкшие
  варианты обрабатываются повторяемо и не создают лишнюю генерацию.
- Экспорт и предпросмотр не смешивают старый принятый набор с новой версией
  расшифровки; удаление и гонки публикации закрываются серверными блокировками.

## Проверка

- `infra/scripts/ci-local.sh`: macOS **625 passed**, сервер **2415 passed,
  1 skipped**, strict lane **41 passed, 1 skipped**; lint, compile, OpenAPI,
  Compose и metadata-only evidence scan — PASS. Коллекция сервера: 2458,
  digest `4884c64dd789e021944b1b88eaf23db32c1891b67bb886d52fdabeb8fe7b7edb`.
- Регрессия maintenance rollback-expiry: **36 passed, 2 warnings** в
  focused deletion/upload/local-purge наборе; тест воспроизводит rollback
  первого объекта и проверяет повторную загрузку следующей встречи.
- Focused PostgreSQL lifecycle set: **103 passed**; candidate/outcome/export/
  template/static set: **122 passed**; Temporal correction set: **14 passed**.
- Production-head migration regression: **1 passed**; it verifies that
  `0037_auth_rate_limit_buckets → 0040_merge_content_regen_share` creates the
  content-lifecycle tables/columns and keeps both maintenance operations,
  including `prompt_optimization`.
- Production RLS live probe не выполнялся: локальный CI корректно отметил
  `postgres_test_database_required`; это не является заявлением о live RLS.

## Совместимость и миграции

- Добавлена merge-миграция `0040_merge_content_regeneration_and_share_heads`;
  она сводит Feature 125 auth/share и Feature 124 lifecycle-ветки. Проверено
  повышение существующей production-схемы с `0037_auth_rate_limit_buckets` до
  `0040_merge_content_regen_share`; Alembic применяет sibling-ветку `0032–0039`
  перед merge-head, публичный API захвата не меняется.
- Миграции `0035` и `0039` сохраняют разрешённую операцию
  `prompt_optimization`, поэтому изолированный worker не теряет доступ после
  обновления RLS helper.
- Если legacy lineage markers блокируют downgrade `0039`, rollback сохраняет
  безопасную схему `0040`, закрывает automatic dispatch и запускает
  compatibility runtime. Старый checkout не запускается против неизвестной
  merge-head схемы; при отсутствии legacy markers обычный rollback возвращает
  предыдущий checkout и схему.
- Старые записи сохраняются через legacy lineage/backfill; native macOS
  system-audio-first capture не изменён.
- История сравнения, отката и ручного выбора старых вариантов остаётся будущим
  отдельным срезом.

## Выкатка и ограничения

- PR: [#4523](https://github.com/yshishenya/crisp/pull/4523).
- Связанные задачи: #4406, #4405, #4403, #4402, #4401, #4400, #4399, #4398,
  #4397.
- Первый execute старой Feature 124 ветки был безопасно остановлен на
  migration gate (production уже был на `0037_auth_rate_limit_buckets`), а
  rollback вернул checkout на предыдущий SHA без изменения схемы. После
  интеграции recovery-ветки dry-run и повторный execute пройдены.
- Production execute: SHA `7e81c90a948e9354316efd61d06e31f829991e9a`, backup
  `/opt/projects/2brain-rec/backups/20260724T160054Z`, dump SHA-256
  `1c8f186f4a12d231487a9988104a34c6f341e417f79d00edfeb7b1ac0954d012`,
  migration head `0040_merge_content_regen_share`. Публичные `health/ready` и
  `health/live` вернули HTTP 200; семь production-контейнеров были
  `running`, с `restart_count=0`.
- Production smoke: `run_id=feature124-postdeploy-20260724-1`, результат
  `smoke_result=pass`, `readiness_verdict=infra_smoke_ready`; контролируемые
  тестовые записи и объекты удалены (`cleanup_result=pass`, 41 database
  records, 3 object keys, без residue).
- После выкладки maintenance-лог проверен четырьмя окнами в период
  `2026-07-24T16:09:56Z`–`2026-07-24T16:10:41Z`: в каждом
  `maintenance_error_count=0`; `MissingGreenlet`, traceback и ошибки
  reconciliation не повторились.
- Ранее выполненный execute на `3d1331ae` был остановлен из-за повторяющейся
  live `MissingGreenlet` в maintenance; исправление корневой причины вошло в
  runtime commit `9d7d3125`, а production получил итоговый документированный
  SHA `7e81c90a`.
- Отдельный live RLS probe не выполнялся: disposable verifier и migration
  gate прошли, но это не является заявлением о live RLS enforcement.
- Локальное owner-only приложение обновлено до `2026.07.24.6`: пакет
  `apps/macos/.build/installer/graf-local.pkg`, SHA-256
  `069b6c2c06c2f609263e2f01af37dfc94f7dd6bae9e6d5ec5dbec1e17d814c84`,
  установлен в `/Applications/GRAF.app`. Bundle ID `pro.2brain.graf`,
  `codesign --verify --deep --strict` и запуск прошли; использована локальная
  identity `GRAF Local Code Signing` без TeamIdentifier, поэтому этот канал не
  является Developer ID/notarized public distribution.
- Публичный Sparkle appcast не выпускался: release-signing manifest остаётся
  неактивным, поэтому обновление выполнено через локальный self-signed
  bootstrap package. Для публичного канала нужны Developer ID, notarization,
  stapling и подписанный appcast.
- После production execute изменялись только release/evidence-документы; их
  финальный `cd-remote.sh --dry-run` также прошёл. Поэтому closeout commit может
  отличаться от deployed runtime SHA, но новый execute для документационного
  delta не требовался.
- Откат merge-head по Alembic намеренно сохраняет sibling-ветку `0039` и
  созданную схему (`0040 → 0037 + 0039`). Обычный rollback возвращает прежний
  сервисный checkout и проверяет совместимость; при legacy markers downgrade
  блокируется, и compatibility runtime работает на сохранённой расширенной
  схеме.
