# GRAF 2026.07.26.4

## Что изменилось

- Исправлен доступ к новой странице «Настройки» внутри macOS webview.
- Ссылка из web cabinet теперь открывает обзор и разделы настроек, а не
  состояние «Функция недоступна».
- Native-раздел «Автозапись» сохранён отдельно и по-прежнему управляет
  настройками записи на этом Mac.

## Проверка

- Focused macOS tests: 65 passed.
- Полный local CI: 640 macOS tests passed; 2438 server tests passed, 1 skipped;
  strict server phase — 41 passed, 1 skipped; lint, compile, compose и
  deployment evidence scan прошли.
- PR: #4611. Spec Kit issue: #4610. Security diff scan: 29/29 путей,
  reportable findings не обнаружены.

## Совместимость и ограничения

- Миграции базы данных и перенос пользовательских данных не требуются.
- Обновление не меняет capture pipeline, native recording policy или формат
  локальной записи.
- Канал обновлений owner-only и подписан локальным ключом GRAF; Developer ID и
  notarization в этой сборке не заявляются.
- Production RLS destructive probe не выполняется локально без отдельной
  production database; после публикации выполняются live/ready и deployment
  smoke.

## Важно для обновления

- Установленные клиенты получают сборку через существующий Sparkle feed.
- Во время активной записи установка штатно откладывается lifecycle gate.
- При проблеме с обновлением сохраняется предыдущий подписанный архив и
  appcast можно безопасно вернуть по процедуре release runbook.
