# GRAF 2026.07.26.5 — прямое открытие приглашения

> Historical release receipt. `.5` used the former local/self-signed channel and
> is the predecessor for the manual Developer ID bootstrap in `.6`; it is not a
> current release instruction.

Дата выпуска: 2026-07-26

Тег: `v2026.07.26.5`

Production SHA: `57dde9fd745a89622f804ac1188eee548e805439`

Feature PR: https://github.com/yshishenya/crisp/pull/4612

Release PR: https://github.com/yshishenya/crisp/pull/4613

GitHub Release: https://github.com/yshishenya/crisp/releases/tag/v2026.07.26.5

## Изменения

- Одноразовая ссылка из письма сразу запускает принятие приглашения и открывает
  разрешённые итоги или запись без обязательного промежуточного preview-экрана.
- GET остаётся без побочных эффектов: личный аккаунт, grant и сессия создаются
  только существующим CSRF-защищённым POST.
- При отключённом JavaScript сохраняется одна видимая fallback-кнопка.

## Проверка

- Focused direct-link matrix: `21 passed`.
- `infra/scripts/ci-local.sh`: macOS `640 passed`; server `2,438 passed / 1
  skipped`; strict PostgreSQL `41 passed / 1 skipped`; lint, compile, Compose и
  deployment evidence scan — `PASS`.
- Кандидат и подписанный update-архив сохранили bundle identity и прошли
  continuity/trust validation против `2026.07.26.4`.
- Публичные ZIP/appcast доступны по HTTPS и проверены по SHA-256.
- Установленный `/Applications/GRAF.app` обновился с `2026.07.26.4` до
  `2026.07.26.5` через `GRAF > Check for Updates…`; после перезапуска
  `codesign --verify --deep --strict` прошёл.

## Production

- Первая guarded-попытка deploy была остановлена transient Docker removal race;
  rollback автоматически завершился успешно. Повторный deploy на тот же
  immutable SHA прошёл.
- Backup/restore rehearsal: `pass`, reference:
  `/opt/projects/2brain-rec/backups/20260726T110215Z`.
- Migration head: `0041_share_account_created_email`; disposable PostgreSQL
  RLS probes, runtime-role checks, Temporal/worker readiness, production smoke,
  automatic dispatch и публичные `/api/v1/health/live` и `/ready` вернули
  `200`.
- Post-deploy retry/backfill/range/normalization maintenance явно остаются
  `required_post_deploy` по CD gate. Live rendered browser smoke не заявляется.

## Public update artifacts

- Feed: https://rec.2brain.pro/static/public/downloads/graf-appcast.xml
- Archive: https://rec.2brain.pro/static/public/downloads/GRAF-2026.07.26.5.zip
- ZIP SHA-256: `88b3fe3a4bd7c80248d2d60ea240e8ab161d6f87fde116907905d35f0106cce0`
- Appcast SHA-256: `33d21d2aa7040328340bd3a4c8500c05a78a9d634982c9f8b0b978fe01b906da`
- Предыдущая версия appcast сохранена по адресу:
  `/opt/projects/2brain-rec/infra/runtime/public-downloads/graf-appcast.xml.pre-v2026.07.26.5-20260726T104454Z`.

## Совместимость и ограничения

- Новых миграций базы данных и переноса пользовательских данных для этой
  функциональной правки нет.
- Канал обновлений owner-only с `GRAF Local Code Signing`; Developer ID и
  notarization для публичной Apple-дистрибуции не заявляются.
- При проблеме обновления предыдущие подписанные appcast и архив остаются
  recoverable rollback reference.
