# GRAF `v2026.08.29.2` — исправление перехода к оплате в macOS

Дата кандидата: 2026-08-29  
Тег: `v2026.08.29.2`  
Основа: merged `master` после PR [#5940](https://github.com/yshishenya/graf/pull/5940)

## Что изменилось

- Встроенный macOS checkout сохраняет контекст оплаты при серверном 303-переходе.
- Разрешение перехода к YooKassa/YooMoney учитывает исходный URL navigation request и активный payment handoff.
- Серверный billing-код, промокоды, миграции, shop ID, секреты и режим YooKassa не менялись.

## Проверка

- Полный `infra/scripts/ci-local.sh --full` на exact SHA `ba5c6a164`: macOS `768 passed`; server `3608 passed / 1 skipped`; performance `1 passed`; strict RLS `52 passed / 1 skipped`; lint, compile, Compose и deployment evidence scan — `PASS`.
- `swift test --package-path apps/macos --filter DesktopCabinet(BillingHandoff|RoutePolicy|NavigationRequestPolicy)Tests` — `36 passed`.
- Локальная RLS hardening boundary сообщает `blocked`, потому что live production PostgreSQL не предоставлен; production enforcement этим локальным прогоном не подтверждается.

## Совместимость и миграции

- Миграций базы данных и изменений формата данных нет.
- Existing embedded macOS cabinet routes сохраняются; изменён только переход к внешнему платёжному провайдеру после checkout redirect.
- Production-магазин YooKassa и серверная production-конфигурация этим выпуском не переключаются.

## Известные ограничения

- Реальный production smoke и публикация подписанного Developer ID/notarized macOS-артефакта выполняются отдельным release/deploy gate.
- Live RLS enforcement требует отдельной проверки на production PostgreSQL.

## Ссылки

- PR: [#5940](https://github.com/yshishenya/graf/pull/5940)
- Связанный billing closeout: [#5848](https://github.com/yshishenya/graf/issues/5848)
