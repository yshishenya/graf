# v2026.08.15.3 — тарифы и оплата внутри GRAF

## Что изменилось

- «Тарифы и оплата» открываются внутри окна GRAF, без системного браузера.
- Сохраняются desktop-сессия, навигация назад/вперёд и embedded shell.
- Переходы к YooKassa разрешены только по HTTPS и узкому списку известных хостов.
- Неизвестные внешние хосты блокируются; токены и финансовые параметры не
  добавляются в URL.

## Совместимость и обновление

- Серверный релиз совместим с предыдущим desktop-клиентом.
- Старый `/billing/handoff` сохранён для обратной совместимости, но новый
  клиентский путь его не вызывает.
- Account/admin/referral-маршруты сохраняют прежнее открытие во внешнем браузере.

## Проверка

- macOS Swift: 671 тестов, PASS.
- Server full: 2960 passed, 1 skipped, PASS.
- Fast CI: 1077 unit-тестов, PASS.
- Contract validation и `git diff --check`: PASS.
- `infra/scripts/cd-remote.sh --dry-run`: PASS.
- GitHub Actions намеренно не запускались.

## Выкатка и ограничения

- PR #5122 (`https://github.com/yshishenya/crisp/pull/5122`) влит в `master`.
- Физический Intel Mac недоступен; universal build и Rosetta-проверка выполнены.
- Публикация Sparkle требует отдельной проверки notarized universal ZIP и live
  appcast; старый appcast имеет известный 404-риск и не считается закрытым этим
  документом.
