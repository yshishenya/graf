# Cross-artifact review — 2026-08-07

## Результат

Артефакты `spec.md`, `plan.md`, `tasks.md`, `contracts/`, `quickstart.md` и
constitution повторно проанализированы; критических противоречий не найдено.
`git diff --check`, Ruff и fast CI проходят. Открытые задачи перечислены ниже,
поэтому это не является разрешением публичного запуска.

## Проверенное покрытие

- 87 задач: 76 отмечены выполненными, 11 остаются открытыми.
- 996 серверных тестов в `infra/scripts/ci-local.sh --fast`: PASS; Ruff и Python
  compile также PASS.
- Миграционный head обновлён до `0051_transient_media_lifecycle`; focused
  billing/account/storage/accessibility/security/UI harness: 46 PASS, 2 warnings;
  отдельная disposable-PostgreSQL lifecycle выборка и credential-free YooKassa
  test-shop harness также проходят.
- Account IA aliases и desktop route-policy assertions добавлены; выбор
  no-archive проходит через multipart/finalize/processing, сохраняется в
  lifecycle rows и имеет bounded purge через существующий object-store worker.
  Отдельная WAV-retention/COGS политика остаётся launch gap.
- Focused security/RLS/refund-boundary checks: 7 PASS; provider refund mutation
  remains absent and tenant context checks pass against disposable PostgreSQL.
- Новые registry/launch-gate/monitoring, accessibility/usability, payment-history
  и audit проверки: запускаются отдельно и не
  содержат provider payload, платёжные идентификаторы или meeting content.
- Checkout, saved-card binding и renewal mutation по-прежнему default-off.
- T018 закрыт: disposable PostgreSQL account/trial lifecycle — 4 passed.
- T076 закрыт: Temporal billing reconciliation/maintenance registration и
  bounded projection tests проходят; embedded billing links теперь передают
  денежные маршруты во внешний браузер, а macOS handoff policy test проходит.

## Блокеры публичного запуска

Открыты: полное DB-backed покрытие account/storage lifecycle, WAV retention/COGS
wiring, реальный merchant test-shop canary и расширенная
Temporal maintenance matrix, moderated/landing и live security review, а также
Russia-first JTBD, dated WTP/usage/COGS и утверждение upgrade-copy. Эти пункты
соответствуют T017/T020/T023/T025/T030/T079/T080/T083–T085/T087.

Это evidence текущего состояния, а не разрешение включать checkout.

## Дополнительная проверка T025/T030/T080

- Storage reservation commit раньше принимал любой `artifact_id` (в том числе
  `None`) и не проверял workspace, verified canonical `meeting-review.m4a`,
  фактический `byte_length` или expiry reservation. Добавлена fail-closed
  проверка в `billing/storage.py`; regression evidence: 17 focused storage and
  source-lifecycle tests passed.
- В модели данных описаны `transcript_imported_at`, `playback_verified_at`,
  policy version и purge evidence для текущего `meeting-transcription.wav` и
  legacy `mic.wav`/`incoming.wav`, но в production lifecycle-моделях и worker
  wiring этих gate-полей пока нет. `source_lifecycle.py` содержит только pure
  decision helpers; автоматический normal source purge после двух gates не
  реализован. Это остаётся T030/launch blocker, а accepted deletion/account
  close purge уже имеет отдельный override path.
- Quota filter исключает source filenames и transient objects на уровне
  playback predicate; добавлены regression checks для `meeting-transcription.wav`,
  `mic.wav` и `incoming.wav`. Полная DB-backed reconciliation, reservation
  admission wiring и object-stat/supersede integration остаются T025.
- Redaction contract покрывает ключи/вложенные JSON и финансовые страницы,
  но live provider/analytics configuration, production log sinks и real-shop
  payload paths не доказаны автоматическими тестами. T080 остаётся manual/live
  security gate; новые тесты не заменяют review конфигурации и runtime traces.
