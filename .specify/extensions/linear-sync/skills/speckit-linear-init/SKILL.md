---
name: speckit-linear-init
description: Create or refresh project Linear sync configuration for Spec Kit work.
---

Run the project command `speckit.linear-init`.

Use this before Linear sync if `.specify/linear.yml` does not exist or the
project/team defaults changed. Do not write secrets to tracked files.

All instructions, issues, and comments produced for the project must be written
in Russian in simple language.
