# Проверка F6792 — 2026-09-11

## Состояние

Реализация в рабочей копии `codex/6792-settings-product-experience`, база `fee34a9d94536543188462b5607d5e6ab3ef5456`. Lane: **high-risk-feature**, полный Spec Kit. Пользователь разрешил довести работу до готовности и выполнить релиз 2026-09-11; коммит, обновление GRAF Dev, PR и релизные операции входят в разрешённый объём. Установленный GRAF Dev проверен ниже. Merge и release ещё не выполнены.

T001–T011 выполнены. T012: штатный GRAF Dev и ручные разделы проверены; окончательный exact-SHA CI ожидается. PR #6922 открыт; публичный релиз ещё не опубликован.

## Сервер и формы

- **256 passed**: `test_settings_ui_contract.py`, `test_settings_view_models.py`, `test_settings_outcomes.py`, `test_calendar_settings_view_models.py`, `test_cabinet_web_shell.py`, `test_billing_ui.py`, `test_billing_accessibility.py`.
- **58 уникальных контрактных/интеграционных проверок с PostgreSQL прошли**: `test_calendar_settings_contract.py`, `test_calendar_settings_flow.py`, `test_settings_ia_flow.py`. Использован `apps/server/scripts/run_local_postgres_tests.sh` с отдельной одноразовой БД. В первом проходе устаревшие ожидания текста исправлены; последняя упавшая проверка повторена отдельно и прошла. После последнего изменения пустого календаря заново прошёл весь `test_calendar_settings_contract.py` (21 проверка). Экспорт семи синтетических страниц — дополнительная временная проверка, не входит в эти 58.
- `NODE_PATH=<bundled node_modules> node --test apps/server/tests/browser/timezone-settings.test.cjs`: **PASS**, включая ошибку сохранения, повтор, отмену и часовой пояс.
- `ruff check` изменённых Python-файлов, `node --check cabinet.js`, `git diff --check`: **PASS**.
- Независимый проверяющий сверил form actions и поля, CSRF, закрытие/отмену аккаунта, прежние значения и маршруты. Новых зависимостей, миграций и изменений API нет.

## macOS и WebKit

`swift test --package-path apps/macos --filter 'DesktopMeetingShellWebViewBoundaryTests|CabinetSidebarRuntimeTests|EmbeddedCabinetRecordingSettingsBridgeTests|EmbeddedCabinetNotificationSettingsBridgeTests|DesktopCabinetWorkspaceTests'`: **85 passed**.

Дополнительная временная проверка в существующем XCTest/WKWebView стенде загрузила семь реально отрисованных шаблонов на синтетических данных с текущими CSS/JS. `WKWebView.pageZoom = 2`, frame 988×680: effective viewport 494 px, меню компактное, нет переполнения документа и основной области в 14 сочетаниях раздела/темы. Последний совместный прогон — **86 passed**. Временный метод и файл экспорта удалены; это отдельное наблюдение, а не новый постоянный тест с зависимостью от временных файлов.

Сохраняются рабочие Start/Stop, индикатор активной записи, локальные границы доступа и version guards. Вход в настройки не сворачивает панель при активной записи или проблеме захвата. Старое внимание к очереди отправки остаётся доступно компактно, новое внимание и явное раскрытие работают через прежние обработчики; при выходе из настроек прежнее автоматическое привлечение внимания восстанавливается.

## Визуальная проверка и пути пользователя

Реальные HTML-шаблоны получены через TestClient на синтетических данных, без обращения к пользовательской базе. Проверено **56 сочетаний**: семь разделов × две темы × ширины 988/1440/360/494 px, высота 680 px. Нет горизонтального переполнения и ошибок JavaScript. Это браузерное и автоматическое WebKit доказательство; оно не заменяет установленный GRAF Dev.

Проверены действия:
- Имя: редактирование, доступное сохранение, отмена с возвратом исходного значения.
- Аккаунт: закрытие скрыто по умолчанию, раскрытие/сворачивание клавишей Enter; запланированное закрытие/срок/отмена защищены контрактными проверками.
- Запись: синтетический ответ существующего bridge, 37 приложений, поиск «Телемост», отсутствие совпадений, очистка поиска, три правила и доступность поиска в первом экране.
- Итоги: создание, редактирование, копирование, отмена и Escape возвращают фокус к инициатору. Все четыре личных действия доступны через нативное раскрытие. Скрытие и удаление сохраняют прежние обработчики и подтверждение удаления.
- Календари: выбор сервиса сразу в первом экране, открытие формы Яндекса, Escape и возврат фокуса. Контрактные и интеграционные проверки покрывают подключённые/пустые/недоступные состояния, права, выбор календарей и ошибки; настоящие учётные данные не вводились.
- Меню: семь названий на широкой области, подпись «Разделы» на узкой, открытие и Escape.
- Системная тема: смена светлой/тёмной темы ОС меняет вычисленный фон. Масштаб 200% проверен дополнительно нативным pageZoom, а не только уменьшением размера страницы.
- Тариф и оплата: существующие проверки owner/member, недоступной цены/оплаты, согласий, промокода и состояния платежа. Платежи не проводились.

Контраст используемых токенов относительно фона панели (тёмная / светлая): основной текст 13.67 / 15.96; вторичный текст 7.04 / 6.87; границы полей 4.50 / 3.78; фокус 8.73 / 7.94. Порог текста 4.5:1 и границ/фокуса 3:1 выполнен для этих пар; это измерение цветов, а не полный аудит каждого пикселя/disabled-состояния.

Синтетические снимки сохранены локально в `output/playwright/f6792/` (ignored). В спецификации хранится интерактивный `prototype.html`; он иллюстрирует направление, рабочая реализация находится в продуктовых шаблонах.

## Governance и convergence

`python scripts/check_spec_kit_governance.py`: **PASS** с изолированным официальным specify-cli v1.0.1 из ref `9118ed15a0ba65053469a94c560ea5d233f75884`, закреплённого в lock. Глобально установленный v1.0.4 не совпадает с lock; глобальные инструменты и bootstrap lock не менялись. Установка через `uv pip` в отдельное временное окружение сохраняет проверяемый commit_id. Проверки frozen doctor не обходились.

Spec Kit prerequisites прошли. Перед converge hooks отсутствуют. Сверены 12 FR, 5 SC, 11 сценариев приёмки, ограничения плана и принципы constitution, относящиеся к записи, доступу, данным и независимой реализации. Новых пропусков в требуемой реализации не найдено; новые задачи не добавлялись. Ручная проверка установленного GRAF Dev выполнена с явно перечисленными ниже ограничениями; окончательное закрытие T012 требует exact-SHA CI. Список проверки требований принадлежит независимому проверяющему; автор реализации не менял его отметки.

## Установленный GRAF Dev

Проверен коммит `c14845cff50113edd04dd1084ace338f89c826e8`, manifest `dev-c14845cff501`. Штатные build → promote → status → smoke: PASS. Сохранены bundle ID, designated requirement и прежняя подпись. Все 13 проверок готовности прошли, migration head `0092_recording_origin_cancel`. Первый build остановился из-за сбоя Docker BuildKit (`concurrent map iteration and map write`); после штатного перезапуска Docker Desktop повторная сборка с COMPOSE_PARALLEL_LIMIT=1 завершилась успешно.

В установленном приложении открыты все семь разделов. Проверены формы создания итогов и подключения календаря, их отмена без сохранения, поиск Zoom, пустой результат, сброс поиска, светлая тема с возвратом прежней тёмной через «Отменить», нативная история «Назад» и доступность «Вперёд». Визуально проверены широкая область с постоянным меню и узкая область с подписью «Разделы», названия и формы без наложений. Панель записи вручную свернулась; индикатор требующего внимания состояния остался доступен.

Ограничения ручной проверки: macOS на стенде сообщает «Нужен доступ к системному звуку», поэтому активная запись вручную не запускалась. Разрешения не сбрасывались и не расширялись. Защита активного управления проверена 85 постоянными Swift-тестами. Первая загрузка кабинета потребовала штатную кнопку «Обновить»; это известное ограничение предыдущего релиза, его исправление не заявляется. Финансовые действия, реальные подключения и удаление аккаунта не выполнялись; эти состояния покрыты контрактными проверками. Матрица двух тем/200% относится к автоматическому WebKit и браузеру, а не к ручной проверке каждого сочетания на установленном приложении.

## GitHub CI и следующий этап

PR: https://github.com/yshishenya/graf/pull/6922. Первый governance-fast https://github.com/yshishenya/graf/actions/runs/34593674844: 1576 passed, 1 skipped, 1 failed. Единственная ошибка — устаревшее ожидание языкового select в test_cabinet_audit_presentation.py; проверка обновлена для скрытого сохранённого locale и видимого русского языка. Локальный повтор этого файла: 2 passed. Повторный CI должен пройти на окончательном SHA до merge; release-full требуется отдельно после фиксации master.

Продуктовый diff, независимо проверенный до коммита: SHA-256 `e28eb65d3a2f6b44bf5f579b5b1cee70654c9f5569904599d02f9b7981b3022e`. После него изменены только тест, документация и release metadata.
