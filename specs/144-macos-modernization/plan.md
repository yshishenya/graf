# Plan

1. **Design System Alignment:** Extract exactly what colors/radiuses were used in the web cabinet (`#0a0a0b` background, `#1c1c1f` surface, 12px border radius) and map them to `DesktopMeetingShellChrome`.
2. **SwiftUI Glassmorphism:** Introduce `Material` backgrounds to floating elements (like the title bar HUD and floating prompts) to emulate the web cabinet's depth.
3. **Typography Refinement:** Use SF Pro variants to mimic the structured feel of Geist/Sora used in web.
4. **Compile & Commit:** Verify swift compilation and commit.
