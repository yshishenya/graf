# UI Contracts

- Must maintain all `accessibilityIdentifier` and `accessibilityLabel` bindings in Swift UI views (e.g. `systemAudio.titlebarRecordingHUD`, `SystemAudioAccessibilityIdentifier.calendarPrompt`).
