# Быстрая проверка Feature 237

## Сценарий

1. Открыть корневой `AGENTS.md`.
2. Найти правило о языке ответов.
3. Подтвердить, что оно одновременно требует простого понятного языка,
   исключает необязательные англицизмы и допускает точные термины с пояснением.
4. Независимый reviewer проверяет
   `checklists/agent-response-language.md`; агент не отмечает его пункты.
5. После реализации выполнить `$speckit-converge` и записать результат в
   финальной задаче `tasks.md`.
6. Выполнить:

```sh
python3 scripts/validate-agent-context.py
python3 scripts/check_spec_kit_governance.py
git diff --check
```

## Ожидаемый результат

Все команды завершаются успешно. В рабочем дереве нет изменений продукта,
сборки, зависимостей, развёртывания или корневого `CHANGELOG.md`.
