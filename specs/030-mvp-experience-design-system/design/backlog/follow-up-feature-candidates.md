# Follow-Up Feature Candidates

1. `031-desktop-experience-shell`: implement native desktop trust shell polish and embedded cabinet container.
2. `032-web-cabinet-review`: implement browser meetings list, upload, processing, and meeting review surfaces.
3. `033-design-system-components`: implement tokens, status badges, meeting rows, review panels, and accessibility rules.
4. `034-route-status-contracts`: enforce route visibility and status labels across app/web.
5. `035-prototype-handoff-implementation`: convert approved Figma frames into production UI tasks.
6. `018-retention-deletion-execution`: implement lifecycle deletion accounting from this design.
7. `017-access-sharing-downloads`: implement sharing/export/browser-only flows after review surface exists.

## Sequencing

Do `031` and `032` first to create visible MVP value. Run `033` alongside them only where component reuse reduces duplication. Keep `017` and broader admin/billing after the core owner value loop works.
