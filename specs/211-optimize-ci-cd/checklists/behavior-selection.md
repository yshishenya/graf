# A3: качество требований выбора проверок

**Created**: 2026-09-12
**Feature**: [spec.md](../spec.md), [plan.md](../plan.md)
**Review Ownership**: independent reviewer; implementation must not change these markers. Checked means requirements reviewed, not code completed.

- [X] CHK001 Область кабинета/настроек и сохранение прежних safety-проверок явно ограничены (FR-025–026).
- [X] CHK002 Одинаковый выбор групп и отсутствие повторов определены для local/fast и нескольких компонентов (US7.1–2, plan map).
- [X] CHK003 План отделён от выполнения/receipt, поля, пустой набор и отсутствие побочных действий определены (FR-027, plan 3–4).
- [X] CHK004 Удаление/rename, потеря обязательного файла или теста и безопасная передача путей имеют однозначный результат (FR-028).
- [X] CHK005 Точная event base, неверная явная база и диагностический fallback не противоречат A1 (FR-028–029).
- [X] CHK006 Dirty/unknown/partial не могут стать свидетельством merge/release; дальнейшие gates названы (FR-027–030).
- [X] CHK007 Время, существующий регрессионный тест и исторический отрицательный контроль имеют проверяемую приёмку (SC-014–015).
- [X] CHK008 Окружение, отсутствующие зависимости, ответственность Full/E02 и правила согласования не оставлены неявными (plan 4–7).
- [X] CHK009 Согласованы ли новые явные режимы с договором проверки активной документации и задачами совместимости? [Consistency, FR-030, contracts/ci-cd-cli.md §Documentation consistency]

## Review

### Независимая проверка — 2026-09-12

Reviewer: `/root/requirements_review`, отдельный проверяющий требований A3/E01. Проверены текущие A3-разделы `spec.md`, `plan.md`, `research.md`, `tasks.md` T043–T047, `data-model.md`, `contracts/ci-cd-cli.md` и `quickstart.md`; действующие правила Spec Kit и конституции; существующий runner и выбранные тесты прочитаны только для оценки полноты требований. Реализация и результаты будущих тестов не подтверждались.

| Пункты | Основание принятия требований |
|---|---|
| CHK001–002 | FR-025–026 и таблица A3 ограничивают карту существующими группами, сохраняют прежние unit/changed/performance и определяют объединение без повторов. `covered_tests` в уточнённом договоре и модели означает прежний unit-набор того же запуска, не сохранённый PASS. Связанные contract-файлы проверяются в behavior-этапе и исключаются из последующего changed-набора. |
| CHK003–004 | FR-027–028, US7.3–5 и договор A3 различают просмотр и выполнение, задают безопасную передачу путей, отказ при потере обязательного доказательства, код 2 для пустого focused-набора и отсутствие установки/сети/receipt у плана. |
| CHK005–006 | FR-028–030 сохраняют event base A1, блокируют неверную явную базу, ограничивают диагностический fallback и не повышают dirty/partial до merge/release evidence. |
| CHK007–008 | SC-014–015 и quickstart задают исторический отрицательный контроль в изолированной копии, фактические время/число тестов и предел 60 секунд в подготовленном окружении. Подготовка окружения, E02, отдельные GitHub/Full/commit gates названы. |

**R1 / CHK009 — устранён и повторно проверен:** исходный раздел `Documentation consistency` запрещал упоминания runner без `--fast`/`--full`, а A3 и T046 требовали документировать `--plan`/`--focused`. Теперь договор разрешает все четыре явных режима, пункт 7 плана требует сохранить запрет неявной команды, T045 включает совместимость `check_active_docs`. Это исправление договора E01, не расширение в E02.

Уточнение о фактическом выполнении принято в договоре A3 и пункте 5 плана: пустой, пропущенный или исключённый обязательный набор, включая именованный rail-тест, не становится PASS из-за успеха соседних тестов; фильтры pytest не могут молча сузить набор. T045 включает skipped proof. Проверка требований не заменяет доказательство этого условия реализацией.

К E02 и следующим этапам остаются отнесены классификация зависимостей unit/БД, сокращение широкого fast, перестройка подготовки Docker и ускорение Full. Они не нужны для принятия ограниченной карты A3 и не являются найденными пробелами её требований.

**Итог требований: PASS, 9/9.** Незакрытых блокирующих вопросов требований A3 не осталось. Этот результат разрешает пройти следующий запланированный gate; он не подтверждает код, выполненные тесты, GitHub checks, merge, deploy или release. Последующие изменения области или договора требуют повторной проверки затронутых пунктов.

### Повторная проверка уточнения перед code review — 2026-09-12

CHK001–002 повторно приняты после уточнения: из behavior-этапа исключается только покрытый широким unit-набором файл; связанные contract-файлы выполняются в behavior-этапе с проверкой фактического исполнения и удаляются из следующего changed-этапа. Это сохраняет исходное требование отсутствия повторов и защиту именованного rail-теста при изменении самого тестового файла. Изменения требований, снимающего защиту или расширяющего E01, нет. Маркеры по-прежнему подтверждают качество требований, а не итог code review.

### Независимое code review и повторная проверка R2 — 2026-09-12

Reviewer: `/root/requirements_review`; применены `code-reviewer` и `ponytail-review`. Прослежены общий выбор, обработка Git-путей, Bash-массивы, запуск подготовленного pytest, JUnit, отказ при пустом/пропущенном rail-доказательстве и отсутствие повторов с прежними unit/changed/performance. Изменения кода проверяющим не выполнялись.

**R2 / P2 — устранён:** `changed_files` возвращал код 1 и при отсутствии неявной базы, и при ошибке сбора путей. При успешном выводе `cabinet.js` из `git diff` и последующем сбое `git ls-files` просмотр возвращал 0 с неполным набором, который мог попасть в focused. Теперь ошибки `diff`/`ls-files` возвращают 2; обе команды останавливаются до выбора/выполнения тестов. Независимый повтор исходного сценария дал `status=2 ci_selection_error=invalid_diff_or_explicit_base` для `--plan` и `--focused`.

Дополнительно проверяющий выполнил `bash -n infra/scripts/ci-local.sh` и четыре регрессии `test_git_collection_failure_never_selects_partial_behavior_tests` существующего CI-контракта: **4 passed, 74 deselected, 0.22s**. Полный CI, приложение, сеть и операции GitHub этой проверкой не запускались. Широкие результаты реализации фиксируются отдельно в quickstart и не приписываются проверяющему.

Проверенный локальный снимок поверх `ad71f2ce4db68d846d7c333213961c5f5f7d5e89`:

- SHA-256 `infra/scripts/ci-local.sh`: `5327a4e6ffd2b43d47d8cecd143f82e20c3e4234cffa4135fb83f830c8b9744e`.
- SHA-256 `scripts/ci-behavior-tests.py`: `0f70d13fc2344370594816d15cd2c7fb62fd97283235d74250be5ddd5b82b0ca`.

**Итог code review: PASS.** Неустранённых actionable findings нет; R1 и R2 закрыты. Неблокирующая внутренняя `--allow-empty` сохраняется для простых аргументов Bash 3.2; она не разрешает пустой публичный focused-результат. Ponytail: обязательного сокращения нет. Это review локального снимка, не GitHub/exact-SHA CI, разрешение commit, merge или release; исходные requirement-маркеры имеют прежний смысл.
