# Реализация: первый участок системного доступа

Дата: 2026-09-06. Ветка: `codex/254-system-admin-console`. Уровень риска: `high-risk-product`. Legacy impact: `retain-with-exception` (см. `spec.md`, срок пересмотра 2027-03-31).

## Коммит и готовность к началу

Пользователь разрешил коммит подготовленных документов и начало реализации. Документы сохранены коммитом `e8286572b` (`[F254] Подготовить спецификацию и план системной консоли`), исходная база — `a389657e607ef8389fcf947b26b158cee6928884`. Перед началом выполнен fetch: новых коммитов `origin/master` относительно базы ветки не было. Проверены все контрольные списки: requirements 5/5; security 6/6; billing 6/6; diagnostics 5/5; UX 5/5; infra 5/5. Исполнитель их состояния не менял. Все 39 задач имели открытые GitHub issues; повторно прочитана #6712.

T001 выполнена в части предусмотренной подготовительной проверки: актуальная база, независимое ревью требований, матрица FR/SC/AC/tasks и владельцы задач. Runtime AC не объявляются пройденными. Issue #6712 остаётся открытой до PR, проверки точного SHA и предусмотренного tracker-policy закрытия.

Новые изменения реализации находятся в рабочем дереве после этого коммита. Push, PR, выпуск, развёртывание и изменения реальных данных не выполнялись.

## Что реализовано в T002

- Добавочная миграция `0086_system_admin_boundary` после фактического единственного head `0085_merge_summary_mediascribe`. Исторические миграции не менялись. Alembic учитывает отдельные схемы при сравнении metadata.
- Схема `system_control`: `principals`, `role_assignments`, `sessions`. UUID системной личности не является UUID пользовательской сессии. Не создаются администраторы из существующих владельцев пространств.
- PostgreSQL-роли `twobrain_rec_system` и `twobrain_rec_system_authority` создаются без входа, повышенных атрибутов и членств. Включение login требует отдельного необязательного секрета bootstrap; обычное развёртывание его не требует. У app/maintenance/media нет USAGE новой схемы.
- Проверяющая SQL-функция принадлежит ограниченной NOLOGIN роли, с фиксированным `search_path`, без PUBLIC EXECUTE. Ей доступны только id/status/auth_version личности и данные сессии/назначения; password_hash ей не выдаётся.
- Для чтения разрешённого набора столбцов `meetings` проверяются настоящий `session_user`, ID личности/сессии, хеш сессионного токена, актуальные статус/auth_version, срок назначения, абсолютный и неактивный срок сессии. Проверка не опирается только на присланную строку роли или GUC.
- Restrictive RLS обязательна поверх старых разрешающих PUBLIC policies. Даже искусственно добавленная permissive allow-all policy не даёт неавторизованное чтение. INSERT/UPDATE/DELETE системной роли отдельно запрещены до реализации команд.
- Выданы только столбцы метаданных встречи. `SELECT *`, title и любые изменения недоступны; текст, аудио, секреты и финансы не подключены.
- Отдельный engine: PostgreSQL system login обязателен, пул 4, overflow 0, ожидание 5 s, statement timeout 2 s. Новый контекст отделён от tenant context; нельзя заменить личность в одной сессии. После commit/rollback восстанавливается прежний проверяемый контекст; новая session не наследует полномочия из pool.
- Повторный bootstrap проверяет атрибуты/членства, точный набор столбцов, наличие обязательной RLS и отсутствие широких табличных прав. Он не выдаёт общий SELECT/DML системной роли.
- Откат пустой схемы проверен. При наличии системной личности миграция отказывается удалять данные атрибуции; оператору потребуется отдельная согласованная миграция.

На момент первого участка T002 оставалась открытой: permission_grants/case context ещё не были реализованы. Текущее состояние после продолжения приведено в последнем разделе; предметные проекции остальных таблиц и права команд остаются в последующих задачах. Переход к публичному входу или выдаче контента до соответствующих аудита/авторизации не выполнялся.

## Совместимость тестового стенда

Старый playback UI harness использовал `Base.metadata.create_all`, который не создаёт отдельные схемы и SQL-политики. Он переведён на существующий `prepare_schema`. При реальном запуске дополнительно выявлены нарушения порядка вставки FK и использование asyncpg pool между разными event loops. Подготовка родительских строк отделена flush; применён существующий в проекте `NullPool` для тестового процесса. Новый тест создаёт этот стенд целиком на чистой БД и проверяет встречи и пустую системную область. Сетевые провайдеры и реальная запись не используются.

## Выполненные проверки

Среда: macOS, Python 3.14 из `uv`, PostgreSQL 17 в одноразовом Docker-контейнере на loopback. Использованы реальные ограниченные роли, миграции и транзакции, синтетические личности/встречи; секреты тестовые. Каждый контейнер удалён штатным runner.

| Проверка | Результат |
|---|---|
| Новый тест до реализации | Ожидаемая ошибка: отсутствует `twobrain_rec_system`; это зафиксированная исходная неготовность |
| `test_system_admin_security.py` + `test_rls_maintenance_context.py` + `test_rls_production_boundary.py` | 38 passed на первом участке |
| `test_rls_postgres_policies.py` + `test_playback_normalization_postgres.py` + `test_rls_tenant_context.py` | 63 passed, 1 skipped: общая тестовая область ролей уже занята предыдущим модулем |
| Пропущенный `test_runtime_role_bootstrap_is_idempotent_and_verifies_privileges` в новом изолированном запуске | passed; причина пропуска проверена отдельно |
| Финальный `test_system_admin_security.py` (19 сценариев) + существующий тест metadata/create_all для summary slots | 20 passed; исправленный playback harness включён |
| Ruff на девяти изменённых Python-файлах | passed |
| `uv run alembic heads` | Единственный head `0086_system_admin_boundary` |
| `git diff --check` | passed |
| Проверка документов Feature 254 и governance проекта | passed |

Команды PostgreSQL запускались из корня репозитория:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_security.py tests/integration/test_rls_maintenance_context.py tests/contract/test_rls_production_boundary.py -x -q
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_rls_postgres_policies.py tests/integration/test_playback_normalization_postgres.py tests/unit/test_rls_tenant_context.py -x -q
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_playback_normalization_postgres.py::test_runtime_role_bootstrap_is_idempotent_and_verifies_privileges -x -q
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_security.py tests/integration/test_meeting_summary_slots.py::test_mapped_metadata_create_all_is_compatible_with_migrated_slot_schema -x -q
```

Одиночная проверка bootstrap фактически также выполнялась первой в запуске с новым harness; bootstrap прошла, обнаруженные затем ошибки harness устранены и проверены финальным запуском. Два предупреждения pytest связаны с уже существующими import rewrite и Starlette/httpx deprecation; не скрывались.

Это проверки начатой реализации. Полный CI, `ci-local --fast`, браузерная приёмка консоли, MFA/last-admin гонки, операции с контентом, финансы/акции, нагрузка, converge и release gates не выполнялись. Они требуются на следующих этапах; эти результаты не являются разрешением выпуска.

## Продолжение: полномочия на объекты и аудит чтения

После следующего поручения пользователя работа продолжена в том же дереве без нового коммита. Прочитаны актуальные контракты, проверены открытые владельцы #6713/T002 и #6714/T003, состояние reviewer-owned списков не менялось.

**Текущее состояние:** основание T002 выполнено. Все отношения, к которым системная роль сейчас имеет доступ, защищены ограниченными grants/RLS. Дополнительные предметные проекции добавляются вместе с их функциональностью в T008–T035; отсутствие преждевременно выданного SELECT на них не является готовым пользовательским экраном. T003 выполнена только в части аудита чтений; предпросмотры, долговечные команды, worker claim/continuation, диспетчер и аудит результатов команд ещё не реализованы. Полная приёмка AC-001/003/036/041 и всей фичи не заявляется.

Добавлена миграция `0087_system_scoped_authority` и серверный модуль `system_admin`:

- Фиксированная матрица шести ролей и 36 известных разрешений. Неизвестное разрешение не получает даже суперадминистратор; временные grants ограничены разрешёнными сочетаниями роли, действия и типа объекта.
- `permission_grants`: точные principal/assignment/version/permission/target, срок не более 24 часов. Замена версии назначения, отзыв и истечение немедленно закрывают grant. Управление выдачей grants и защита последнего администратора остаются T006; обычные роли не могут писать в таблицу grants.
- `SystemDatabaseContext` содержит тип/UUID цели, контекст обращения и ссылку на событие аудита. Неполные и неизвестные сочетания отклоняются; глобальный запрос допускает отсутствие обоих полей цели. Если метаданные запрошены с конкретной целью, RLS не выдаёт другие встречи.
- `case_contexts` в текущем внутреннем сервисе связывает одну точную цель, причину и административную сессию; несколько объектов обслуживаются отдельными контекстами. Срок не длиннее абсолютного срока сессии. Такой контекст не даёт разрешений: администратор с доступом только к метаданным может открыть обращение, но не прочитать расшифровку.
- `audit_events` сохраняет фактические actor/session/role/permission/target, результат и снимок причины. Автор и результат выводятся SQL-функцией из подтверждённой сессии и текущих полномочий, не передаются свободными полями. Системный runtime не получает INSERT/UPDATE/DELETE/TRUNCATE этой таблицы; запись идёт только через узкую функцию, чтение — по `audit.read`.
- Разрешённое и запрещённое чтение записываются отдельной транзакцией. Отказы по известной отозванной сессии сохраняют фактического автора, но не создают действующее полномочие. Неизвестные токены остаются ответственностью будущего аудита входа T005.
- `authorize_access` возвращает контекст только после commit. Дополнительно SQL-проверка отвергает событие, записанное текущей ещё не завершённой транзакцией (`writer_transaction`), поэтому собственного незакоммиченного INSERT недостаточно.
- Разрешение на содержимое связано с сессией, обращением, конкретной целью, действием и событием не старше 60 секунд. Прослушивание не разрешает скачивание; перенос контекста в другую сессию и использование старого события запрещены. Действующая роль/grant проверяются снова при каждом обращении.
- Недоступность аудита распространяется как ошибка БД и не выдаёт разрешение. На этом этапе это проверенный механизм полномочий: реальные текстовые/аудио endpoints и проверки ревизии/удаления подключаются в T010–T012, сами байты ещё не выдаются.
- Bootstrap проверяет новый точный набор столбцов аудита. Общие права app/maintenance/media не расширены, секреты и прямые grants системных учётных записей им не выданы.

Новые проверки сначала воспроизвели отсутствие функции системных разрешений; затем выполнены на PostgreSQL 17:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_authority.py tests/integration/test_system_admin_security.py tests/integration/test_rls_maintenance_context.py tests/contract/test_rls_production_boundary.py tests/unit/test_rls_tenant_context.py -x -q
# 78 passed; до последнего добавления снимка причины в audit event.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_playback_normalization_postgres.py::test_runtime_role_bootstrap_is_idempotent_and_verifies_privileges tests/integration/test_system_admin_authority.py tests/integration/test_system_admin_security.py tests/integration/test_meeting_summary_slots.py::test_mapped_metadata_create_all_is_compatible_with_migrated_slot_schema -x -q
# 44 passed, без skipped; финальная схема и снимок причины включены.
```

Ruff, документный validator, project governance и `git diff --check` прошли. Единственный Alembic head: `0087_system_scoped_authority`. Полный CI и выпуск не выполнялись. Задачи #6712/#6713 остаются открыты до PR и его проверок; новые изменения не закоммичены и не опубликованы.

## Продолжение реализации: команды, вход и первые страницы

Добавлены миграции 0088–0091. Это рабочий промежуточный участок, а не завершение T003–T008 или всей фичи.

- Предпросмотр двух команд встречи хранится 5 минут; проверяется монотонная версия встречи. Commit сохраняет команду и аудит вместе, повтор ключа возвращает прежнюю операцию. Узкие worker-функции проверяют текущую роль/сессию до начала и точную цель/ссылку/fence при продолжении; SQL-проверки покрывают отзыв и конфликт версии. Реальный Temporal dispatcher, доменные handlers, batch и scheduled approvals ещё не подключены.
- Отдельный ASGI entrypoint, изолированные файлы секретов, выключенные по умолчанию флаги, host/Origin/CSRF, host-only cookies, no-store/CSP, ограничение тела 64 KiB. Добавлен необязательный Compose overlay и шаблон отдельного nginx virtual host; production не изменён.
- Пароль scrypt N=131072/r=8/p=1; два параллельных вычисления и очередь 10. Отмена HTTP-запроса не освобождает вычислительный слот до завершения потока. TOTP проверен по RFC6238, seed шифруется AES-GCM с AAD principal UUID. Счётчик одноразового кода и версии challenges проверяются под блокировкой. Предусмотрены enrolment/recovery/password reset/step-up и CLI init/recover. Recovery не выдаёт прикладную сессию до нового MFA. Ротация ключей, повторные приглашения, все лимиты/отказы и полная эксплуатационная проверка ещё требуют завершения T005–T007.
- SQL-назначения административных ролей сериализуются на singleton и principal locks; есть защита последнего бессрочного суперадминистратора и запрет изменения собственной роли. Проверена конкурентная попытка снять два последних назначения. Runtime API выдачи временных grants ещё не подключён.
- Страницы входа/активации/сброса пароля, текущего доступа, встреч, пользователей, администраторов и аудита. Существующие страницы ещё не покрывают все требуемые поля/фильтры/сортировки/действия. Полная браузерная приёмка T036 не выполнялась.
- Пользовательская проекция показывает назначенный тариф и состояние личной подписки. Суммы подтверждённых счетов и наблюдаемых возвратов доступны только billing.read; корпоративные платежи не попадают в личные итоги участника. Это ещё не общий resolver возможностей и не управление каталогом/подписками/акциями.
- Для совместимости со старыми public RLS-функциями user projection использует фиксированный `pg_catalog,public,pg_temp`; все собственные отношения квалифицированы схемой. Bootstrap запрещает runtime-ролям CREATE в доверенной public schema; прямые SELECT системной роли на users/payment tables не выдавались.

Подтверждённые проверки на изолированном PostgreSQL 17, только синтетические данные:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_operations.py tests/integration/test_system_admin_authority.py tests/integration/test_system_admin_security.py -x -q
# 49 passed: операции + прежняя граница доступа до последующих auth-миграций.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_login.py tests/integration/test_system_admin_management.py tests/contract/test_system_admin_app.py -x -q
# 14 passed: сброс пароля, последний администратор под гонкой, вход и HTTP boundary.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_login.py tests/integration/test_system_admin_management.py tests/integration/test_system_admin_operations.py tests/integration/test_system_admin_authority.py tests/integration/test_system_admin_security.py tests/contract/test_system_admin_auth.py tests/contract/test_system_admin_app.py tests/integration/test_rls_maintenance_context.py tests/unit/test_rls_tenant_context.py -x -q
# 92 passed: версия до добавления user projection и почтовых HTTP-маршрутов.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_users.py tests/integration/test_system_admin_login.py tests/integration/test_system_admin_security.py tests/contract/test_system_admin_app.py -x -q
# 35 passed: user projection, финансовая изоляция и миграции до 0091 включительно.
```

Не было реальных писем, приглашений, платежей или чтения пользовательского содержимого. Полный CI, нагрузка, Temporal/MinIO сценарии, converge и PR ещё предстоят. Контрольная проверка origin/master не показала новых исходных коммитов относительно базы ветки на этом этапе.

## Продолжение: административные формы и защищённая расшифровка

Добавлена миграция `0092_system_meeting_content`. Новые задачи не отмечались завершёнными: это продолжение T003–T013, все оставшиеся требования сохраняются.

- Исправлена нестабильная выдача сессии: обе SQL-функции фиксируют один момент времени для issued/MFA/interaction/12-hour expiry. Проверяются точное равенство границ, успешный вход и enrolment.
- Работают HTTP и формы приглашения/повтора, изменения назначения, временных объектных прав и отзыва. Результат отправки хранится на challenge; разрыв после отправки оставляет `sending` с понятной неопределённостью. Повтор отзывает старые ссылки, ограничен по частоте. Реальных писем не отправляли.
- После ожидания блокировки повторно проверяется свежесть MFA в административных изменениях. Изменение роли приглашённого не активирует его автоматически.
- Отдельные POST-маршруты контекста разбора, чтения и проверки текущего права. Контент требует role/grant + конкретную встречу + case + ранее закоммиченный аудит. RLS закрывает текст при отзыве или начале удаления, даже при permissive PUBLIC policy. Не выдаются downloads/provenance или полный SELECT таблиц.
- Расшифровка использует общий `effective_processing_result_query`, последнюю принятую неизменяемую ревизию и 100 фрагментов на страницу. Переход страниц закреплён за результатом; смена версии даёт конфликт. Поиск идёт через POST, `%` трактуется буквально. JS использует textContent и очищает содержимое при закрытии/скрытии вкладки/отзыве; запоздалый ответ поиска не заменяет более новый.
- Bootstrap и старт процесса проверяют обязательные restrictive RLS gates новых таблиц. Необязательная почтовая конфигурация добавлена в отдельный Compose service. Обычное приложение не получает системные секреты.
- Запуск созданной попытки вынесен из HTTP-обработчика в общий `processing/dispatch.py`. Пользовательский маршрут использует прежнее поведение; общая функция сохраняет WORKFLOW_STARTED до Temporal. Административные доменные handlers и dispatcher пока не подключены.

Проверки на одноразовом PostgreSQL 17:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_content.py tests/integration/test_system_admin_users.py tests/integration/test_system_admin_login.py tests/integration/test_system_admin_management.py tests/integration/test_system_admin_operations.py tests/integration/test_system_admin_authority.py tests/integration/test_system_admin_security.py tests/contract/test_system_admin_auth.py tests/contract/test_system_admin_app.py tests/integration/test_rls_maintenance_context.py tests/unit/test_rls_tenant_context.py -x -q
# 104 passed, до последующих CSRF/UI исправлений и общего dispatch extraction.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/contract/test_system_admin_app.py tests/integration/test_system_admin_login.py tests/integration/test_system_admin_management.py tests/integration/test_system_admin_content.py -x -q
# 23 passed, включая стабильный CSRF для нескольких вкладок.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_processing_attempts.py tests/integration/test_media_revision_reprocess.py tests/contract/test_processing_status_contract.py -x -q
# 36 passed после вынесения общей отправки в Temporal.
```

Playwright/установленный Chrome, отдельный локальный HTTPS harness с синтетической почтой и существующими тестовыми встречами: пароль → TOTP → консоль; приглашение → почтовый сервис принял; причина разбора → текст с таймкодами; literal `%` → пустая выдача; поиск фрагмента → совпадение; экран 375×812; отзыв сессии → очистка и возврат ко входу <=15 секунд. При первом отзыве найдена JS-гонка обновления отсоединённой страницы; исправлена и повторно проверена: переход ко входу выполнен без JS pageerror. Это частичная браузерная проверка, не полная T036/доступность/10 support cases. Снимок узкого экрана сохранён локально вне git.

Ruff, `node --check`, документный validator и project governance проходят. Полный CI, нагрузка/60min outage, Temporal/MinIO административные сценарии, billing/каталог/промокоды, converge и release-ready PR остаются незавершёнными. Нельзя выдавать этот участок за готовую Feature 254.

## Продолжение: выполнение команд и история встречи

Миграции `0093_system_domain_lineage` и `0094_system_meeting_overview` добавляют привязку существующих предметных процессов к системным операциям и ограниченные проекции истории. Это промежуточная реализация T003/T010/T013/T014/T015; фича и PR ещё не готовы к выпуску.

- Исправлено тестовое окружение maintenance: оно воспроизводит штатные public grants bootstrap, сохраняя RLS и настоящий отдельный login. Первое падение проверяло отсутствие grants, а не новый trigger.
- `processing_workflows` и `meeting_deletion_requests` содержат неизменяемую ссылку на системную операцию. Создание проверяет claim, target, domain_ref и тип команды; системное удаление не допускает подстановки product user/device ни при INSERT, ни при UPDATE.
- Служебный процесс читает только узкий список системных команд. Claim и предметная запись фиксируются вместе до внешнего эффекта. При откате до commit операция остаётся в очереди и может быть отменена отзывом полномочий. После фиксации повтор наблюдает тот же domain_ref, не создавая новую обработку/удаление.
- Запуск обработки использует общее ядро допуска и dispatch. Оригинальные owner/device нужны только для исполнения существующего pipeline; инициатор закреплён через system_operation_id. Ошибки допуска сохраняются разрешёнными кодами. Старый опубликованный результат остаётся в БД.
- Удаление использует существующую saga, отчёт и purge reconciler. Отзыв browser session не мешает завершить уже зафиксированное удаление. Состояния резервных и локальных копий не сводятся к безусловному успеху.
- Добавлены HTTP preview/commit/status и формы причины, последствий, свежего MFA, подтверждения и просмотра состояния. Повтор после потерянного ответа сохраняет idempotency key. Прямой произвольный callback/SQL/type запрещён.
- Добавлена карточка метаданных, отдельные постраничные версии и попытки обработки, времена, технические причины и состояния отчёта удаления. Проекции не содержат заголовок, расшифровку, storage keys или provider credentials.

Проверки:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_operations.py tests/integration/test_system_admin_security.py tests/integration/test_system_admin_login.py tests/integration/test_processing_attempts.py tests/integration/test_meeting_deletion_workflow.py -x -q
# 58 passed: команды/claim, реальная предметная обработка с FakeTemporal, удаления,
# crash до commit, отзыв до/после эффекта, старые processing/deletion сценарии; схема до 0093.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_login.py tests/contract/test_system_admin_app.py -x -q
# 16 passed: HTTP preview/commit/idempotency/status, отключённые команды, cookie/CSRF.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_login.py tests/integration/test_system_admin_security.py -x -q
# 28 passed: миграция 0094, downgrade, bootstrap и вход.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_content.py tests/integration/test_system_admin_login.py -x -q
# 12 passed: карточка, HTTP история и 105 попыток без пропусков/дубликатов;
# отсутствие содержимого и storage secret в метаданных, отзыв доступа.
```

Это не реальный Temporal/MinIO прогон и не полная браузерная приёмка новых форм. Аудио/экспорт/retained diagnostics, account closure, полный billing/тарифы/акции, телеметрия, метрики и общие release gates остаются обязательной работой. Реальные письма, платежи и production действия не выполнялись.

## Продолжение: аудио и серверная проверка выдачи

Миграция `0095_system_media_access`, модуль `system_admin/media.py`, HTTP и обычный браузерный плеер реализуют выдачу сохранённого канонического аудио. Подробности операторской конфигурации — в quickstart. Реальный файл пользователя не использовался.

- Сервер проверяет существующий canonical validator `_is_stored_review_m4a`, согласованность normalization job/revision/artifact и размер объекта. Прямой storage URL не выдаётся; media source возвращается только внутреннему обработчику после сохранённого аудита.
- Одноразовая активация на 60 секунд, проверенный объектный grant/session/case, закреплённый хеш метаданных источника. После первого GET отдельная host-only HttpOnly cookie разрешает Range только этой сессии на 15 минут. Подмена источника, удаление, отзыв и чужая сессия закрывают доступ; открытый поток проверяется перед каждым блоком.
- Одновременно не более четырёх потоков на процесс; переполнение 429/Retry-After. Новые tickets не более десяти в минуту на principal. Служебный worker удаляет истёкшие технические tickets; неизменяемый аудит сохраняется.
- Запись `playback_stream_prepared` или `download_stream_prepared` в существующем журнале имеет id системного audit event, без подстановки product actor. Отчёт удаления учитывает факт подготовленной выдачи, не обещая доказательство полного скачивания.
- Отдельная необязательная конфигурация MinIO reader и GetObject-only policy. Writer credential не передаётся console. Отсутствие reader сохраняет работоспособность остальных страниц и закрывает аудио.

Проверки 2026-09-07:

```sh
apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_media.py tests/integration/test_system_admin_operations.py tests/integration/test_system_admin_security.py tests/integration/test_system_admin_content.py tests/integration/test_system_admin_login.py -x -q
# 46 passed до добавления ограничения одновременных потоков.

apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_media.py -x -q
# 3 passed после ограничения: PostgreSQL + synthetic storage, аудио-only grant
# не выдаёт download, Range/attachment/416, повтор активации, смена источника,
# отзыв между блоками, связь system actor и egress audit, 429 и освобождение слота.
```

Браузер Chrome через Playwright: полный password/TOTP вход; канонический синтетический M4A 40 секунд успешно воспроизводится (readyState=4, advancing currentTime), перемотка к 10 секунде; окно аудио визуально проверено на 375×812. Карточка метаданных показывает сохранённую историю. Предпросмотр команды показывает введённую причину, последствия, срок, MFA и отдельное подтверждение. Снимки вне git. Это частичная UX проверка, не T036 и не полная доступность/нагрузка; production Temporal/MinIO, exporter/retained diagnostics и остальные задачи остаются обязательными.

## Продолжение: версии тарифов и перенос исторических условий

Добавочная миграция `0096_billing_version_pins` сохраняет старые ID и UNIQUE(plan_code,version), добавляет идентичность тарифа, месячную/годовую цены одной версии, возможности и display terms по ограниченной схеме, nullable ссылки подписки/paid grants. Новые продажи миграцией не включаются. Опубликованная или закреплённая экономика защищена триггером БД; месячная и годовая цены блокируются через одну версию. Согласие recurring_authority_version не заменяется новым schedule_version.

`billing/catalog_migration.py` сопоставляет только точную версию из подтверждённых invoice/operation/grant, проверяет исходную полную цену отдельно от разовой скидки, интервалы и доказанное продолжение реферальными днями. Нет подстановки последнего предложения. Неоднозначная/несовпавшая история сохраняется как legacy_pinned с исходными snapshots и причиной разбора. Пакеты имеют watermark application_version каждой подписки; незавершённая транзакция откатывает и данные, и watermark. Изменение экономических полей также снимает watermark триггером. CLI `scripts/backfill_billing_catalog.py` допускает только настоящий maintenance login и выводит только счётчики, без snapshots/секретов.

Проверки на изолированном PostgreSQL 17:

- Исходный тест нового модуля сначала завершился ошибкой отсутствующего `catalog_migration`.
- `test_system_admin_billing.py` + `test_system_admin_security.py` + `test_renewal_charge.py`: **36 passed** до добавления schedule invalidation.
- После schedule invalidation: billing + billing_domain + system_admin_users: **16 passed**.
- Итоговый billing, включая вызов реального операторского CLI с отдельной ролью: **3 passed**. Проверены точная старая цена, разовая скидка, повтор/catch-up, отсутствие изменения денег и согласия, legacy mismatch, два периода одной версии, запрет редактирования условий и typed capabilities.

Это основание T016, а не завершённое управление тарифами. Общий resolver возможностей, редактор тарифов, переходы и акции остаются обязательными следующими задачами. Полный CI, проверка конкурентной публикации и полный миграционный rehearsal ещё не выполнены.


### Подключение точной цены к продлению и защита отправки

`plan_due_renewals` больше не выбирает последнюю разрешённую цену. При необходимости выполняет точную историческую привязку под блокировкой подписки; создаёт invoice из pinned version/price, включая schedule_version отдельно от согласия. Закрытие старого предложения не меняет цену продления. Неоднозначные условия блокируют создание счёта. Проверка реальным PostgreSQL обнаружила прежнюю ошибку flush: invoice мог вставляться раньше operation; порядок исправлен без промежуточного commit.

Перед запросом провайдеру сохраняется state=processing и аудит claim. Тот же вызов повторно получает блокировки и свежие owner/consent/schedule/payment method; обычный повторный worker уже не может отправить POST для processing. Сбой после commit оставляет обязательную сверку, а не новую попытку оплаты. Проверены независимое чтение committed claim перед имитатором провайдера, потеря процесса, отсутствие повторной отправки и отзыв согласия другой транзакцией между claim и повторной проверкой. Также исправлено чтение ORM-атрибутов после rollback в раннем возврате. Проекция Free на точной границе срока не увеличивает schedule_version и не отменяет запланированный платёж.

Денежные колонки расширены до bigint без изменения значений; новые цены ограничены точными безопасными целыми значениями. В T037 остаётся проверка длительности блокировок/переписывания таблиц на восстановленной базе.

Тестовый RLS helper теперь переиспользует migration-owned maintenance role только после проверки адреса одноразового loopback PostgreSQL. Эта роль сохраняется до удаления контейнера, поскольку несколько тестовых БД могут ссылаться на неё. Первоначальный общий прогон остановился после 42 passed на повторном создании роли; после исправления отдельный RLS набор дал 10 passed, а общий billing набор — 68 passed. Проверка code-reviewer и ponytail-review выполнена: сохранены существующий планировщик/ledger/провайдер, новые зависимости и универсальные слои не добавлены.

Старые неотправленные scheduled operations без schedule_version переводятся в manual_resolution; совместимый перенос/пересоздание их планов входит в незавершённый T018 и блокирует release. Уже отправленные платежи продолжают прежнюю сверку. Потерянный provider ID требует существующего ручного разбора; интерфейс этого разбора входит в T021. Реальный платёжный провайдер не вызывался.

## Продолжение: неизменяемые индивидуальные назначения

Миграция `0097_billing_access_adjustments` добавляет отдельные access adjustments и append-only revocations. Назначение не создаёт paid grant/invoice и не переписывается при отзыве. Сохраняются объект, пользовательская область, источник, версия тарифных прав, срок, причина и ссылка на начатую системную команду для admin source. RLS позволяет обычному пользователю читать только свою область и не позволяет создавать назначения; дополнительные restrictive policies закрывают прямой system credential. Административные записи принимаются только от настоящего maintenance login с соответствующей claimed billing operation; HTTP/worker транспорта этих команд пока нет.

`billing/admin_grants.py` проверяет типы и источники, идемпотентность, календарные дни в закреплённой timezone, непересечение точных правил под workspace lock. Расчёт сохраняет обязательный запрет, ставит индивидуальный deny выше allow, отключает unlimited при exact processing limit, возвращает processing extras отдельными источниками по ближайшему expiry. Подарочный интервал требует конкретной опубликованной версии и явного append/overlay; append не может пересечь прежний период доступа. Исходные счётчики, оплаченные даты и счета не изменяются. Разрушительный откат после ledger записей запрещён.

Проверки:

- Первый запуск тестов нового сервиса получил ожидаемую ошибку отсутствующего `admin_grants`.
- На PostgreSQL проверены повторы и конфликт ключа, отзыв отдельной записью, отсутствие новых счетов, параллельные exact limits (ровно один принят), порядок дополнительных квот, hard deny, expiry, сохранение committed/reserved counters, недоступность записи обычной роли и чужого subject, подарочная версия/expiry/revoke и календарный DST.
- После исправления устаревшего тестового списка миграций общий набор access + billing + RLS + system security дал **39 passed**. Проверка отката теперь использует настоящую цепочку Alembic вместо ручного списка. Отдельно доказано, что display state `not_applicable` не разрешает удалить сохранённый финансовый snapshot.

T017 остаётся открытой: ledger и расчёт пока не подключены ко всем клиентам и реальному распределению/расходу резервов; разные виды окон, миграция старого расхода безлимита, lineage после объединения аккаунтов и сдвиг расписания подарком остаются обязательными. T020 — предпросмотр, командный транспорт, HTTP и интерфейс назначений — также не завершена. Эти ограничения блокируют выпуск, а не являются разрешённым сокращением функции. Полный CI и production действия не выполнялись.

## Общий расчёт и распределение квоты обработки — T017, продолжение 2026-09-07

Сохранён предыдущий проверенный участок назначений: `cd3c81ed5` (`feat(billing): добавить журнал назначений прав и квот`). Следующий участок подключает коммерческий расчёт к фактическому запуску и повторной обработке, сохранению результата и резервированию места для канонического аудио.

- `resolve_entitlements` читает закреплённую версию активной подписки и применяет журнал назначений. Старые известные тарифы используют прежние условия; произвольный активный код без закреплённых capabilities не получает выдуманные права. Истечение оплаченного периода возвращает бесплатные условия. Отдельная квота хранения прибавляется один раз, фактический расход не изменяется.
- Миграция `0098_processing_quota_allocations` добавляет распределение каждого резерва между базовым лимитом и конкретными источниками дополнительных секунд. Прежние резервы переносятся как базовые без обнуления счётчиков. Секундные счётчики расширены до bigint. Связи с резервом, окном и назначением проверяют совпадение пространства. Системная роль не имеет прямого доступа к новой таблице.
- Дополнительные секунды выделяются после базовых, в порядке ближайшего окончания и ID источника; частичный расход соблюдает этот порядок. Расход дополнительной квоты учитывается за всю жизнь источника, включая прежние окна. Повтор ключа с другой продолжительностью отклоняется.
- Платная/пробная безлимитная обработка тоже получает резерв и измеряет фактические секунды. Снижение до конечного лимита учитывает прежний расход. Уже принятый активный резерв сохраняет распределение после истечения/отзыва подарка, снижения коммерческого лимита и календарной границы. Истечение самого резерва по-прежнему требует повторного допуска; обязательные запреты не отменяются.
- При освобождении и частичном потреблении счётчик резервов пересчитывается из оставшихся активных резервов. Воспроизведена и исправлена прежняя ошибка: после нового допуска очистка истёкшего резерва могла повторно вычесть его секунды и занизить резерв другой обработки. Изменения журнала и дедупликация источников сериализуются блокировкой пространства; ORM перечитывает заблокированные строки после ожидания.
- Ограниченная роль `twobrain_rec_media` получает только SELECT каталога и назначений в своём контексте, чтобы использовать общую ёмкость при сохранении аудио. Обновлены production bootstrap и существующие фикстуры точных ролей; широкого DML на эти таблицы ей не добавлено. Инвентарь RLS включает каталог, назначения и allocations.
- Прежний тест отсутствия резерва у платного пользователя изменён на проверку измеренного расхода. Проверка старых account-linking policies теперь выбирает PUBLIC policies: отдельные разрешения системной authority проверяются собственным набором system-admin security, а не подменяют старую матрицу.

T017 не объявляется завершённой. Ещё требуются все предусмотренные виды окон, перенос ранее не измерявшегося безлимитного расхода, сохранение происхождения данных при объединении аккаунтов и полная проекция used/reserved/remaining для интерфейсов. Все потребители функций/прав, покупка и клиенты относятся к продолжающейся T018; административные команды назначения и формы — T020. Production-миграция, полный CI и PR готовности не выполнялись.

Проверка этого участка: **111 passed, 2 skipped** на изолированном PostgreSQL 17. Набор: `test_system_admin_entitlements`, `test_playback_normalization_postgres`, `test_playback_normalization_finalize`, `test_rls_postgres_policies`, `test_system_admin_security`, `test_processing_pickup`, `test_processing_result_idempotency`, `test_processing_attempts`, unit `test_billing_entitlements`. Пропущены старые проверки `test_runtime_role_bootstrap_is_idempotent_and_verifies_privileges` и `test_rls_verifier_reuses_existing_role_without_global_state_mutation`: они требуют отсутствия заранее созданных runtime-ролей, а миграция системной области уже создаёт maintenance role. Эти пропуски не считаются PASS и не заменяют обязательную проверку bootstrap перед выпуском. Поведение новых назначений/резервов проверено также через настоящий app login, сохранение аудио — через настоящий ограниченный media login. Реальные данные и внешний платёжный провайдер не использовались.

## Совместимость заранее запланированных продлений — T018, 2026-09-07

Планировщик умеет дополнить старую scheduled operation отсутствующими ссылками на тариф/цену и версией расписания до наступления срока платежа. Для этого совпадают исходный финансовый снимок операции, неизменённый снимок счёта и точные закреплённые условия подписки; проверяются плательщик/версия согласия, неизменённая дата, ключ периода, pending invoice и отсутствие provider ID. Денежные поля и snapshot счёта не переписываются. Связь добавляется к операции с отдельным `renewal.legacy_schedule_pinned`; повтор не создаёт второй счёт/аудит.

После наступления срока автоматическое дополнение запрещено: старый writer мог отправить POST и потерять транзакцию до сохранения provider ID. Такие строки требуют сверки, как и несовпадающие цена/согласие/частично заполненные pins. Отсутствие provider ID не объявляется доказательством отсутствия списания. Старые writers должны быть остановлены на предусмотренном миграционном барьере.

Проверено **29 passed**: реальный PostgreSQL `test_system_admin_billing`, `test_renewal_lifecycle`, unit `test_renewal_charge`. Проверены сохранность исходного invoice snapshot, повтор планирования, отрицательные случаи на сроке платежа, изменённой цене/согласии, имеющемся provider ID и несогласованных снимках. Платёжный провайдер не вызывался. Полная T018 остаётся открыта: все произвольные тарифы/потребители, единый порядок блокировок и сверка неоднозначных старых операций ещё не завершены.

Для операций продления устранён обратный порядок блокировок: планирование, проверка границы периода и списание используют advisory lock пространства, затем строку пространства, затем подписку; строки операций берутся после них. Первичная выборка кандидатов не удерживает строковые блокировки, пакет обрабатывается в устойчивом порядке пространств. После ожидания перечитываются состояние подписки/плательщика и границы допуска. Это согласует ветвь продления с domain lock покупки и подтверждения платежа; полный аудит остальных billing writers остаётся частью T018.

Проверено **62 passed**: system-admin billing, renewal charge/lifecycle, checkout YooKassa, billing webhooks, entitlements, billing safety и renewal resolution contracts. Новый PostgreSQL-сценарий держит пространство в одной транзакции и запускает продление в другой: ожидающее продление не захватывает подписку раньше пространства (`FOR UPDATE NOWAIT` отдельной транзакции остаётся доступным); после снятия блокировки операция завершается. Проверки lost response/consent revoke после durable claim также прошли. Реальный провайдер не вызывался.

## Оплаченные возможности произвольного тарифа — T018, 2026-09-07

Подтверждение первоначальной оплаты и продления принимает произвольный поддерживаемый код по двум согласованным финансовым снимкам счёта/операции. Сверяются точная версия, price ID, возможности, период, суммы, валюта и плательщик. Отличия типов JSON (например, `1` вместо `true`) не считаются совпадением. Нельзя выбрать последнюю опубликованную версию вместо оплаченной. Закрытие продаж/retirement не отменяет обязательство по прежнему счёту.

Платёж сохраняет plan version в журнале оплаченных интервалов и version/price в подписке. Для новых управляемых RUB-тарифов календарный месяц/год вычисляется в Europe/Moscow, для исторических счетов сохранено прежнее UTC-поведение. Расписание меняет отдельную версию; повтор подтверждения не продлевает срок и не увеличивает её. Устаревший schedule у нового тарифа переводится на сверку. Старый счёт personal после управляемого тарифа очищает несовместимые pins до промежуточного flush и сохраняет прежние условия оплаты.

Планировщик и проекция окончания оплаченного периода охватывают новые коды. Проверен путь `research_plus`: месяц/год → скидка на первую оплату → доступ с конечной квотой 24000 секунд → повтор оплаты → закрытые продажи → продление по полной прежней цене → Free на границе до подтверждения → повтор подтверждения продления. Calendar case: 31 января по Москве при ещё 30 января UTC; месяц заканчивается 29 февраля високосного года по Москве. Подтверждение нового тарифа также проверено под настоящим login `twobrain_rec_app` с пользовательским контекстом, без расширения его прав.

Проверено **74 passed**, без пропусков: `run_local_postgres_tests.sh --focused` для integration `test_system_admin_billing`, `test_renewal_lifecycle`, `test_checkout_yookassa`, `test_billing_webhooks`; unit `test_billing_entitlements`, `test_renewal_charge`; contract `test_billing_safety_contract`, `test_renewal_resolution`. Среда: изолированный PostgreSQL 17, только синтетические счета/методы оплаты, без обращений к реальному провайдеру. Ruff, governance и проверка документов прошли. Полная T018 остаётся открытой: покупательские формы/API/macOS и другие потребители, оставшиеся writers/блокировки и миграционный барьер ещё требуют реализации. Полный CI, PR и production-проверки не выполнены.

## Общая блокировка покупки и сверки — T018, 2026-09-07

Существующая блокировка продления перенесена в общий `billing/subscription.py::lock_billing_subscription`: advisory lock области → строка пространства → подписка. Подтверждение первоначального платежа, продолжение/создание покупки, отмена/включение автопродления, отзыв платёжного метода, сверка provider events и обе транзакции Temporal activity теперь берут её до строк счёта/операции. Проверки ранее загруженных ORM-строк перечитывают актуальные значения после ожидания.

Сверка первоначальных платежей сначала выбирает кандидатов без блокировок, затем обрабатывает их в устойчивом порядке пространств и повторно проверяет исходные критерии под блокировкой. Изменённый во время ожидания кандидат не вызывает провайдера. Конкурентный PostgreSQL-сценарий удерживает пространство, запускает общий helper/подтверждение оплаты/сверку и проверяет возможность взять подписку, операцию и счёт отдельной транзакцией через NOWAIT. Только после освобождения пространства обработка завершается.

Отрицательная проверка `1` вместо `true` теперь явно сохраняет испорченный JSON в БД через `flag_modified`: равенство Python-словарей раньше могло оставить подмену только в памяти ORM. Это проверка реальных persisted snapshots при повторном чтении, а не только объектов текущей сессии. Тестовые имитации запросов сверки обновлены под общий helper; полномочия и фактические эффекты по-прежнему проверяются PostgreSQL-сценариями.

Оставшиеся writers реферальных дней, доплат за хранение и административных переходов ещё требуют согласования. Этот участок не завершает T018 и не является релизной приёмкой.

Проверено **103 passed**, без пропусков: `run_local_postgres_tests.sh --focused` с integration `test_system_admin_billing`, `test_renewal_lifecycle`, `test_checkout_yookassa`, `test_billing_webhooks`; unit `test_billing_entitlements`, `test_billing_observation`, `test_billing_renewal_workflow`, `test_renewal_charge`; contract `test_billing_safety_contract`, `test_renewal_resolution`, `test_billing_security`. Ruff, governance, document validator и diff check прошли. Только изолированный PostgreSQL 17 и синтетические данные; внешних платежей не было. Полный CI/PR/release gates остаются впереди.

## Единые остатки квот и страница использования — T017/T018, 2026-09-07

Страница `/billing/usage` использует тот же расчёт базовой и дополнительных квот, что допуск обработки. Показаны фактический расход текущего месяца, живые резервы и доступный остаток каждого источника; дополнительные секунды расходуются за весь срок назначения и не восстанавливаются при смене месяца. Безлимит отличён от нулевого остатка. Ёмкость хранения учитывает индивидуальные назначения. Чтение не освобождает резервы и не меняет счётчики. Shared lock пространства до чтения прав удерживает согласованность источников и счётчиков с коммерческими/usage writers. Количественные данные остаются скрытыми для member/corporate_owner.

Проверено на окончательном коде **89 passed**, без пропусков: `run_local_postgres_tests.sh --focused` для integration `test_system_admin_entitlements`, `test_processing_pickup`, одного сценария продолжения checkout из `test_web_owner_session_context`; contract `test_billing_ui`, `test_billing_accessibility`. Дополнительно маршруты оплаты из `test_web_owner_session_context -k 'billing or checkout or plan_catalog'`: **6 passed, 63 deselected**. Предыдущий более широкий запуск был прерван timeout соединения asyncpg до входа в billing route и не считается успешным; этот сценарий отдельно прошёл в окончательном наборе.

Браузер Chrome на изолированном synthetic PostgreSQL: 1440×1000, 390×844, reflow 720×500 (эквивалент 200% при исходной ширине 1440); возврат к оплате и назад, горизонтальная прокрутка таблицы клавиатурой, ноль ошибок/предупреждений консоли. Исправлен обнаруженный дефект min-width у grid item: теперь scrollWidth страницы равен viewport, прокручивается только таблица. Снимки хранятся вне git; временные процесс, браузер и контейнер удалены. Это профильная проверка страницы, не вся браузерная приёмка F254. Другие типы окон и остальные потребители прав ещё требуют реализации. Полный CI, PR и выпуск не выполнены.

## Профиль API использует назначенные возможности — T018, 2026-09-07

`GET /api/v1/auth/me` больше не определяет безлимит и ёмкость по коду personal. Общий resolver возвращает название закреплённой версии, источник free/trial/paid/gift и срок; API показывает фактические boolean-возможности, форматы экспорта и остатки общего usage ledger. Индивидуальный запрет отражается сразу, отзыв назначения возвращает базовые условия, уже измеренный расход не обнуляется. Новые поля добавлены без удаления старых; количественные значения и финансовые сроки member не раскрываются. Commercial flags не заменяют обязательные серверные политики. Новый активный код без точного pin возвращает 503 с безопасным кодом, истёкший платный период возвращает Free.

На PostgreSQL/HTTP проверены произвольный оплаченный тариф, подарок того же тарифа, member корпоративного пространства, дополнительные секунды/байты, запрет скачивания, частичный расход/резерв, отзыв всех назначений, cutoff и потеря pin. **99 passed**, без пропусков: integration `test_system_admin_entitlements`, `test_tenant_authorization`, contract `test_auth_contracts`. После добавления названия из закреплённой версии отдельно проверен окончательный resolver/API: **35 passed**, без пропусков, integration `test_system_admin_entitlements`, `test_system_admin_billing`. Ruff и diff check прошли. Это не завершает T018: остальные страницы покупки/оплаты, writers, миграция и приёмка остаются обязательными. В Swift-исходниках не найдено отдельного consumer `plan_code`/`processing_unlimited`/`auth/me`; встроенный кабинет получает серверный HTML, его проверка не заменяется API-тестами.

## Текущий тариф, закреплённая цена и возобновление — T018, 2026-09-07

Страницы `/billing` и `/billing/subscription` читают общий resolver: произвольное название тарифа, назначенные возможности, конечные квоты, подарок отдельно от платной подписки. Сводка показывает фактический доступный остаток и не обещает безлимит всем платным тарифам; member/corporate_owner не видят количественный расход. Доступ по подписке не назван суммой реально оплаченных дней: исторические бонусы могли увеличить этот срок. Ошибочный pin даёт безопасную страницу 503/no-store для сводки, использования и управления подпиской.

Чтение закреплённых условий выделено из существующего renewal planner в `read_pinned_subscription_catalog`; оно не запускает backfill и не выбирает цену действующих продаж. Планировщик сохраняет прежний backfill перед этим чтением. Закрытая/retired версия остаётся действующей для исторической подписки; цена без подтверждённого pin показывается неизвестной. При подарке поверх другой платной подписки название в управлении относится к оплаченной подписке, а текущий назначенный доступ показывается в сводке.

Возобновление сохраняет ссылки показанной версии/цены, версию расписания, точную дату и версию согласия в форме. POST повторно проверяет их под общей блокировкой пространства/подписки; отсутствующие или устаревшие поля возвращают conflict. При неизвестных условиях форму не показываем, прямой POST не включает автопродление. Старые открытые формы требуют обновления страницы. Повтор успешной отправки идемпотентен; ни счёт, ни операция платежа не создаются. Проверен перенос срока между чтением и отправкой при неизменной версии согласия.

Проверки: **119 passed**, без пропусков — PostgreSQL integration `test_system_admin_entitlements`, `test_system_admin_billing`, `test_renewal_lifecycle`; unit `test_renewal_charge`; contract `test_billing_ui`, `test_billing_accessibility`. После уточнения отображения суммы/разметки формы — отдельно **59 passed** шаблонных проверок. Дополнительно `test_web_owner_session_context -k 'billing or checkout or plan_catalog'`: **6 passed, 63 deselected**. В первом проходе новый сценарий потери pin использовал уже устаревшую версию согласия после успешного возобновления и корректно получил conflict; исправлена тестовая посылка, окончательный сценарий проверяет отказ по недоступным условиям.

Chrome, синтетический тариф «Команда Плюс», изолированный PostgreSQL: сводка/подписка на 1440, 1024, 720 (reflow эквивалент 200%) и 390 px; ширина содержимого равна доступной ширине без обрезания; клавиатурой флажок → Tab → Enter → подтверждённое возобновление, затем отмена и возврат к сводке. Ошибок/предупреждений консоли нет. Синтетический способ оплаты не связан с провайдером, worker платежей не запускался. Это профильная проверка двух страниц, не полная приёмка фичи. Снимки вне git.

T018 остаётся открытой: каталог для покупки, создание заказа, public offers, остальные потребители, writers рефералов/хранения и миграционный барьер ещё требуют реализации. Полный CI, push/PR и выпуск не выполнялись.

## Источник публичных предложений — T018, продолжение 2026-09-07

`read_public_catalog` собирает цены только по явно выбранному `BillingPlan.current_version_id`, при открытых продажах, опубликованной/effective версии и публичной аудитории. Месяц/год принадлежат одной версии; неполная или ошибочная пара не публикуется. Более новая неподключённая версия не меняет цену; admin/invitation предложения не появляются в публичном ответе. Закрытие продаж и exact effective_until исключают предложение. До первой управляемой публикации personal сохраняется прежнее чтение legacy-каталога. После такой публикации даже очищенный current pointer не оживляет старые legacy-продажи. Существующий reader personal для HTML использует этот общий источник.

На PostgreSQL проверены два опубликованных номера с выбором старого, переключение pointer, закрытие, временная граница, неполная цена, скрытые аудитории, legacy-переход и запрет возврата к старой продаже. Тот же публичный результат прочитан под настоящим `session_user=twobrain_rec_app` и пользовательским tenant context без расширения прав. **27 passed, 63 deselected**, без skipped: integration `test_system_admin_billing` и профильные billing/checkout/plan_catalog сценарии `test_web_owner_session_context`. Ruff и diff check прошли. Первый запуск выявил отсутствие legacy parent в очищенной тестовой fixture; тест теперь создаёт его явно, окончательный проход успешен.

Это read-проекция предложений, не разрешение выставить счёт. Формы выбора всех новых кодов и money admission ещё не подключены: новая покупка обязана повторно проверить отправленные точные условия до выставления счёта. Барьер совместимых writers и полная T018 остаются открытыми.


## Выбор и покупка управляемых тарифов — T018, продолжение 2026-09-07

Реализован общий путь списка/checkout/preview/start для публичного произвольного кода. Название, обе цены, квота и ёмкость берутся из выбранной версии; generic paid больше не означает безлимит. Текущий доступ на странице сравнения использует resolver. Таблица поддерживает произвольное число предложений, горизонтальную прокрутку с клавиатуры и мобильное представление. Активная платная подписка не заменяется новой покупкой.

expected_quote сравнивает показанный каталог и фактический выигравший discount до любых invoice/reservation/referral effect. Повтор ключа сверяет actor и исходную форму; одинаковый запрос восстанавливает прежний invoice после закрытия продаж, изменённый план/период/код отклоняется. Общий validate_paid_catalog проверяет исторические финансовые снимки перед отправкой и подтверждением managed платежа. Legacy personal continuation сохранён. Миграция 0099 добавляет только узкое право блокировки каталога для настоящего app session_user/request context; UPDATE каталога приложению не выдаётся. План и выбранная версия удерживаются до commit счёта, сеть начинается после освобождения.

Проверки:

- Изолированный PostgreSQL 17 + HTTP: **144 passed**, без skipped, `/tmp/f254-checkout-complete.log`. Файлы: integration test_managed_checkout/test_system_admin_billing; unit test_initial_checkout_recovery/test_cabinet_audit_fixes; contract test_billing_security/test_billing_ui/test_billing_accessibility. Все новые HTTP-сценарии идут под `session_user=twobrain_rec_app`, NOSUPERUSER/NOBYPASSRLS/NOINHERIT, с явно отозванными INSERT/UPDATE/DELETE каталога. Provider заменён синтетическим; money requests не отправляются.
- Дополнительная проверка прежнего personal-каталога с новой формой: **1 passed, 14 deselected**, `/tmp/f254-checkout-legacy.log`. Настоящий app login, старые month/year записи, сохранение старого формата snapshot и точной цены 79000 копеек.
- Проверены month/year, missing/stale quote, смена опубликованного pointer/цены, expiry/close, обязательное согласие, неизвестный явный promo, изменение скидки/версии до оплаты, однократный резерв, параллельный одинаковый POST, подтверждение после закрытия продаж. Две отдельные PostgreSQL-транзакции доказывают, что close плана/версии ждёт admission, а после commit новый checkout уже видит закрытие. Отсутствующий context и прямое редактирование каталога запрещены.
- Ранние проходы не считаются PASS: тестовая настройка callback URL была неполной; в новой проверке контекста исправлено сравнение SQL NULL; в новом reader списка добавлен обязательный subject_user_id resolver; старые шаблонные fixtures приведены к существующей общей проекции. Итоговый набор выше выполнен после исправлений.
- Chrome через Playwright CLI, отдельная synthetic БД/ограниченная app role: 8 карточек, размеры списка 1440/1024/720/390; document.scrollWidth равен viewport. В таблице подтверждены focus и горизонтальная прокрутка ArrowRight до конца. Форма — 1440/720/390/320 без overflow; переход с выбранного тарифа, смена года на месяц, native required checkboxes, Space/Tab/Enter и POST с сохранением счёта. Повторное открытие показывает существующий платёж. Backend provider синтетический; переход браузера на его выдуманный путь ЮKassa завершился внешним 404 (одна внешняя console error), успешной реальной оплаты этот browser run не доказывает. В приложении до внешнего перехода ошибок консоли не было. HTTP/provider-double проверки выше подтверждают точные отправленные параметры.
- Просмотрены изображения `f254-plans-desktop.png` и `f254-checkout-mobile.png` в task visualization directory. Это только синтетический стенд. Browser session f254-checkout закрыта, harness остановлен, Docker graf-f254-checkout-qa удалён; прежние ресурсы других проходов не изменялись.

Ponytail review этого участка: используются существующие каталог, финансовый validator, право session_user, формы и провайдер; хеширование stdlib, без новых зависимостей/платёжных сущностей. Из формы/проекции убрано лишнее поле checkout_url, URL строится шаблоном из проверенного кода. T018 остаётся открытой: consumers хранения/рефералов/maintenance, полная миграция старых writers и общий жизненный цикл ещё не закончены. T019–T039 не объявляются завершёнными. Полный CI, push, PR и выпуск не выполнялись.


## Хранилище и публичное предложение — T017/T018, продолжение 2026-09-07

`/billing/storage` читает тот же resolver под shared workspace lock: управляемая подписка, подарок и индивидуальная ёмкость не подменяются прежними 250 MB. Member не получает финансовую страницу; потерянный pin даёт прежнюю безопасную 503. Только реальные варианты общего предела выше текущей доступной ёмкости показываются как возможное увеличение; цены дополнений по-прежнему не опубликованы и платёжный handler не выдумывается. Eligibility отделена от кода personal: нужна действующая платная подписка, при этом счёт за дополнение не создаётся без поддерживаемого ценового контракта.

`public/offers.py` использует read_public_catalog вместо своего выбора максимальной версии. Публичная landing-карточка остаётся ограниченной прежними утверждёнными рекламными/юридическими условиями personal; другие предложения доступны в кабинете. Для управляемой версии проверяются обещанные в карточке коммерческие возможности. Закрытый managed personal не возрождает старые legacy-цены. `/offer` при отсутствии этого публичного предложения больше не утверждает, что все платежи GRAF запрещены: показывает ссылку на точные условия в кабинете. Новые платёжные полномочия это не выдаёт.

Проверки: **60 passed** по entitlements/billing UI (`/tmp/f254-storage-assigned.log`); окончательный объединённый набор **92 passed**, без skipped (`/tmp/f254-public-storage-verified.log`): unit test_public_landing, integration test_managed_checkout/test_system_admin_entitlements, contract test_billing_ui. Отдельный новый HTTP-сценарий под настоящей app role и без browser cookie доказывает public request с zero tenant: утверждённый managed personal виден, после close старая legacy-пара больше не появляется, legal page не обещает глобально закрытый магазин. Owner paid/gift показывает 5 GB, member получает redirect, неверные pins — 503. Первый запуск всех public unit tests без PostgreSQL завершился setup error после пяти pure tests; подтверждённый результат — изолированный PostgreSQL-прогон 92 tests, не этот неполный запуск.

Остаются открытыми старые writers хранения/реферальных периодов и весь заявленный полный объём фичи. Полный CI/PR/release не выполнялись.

## Коммерческие права на реальные скачивания и экспорт — T017, 2026-09-07

Продолжение на базе `d4a58f5eb8771dd4ae8dd3799d91346f41269823`, lane high-risk-product. Подключены `audio_download`, `content_export` и разрешённые форматы к discovery, обычным/общим скачиваниям, прямому POST экспорта, созданию и повторной выдаче пакетов. Existing ACL/policy/deletion/revision/audit остаются обязательными; системный доступ суперадминистратора не переводится на коммерческие права пользователя. Подробный контракт — contracts/api.md, раздел «Коммерческие ограничения выдачи».

Новый `test_commercial_egress.py` использует настоящий session_user `twobrain_rec_app` без SUPERUSER/BYPASSRLS/INHERIT; HTTP/ACL/resolver/RLS не подменяются. Seed идёт через отдельную owner session, storage — существующий synthetic fake. Проверяются workspace/user deny, revoke, независимость прослушивания, точные форматы, общий доступ внутри/между пространствами, запрет готового пакета, отзыв во время подготовки, workspace lock и временная граница после чтения аудио.

Во время проверки найдены и исправлены два дефекта: shared route с анонимным user_id скрывал личное назначение получателя; незавершённый аудит удерживал KEY SHARE пространства и препятствовал отзыву в другой транзакции. Ожидание второго дефекта подтверждено через pg_stat_activity/pg_blocking_pids на одноразовой БД: app session idle in transaction блокировала writer с Workspace FOR UPDATE. Writer отменён только в этом тестовом контейнере; процесс завершился FAIL, контейнер удалён runner. Это не PASS. Аудит теперь коммитится до render, затем выполняется финальная проверка.

Промежуточные прогоны: прежние artifact/transcript регрессии — 51 passed до последних дополнений. Первый новый прогон — 3 passed и ошибка URL в новом тесте пакета; исправлен `/download`. Следующий — 4 passed и воспроизведённое ожидание отзыва. Затем — 6 passed и неверный synthetic fixture внешнего адресата: обычный внутренний grant не является подтверждённым внешним приглашением; fixture дополнен verified ExternalIdentity и metadata принятого приглашения без ослабления runtime ACL. Эти неуспешные наборы не считаются общим PASS.

Браузер Chromium/Playwright CLI, отдельные synthetic PostgreSQL/HTTP harness: md-only назначенный тариф, открытие меню/диалога с Enter, единственный Markdown, disabled TXT copy, успешное фактическое скачивание `.md` (download failure=null), закрытие диалога и возврат фокуса на «Ещё». Ширины 1440/720/390/320: document width равна viewport, диалог внутри окна. Screenshot `f254-export-mobile.png` в локальном каталоге визуализаций просмотрен, в git не добавлен. На странице встречи console errors/warnings=0; на первоначальной публичной landing были только две font-preload warnings. Реальные письма/платежи/данные не использовались. Browser session `f254-egress`, процесс harness и только контейнер `graf-f254-egress-qa` закрыты/удалены; ранее существующие ресурсы других проходов не затронуты.

Окончательные автоматические результаты: `run_local_postgres_tests.sh --focused tests/integration/test_commercial_egress.py tests/integration/test_artifact_egress_policy.py tests/integration/test_transcript_export_egress.py tests/integration/test_shared_with_me.py tests/integration/test_recording_share_public_link.py -x -q` — **84 passed**, без skipped, 373.38 s (`/tmp/f254-egress-verified.log`). После добавления последней временной проверки аудио: `--focused tests/integration/test_commercial_egress.py tests/integration/test_artifact_egress_policy.py tests/contract/test_transcript_export_contract.py tests/contract/test_transcript_export_no_secret_egress.py tests/contract/test_recording_share_invitation_contract.py tests/unit/test_artifact_egress_view_models.py tests/unit/test_artifact_egress_audit.py -x -q` — **51 passed**, без skipped, 120.26 s (`/tmp/f254-egress-final-boundaries.log`). Оба процесса завершены с кодом 0, контейнеры удалены runner. Два предупреждения каждого набора: PytestAssertRewriteWarning для уже импортированной postgres fixture и StarletteDeprecationWarning для httpx/TestClient; это не ошибки тестов. Ruff, `node --check`, governance, artifact validator и diff check проходят. Проверка сложности по ponytail-review: новых зависимостей/универсальных интерфейсов нет, использованы общий resolver и существующие ACL/egress/шаблоны; отдельных рекомендаций по сокращению этого участка нет. T017 не отмечен выполненным: sharing/AI/archive consumers, другие окна квот и историческая миграция ещё обязательны. Административные формы назначения и остальные задачи Feature 254 остаются открытыми; PR/release readiness не заявлена.

## Обмен встречами: допуск новых приглашений — T017, в работе 2026-09-07

На базе `969a3534d` подключается `meeting_sharing` к новым прямым grants, внешним приглашениям и ротации токена. Порядок Workspace FOR SHARE → Meeting; отказ с аудитом до создания grant/invitation и до запуска доставки. Ранее допущенные приглашения и grants остаются отдельными разрешениями: завершение доставки/принятия не отменяется автоматически тарифным изменением, владелец может явно отозвать доступ. SharePanelState показывает policy_blocked/причину, сохраняет список прежних доступов и отзыв; поля нового поиска/приглашения отключаются. Domain ACL.can_share означает возможность управлять уже выданным доступом; коммерческий допуск новых приглашений отражается отдельно в share.capability_state.

В первом actual-role прогоне найдена самостоятельная проблема старого успешного создания внутреннего grant: запрос WorkspaceMembership другого пользователя скрыт self-only RLS, HTTP вернул grantee_not_found. Это не исправление запрета и не успешный grant. Нужен отдельный ограниченный путь проверки/поиска адресата; расширять всем пользователям прямое чтение membership/identity и ослаблять RLS ради теста нельзя. Проверки запрета используют заранее выданный synthetic grant и проверяют ротацию/отзыв через реальный HTTP; они не доказывают успешное новое внутреннее приглашение. Этот положительный сценарий обязателен до выпуска и остаётся незакрытым в T017.

Обновлён origin/master: `7ff2c5c77` (три новых коммита относительно общей базы, включая объединённый выпуск v2026.09.07.1). Интеграция с основной веткой ещё не выполнена; готовность PR на этом основании не заявлена.

Проверки допуска обмена завершены: `run_local_postgres_tests.sh --focused tests/integration/test_commercial_egress.py tests/integration/test_meeting_share_links.py tests/integration/test_recording_share_public_link.py tests/integration/test_shared_with_me.py tests/contract/test_recording_share_invitation_contract.py -x -q` — **56 passed**, без skipped, 222.99 s (`/tmp/f254-sharing-verified.log`). Предыдущий повтор имел ошибку нового теста: диалог подгружается отдельным `/meetings/{id}/share`, а не включён в основной HTML; проверка исправлена на фактический fragment endpoint. Это не исправление найденного grantee_not_found. Ruff/node/governance/artifact validation/diff check — PASS.

Chromium: synthetic тариф с meeting_sharing=false, открытие диалога с клавиатуры, disabled поиск/ротация, отзыв по Enter завершился удалением строки. На 320 px обнаружено настоящее переполнение grid внутри диалога; исправлены minmax(0,1fr), min-width и перенос текста кнопок. Повтор после загрузки актуального CSS (кеш старого статического URL отключён для CSS-запросов, ответы не подменялись) на 1440/720/390/320: scrollWidth=clientWidth диалога и ни один input/button/p/strong не выходит справа. Изображение `f254-sharing-mobile.png` просмотрено, хранится локально. Console страницы встречи: 0 errors/0 warnings. Browser `f254-sharing`, harness и отдельный `graf-f254-sharing-qa` закрыты. Изменения не отправлены на production; отложенный дефект положительного пути, остальные consumers/задачи и интеграция с master остаются обязательными.

## Интеграция master — 2026-09-07

Все 19 коммитов перенесены на origin/master `7ff2c5c77`; проверенный source HEAD до объединяющей миграции — `2e92560fb7ede4b02c1a3557b962b8798dc9fcbc`. Сохранены независимые от Temporal задачи maintenance, пользовательский часовой пояс billing и новые проверки устройств auth. Системный reconciler запускается в Temporal-группе. Миграция 0100 объединяет 0099_checkout_catalog_admission и 0087_merge_calendar_timezone, без изменения таблиц; `alembic heads` показывает единственный 0100_merge_admin_settings.

`run_local_postgres_tests.sh --focused tests/integration/test_managed_checkout.py tests/integration/test_system_admin_entitlements.py tests/integration/test_commercial_egress.py tests/unit/test_maintenance_worker.py tests/contract/test_billing_ui.py tests/contract/test_auth_contracts.py -x -q` — **158 passed**, без skipped, 168.08 s, runner exit 0, одноразовый контейнер удалён. Лог `/tmp/f254-master-integration.log`. Два прежних предупреждения pytest/httpx. Проверки до rebase остаются историческими; full CI и готовность выпуска не заявлены.

## Адресат под RLS и допуск архивирования — T017, 2026-09-07

База `f807a50a4`, high-risk-product. Миграция 0101 добавляет ограниченную boolean-проверку членства адресата в контексте встречи. Прямое чтение чужих membership остаётся недоступно; поиск и создание проходят реальный session_user twobrain_rec_app. Existing recipient proof используется для проверки уже имеющегося доступа без повышения роли. API ACL/подтверждение внешнего адреса/коммерческий запрет остаются отдельными обязательными проверками. Runtime bootstrap и тестовая роль выдают одинаковое точное EXECUTE.

Первый прогон новых тестов завершился **12 passed, 1 failed** из-за отсутствующего EXECUTE у пересозданной после миграции тестовой роли (`/tmp/f254-share-lookup.log`); это не PASS. Ошибка исправлена в её настройке, а не расширением PUBLIC или RLS. Последующий `--focused tests/integration/test_commercial_egress.py tests/integration/test_meeting_share_links.py tests/integration/test_shared_with_me.py -x -q` — **41 passed**, без skipped, 166.08 s, exit 0 (`/tmp/f254-share-lookup-verified.log`). Включены положительные поиск по verified email/создание/открытие, duplicate=409, отказ для неактивного membership/identity, другой организации, удаления, неверного workspace/meeting/user и worker/пустого контекста.

Архивирование проверяет общий resolver до подготовки и под shared Workspace lock перед финальным приёмом. Явный false допускает существующий transient-путь; уже принятую политику не переписывает поздний retry. Ручная загрузка проверяет право до первого storage write и закрывает spool при раннем отказе. Новый коммерческий 403 в macOS классифицируется как storageQuota, а не утрата входа. Веб показывает причину и возвращает выбранный файл в диалог, сохраняя включённый переключатель до явного действия пользователя.

`--focused tests/integration/test_commercial_egress.py tests/integration/test_finalize_integrity.py tests/integration/test_manual_media_upload.py tests/integration/test_finalize_processing_autostart.py -x -q` — **54 passed**, без skipped, 122.75 s (`/tmp/f254-archive-admission.log`). После добавления проверки сохранения обоих принятых вариантов: `--focused tests/integration/test_commercial_egress.py tests/integration/test_system_admin_security.py -k 'archive or optional_runtime_bootstrap' -x -q` — **5 passed, 36 deselected**, без skipped, 19.14 s (`/tmp/f254-archive-retry-bootstrap.log`). Оба runner exit 0, контейнеры удалены. Два прежних предупреждения pytest/httpx. `swift test --package-path apps/macos --parallel --num-workers 1 --filter DesktopUploadClientTests` — **48 XCTest**, exit 0 (`/tmp/f254-swift-upload.log`); заключительные «0 tests» относятся к отдельному Swift Testing runner.

Chromium/Playwright CLI, отдельный synthetic PostgreSQL/HTTP harness: загрузка с archive=true возвращает коммерческий отказ; с Enter открывается восстановление, выбранный файл остаётся, archive=true и фокус на переключателе. После Space и Enter появляется «На сервере · Обрабатываем»; persisted последняя UploadSession — finalized, archive_audio=false. Диалог на 1440/720/390/320 имеет clientWidth=scrollWidth (538/538/354/284). Локальный screenshot f254-archive-recovery.png просмотрен, в git не включён. В console один ожидаемый network 403, без JS-ошибок приложения. В первой команде запуска harness был неверный относительный PYTHONPATH; исправлен абсолютным. Проверочный скрипт после успешного приёма не смог прочитать multipart postData (null); результат дополнительно проверен состоянием UI и БД. Эта ошибка скрипта не считается отдельным успешным assertion. Другие записи/письма/платежи не использовались.

Ruff, node syntax, diff check, governance и artifact validator PASS; проверка сложности ponytail-review не нашла лишней зависимости/абстракции. T017/T018 и выпуск ещё не закрыты: AI consumers, остальные окна/lineage, порядок блокировок и дальнейшие административные разделы остаются обязательными. Full CI, convergence, PR и production не выполнялись.

## Рабочие права БД и порядок обработки/удаления — T017/T018, 2026-09-07

Миграция 0102 исправляет реальный worker RLS: SELECT Workspace FOR UPDATE раньше возвращал NULL. Первый actual-role тест воспроизвёл отказ (1 failed); после миграции и bootstrap проверены app/media, видимость только текущего пространства, запрет UPDATE name и id=id и ожидание конкурирующего writer: **3 passed, 34 deselected**, без skipped (`/tmp/f254-worker-scope-final.log`).

Общий processing fence берёт Workspace до Meeting. Подключены допуск, статусы/ошибки, импорт, освобождение резерва, закрытый workflow, сбой dispatch, запрос/повтор удаления, retention scan и оба reconciler удаления. Перед сетевым чтением результата MediaScribe транзакция завершается, после ответа fence проверяется заново. Оба пути создания baseline после импорта также начинают с Workspace до будущего учёта квоты.

Первый refactor transient discovery нарушал пропуск занятой ранней встречи при LIMIT1: наборы **25 passed/1 failed** и **9 passed/1 failed** (`/tmp/f254-processing-import-locks.log`, `/tmp/f254-processing-network-final.log`). Исправлено: Workspace SKIP LOCKED по id, Meeting SKIP LOCKED до LIMIT, commit discovery, повтор всех eligibility predicates под Workspace→Meeting→Workflow. Финальный transient/provider набор **14 passed** (`/tmp/f254-processing-network-verified.log`). Набор processing failures/system operations/system content/meeting deletion/commercial egress — **72 passed**, без skipped (`/tmp/f254-processing-deletion-final.log`).

Дополнительный обзор callers обнаружил обратный порядок в fail_processing_attempt_dispatch, reconcile_closed_processing_workflow_result, retention и deletion reconciliation. Исправлены общими fences; конкурентный тест actual app role проверяет admission/dispatch failure/closed workflow: ожидающий Workspace вызов ещё не держит ни Meeting, ни Workflow. Окончательные наборы: commercial egress + meeting deletion + retention policy + transient purge + processing attempts — **57 passed**, 175.59 s (`/tmp/f254-lock-callers.log`); provider happy path + processing failures + deletion dependency — **32 passed**, 52.04 s (`/tmp/f254-import-quota-final.log`). Все runner завершились exit0 и удалили собственные контейнеры. Provider happy path отдельно проверяет доступность Workspace/Meeting для другого соединения во время fetch_result.

Это проверки текущего участка, не всей Feature254. Порядок remaining billing maintenance/referral/storage writers, миграционный барьер, другие окна квот и административные формы остаются обязательными; T017/T018 открыты. Full CI и production не выполнялись.

## Допуск создания итогов по правам — T017, 2026-09-07

Единый генератор создаёт резюме и структурированные результаты одним вызовом. Новые запросы требуют обоих флагов ai_summary/ai_outcomes; Workspace→Meeting сериализует их с назначением/отзывом прав. Все три HTTP entrypoints и автоматические import/reconciliation используют общий resolver. Точный повтор принятого candidate сохраняет прежнюю identity после запрета; явный новый refresh запрещён. Архивные результаты и уже принятые workers не отменяются тарифом. При импорте отказ не портит расшифровку и проецируется как blocked с безопасным кодом вместо бесконечного generating. Недоступные условия дают безопасный 503; данные ошибки каталога не раскрываются.

Проверки actual app role: оба флага, индивидуальный/workspace scope, все три HTTP entrypoints, автоматический отказ без новых slot/attempt/dispatch даже при commit caller, восстановление права, сохранение уже принятого candidate, чтение его transcript snapshot worker, точный повтор без дубликата, недоступный каталог, конкурентное назначение перед admission. Новые проверки используют только synthetic данные.

Прогоны: первоначальный AI/dispatch/slots — **33 passed**, 41.77 s (`/tmp/f254-ai-admission.log`); дополнительные AI и UI contracts — **43 passed**, 76.11 s (`/tmp/f254-ai-extra.log`); AI/import/dispatch — **25 passed**, 50.39 s (`/tmp/f254-ai-import-final.log`). После добавления точного replay и конкурентного writer: AI + import + slots + UI contracts — **70 passed**, без skipped, 115.44 s (`/tmp/f254-ai-verified.log`), exit0, контейнер удалён.

Chromium/Playwright на отдельном synthetic HTTP8102/PostgreSQL55512 обнаружил ошибку: общий обработчик 403 очищал встречу при коммерческом отказе. Код commercial_ai_generation_denied добавлен только в существующий allowlist ошибок действия итогов. Повтор в браузере: Enter на «Создать итоги», видимое русское сообщение в live status, URL встречи сохранён, вкладка расшифровки доступна по Enter. Ширины1440/720/390/320 совпадают с document.scrollWidth; screenshot f254-ai-denied.png просмотрен и не коммитится. Единственная console error — ожидаемый HTTP403, без ошибок JS. Node runtime regression проверяет сохранение встречи при этом коде и обязательное закрытие при revoked device, unknown403,401 и404: **1 passed, 7 deselected** (`/tmp/f254-ai-browser-regression.log`).

Ограничения: независимых вызовов только summary или только outcomes нет; это описано в контракте. T017 остаётся открытым из-за других окон квот и исторического lineage. Биллинг, формы администрирования, акции и прочие задачи Feature254 не завершены. Full CI, PR и production в этом участке не выполнялись.

После ponytail-review убрано повторное чтение уже заблокированной встречи в automatic helper; новых зависимостей/универсальных слоёв нет. Окончательная проверка AI + dispatch, включая Node regression: **19 passed**, без skipped, 38.81 s (`/tmp/f254-ai-closeout.log`), exit0, контейнер удалён. Synthetic browser f254-ai, HTTP harness и только контейнер graf-f254-ai-qa закрыты. Остальные ранее существовавшие ресурсы не затронуты.

## Назначенная ёмкость при подготовке аудио — T017/T018, 2026-09-07

Найдена несогласованность: normalization читал назначенные права под прежним storage advisory lock, а adjustments удерживали Workspace row lock. Перед новым резервом и публикацией добавлен порядок storage advisory → Workspace → Meeting → Job/Attempt, совместимый с lock_billing_subscription. Повторное взятие storage lock при расчёте резерва остаётся транзакционным и безопасным. Перед записью объекта существующий commit освобождает locks; публикация проверяет права заново. Уже выданный активный резерв сохраняется при снижении лимита.

Проверки workflow/restart/idempotency/deletion/storage quota — **38 passed**, без skipped, 39.49 s (`/tmp/f254-storage-workspace.log`). Конкурентные проверки настоящих вызовов admission и publication: ожидающий изменение прав normalizer ещё не держит Meeting/Job; снижение до одного байта запрещает новое принятие, но не теряет ранее выданный резерв — **2 passed**, 7.35 s (`/tmp/f254-storage-race.log`). Actual-role тест был переведён с Connection на Session для вызова настоящего helper; первый запуск ошибся на старом apply_tenant_context_to_connection (**1 failed**, тестовый AttributeError, `/tmp/f254-storage-actual-role.log`). Исправлен вызов контекста, без изменения политик доступа: app/media — **2 passed,16 deselected**, 4.20 s (`/tmp/f254-storage-actual-role-verified.log`). Полный media-role normalization/re-hydration/publish и locks against deletion — **3 passed,10 deselected**, 5.00 s (`/tmp/f254-storage-media-final.log`). Все успешные runner завершены exit0, собственные контейнеры удалены. Ruff/diff check PASS.

Обзор сложности: один общий helper для двух границ, без новых зависимостей, таблиц и второго механизма резервирования. Общий порядок maintenance/referral/account writers и migration barrier остаются открыты в T018; этот участок их не закрывает.

## 2026-09-07 — каталог биллинга, подписки и эксплуатационные проекции

- `apps/server/scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_catalog.py -x -q` — **3 passed**: создание/публикация тарифа и акции, одноразовая выдача кодов, предпросмотр/идемпотентный commit/исполнение и отзыв назначения подписки, HTTP-страницы и API разделов платежей, статистики, очереди, обращений, устройств, интеграций, зависимостей, хранилища, оповещений и настроек.
- Полный связанный набор системной консоли (`test_system_admin_login.py`, `test_system_admin_management.py`, `test_system_admin_operations.py`, `test_system_admin_authority.py`, `test_system_admin_security.py`, `test_system_admin_users.py`, `test_system_admin_content.py`, `test_system_admin_media.py`, `test_system_admin_billing.py`, `test_system_admin_entitlements.py`, `test_system_admin_catalog.py`, auth/app contracts) — **136 passed**, 2 существующих предупреждения.
- Исправлена совместимость PostgreSQL `json`/`jsonb` в проекции счетов: `plan_snapshot` явно приводится к `jsonb` перед оператором `?`.
- Миграция `0105_system_admin_observability_console` добавляет только security-definer чтение под `twobrain_rec_system_authority`; обычный сервер и maintenance не получают системный доступ. Проекции ограничены разрешениями `operations.read`, `support.read`, `devices.read`, `integrations.read`, `analytics.read`, `settings.read` и не возвращают содержимое встреч, секреты или ключи объектов.

Проверка миграционной цепочки после добавления эксплуатационных проекций: `scripts/run_local_postgres_tests.sh --focused tests/integration/test_postgres_migrations.py -x -q` — **16 passed**, без skipped, 54.75 s, runner exit 0; проверены чистое развёртывание, обновление существующих баз, единственная голова `0105_system_admin_observability_console`, безопасное понижение и ограничения длины revision IDs. Одноразовый PostgreSQL контейнер удалён runner. Остались два стандартных предупреждения pytest/httpx, ошибок тестов нет.

После исправления typed metadata в миграциях `0099`, `0101`, `0102`, `0103`, `0104` и сортировки импортов повторён полный связанный набор системной консоли: **136 passed**, без skipped, 100.84 s, runner exit 0; контейнер удалён. `compileall`, `node --check`, `ruff`, `git diff --check`, governance и artifact validator проходят.

## Финальный быстрый контур перед PR — 2026-09-07

После исправления порядка импортов в тестах (коммит `474cfabaf806174930b72483935af6b4c7905755`) повторён `infra/scripts/ci-local.sh --fast`. Контур завершился `ci_local_result=pass`, `run_id=ci-fast-474cfabaf806-725b52695204`, exit 0.

Зафиксированные этапы: development process preflight, Spec Kit governance, **224 governance tests**, portable harness self-test, macOS legacy audio guard, Swift build, **830 macOS tests (1 штатный skip)**, macOS contract validation, **1519 серверных unit-тестов**, **308 изменённых серверных тестов**, server lint, Python compile, shell syntax, **66 CI contracts**, production compose config, deployment evidence scan, diff whitespace и active CI documentation consistency. Runner удалил изолированный PostgreSQL-контейнер; два стандартных предупреждения pytest/httpx сохранены в выводе.

Этот результат подтверждает локальный быстрый контур на точном SHA ветки. Он не заменяет GitHub `governance-fast`, полный `ci-local --full`, браузерный полный набор, нагрузочную проверку, deployment review или release/deploy gates. Reviewer-owned checklist и задачи `tasks.md` исполнитель не закрывает автоматически.

## Исправления по результатам проверки акций — 2026-09-07

Повторная проверка платёжного контура выявила три ошибки уровня P1. Они устранены до обновления PR:

- Выпущенный индивидуальный код записывался в `promotion_codes`, но checkout искал только хеш кода кампании. Загрузка кампании теперь сначала ищет собственный хеш индивидуального кода, проверяет его состояние и сохраняет идентификатор кода в неизменяемой модели оформления.
- Подарочная кампания проходила через скидочную модель с нулевой скидкой и отклонялась общей проверкой. Введён отдельный `benefit_kind=gift` с ограниченным числом дней; подарочное оформление не создаёт счёт, платёж или обязательное согласие на автопродление.
- При оформлении повторно не проверялись аудитория кампании, назначенный пользователь и бюджет. Checkout заново проверяет категории `all`, `selected`, `never_paid`, `first_purchase`, `former_paid`, активный платный доступ, историю оплат и доступный бюджет с учётом резервов и округления суммы выгоды.

Для подарочного доступа обычной роли приложения не выдан прямой `INSERT` в `billing_access_adjustments`. Миграция `0106_promotion_gift_redemptions` добавляет узкую `SECURITY DEFINER` функцию `public.billing_redeem_promotion_access(...)`: она принимает только запросный контекст настоящего `twobrain_rec_app`, сверяет tenant, блокирует пространство и делает идемпотентную запись источника `promotion`. Bootstrap выдаёт приложению только `EXECUTE` этой функции и отзывает прямую вставку. Тестовый probe-role получает те же ограничения после создания, при этом `USAGE` системной схемы не появляется.

Состояния индивидуального кода синхронизированы с резервированием: `available → reserved → redeemed`, а отменённые и истёкшие резервы возвращают код в `available`. Погашение кампании без индивидуального кода сохраняет прежний путь по хешу кампании. Бюджет учитывает полную каталожную стоимость подарка и только фактическую скидку для скидочного кода; повторное применение защищено блокировками и идемпотентностью.

Проверки после исправлений:

- `scripts/run_local_postgres_tests.sh --focused tests/integration/test_promo_checkout.py tests/integration/test_managed_checkout.py -x -q` — **23 passed**, 35.95 s, exit 0.
- `scripts/run_local_postgres_tests.sh --focused tests/integration/test_managed_checkout.py -k 'gift_promo or promo_audience' -q` — **2 passed**, 17 deselected, exit 0.
- `scripts/run_local_postgres_tests.sh --focused tests/integration/test_postgres_migrations.py -x -q` — **16 passed**, 31.92 s, exit 0; единственная голова — `0106_promotion_gift_redemptions`, downgrade не удаляет подарочные погашения молча.
- `scripts/run_local_postgres_tests.sh --focused tests/integration/test_system_admin_security.py tests/integration/test_rls_postgres_policies.py -x -q` — целевые проверки границы прошли; полный файл RLS — **36 passed**, 15.01 s, exit 0. Проверены отсутствие `USAGE` системной схемы и отсутствие прямой вставки приложения.

Рабочая голова миграций теперь `0106_promotion_gift_redemptions`. В PR сохранены отдельные ограничения по браузерному/e2e, rehearsal, нагрузке и release-full; эти проверки не объявляются выполненными этим разделом.
