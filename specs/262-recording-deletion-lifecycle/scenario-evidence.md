# F262: сопоставление S01–S51 с проверками

Дата сопоставления: 2026-09-09 UTC (локальные журналы: 2026-09-10 +05). Область: прочитанные тематические Swift, PostgreSQL/API и Chromium тесты рабочей ветки F262. Это каталог проверяемых утверждений и границ, **не 51 безусловная отметка PASS**. Новые scoped-наборы acceptance (selection/filter/lastRow/aliases/partialBatch/timing), recovery (offline/reimport), bulk (100 операций), legacy (2 теста), restore-fence (1 тест) завершены успешно; итоговые журналы и SHA связывает основной исполнитель в `evidence.md`. Результаты запусков, точный SHA и установленная версия фиксируются отдельно в `evidence.md`. Изменения в тестах после запуска требуют повторной проверки затронутого набора.

`Авто — подтверждено` означает, что действительные assertions полностью проверяют указанную ограниченную область сценария. Повторять каждый такой инвариант вручную не требуется. `Частично` означает конкретный содержательный пробел между assertions и заявленным сценарием, а не автоматическое требование ручного повтора всех шагов. `Нет прямого теста` означает отсутствие такой проверки в прочитанном тематическом наборе; это не утверждение об отсутствии реализации. В колонке остатков перечислены различия между действительными assertions и полным сценарием, без переноса ответственности на пользователя. Проверка установленного GRAF Dev остаётся общей проверкой интеграции приложения, а восстановление инфраструктуры — отдельным release gate; их отсутствие не отменяет подтверждённые автоматикой API/модель/DOM-инварианты.

## Обозначения файлов

Swift имена ниже — методы XCTest. В `DesktopUploadQueueV5Tests.swift` фактическое имя класса — `DesktopUploadQueueTests`.

| Код | Файл |
|---|---|
| DL | [RecordingDeletionLifecycleTests.swift](../../apps/macos/Shared/Tests/RecordingDeletionLifecycleTests.swift) |
| Q | [DesktopUploadQueueV5Tests.swift](../../apps/macos/Shared/Tests/DesktopUploadQueueV5Tests.swift) |
| C | [DesktopUploadCustodyProjectionTests.swift](../../apps/macos/Shared/Tests/DesktopUploadCustodyProjectionTests.swift) |
| SH | [DesktopMeetingShellWebViewBoundaryTests.swift](../../apps/macos/Shared/Tests/DesktopMeetingShellWebViewBoundaryTests.swift) |
| PL | [LocalRecordingPlayerTests.swift](../../apps/macos/Shared/Tests/LocalRecordingPlayerTests.swift) |
| OC | [test_recording_origin_cancellation.py](../../apps/server/tests/integration/test_recording_origin_cancellation.py) |
| LP | [test_local_purge_coordination.py](../../apps/server/tests/integration/test_local_purge_coordination.py) |
| HX | [test_cabinet_hx_delete_feedback.py](../../apps/server/tests/integration/test_cabinet_hx_delete_feedback.py) |
| ACCESS | [test_deletion_lifecycle_blocks_access.py](../../apps/server/tests/integration/test_deletion_lifecycle_blocks_access.py) |
| MIG | [test_recording_deletion_migration.py](../../apps/server/tests/integration/test_recording_deletion_migration.py) |
| SYNC | [test_recording_sync_conflicts.py](../../apps/server/tests/integration/test_recording_sync_conflicts.py) |
| PROC | [test_recording_sync_processing.py](../../apps/server/tests/integration/test_recording_sync_processing.py) |
| WF | [test_meeting_deletion_workflow.py](../../apps/server/tests/integration/test_meeting_deletion_workflow.py) |
| OUT | [test_meeting_outcomes_deletion.py](../../apps/server/tests/integration/test_meeting_outcomes_deletion.py) |
| RACE | [test_recording_workflow_deletion_races.py](../../apps/server/tests/integration/test_recording_workflow_deletion_races.py) |
| NORM | [test_playback_normalization_deletion.py](../../apps/server/tests/integration/test_playback_normalization_deletion.py) |
| DEP | [test_dependency_deletion_states.py](../../apps/server/tests/unit/test_dependency_deletion_states.py) |
| MIX | [mixed-meeting-list.test.cjs](../../apps/server/tests/browser/mixed-meeting-list.test.cjs) |
| UP | [manual-upload-deletion.test.cjs](../../apps/server/tests/browser/manual-upload-deletion.test.cjs) |
| BA | [recording-deletion-acceptance.test.cjs](../../apps/server/tests/browser/recording-deletion-acceptance.test.cjs) |
| BR | [recording-deletion-recovery.test.cjs](../../apps/server/tests/browser/recording-deletion-recovery.test.cjs) |
| BULK | [RecordingDeletionBulkAcceptanceTests.swift](../../apps/macos/Shared/Tests/RecordingDeletionBulkAcceptanceTests.swift) |
| LEG | [RecordingDeletionLegacyAcceptanceTests.swift](../../apps/macos/Shared/Tests/RecordingDeletionLegacyAcceptanceTests.swift) |
| REST | [test_recording_deletion_restore_fence.py](../../apps/server/tests/integration/test_recording_deletion_restore_fence.py) |

`MIX` — один исполняемый сценарий без отдельных named test functions. Его части ниже обозначены точными комментариями либо уникальными assertion messages. `UP` содержит `scenario(browser, action)` с тремя именованными значениями `action`; они выводятся в журнале отдельно. Маршруты сервера в браузерных тестах подменены синтетическими ответами. Они проверяют настоящий `cabinet.js` и DOM-обработчики, но не являются сквозным запуском клиента против настоящего сервера.

## Матрица

| ID | Имеющиеся проверки и фактически проверяемый результат | Покрытие и остаток |
|---|---|---|
| S01 | ACCESS `test_deleting_meeting_is_hidden_from_list_and_blocks_original_content_routes`: 202, отсутствие в API-списке, запреты download/share/detail, доступный отчёт без содержимого. HX `test_non_hx_web_deletion_request_returns_to_meeting_list`, `test_deletion_index_and_report_remain_reachable_after_row_disappears`: no-HX redirect и web/desktop индекс отчётов. | Авто — подтверждены серверное удаление, запрет контента и доступность отчёта, включая no-JS HTTP-контракт. Установленная интеграция проверяется отдельно, ручной повтор этих API assertions не требуется. |
| S02 | DL `testDeletedServerTruthBlocksPlaybackProjectionEvenWhenFileIsValid`; C `testServerDeletionWinsOverStaleUploadedAndFinalizedFlags`; Q `testPendingLocalPurgeDeletesLocalArtifactsBeforeAcknowledgement`. MIX: `An absent server row (deletion, filters or pagination) cannot resurrect a local alias.` | Установленный 0e19 — подтверждено: настоящий WAV upload и связанная локальная fixture дают одну строку/флажок; принятое delete скрывает строку/count без local alias. После исправления origin только собственной fixture (--initial → фактический --manual) и штатного rollback/возврата с перезапуском native executor повторил purge: terminal_deleted, server_meeting_deleted, retainedfalse, файлы отсутствуют. GUI после перезапуска не наблюдался; итог подтверждён реальными persisted queue/filesystem. 1334 мс включают транспорт и не являются SC-002 от receipt. |
| S03 | DL `testLateUploadTransitionCannotEraseTombstone`, `testAcceptedOperationNeverRegressesOnLateResponse`. MIX `stale HTML cannot resurrect accepted deletion`. BA `lastRowAndStaleProjection` (`lastRow`): последняя строка удалена, count=0, selection скрыт, focus на заголовке; вставленный устаревший HTML удаляется. UP `late-upload-after-web-delete`: задержанный XHR не возвращает карточку/ссылку загрузки. | Авто — подтверждены монотонная native-модель и защита DOM от stale HTML/upload callback. Один установленный интеграционный путь остаётся общим gate, не отдельным повтором каждого assertion. |
| S04 | DL `testPersistedIntentBlocksEveryLocalActionAndSurvivesRestart`; BULK `testHundredMixedOperationsKeepIndividualTruthAndResumeAfterRestart`: 100 операций записаны в файл, новый экземпляр сервиса восстанавливает их, повторяются только unresolved, ID и принятые receipts сохраняются. | Авто — durable restart и отсутствие повторов terminal результатов. PURGE testSuspendedUploadDoesNotBlockDeletionAndConcurrentExecutorDoesNotDuplicateIt дополнительно подтверждает независимость deletion от задержанного upload, отсутствие дублирующего executor и запрет позднего playback. Аварийное завершение процесса ОС не моделируется; реальное повторное чтение сохранённой очереди проверено. |
| S05 | BULK `testHundredMixedOperationsKeepIndividualTruthAndResumeAfterRestart`: для операции 99 сохранён серверный receipt ID, первый ответ теряется через URLError.timedOut, после restart возвращается исходный receipt; 404 отдельно разрешается lifecycle lookup. HX `test_repeated_deletion_returns_same_receipt_without_restarting_purge`. | Авто — подтверждены lost-response/restart и тот же receipt в модели HTTP-клиента; настоящий API подтверждает повторяемость серверного ответа. Дополнительный обрыв реального TCP строго после commit не считается уже выполненным. |
| S06 | DL `testDeletionRequestsAreDurableScopedAndIdempotent`; HX `test_repeated_deletion_returns_same_receipt_without_restarting_purge`; OC `test_cancel_before_create_is_durable_and_repeatable`. | Авто — новый PostgreSQL concurrent test синхронизирует два авторизованных запроса после чтения одной живой Meeting. Итог: одинаковые receipt/epoch, один request/report/audit и вызов очистки, отсутствие дублирующих task/artifact identities. Это управляемая серверная конкуренция, не две GUI-вкладки. |
| S07 | DL `testNeverSubmittedDeletionIsOfflineAndOneUnsafePackageDoesNotBlockAnother`: client=nil, intent до файлов, безопасный пакет удаляется, tombstone сохраняется при restart. DL `testHundredLocalPackagesCleanWithinBudgetAndRemainDeletedAfterRestart`: 100 пакетов и 100 перечитываний. | Авто — отсутствие native клиента, очистка файлов и tombstone; RECOVERY testRestoredFilesCannotResurrectTerminalOriginDuringRealScan дополнительно восстанавливает реальные WAV/AAC и запускает настоящий rescan нового сервиса: нет новой записи/playback/upload. Физическое отключение сети Mac не выполнялось. |
| S08 | DL `testPersistedIntentBlocksEveryLocalActionAndSurvivesRestart`, `testTypedDeleteRetriesSameOperationAfterRestartAndRejectsAnotherSession`: pending запрещает playback, сохраняется и ждёт повторной связи. | Авто — Chromium offlineCopy использует настоящий offline браузера и native ответ accepted0/pending1: честное ожидание подключения, open/select недоступны, обещания очистки нет. Native durable intent, запреты play/upload и restart подтверждают DL. Это составное доказательство контракта, не отключение сети самого Mac. |
| S09 | BR S09 в `main`: настоящий loopback HTTP и `context.setOffline(true)`; POST не доходит до сервера, строка остаётся, ошибка видна, кнопка «Повторить», обещания сохранённой команды нет. После reconnect явное повторное подтверждение удаляет строку; сервер получает ровно один запрос. | Авто — подтверждён browser offline/error/reconnect/retry. Закрытие вкладки не воспроизводится, но тест прямо проверяет отсутствие обещания долговечной команды; интерфейс не подменяет ошибку native pending-состоянием. |
| S10 | OC `test_origin_lock_orders_both_interleavings[first_action=create]` и `[first_action=cancel]`: вторая операция ждёт удерживаемый PostgreSQL lock; create даёт 200 или 409 согласно порядку, поздний create запрещён. Дополнительно `test_concurrent_cancel_and_create_never_leave_live_recording`. | Частично: управляемая серверная гонка охвачена в обоих порядках; native delete при отсутствии meetingId и применение ответа в интерфейсе — отдельный путь. |
| S11 | OC `test_cancel_existing_origin_uses_meeting_deletion_receipt`: тот же origin находит существующую встречу, выдаёт receipt с её ID, повтор не меняет результат. | Частично: исходный create-ответ тестом получен; реальная потеря ответа в native-клиенте и дальнейший restart не воспроизводятся. |
| S12 | OC `test_cancel_before_create_is_durable_and_repeatable`, `test_origin_lock_orders_both_interleavings[first_action=cancel]`: сначала marker, поздний create получает `recording_deletion_active`. | Авто — PostgreSQL origin lock запрещает поздний create. SCOPE test_origin_only_receipt_creates_no_meeting_or_meeting_purge_foreign_keys дополнительно проверяет после cancel/repeat/lifecycle/late create ровно 1 marker и 0 Meeting/request/report/artifact/task/audit/retention; обработка не получает созданную Meeting. |
| S13 | NORM `test_deletion_does_not_wait_for_inflight_upload_and_removes_the_late_output`: управляемое ожидание загрузки, deletion завершается до её освобождения, поздний объект очищен, canonical не опубликован. NORM `test_deleted_local_attempt_rechecks_forever_and_removes_a_late_object`; DL `testLateUploadTransitionCannotEraseTombstone`; UP `late-upload-after-web-delete`. | Авто — прежние normalization/callback fences дополнены 4 PostgreSQL проверками test_upload_session_lifecycle.py: create/part guard, late cleanup journal и finalize commit fence. Настоящая ручная гонка TCP и физическое объектное хранилище не заявляются. |
| S14 | OUT `test_delete_fence_rejects_late_slot_cas_without_republishing`, `test_deletion_purges_summary_slots_but_retains_generation_call_ledger`; RACE `test_deletion_wins_pending_egress_and_preserves_completed_observability_delivery`: отмена workflow, блокировка stale export, retained observability отдельно. | Частично: публикация итогов и серверные запреты проверены. Настоящие MediaScribe/Temporal callbacks во время удаления и STT-публикация требуют своей интеграционной приёмки. |
| S15 | SH `testActiveRecordingStopBoundaryDoesNotDependOnEmbeddedSurfaceState`; MIX проверяет disabled checkbox и отсутствие open при `canDelete:false, canOpen:false`. | Авто — Stop/проекция дополнены RECOVERY testSavingAndUnboundCaptureRequireExplicitActionBeforeBindingOrDeletion: настоящие enqueueSaving/enqueue и запрет удаления writer. Управление активной записью сохраняет независимый Stop; синтетический тест не является записью пользовательской встречи. |
| S16 | Q `testSavingStateRoundTripsAndCannotUpload`, `testSavingItemBecomesReadyWithTheSameIdentity`: saving сохраняется, переход к ready сохраняет identity, второго item нет. | Авто — RECOVERY testSavingAndUnboundCaptureRequireExplicitActionBeforeBindingOrDeletion вызывает настоящий saving путь, проверяет запрет delete и тот же ID после сохранения. Запись пользовательской встречи/прерывание реального writer не выполнялись. |
| S17 | Q `testDamagedItemCannotBeSentAndCanBeDeletedLocally`: damaged пакет нельзя отправить, штатное локальное удаление убирает каталог. Q `testLocalPlaybackRejectsMissingTrackAndPathOutsideRecordingRoot`: отсутствующий/нечитаемый playback запрещён. | Частично: файловые и model guards охвачены; независимые play/delete в установленном интерфейсе проверить отдельно. |
| S18 | BA `mixedFrozenSelection` (`selection`): в одной команде выбран local-only и server-only, поздняя новая запись не добавляется. MIX: checkbox у local, выбор/фокус переходят на server alias. BULK `testHundredMixedOperationsKeepIndividualTruthAndResumeAfterRestart`: 100 origin/meeting операций и индивидуальные результаты. | Авто — Chromium threeTypes одновременно выбирает local-only, uploaded local/server alias и server-only:3 строки / 3 цели, один диалог и команда, alias не дублируется; область удаления указана отдельно. Local-only и unknown creation проверены дополнительно. Native deletionIsLocalOnly истинно только при доказанном отсутствии попытки создания и server IDs;17 Swift boundary PASS. |
| S19 | BULK `testHundredMixedOperationsKeepIndividualTruthAndResumeAfterRestart`: 33 accepted, 33 rejected, 34 resolving; успех не откатывается, 404 не мешает остальным, lost response разрешается после restart. OC `test_cancellation_rate_limit_retains_retry_after_and_does_not_cancel_origin`: 429 с Retry-After без marker. | Авто — 100 mixed queue results и независимый 403 дополнены RECOVERY testRetryAfterSurvivesRestartAndDefersEveryOperationInScope: настоящий HTTP 429 / Retry-After 120, 2 операции, сохранение срока и тех же IDs/targets после restart до/после срока. Ошибка одной цели не откатывает принятую. |
| S20 | BA `mixedFrozenSelection` в режимах `selection` и `filter`: при открытом подтверждении приходит новая local row и HTMX событие; затем фильтр убирает выбранную local row. Отправляются только первоначальные local-a и исходный meeting ID. | Авто — подтверждена замороженная выборка при новой строке, HTMX и смене фильтра. Дополнительный ручной повтор инварианта не требуется. |
| S21 | MIX `Selection and focus follow the local alias when its server row arrives.`: checked и document.activeElement переходят на server row. BA `mixedFrozenSelection`: команда сохраняет исходные выбранные identities. | Авто — подтверждены перенос выбора/фокуса и отсутствие добавления новой строки. Частично: число серверных операций после alias-перехода с последующим подтверждением отдельно не утверждается. |
| S22 | MIX: семь сортировок, поиск по дате, access/status фильтры, отсутствующий server row и `not-on-this-page`, count после поиска. | Частично: отсутствие local обхода фильтра/страницы проверяется; полная серверная пагинация и совокупный count при нескольких удалениях отдельно не проверены. |
| S23 | BA `lastRowAndStaleProjection` (`lastRow`): после accepted последняя строка отсутствует, count=0, toolbar selection скрыт, «Записей пока нет», document.activeElement — заголовок. Старая server row при HTMX не возвращается. | Авто — подтверждён полный DOM-результат удаления последней строки с native bridge response. Сервер и настоящий native bridge проверяются своими API/queue наборами и общей установленной приёмкой. |
| S24 | BA `realTimerAndReconnect` (`timing`): настоящий 30-секундный таймер cabinet.js; серверное событие скрывает строку менее чем за 60 секунд, после online — менее чем за 5 секунд. PL `testManagedSourceIsReleasedOnDeletionAccessLossAndReplacement`; Q `testPendingLocalPurgeDeletesLocalArtifactsBeforeAcknowledgement`. | Установленный 43f: удаление собственной server-only встречи второй HTTP-сессией отражается исчезновением строки и upload card через ≤17,453 с после 202. Chromium periodic/reconnect последний единичный образец 29998/16 мс. Интеграция server+local purge подтверждена отдельным S02; один образец не p95. |
| S25 | LP `test_expired_local_purge_converges_report_and_accepts_verified_late_ack`: искусственно просроченное задание, отчёт expired, поздний verified ACK меняет его на acknowledged. | Авто — PURGE testLostAcknowledgementRestartsAndRetriesExpiredTaskWithoutAnotherDelete подтверждает потерю ACK, restart и повтор прежнего expired ACK без нового DELETE; testRealFilesystemFailureDoesNotBlockAnotherPackageAndRetriesAfterRepair — cleanup после repair/restart. Отключение установленного Mac на 7 дней не выполнялось. |
| S26 | LP `test_new_device_gets_own_task_without_acknowledging_old_device`, `test_ensure_task_is_repeatable_and_rejects_live_recording`: новое устройство получает другой task ID; старое остаётся pending. | Частично: проверены API и правильное разделение задач, но смена регистрации установленного клиента с сохранённым локальным origin не пройдена. |
| S27 | DL `testTypedDeleteRetriesSameOperationAfterRestartAndRejectsAnotherSession`, `testExecutionScopeCannotBeBorrowedByAnotherActor`; OC `test_execution_scope_change_rejects_cancellation_before_any_mutation`; HX `test_deletion_receipts_and_index_do_not_cross_workspace_access`. | Авто — PURGE testScopeChangeDuringSuspendedResponsePreventsOldScopePurgeAndAcknowledgement переключает actor во время задержанного ответа: прежний purge/ACK запрещены, playback не разрешён. API denial и browser revocation проверены отдельно; это не ручное переключение пользовательского пространства. |
| S28 | DL `testUnavailableAccessBlocksContentButDoesNotProveDeletion`, `testUnavailableTargetDoesNotBlockNextDeletionAndNeverAuthorizesPurge`; Q `testPendingLocalPurgeReconcilesBeforeDeletingLocalArtifacts`; PROC `test_unavailable_deletion_target_has_no_receipt_and_next_target_can_be_deleted`; UP `unavailable`. | Частично: запреты/отсутствие receipt/сохранение файлов и удаление карточки проверяются. Единый установленный путь с реальным отзывом прав не выполнен в этих тестах. |
| S29 | DL `testDeletedServerTruthBlocksPlaybackProjectionEvenWhenFileIsValid` сочетает deletion с metadata mismatch. SYNC `test_sync_state_reports_server_deleted_conflict_and_hides_review`; C `testServerDeletionWinsOverStaleUploadedAndFinalizedFlags`. | Частично: приоритет deletion над metadata/stale flags проверен; одновременный device conflict и deletion через реальный sync-response не выделен в отдельную проверку. |
| S30 | PL `testManagedSourceIsReleasedOnDeletionAccessLossAndReplacement`: настоящий AVPlayerView, currentItem/связь player освобождены, окно закрыто; unrelated ID не закрывает окно. | Установленный 43f: pause/seek00:50/play/delete закрывают managed player и удаляют own package. Повтор на db76: player window absent, folder absent, terminalDeleted/retainedfalse. Component test проверяет освобождение источника. Единый высокоточный замер остановки звука/строки/файлов не заявляется; строка отдельно 994 мс. |
| S31 | WF `test_deletion_report_includes_safe_post_egress_limits_from_download_and_export_audit`, `test_deletion_report_treats_stream_prepared_events_as_post_egress`; Q `testLocalPurgeExportTaskDoesNotDeleteRecordingBuffers`. | Частично: отчёт учитывает пределы после выдачи и не раскрывает содержимое. Текст подтверждения и ранее открытый внешний плеер/экспорт на Mac отдельно не проверяются. |
| S32 | DL `testNeverSubmittedDeletionIsOfflineAndOneUnsafePackageDoesNotBlockAnother`: ошибка unsafe package не мешает безопасному. LP `test_failed_local_purge_acknowledgement_updates_report_without_private_payload`: failed приводит к retryable_failed. | Авто — PURGE testRealFilesystemFailureDoesNotBlockAnotherPackageAndRetriesAfterRepair создаёт реальный отказ unlink через POSIX 0500. Другой пакет очищается, после исправления доступа и перезапуска очистка остатка завершается. Неизвестный или неполный результат не получает verified. |
| S33 | Q `testLocalPurgeAckFailureDoesNotFinalizeLocalQueueState`: файлы уже удалены, ошибка ACK не объявляет terminal/verified. Q `testLocalPurgeReconciliationErrorPreservesAlreadyDeletedTruth`: известный пакет отсутствует, ACK возможен при ошибке reconcile. | Авто — PURGE testLostAcknowledgementRestartsAndRetriesExpiredTaskWithoutAnotherDelete проверяет потерянный ACK, сохранение очереди, новый экземпляр сервиса и повтор того же expired task ACK без нового DELETE Meeting. Это восстановление состояния сервиса, не принудительное завершение процесса ОС. |
| S34 | Q `testLocalPurgeUnknownSyncStateDoesNotDeleteRecordingBuffers`, `testLocalPurgeReconciledMeetingMismatchDoesNotDeleteRecordingBuffers`; DL `testTypedDeleteRetriesSameOperationAfterRestartAndRejectsAnotherSession` оставляет accepted без local package, не verified. | Авто — PURGE testMissingMappingAndSymlinkNeverClaimVerifiedCleanup проверяет отсутствующий mapping/package без ложного verified ACK. Отсутствие одного пути само по себе не доказывает физическую очистку. |
| S35 | Q `testLocalPurgeRefusesPathsOutsideRecordingRootAndReportsFailedAck`, `testLocalPlaybackRejectsMissingTrackAndPathOutsideRecordingRoot`; DL `testNeverSubmittedDeletionIsOfflineAndOneUnsafePackageDoesNotBlockAnother`. | Авто — PURGE testMissingMappingAndSymlinkNeverClaimVerifiedCleanup использует настоящий симлинк за пределы root; внешние bytes сохранены, verified не выдан. Враждебная конкурентная подмена во время системного вызова не выполнялась. |
| S36 | DL `testFailedAtomicSaveDoesNotPublishDeletionInMemory`: path родителя занят обычным файлом, сохранение бросает ошибку, операция не появляется в памяти. | Авто — RECOVERY testUserDeletionSaveFailureLeavesFilesAndMemoryUntouchedBeforeAnyNetwork вызывает настоящий requestDeletion при отказе атомарной записи: bytes/cache прежние, сетевых вызовов нет, файлы сохранены. Реальная ошибка записи достаточна без заполнения диска. Пользовательское сообщение об этой конкретной файловой ошибке отдельно не проверялось. |
| S37 | Q `testMalformedQueueDocumentIsQuarantinedAsBlockedCustodyTruth`; DL `testLegacyOwnershipIsUnavailableAndFutureQueueIsPreserved`, `testVerificationRequiresAcceptedDeletion`. LEG `testFilledV2QueueBindsOnlyMatchingOwnerAndKeepsDeletionFenceAfterRestart`: заполненный v2, реальная сохранённая очередь, владелец и tombstone переживают restart, файлы не теряются. | Авто — карантин, неизвестная схема и сохранённый v2 fence дополнены RECOVERY testRestoredFilesCannotResurrectTerminalOriginDuringRealScan: реальные WAV/AAC восстановлены, новый сервис и rescan не создают доступную запись или отправку. Tombstone сохраняется; откат очереди до удаления не поддерживается. |
| S38 | LEG `testFilledV2QueueDoesNotBindUnknownOwnerOrDeleteFiles`: missing/different-meeting/denied не назначают владельца, запрещают open/delete, файлы сохранены до и после restart. LEG `testFilledV2QueueBindsOnlyMatchingOwnerAndKeepsDeletionFenceAfterRestart`: только matching owner привязывается; подтверждённое deletion скрывает проекцию и блокирует play, самостоятельного purge нет. | Авто — заполненная v2 проверяет unknown/different/denied и matching-owner-deleted с сохранением файлов и tombstone после перезапуска. Текущий 0e19 установлен штатно. Несовместимый старый бинарник поверх v3 не используется; контрактное доказательство v2 не называется ручной миграцией каждого пользовательского объекта. |
| S39 | DL `testOldServerCannotMutateDeletionBeforeCapabilityConfirmation`: absent/unknown version оставляет resolving/serverUpdate, мутаций ноль. DL `testDeletionBridgeRejectsUnknownVersionAndUnboundedSelection`; MIX ошибка старого bridge с предложением обновить приложение. | Контрактная совместимость — неподтверждённый capability оставляет pending/serverUpdate без мутаций; старый bridge предлагает обновить приложение; старые create/part/finalize API сохраняют deletion guards. Старые бинарники до v3 не устанавливались поверх v3. Совместимый rollback F262 не подменяет приёмку старого приложения; границы указаны в evidence.md. |
| S40 | MIG: `test_upgrade_preserves_existing_deletion_evidence_and_enforces_unique_tasks`, `test_duplicate_preflight_preserves_all_evidence_and_previous_schema`, `test_downgrade_refuses_to_erase_cancellation_markers`. LEG: оба заполненных v2 сценария. BULK: 100 pending/accepted/rejected операций восстанавливаются без потери ID/receipt. | Автоматические migration/preflight/unsafe-downgrade/v2/restart проверки дополнены штатным установленным rollback db76 → 43f → db76: v3/0092 и 78 identities / 17 terminal markers / 1 operation сохранены. После rollback API report200/detail404; после возврата API login429, сохранность очереди PASS. Полное операторское восстановление backup — отдельный release gate. |
| S41 | MIX: проверяет alias focus, `.check()` и `.click()` для подтверждения/отмены. SH `testActiveRecordingStopBoundaryDoesNotDependOnEmbeddedSurfaceState` проверяет границу Stop. | Подтверждено — пользователь лично проверил VoiceOver и сообщил, что всё работает. Клавиатура WKWebView проверена на db76; Chromium focus test выполняет Space/Tab/Enter/Escape, 20 публикаций для четырёх контролов, handoff/filter/removal. Личная приёмка не приписывается автоматическому средству. |
| S42 | DEP `test_dependency_report_groups_external_workflow_temp_and_diagnostics_truth`, `test_local_purge_aggregate_prioritizes_unfinished_or_failed_device_truth`; LP `test_failed_local_purge_acknowledgement_updates_report_without_private_payload`; ACCESS запрет контента при deleting. | Частично: модели отчёта различают состояния. Нет остановки реальных сервисов очистки/MediaScribe/backup; DEP использует искусственные строки, не подтверждает runtime retention observability. |
| S43 | REST `test_restored_old_transcript_and_audio_cannot_bypass_retained_deletion_fence`: реальные PostgreSQL строки прежних MediaRevision/TrackArtifact/ProcessingResult/TranscriptSegment/DiarizationSegment и синтетические аудиообъекты восстановлены после удаления; deleted_at/state/epoch сохранены; API/web/desktop detail, transcript/audio download и playback закрыты, отчёт доступен, чужая незатронутая встреча видна. | Авто — подтверждён запрет выдачи восстановленного контента при сохранённом fence. Не выполнялся операторский restore backup вместе с удалённой до удаления Meeting-строкой; экспорт/повторное применение deletion ledger при полном restore — отдельный release gate. |
| S44 | BULK `testHundredMixedOperationsKeepIndividualTruthAndResumeAfterRestart`: принятые/отклонённые операции неизменны после retry остальных. BA `successfulBatchSurvivesLaterFailure` (`partialBatch`): первый lifecycle batch скрывает удалённую строку и обновляет count, второй 503 сохраняет неизвестную строку. MIX `no refresh storm for unchanged rejection`. | Авто — подтверждены независимые результаты очереди, сохранение уже применённого успешного batch и актуальный count при следующем сбое. Частично: один поздний общий native bulk callback со смешанными per-item результатами отдельно не моделируется. |
| S45 | BA `boundedAliasesCannotStarveRows` (`aliases`): 100 upload aliases и 100 server rows, все 200 проверены, каждый request ≤100; `successfulBatchSurvivesLaterFailure` не теряет успех раннего batch. BULK: 100 операций без starvation и повторных terminal вызовов. DL `testHundredLocalPackagesCleanWithinBudgetAndRemainDeletedAfterRestart`. | Авто — bounded batches и 100 operations дополнены PURGE testSuspendedUploadDoesNotBlockDeletionAndConcurrentExecutorDoesNotDuplicateIt: задержанный upload не блокирует deletion, второй executor не дублирует, поздний upload не снимает запрет. Несколько реальных окон и частая активация не являются отдельным нагрузочным прогоном. |
| S46 | DL `testServerOnlyOperationSurvivesQueueRoundTripWithoutAudioItem`, `testDeletionRequestsAreDurableScopedAndIdempotent`: metadata-only операция сохраняется в том же store, items пуст. | Частично: модель и disk reload проверены. Полный native server-only delete из интерфейса с restart требует приёмки установленного клиента. |
| S47 | Q `testDamagedItemCannotBeSentAndCanBeDeletedLocally` применяет trusted synthetic local-only fixture; DL `testLegacyOwnershipIsUnavailableAndFutureQueueIsPreserved` запрещает неизвестное владение. | Авто — RECOVERY testSavingAndUnboundCaptureRequireExplicitActionBeforeBindingOrDeletion использует настоящие enqueueSaving/enqueue: processDueItems и смена аккаунта не привязывают и не отправляют unbound; только явный retry привязывает. Неизвестный legacy владелец сохраняет запреты. |
| S48 | BR S48 в `main`: один и тот же WAV дважды передан реальным XHR/FormData синтетическому HTTP-серверу. Между импортами первый meeting удалён через кнопки; origins и meeting IDs различны, SHA-256 файлов одинаков. Новый detail доступен после lifecycle poll, старый href отсутствует. | Авто — подтверждён явный повторный импорт того же содержимого как новой записи, независимый от прежнего удаления. Это клиентский контракт с синтетическим сервером, не экспорт настоящего пользовательского файла. |
| S49 | `test_recording_deletion_scope_acceptance.py`: настоящий PostgreSQL, owner/admin удаляет другого creator; requested_by сохраняет actor/device; новая session и свежий app возвращают прежний receipt; снятие роли запрещает обеим сессиям повтор/live delete/report без мутации. DL `testExecutionScopeCannotBeBorrowedByAnotherActor` проверяет различие scopes и meeting target; HX `test_deletion_receipts_and_index_do_not_cross_workspace_access` проверяет отказ чужому пространству. | Авто — manager success и повторная авторизация подтверждены через настоящие API/AuthSession и свежие app/engine. Это не перезапуск процесса ОС; client scope дополнительно проверяет DL. |
| S50 | OC `test_cancel_before_create_is_durable_and_repeatable`: origin receipt без meeting_id, повтор тот же; OC `test_lifecycle_batch_returns_metadata_receipts_and_bounds_selection`: canceled_before_creation с receipt; DL `testVerificationRequiresAcceptedDeletion` отделяет accepted от verified. | Авто — SCOPE test_origin_only_receipt_creates_no_meeting_or_meeting_purge_foreign_keys подтверждает один marker и отсутствие Meeting/request/report/artifact/task/audit/retention после cancel/repeat/lifecycle/late create. Локальная очистка и различие receipt/verified проверяются DL; отсутствие серверной Meeting не создаёт фиктивный FK. |
| S51 | OC `test_lifecycle_batch_returns_metadata_receipts_and_bounds_selection`: missing meeting → unavailable без раскрытия контента; PROC `test_unavailable_deletion_target_has_no_receipt_and_next_target_can_be_deleted`; UP `deletion_accepted` и `unavailable` отзывают единственную upload alias. PL освобождает проигрываемый источник при потере доступа. | Авто — meeting lookup/access denial, browser revocation и component player release дополнены PURGE scope-change при отложенном ответе: denied/old scope не разрешает purge/ACK. Установленный remote-delete образец S24 server-only подтверждает исчезновение строки; двухпользовательский ручной revoke открытого чужого контента не выполнялся. |

## Дополнительные приёмочные доказательства 2026-09-09 UTC

Эта таблица дополняет и уточняет остатки строк выше; отсутствие одного общего сквозного теста не отменяет совместное доказательство реального сервера, очереди и DOM.

| Сценарии | Новое прямое доказательство | Граница |
|---|---|---|
| S15–S16, S47 | RECOVERY `testSavingAndUnboundCaptureRequireExplicitActionBeforeBindingOrDeletion`: настоящие enqueueSaving/enqueue, запрет удаления writer, тот же ID после сохранения; смена аккаунта и processDueItems не привязывают/не отправляют unbound; явный retry привязывает. | Авто; не имитируется настоящая запись пользовательской встречи. |
| S19 | RECOVERY `testRetryAfterSurvivesRestartAndDefersEveryOperationInScope`: настоящий HTTP 429/Retry-After 120, 2 операции, сохранённый срок, restart до/после срока, те же IDs/targets. | Авто — 100 mixed queue results и независимый 403 дополнены RECOVERY testRetryAfterSurvivesRestartAndDefersEveryOperationInScope: настоящий HTTP 429 / Retry-After 120, 2 операции, сохранение срока и тех же IDs/targets после restart до/после срока. Ошибка одной цели не откатывает принятую. |
| S25, S32–S33 | PURGE `testRealFilesystemFailureDoesNotBlockAnotherPackageAndRetriesAfterRepair`, `testLostAcknowledgementRestartsAndRetriesExpiredTaskWithoutAnotherDelete`: реальные временные файлы, POSIX отказ, repair/restart; повтор прежнего expired ACK без нового delete. | Авто; не требуется аварийное завершение пользовательского приложения. |
| S34–S35 | PURGE `testMissingMappingAndSymlinkNeverClaimVerifiedCleanup`: отсутствующий пакет и симлинк за root не получают verified; внешние bytes сохранены. | Авто; статический симлинк, не атакующая подмена во время системного вызова. |
| S36 | RECOVERY `testUserDeletionSaveFailureLeavesFilesAndMemoryUntouchedBeforeAnyNetwork`: requestDeletion не сохраняет intent при реальном отказе atomic destination, bytes/cache остаются прежними,0 сетевых вызовов. | Авто — RECOVERY testUserDeletionSaveFailureLeavesFilesAndMemoryUntouchedBeforeAnyNetwork вызывает настоящий requestDeletion при отказе атомарной записи: bytes/cache прежние, сетевых вызовов нет, файлы сохранены. Реальная ошибка записи достаточна без заполнения диска. Пользовательское сообщение об этой конкретной файловой ошибке отдельно не проверялось. |
| S07, S37 | RECOVERY `testRestoredFilesCannotResurrectTerminalOriginDuringRealScan`: восстановлены настоящие WAV/AAC, новый service и scanAndEnqueueCompletedRecordings не создают ID/строку/playback/upload. | Авто; сохранённый tombstone обязателен, откат очереди до удаления не поддерживается. |
| S27, S51 | PURGE `testScopeChangeDuringSuspendedResponsePreventsOldScopePurgeAndAcknowledgement`: смена actor пока ответ задержан, поздний ответ не удаляет/не подтверждает чужой пакет, playback запрещён. | Авто; API denial и browser revocation подтверждены отдельно. |
| S04, S45 | PURGE `testSuspendedUploadDoesNotBlockDeletionAndConcurrentExecutorDoesNotDuplicateIt`: upload задержан, deletion принимается раньше его завершения, второй executor не дублирует, поздний upload не разрешает контент. | Авто; проверка независимости исполнителей, не нагрузка нескольких окон. |
| S13 | `test_upload_session_lifecycle.py`: create/part guard, late cleanup journal, finalize commit fence —4PASS. | Авто — прежние normalization/callback fences дополнены 4 PostgreSQL проверками test_upload_session_lifecycle.py: create/part guard, late cleanup journal и finalize commit fence. Настоящая ручная гонка TCP и физическое объектное хранилище не заявляются. |
| S24, S30 | Установленный GRAF Dev 43f7074: pause/seek/play/delete закрыл local player, own folder отсутствует; отдельная сессия удаляет visible meeting, row+upload card отсутствуют через ≤17,453 с после 202. | Один установленный образец; подробности в evidence.md. |
| S41 | `local-recording-focus.test.cjs`: Space/Tab/Enter/Escape, 20 одинаковых native publications для 4 контролов, сохранение узлов/focus, handoff, фильтры/время/удаление. | Подтверждено — пользователь лично проверил VoiceOver и сообщил, что всё работает. Клавиатура WKWebView проверена на db76; Chromium focus test выполняет Space/Tab/Enter/Escape, 20 публикаций для четырёх контролов, handoff/filter/removal. Личная приёмка не приписывается автоматическому средству. |

RECOVERY: `apps/macos/Shared/Tests/RecordingDeletionRecoveryAcceptanceTests.swift` (4 PASS). PURGE: `apps/macos/Shared/Tests/RecordingDeletionPurgeAcceptanceTests.swift` (5 PASS). Новых продуктовых ошибок эти наборы не выявили. Остатки S49/S50 уточнены в основных строках по 3 новым PostgreSQL тестам.

## Как использовать при завершении T019

Сопоставление закрывает инвентаризацию 51 сценария, но само по себе не закрывает T019/T022/T023. При принятии каждого сценария связать: названные тесты и журнал их запуска на финальном SHA; отдельный установленный сценарий, если он нужен; конкретные оставшиеся ограничения. Не заменять проверку UI одним сходным названием теста и не считать отсутствие найденной ошибки успехом.

Прежние пробелы S09/S20/S23/S48 закрыты прямыми браузерными assertions в описанной области; S38 закрыт заполненными v2 проверками, S43 — тестом восстановленного контента при сохранённом fence. S02 завершён: установленный UI/server delete и native purge после штатного перезапуска подтверждены. C1/C2 принимаются по совокупному evidence; перед ready PR остаётся привязать окончательный коммит документов к установленному SHA и CI; заключительный smoke 0e19 прошёл 13/13. Текущий 0e19 установлен, VoiceOver лично принят пользователем, совместимое восстановление v3/0092 подтверждено. Полный операторский restore и публичная поставка относятся к выпуску. Остатки по конкретным гонкам и недостающим assertions перечислены в строках; не требуется дублировать автоматические инварианты ручными проверками.

## Дополнение на 0e19: S06/S08/S18 и независимый итог

Новые проверки находятся в test_recording_deletion_scope_acceptance.py (4 PostgreSQL PASS), DesktopMeetingShellWebViewBoundaryTests.swift (17 Swift PASS) и recording-deletion-acceptance.test.cjs (8 Chromium PASS, включая threeTypes/offlineCopy). Cabinet shell 92 PASS, дополнительные local-only/unknown и focus проверки PASS. Журналы и SHA перечислены в evidence.md. Независимый final_convergence повторно проверил focus/threeTypes/offlineCopy и не выявил новых дефектов реализации. S02 физически очищен после штатного перезапуска, итог конвергенции реализации/приёмки — converged. Заключительный smoke 0e19 прошёл13/13. Готовность PR требует required check финального SHA; tasks/checklists этим сопоставлением не меняются.
