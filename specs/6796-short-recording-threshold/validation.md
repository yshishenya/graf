# Проверка реализации F6796

Lane: high-risk-feature (capture/storage/deletion/UX), полный Spec Kit. Ветка codex/6796-short-recording-threshold, база ad71f2ce4db68d846d7c333213961c5f5f7d5e89. Код и стенд зафиксированы в 8f68e8697497582dc33d6d7148d84844057686ca; PR #6950. Это первоначальная база; последующие runtime исправления и приёмка указаны ниже, exact PR HEAD и его CI — в описании PR.

## Текущее состояние после завершения приёмки

T001–T010 выполнены. Новая установленная сборка
`47b6d87d191216f8cbd111470fe98232d1c3c8e5` прошла необходимую повторную
матрицу после исправлений review,127 профильных тестов и независимую проверку.
Результаты описаны в последнем разделе. Озвучивание VoiceOver остаётся
native-unverified после пользовательской отмены ручной процедуры; #6958 открыт.
Финальный коммит меняет только отчёт и требует собственных GitHub checks.
Merge/закрытие issues требуют также разрешённых review threads; release-full,
публикация и deployment этим отчётом не подтверждаются.

## Локальные проверки

- `swift test --package-path apps/macos --filter 'ShortRecording|LocalRecordingWriter|CaptureRecoveryService|DesktopUploadQueueTests'`: PASS, 95 тестов, 0 ошибок. Включена реальная финализация 30 секунд синтетического аудио, 480000 кадров WAV и ненулевые первые кадры.
- `swift build --package-path apps/macos --product TwoBrainRecApp`: PASS; установленная проверка описана ниже.
- `git diff --check`, `validate-changelog-fragments.py`: PASS.

- Требования: requirements 6/6, safety 8/8, независимый analyze PASS; GitHub issue canon PASS (300 задач проверено).
- Порог: 479999/480000/480001 кадров, обе штатные причины, неизвестная частота/кадры, авария, отсутствие новой причины у старого вызова.
- Writer: решение записано вместе с финальным manifest; старый клиент видит blocked. Quit во время Stop отменяет правило до решения; после решения применяется повторяемая очистка.
- Queue: устаревший saving, маркер после перезапуска, отсутствие строки, конфликтные ID/пути/server handoff, symlink escape, повтор после частичной очистки, исторический короткий пакет. Upload client не вызывается для отклонённых пакетов.
- Уведомление: не получает фокус, заменяется, закрывается и исчезает примерно через 6 секунд. Это автоматическая проверка свойств, не ручная приёмка VoiceOver/тем/Spaces.
- Governance: первоначальный отказ из-за локального specify 1.0.6 при lock 1.0.1 устранён запуском проверки с изолированным specify-cli по закреплённому ref 9118ed15a0ba65053469a94c560ea5d233f75884. Глобальная установка и lock не изменены. check_spec_kit_governance.py PASS.

## Первоначальное состояние до установленной приёмки

T006 остаётся открытой из-за сбоя захвата на установленном GRAF Dev. Обычная остановка до/после 30 секунд, автоматический сценарий, Stop ≤2 секунд, VoiceOver, темы и сообщение поверх другого приложения не подтверждены. PR можно проверять по коду, но обязательная native приёмка блокирует слияние. Merge, публичный выпуск и production deploy не выполнены.

Issues #6943–#6949 и #6951 остаются открытыми до соответствующей приёмки и слияния. Локальные тесты и smoke стенда не заменяют проверку реального захвата.

## Согласованность после реализации

speckit-converge: проверены FR-001–FR-009, SC-001–SC-004, обе истории и пять решений плана. Недостающего/противоречащего/необоснованного кода не выявлено; новые задачи не добавлены. Подтверждение пользовательских критериев SC-004 на установленной сборке и итоговые CI/приёмка остаются в существующей T006; полного завершения feature нет.

Независимый code review: Approved, CR1 закрыт; см. code-review.md. T001–T005 завершены локально, T006 pending. Автокоммит hooks выключены; hooks converge отсутствуют.

## Продолжение до PR ready

Пользователь 2026-09-12 разрешил все действия до PR ready. Коммит реализации 8621cded103f4ba0d724aea4282a009b2e307016, PR #6950. GitHub governance-fast PASS: https://github.com/yshishenya/graf/actions/runs/34694733949 ; pr-metadata PASS. Эти результаты относятся к исходному implementation SHA; дополнение стенда требует нового CI.

T007: точный перенос только трёх файлов harness из e964bba55ed594f8bdccf94068d75fd07590c1b5 (F6793). `uv run --project apps/server --extra dev python -m pytest tests/governance/test_graf_local_adapter.py -q`: 43 PASS. Вторая функция/native settings не перенесена. Исходный shared Dev освобождён владельцем F6793; штатная сборка/установка F6796 разрешена, проверка exact SHA/lock не обходилась.

## Установленный стенд и текущий блокер

2026-09-12 штатный dev-harness выполнил build → promote → smoke для
8f68e8697497582dc33d6d7148d84844057686ca, manifest `dev-8f68e8697497`.
Единственный GRAF Dev установлен и запущен с сохранённой подписью/идентичностью.
Smoke: 13/13 PASS, включая exact_source_sha, app_identity, app_presentation,
сервер, хранилище и обработчики. Это подтверждение стенда, не качества захвата.

Для этого SHA GitHub governance-fast PASS:
https://github.com/yshishenya/graf/actions/runs/34695023497 ;
pr-metadata PASS: https://github.com/yshishenya/graf/actions/runs/34695023443 .
T007 завершена: 43 pytest, независимый infra-review, штатная сборка/установка,
smoke и CI подтверждены. Последующее изменение evidence требует CI на новом
PR HEAD; квитанции установленного приложения остаются привязаны к 8f68e8697497.

Три попытки реальной записи синтетического звука завершились до порога:
`status=failed`, `failureReason=capture_failed`,
`captureFailureCode=render_reference_missing`, `shortRecordingDiscarded=nil`.
Каждый аварийный фрагмент сохранён; правило короткой штатной записи к нему
не применилось. Это PASS сохранения аварийного фрагмента, а не PASS штатного
отбрасывания. Старые записи и пользовательские настройки не менялись.

Независимый разбор capture_paths: RecordingAudioTimeline.swift и
SystemAudioCaptureService.swift совпадают с базой ad71f2ce4. Ошибка означает
непокрытый промежуток системного аудио; небольшие расхождения до 48 кадров
при 48 кГц уже компенсируются. Доступные метаданные не содержат размера
промежутка и соседних временных меток, поэтому конкретная причина на стороне
источника пока не установлена. Порядок поступления обычных задержанных блоков
сам по себе не объясняет разрыв: вывод ограничен более медленным источником.

Следующий шаг для снятия блокера T006: получить только технические метаданные
первого непокрытого промежутка (источник, ожидаемое/полученное начало, число
кадров, частота, discontinuity), установить и устранить причину в захвате,
затем повторить установленную матрицу quickstart. Порог/защиту целостности
аудио ради прохождения проверки не ослабляли. Исправление источника аудио
не включено в F6796 без установленной причины.

Итог convergence: implementation ready; tracker pending. Проверка кода
одобрена, но T006 и пользовательская приёмка остаются открытыми. Перевод PR
из draft в ready for review означает приглашение к проверке кода и не снимает
блокер слияния.

## Продолжение T008

Добавлена разовая локальная диагностика источника и временной шкалы, без
изменения PTS/аудио/допуска. Проверка требований capture-diagnostics 3/3 PASS,
согласованность spec/plan/tasks проверена независимым reviewer. T008 → #6954,
канон GitHub проверен. Исходный PR HEAD 8942e821329f прошёл также автоматические
Code Review и Security Review Codex; новых замечаний не опубликовано.

`swift test --package-path apps/macos --filter 'RecordingAudioTimelineTests|SystemAudioSampleExtractor|ShortRecording|LocalRecordingWriter|CaptureRecoveryService|DesktopUploadQueueTests'`: 125 PASS, 0 ошибок. Синтетический разрыв 10 ms
даёт `recording_timeline_gap`, прежний renderReferenceMissing и сохраняет
480 кадров обработанного префикса. Подтверждение причины реального сбоя
и T006 пока открыты.

T008, уточнение наблюдаемости: контрольный NSLog виден в stderr, но не
прочитан через доступный unified log. Метаданные подключены к существующему
AppLog через optional diagnosticLogger; фактическая запись файла выполняется
на utility queue, вне очередей захвата. Проверка с collector подтверждает
точные допустимые поля и одно сообщение при двух разрывах. Повтор профильной
матрицы: 125/125 PASS. Первый диагностический commit 3ba80f4dd прошёл штатную
сборку и GitHub governance-fast run 34709828328; установлен не был, поскольку
общий стенд занят F6793. Новый вывод в AppLog требует нового SHA/CI/build.


## Повторная проверка 2026-09-13

Пользователь разрешил доведение до готовности к релизу. Ветка без потери
изменений синхронизирована fast-forward с remote HEAD
`3cec40582e2a93529b72983796aef61c49056656` (включает master релиза
v2026.09.13.1). Независимый reviewer capture_paths подтвердил отсутствие
изменений capture/Stop/manifest/queue/notice относительно e2238c653; смежное
продление auth cookie не обходит проверки очереди.

- 125 профильных XCTest — PASS, 0 ошибок.
- Changelog fragments, git diff --check, Spec Kit governance — PASS.
  Governance использует существующее изолированное окружение specify 1.0.1;
  глобальный specify 1.0.6 и bootstrap lock не изменены.
- GitHub governance-fast на 3cec40582 — PASS:
  https://github.com/yshishenya/graf/actions/runs/34719430230
- GitHub pr-metadata на 3cec40582 — PASS:
  https://github.com/yshishenya/graf/actions/runs/34719430217
  Первоначальные отказы после merge вызваны старым Exact source SHA в PR body;
  описание исправлено. Это не обнаруженная регрессия кода.
- Штатные build → promote → smoke установили `dev-3cec40582e2a`;
  все 13 smoke-проверок прошли, идентичность приложения сохранена.

На том этапе причиной остановки приёмки был инструмент управления интерфейсом CUA
читает GRAF Dev; getApp, getAXState и Raise выполнены. Вызов нажатия Start
недоступен через диспетчер инструмента. Вторая рабочая сессия независимо
подтвердила ту же недоступность действий. Mac не считался заблокированным.
Новая запись не была запущена; новых диагностических событий нет. Нельзя
считать этот прогон воспроизведением или устранением render_reference_missing.

Общий Dev-слот возвращён владельцу проверки F6793; последующая установка
может изменить active manifest. Квитанция выше относится к выполненному
прогону 3cec40582, а не обещанию неизменного установленного приложения.
T006 и T008 остаются открыты. После восстановления управления нужны штатная
установка текущего кандидата, реальный Start/Stop, классификация разрыва,
при необходимости исправление причины и полная матрица quickstart.
Порог и защита аудио не ослаблялись. Готовность к релизу не подтверждена.


## Установленная приёмка 2026-09-13: AppleScript

После явного разрешения владельца обычные кнопки GRAF Dev проверены через
macOS Accessibility/AppleScript. Ограничение CUA больше не блокирует управление.
Проверенный источник: `6991154bf79b5cd9cb64d32e907acebb95f319a6`,
manifest `dev-6991154bf79b`; штатные build/promote и smoke 13/13 PASS.
Required governance-fast и pr-metadata PASS на этом SHA:
https://github.com/yshishenya/graf/actions/runs/34720092916 и
https://github.com/yshishenya/graf/actions/runs/34720092963.

- Ручная длительная запись: saved, 1 160 467 кадров при 16 кГц (72,5291875 с),
  3 481 401 кадр при 48 кГц; difference=0; ptsGap/underrun/overrun/processError=0.
- Штатная запись около 10 с: short_discarded, новый каталог удалён,
  Start доступен примерно через 1 с после Stop.
- Запись около 5 с: правильный текст уведомления виден через 0,4 с,
  отсутствует через следующие 6 с; фокус остался в другом приложении.
- Start/Stop через меню GRAF: short_discarded; уведомление видно поверх
  другого приложения после скрытия окна, фокус не захвачен.
- Попытка длительной записи с выключением микрофона завершилась
  render_reference_missing. Разрыв появился ДО выключения микрофона.
  Аварийный фрагмент сохранён, shortRecordingDiscarded отсутствует.

Числовые данные сбоя: declared_frames=decoded_frames=960, rate=48000,
channels=1, declared_gap_ms=decoded_gap_ms=15,4375;
expected_frame=42981, requested_frame=43722 (разница 741),
input_frames=converted_frames=960. ProcessedFrameCount=90,
hostUnderrunCount=1, ptsGapCount=2, processErrorCount=0,
nonFiniteSampleCount=0, processingTimeP95Ms=1.

Доказан разрыв raw PTS между принятыми SCK-буферами до преобразования.
Физическая потеря 741 отсчёта не доказана: прежняя диагностика не сопоставляет
outputPTS и CoreMedia duration. T008 расширяет то же единичное событие
относительными величинами; алгоритм/допуск и аварийное сохранение не меняются.
T006/T008 остаются открыты. Остались полная матрица quickstart, повтор
с выключенным микрофоном, автоматический путь, перезапуск, VoiceOver,
светлая тема и проверка начала аудио. Снимки экрана и аудио в git не включены.

После прогона общий Dev-слот передан F6793, выполнен штатный rollback на
`0ec9eed61cf35a7ba4869e17db01a9a3d1a05395` (smoke 13/13).
Квитанция выше относится к выполненному прогону, а не текущей версии Dev.


Расширение T008 после этой приёмки: 126 профильных XCTest PASS (включая
синтетическое различие raw/output PTS, invalid/indefinite/infinite duration и
неизменность извлечённого аудио); Spec Kit governance, changelog и diff-check
PASS. Эти проверки не доказывают результат следующего установленного прогона.


## Реальный прогон расширенной диагностики

Источник `ddb5376a82ee3b5acf8c70d78dcdbef7ebb556f1`; build/promote и smoke
13/13 PASS. GitHub governance-fast/pr-metadata PASS:
https://github.com/yshishenya/graf/actions/runs/34745770214 и
https://github.com/yshishenya/graf/actions/runs/34745770215.
Непрерывный синтетический тон начат за 2 с до Start через меню:
штатно сохранено 583265 кадров16кГц (36,4540625 с), разрывов/ошибок нет.
В первых100мс WAV обнаружен тон440Гц (амплитуда0,03652, RMS0,02585);
начало записи сохранено. Это количественная проверка только созданного тона.

Следующий прогон с тем же заранее начатым тоном снова завершился аварийно
до подтверждения выключения микрофона. Входной разрыв720кадров/15мс:
raw/output gaps=15мс; previous/current declared=decoded=960@48кГц;
duration/outputDuration=20мс у обоих блоков; output-minus-raw=0;
arrival gap=31,8478мс; max completed callback=0,170083мс.
Timeline expected=42292, requested=43012; processedFrameCount=88,
underrun=1, ptsGap=2, processError/nonFinite=0, p95=0,5мс;
аварийный префикс сохранён, shortRecordingDiscarded отсутствует.
Это исключает различие raw/outputPTS и долгую работу завершённых callbacks
как объяснение данного сбоя. Конвертер не создаёт этот входной разрыв.

Системный журнал replayd при запуске сообщает clockMightDrift=1 для
аудио- и host-часов; overload в журнале наблюдался уже после остановки и
не доказывает причину предыдущего разрыва. Следующая проверка — диагностическое
преобразование PTS через SCStream.synchronizationClock. Физическая потеря
отсчётов и исправление причины пока не доказаны. T006/T008 открыты.
После прогона слот передан F6793, штатно установлен их2b33941; текущий Dev
не приравнивается к проверенному ddb5376. Личные данные/аудио/скриншоты вgit
не включены.


Дополнение converted_host_gap_ms: 126 профильных XCTest PASS; независимый
code review Approved; Spec Kit governance/changelog/diff-check PASS.
Установленное значение нового поля пока не получено.


## Установленный прогон с пересчётом часов

Источник `5c7350e848310fbd14bbef01fd264af07b49b88b`: штатные build/promote,
smoke13/13 PASS; GitHub governance-fast/pr-metadata PASS:
https://github.com/yshishenya/graf/actions/runs/34746431247 и
https://github.com/yshishenya/graf/actions/runs/34746431251.

Первый запуск с заранее начатым синтетическим тоном завершился ошибкой до
запланированного выключения микрофона. Raw/output/converted_host gaps все
15,2083мс; expected41345→requested42075 (730кадров); оба блока960@48кГц,
CoreMedia durations20мс; arrival32,0025мс, max callback0,458875мс.
Аварийный префикс сохранён; 86 обработанных10мс кадров, ptsGap2, underrun1,
processError/nonFinite0. Пересчёт часов не объяснил наблюдаемый разрыв.

Повторный запуск с тем же сигналом: микрофон выключен через5с, silenced
33,499с; запись штатно сохранена: 610702кадра16кГц (38,168875с),
1832106кадров48кГц; ptsGap/underrun/overrun/processError/nonFinite=0;
Start доступен через1с. Это PASS учёта системного аудио при выключенном
микрофоне; предыдущий аварийный запуск не исключён из результатов.
Общий Dev-слот после завершённой записи передан F6793, установлен b31671.

Подтверждённая классификация: разрыв временной позиции между доставленными
SCK аудиоблоками до преобразования, не расхождение чисел кадров/выходногоPTS
или исправляемое наблюдаемым CMSyncConvertTime отличие. Причина в самом
источнике и наличие физического пропуска PCM остаются неразличёнными.
Порог, допуск48кадров и обработка ошибки не изменялись.

Дополнительный офлайн опыт использует только сгенерированный440Гц сигнал:
12 assert-проверок различают две фазовые гипотезы для720/730/741кадров;
кратный периоду разрыв, слабый/смешанный сигнал дают unknown; простое
44,1→48кГц преобразование проверено. Опыт не использует реальное аудио,
не включён в приложение и не доказывает причину установленного сбоя.
Фазовый анализ в production callback не добавлялся.


## Дополнительная проверка интерфейса 5c7350e84

Штатный rollback из b31671 восстановил тот же5c7350e84. Прямой promote
старого manifest сначала корректно отказал stale parent; защиту не обходили.
Проверена последовательность двух штатных коротких записей: после первого
Stop окно уведомления появилось, при новом Start исчезло за0,5с; оба
short_discarded подтверждены, новых сохранённых каталогов нет.

Через обычные настройки аккаунта GRAF Dev выбрана светлая тема, исходная
тёмная зафиксирована до изменения. После короткой записи панель уведомления
дождалась появления в пределах2с; снимок только её области показывает
полный читаемый текст на светлом фоне. В конце восстановлены исходные
radio values light0/dark1/system0. Предыдущая тёмная панель проверена на699.

VoiceOver был временно включён. В его штатных настройках управление через
AppleScript выключено; настройку не меняли. Чтение последней фразы не
подтвердилось, поэтому это НЕ native VoiceOver PASS. Соседняя задача F6793
передала прямую просьбу пользователя больше не проверять VoiceOver;
проверка прекращена, VoiceOver возвращён в исходное выключенное состояние,
VoiceOver Utility закрыта. Приёмка F6793 не выдана за приёмку уведомления
F6796. Вызов NSAccessibility.post проверен чтением кода и выполняется внутри
тестируемого show. Тесты проверяют панель и жизненный цикл; доставка
announcement и фактическое озвучивание не проверены.

UI/runtime передан F6793: запись закончена, тема восстановлена, настройки
автозаписи не менялись, Zoom не запускался. Автоматический путь и перезапуск
F6796 остаются открытыми. T008 закрыта в пределах классификации с подробной
квитанцией; первопричина открыта отдельно в #6958 (T000 triage по canon).


## Автоматическая запись и настоящий перезапуск, 2026-09-13

Установленный runtime: `5c7350e848310fbd14bbef01fd264af07b49b88b`.
Возврат с согласованного стенда F6793 выполнен штатным rollback;
13 проверок smoke PASS. Правило Zoom осталось `ask`, все сохранённые
настройки MeetingDetection совпали с исходными.

Первый локальный тест звука Zoom дал штатные автоматические Start/Stop,
но запись длилась около 46 секунд и сохранилась: это проверка длинного
автоматического пути, а не короткого. Открытый обычный GRAF тоже среагировал
на Zoom и штатно автоостановился. Его временно закрыли перед повтором;
эту запись не исследовали и не удаляли, production не является стендом
F6796. Повтор выполнялся только при работающем GRAF Dev.

Повторный прогон: `prompt_timeout` в 08:57:00 UTC, `recording.started`
в 08:57:01, закрытие Zoom через 2 секунды после обнаружения нового каталога,
`recording_stop_requested reason=meeting_ended` в 08:57:20,
`recording.short_discarded` в 08:57:21. Примерно 20 секунд по журналу,
порог применился по кадрам, без ручного Stop и изменения политики.
В 08:57:22 завершилось обновление очереди после discard. Созданный этой
проверкой каталог исчез; AX обнаружил панель уведомления GRAF Dev.
Снимок области перекрыт уведомлением Krisp, поэтому он не считается
визуальным доказательством текста. Текст/темы/фокус подтверждены отдельными
ручными прогонами выше. Никакого звонка участникам не было.

Повторный `promote` активного manifest вернул `idempotent=true` и НЕ
засчитан за перезапуск. Затем штатный `rollback --live` на тот же manifest
действительно переустановил/перезапустил Dev и проверил 13 smoke gates.
PID изменился; важнее, после запуска завершились `app_appeared` в
09:00:00 UTC и `coalesced_app_appeared` в 09:00:01 — оба обновления очереди.
После них каталог отсутствует, строк по directory/session ID в очереди 0.
Прямой read-only запрос к локальной Dev PostgreSQL по `local_recording_id`
только этой синтетической записи вернул 0 встреч. Реальная запись не
восстановилась и не появилась на сервере; отсутствие вызова upload client
при маркере дополнительно покрыто профильными тестами.

Локальные доказательства (за пределами git, без содержимого аудио):
`graf-6796-auto-short-baseline.json`, `graf-6796-auto-short-session-ids.json`,
`graf-6796-after-discard-restart-live.log`,
`graf-6796-auto-server-absence.json`. Сам факт нового PID или идемпотентный
promote не подменяют приёмку восстановления.

Повторная сверка speckit-converge: FR-001–009, SC-001–004, обе истории
(6 сценариев), 5 решений плана и применимые ограничения constitution.
Новых пробелов реализации нет; задачи convergence не добавлялись.
Writer/queue/Stop/notice между прежними установленными прогонами и5c не
менялись; поздние runtime изменения ограничены диагностикой источника.
T008 завершена как классификация; #6958 остаётся открытой.
Доставка/озвучивание VoiceOver native-unverified, ручная процедура отменена
пользователем. Это исключение не выдаётся за PASS.


Общий стенд после приёмки: штатный rollback восстановил
`8d816b478de5cc13a5331a4865e33923469d71cd` F6793, 13 smoke gates PASS.
Zoom закрыт, исходные правила автозаписи сохранены, тема не менялась (Dark),
VoiceOver выключен. В обычном GRAF восстановлено исходное running-состояние;
обычное меню показывает Start, активной записи нет. GRAF Dev также без записи,
выполнено «Открыть GRAF», встроенная webarea доступна. UI/runtime явно передан
F6793; дальнейших действий с приложениями эта задача не выполняет.


## Исправления review T009/T010

Оба GitHub замечания воспроизведены до runtime изменений: настоящий writer
с1_439_999 кадрами48k сохранял запись при userRequested и meetingEnded;
scanner удалял файлы при чужих ссылках. Это ошибки F6796, не прежний #6958.

Исправлены два общих места. Policy восстанавливает точные48k кадры по
существующим review.frameCount−aacPresentationFrameDelta с проверкой
переполнения и сравнивает с1_440_000; completeness обоих tracks сохраняется.
Предикат допустимого delta использует диапазон вместо abs(Int64.min).
Cleanup до любых save/remove учитывает6 явных/вычисляемых путей через
существующие helpers, независимо от completeness. Неизвестная длительность
и неоднозначное владение сохраняют данные, конвертер/формат/схема не меняются.

127 профильных XCTest PASS,0 ошибок,22,76с. Реальный writer/converter для
1_439_999/1_440_000/1_440_001 проверен по обеимStop причинам: WAV16k во всех
случаях имеет480000 кадров, а решение различается правильно. Проверены
сохранённый marker, первые ненулевые кадры, отсутствующий/экстремальный delta,
переполнение и неположительная длина.14 сочетаний чужих ссылок сsaving/без,
manifest/mic/system, ../, symlink ancestor, derivednested и соседнийshort2;
повторscanner сохраняет байты и полные persisteditems при конфликте.
Derivednested также проверяется через directoryPath и не объявляется
изолированным доказательством каждой вычисляемой ветви.

Журналы внеgit: graf-6796-review-red.log и
 graf-6796-review-green-verified.log. Две промежуточные ошибки самих новых
assert исправлены: loadItems — проекция UI, а не persistedrows; baselineitems
нужно декодировать доscanner, чтобы ISO8601 округлениеDate обеих сторон
совпадало. Проверка сохранённых строк не ослаблена; runtime от этого не менялся.

Предыдущая приёмка5c остаётся evidence автоматического пути, меню и presenter.
Новому runtime обязательны build/promote/smoke, короткий/длинныйmanualStop
и настоящийrestart со сканированием. T006 до этого открыта.


## Итоговая приёмка исправленного runtime47b6d

Exact runtime SHA: `47b6d87d191216f8cbd111470fe98232d1c3c8e5`.
Штатные build/promote и 13 smoke checks PASS. GitHub
[governance-fast](https://github.com/yshishenya/graf/actions/runs/34749290338)
и [pr-metadata](https://github.com/yshishenya/graf/actions/runs/34749290251)
PASS на этом SHA. Runtime содержит ровно независимо проверенные model/queue
blobs, указанные в capture-diagnostics-review.md.

Первый короткий запуск снова прервался с прежним render_reference_missing:
status failed, shortRecordingDiscarded отсутствует, доступный WAV сохранён.
Это остаётся #6958, не скрывается и не считается штатным PASS.

Повторный короткий Start/Stop выполнен успешно. Текст сообщения прочитан
через AX непосредственно из панели GRAF Dev: «Запись короче 30 секунд не
сохранена». От нажатия Stop до чтения сообщения1,497с; Start снова доступен.
Каталог этой проверки удалён, строки по её directory/session ID в очереди0,
точечный read-only запрос Dev PostgreSQL по local_recording_id вернул0 встреч.

Длинная запись сохранена без capture failure:1 800 164 канонических кадров48кГц
(37,5034167с),600055 кадров16кГц, AACdelta0. Контрольный тон440Гц присутствует
в первых100мс WAV: amplitude0,03637/RMS0,02580. Проверены только синтетический
сигнал и технические метаданные. ptsGap/underrun/overrun/processError/nonFinite0,
p95 обработки1мс. Start после Stop доступен.

Настоящий restart через штатный rollback на тот же47b6d manifest:13 smoke
PASS, новый PID. В09:25:33 UTC завершились app_appeared и coalesced_app_appeared
queue scans. После них короткий каталог отсутствует, queue0 и Dev server0;
запись не восстановилась и не была отправлена. Локальные доказательства:
`graf-6796-final-short-result.log`, `graf-6796-final-long-metadata.json`,
`graf-6796-final-restart.log`, `graf-6796-final-restart-metadata.json`.

Независимый reviewer той же задачи прочитал эти доказательства и подтвердил
Code+installed Approved. Полная прежняя auto/menu/theme матрица5c сохраняется
для неизменённых маршрутов и presenter; обе причины Stop дополнительно
проверены шестью настоящими writer boundary cases нового runtime. Повторное
включение VoiceOver, изменение настроек или внедрение повреждённых строк
в установленный Dev не требовались и не выполнялись.

После приёмки штатный rollback вернул `8d816b478de5cc13a5331a4865e33923469d71cd`
F6793,13 smoke PASS. Dev без записи, выполнено «Открыть GRAF»; настройки и тема
не менялись. UI/runtime явно передан владельцу F6793. Это готовность принятой
реализации к слиянию после финальных GitHub gates, не доказательство выпуска.

Повторная speckit-converge после T009/T010:9FR,4SC,6сценариев обеих историй,
5 решений плана и применимые capture/privacy/deletion/UX ограничения проверены.
Новых пробелов реализации нет; задачи не добавлены. Числовая граница48кГц и
ownership policy соответствуют исправленным plan/data-model/quickstart.
