# Feature 200: проверка Windows от 2026-09-06

Статус: работа продолжается. Этот документ не является разрешением выпуска.

## Основа и область

- Ветка: `codex/200-windows-desktop-app`.
- Синхронизирован master `6ff8db3ee18dc7faf52fd8a31c8bade97c5ca548`.
- Merge commit: `bb4fc1facc2d82355d165f1b7300efd4a2ff0990`.
- Текущие изменения реализации ещё не зафиксированы коммитом.
- Высокорисковый действующий срез Spec Kit, T080–T084. Требования проверены
  независимым рецензентом; задачи остаются открыты до фактической приёмки.
- Сервер и macOS не изменены и не развёрнуты.
- Последний `git fetch origin master` обнаружил `a389657e607ef8389fcf947b26b158cee6928884`
  (PR #6661): настройки моделей итогов через Langfuse. Он затрагивает сервер,
  инфраструктуру и связанные документы, но не Windows/macOS-клиенты. В эту
  ветку новый коммит ещё не влит; база собранного клиента указана выше.
- Legacy Impact: `remove`, только в Windows-срезе. Пользовательские записи и
  рабочие серверные/macOS пути не удалялись.

## Подтверждено

| Проверка | Результат | Граница доказательства |
|---|---|---|
| WebView2 Runtime | Установлен, версия 151.0.4129.107 | Отсутствие Runtime не было причиной показанной сетевой ошибки |
| DNS гостевой Windows | Прокси Parallels не отвечал; прямое разрешение работало | Исправление только DNS интерфейса гостя; Mac/VPN не менялись |
| GET `/login` из Windows после исправления DNS | HTTP 200 | Не является входом в аккаунт |
| Снимок Windows-окна после исправления DNS | Показана страница входа ГРАФ | Снимок предшествует последнему изменению нативных окон |
| CMake на Mac, AppleClang, C++17, warnings-as-errors | 20/20 PASS | Переносимые ветви, не WinUI/WASAPI |
| CMake в Windows 11 ARM64, x64 Debug, VS2022/SDK10.0.26100 | 20/20 PASS | Текущий на момент прогона снимок; последующие изменения требуют повтора |
| Реальный Media Foundation | Синтетические кадры → AAC/M4A → IMFSourceReader; формат и длительность проверены | Нет захвата устройств или частного аудио |
| Деградированный пакет и повреждённый файл | Проверки сохранённой длительности, запрета неверного playback и выхода за хранилище PASS | Не тест выключения физического устройства |
| Гонки capture/upload | Целевые ThreadSanitizer-прогоны PASS | Переносимая среда; Windows-сценарии отдельно |
| Release x64 MSBuild | Собран и связан обновлённый WinUI/WebView клиент | Это не визуальная приёмка |
| Ошибка запуска WinUI | Отладчик локализовал E_FAIL в конструкторе XamlControlsResources до Window/WebView | После добавления обязательного IXamlMetadataProvider процесс жив, окно GRAF создано |

### Снимок до продолжения проверки аккаунта

- Переносимый CMake-набор: **20/20 PASS, 6.74 с**, после всех изменений.
- Windows CMake, x64 Debug: **20/20 PASS, 3.22 с**, одна сборка и один прогон
  без повторов или исключений. Это полный набор Windows CMake, **не Full CI**.
- `CaptureFaultStateTests`: **PASS, 0.14 с**. UTF-8/UTF-16, асинхронный запуск,
  отмена, предел запуска 5 с, ожидание завершения устройства, отложенная
  финализация и однократное завершение. Через Windows API заново получены
  **2 устройства** после преобразования идентификаторов; аудиозахват не включался,
  имена и идентификаторы не выводились.
- `V5LocalRecordingWriterTests`: **PASS, 0.36 с**. Настоящие Media Foundation
  AAC/M4A и IMFSourceReader, 100 синтетических кадров, длительность 900–1100 мс.
- `DesktopUploadCustodySmokeTests`: **PASS, 0.30 с**. Настоящая Корзина,
  сохранность занятого файла, отказ для сетевого пути. За этот прогон в Корзину
  перемещена ещё одна небольшая синтетическая папка; её можно восстановить.
  Корзина не перечислялась и не очищалась, пользовательские записи не удалялись.
- `WindowsDiagnosticsRedactionTests`: **PASS, 0.11 с**. Безопасная структура,
  обязательное хеширование входа, размеры, числовые границы и отсутствие
  устаревших фиктивных счётчиков. Дополнительный переносимый ASan/UBSan PASS.
- MSBuild Release x64: **сборка и связывание PASS**. Запуск этой сборки после
  восстановления штатных скруглений WinUI: процесс жив и отвечает, заголовок
  главного окна `GRAF`. Повторный запуск завершился, остался один экземпляр.
  Это проверка запуска, не визуальная проверка содержимого окон.
- SHA-256 EXE:
  `423bf3dc15b170f932f212c8d3d78b693cc0cc49523e241443bf0d8f14ff55df`.
- Сводный SHA-256 исходников Windows, перечисленных `rg --files apps/windows`,
  отсортированных по `LC_ALL=C` и хешированных через `shasum -a 256`:
  `cd6990568a7832e450f19ea82d26565ab6eada65e0763478aacaa076cddb4299`.
- `rsync -ain` подтвердил отсутствие расхождений исходников с зеркалом сборки;
  сгенерированные `out/obj/bin/.vs/vendor` исключены. `git diff --check` и
  проверка changelog-фрагмента — PASS.
- В сборке приложения остались предупреждения внешних заголовков WebRTC и
  переопределения уровня предупреждений. В CMake — предупреждение MSB8064 о
  регистре пути общей папки Parallels. Ошибок сборки нет; результат не называется
  сборкой без предупреждений.

Команды итогового Windows-набора:

```powershell
cmake --build C:\Temp\graf-f200-tests --config Debug --parallel 2
ctest --test-dir C:\Temp\graf-f200-tests -C Debug --output-on-failure --timeout 30 --interactive-debug-mode 0 --parallel 1 --verbose
```

### Более ранние прогоны

Предыдущий Windows-прогон выполнялся с `ctest -C Debug --output-on-failure
--timeout 30 --interactive-debug-mode 0 --parallel 1`; 20/20, 2.60 с.

После него отдельно пройден новый `DesktopUploadCustodySmokeTests`, 1/1,
0.44 с: настоящий `IFileOperation` сохраняет занятый файл, отклоняет сетевой
путь, перемещает разблокированную синтетическую папку в Корзину и подтверждает
назначение через `PostDeleteItem`. Осталась одна небольшая тестовая папка,
восстановимая из Корзины; пользовательские записи не удалялись, Корзина не
перечислялась и не очищалась.

`infra/scripts/ci-local.sh --fast` завершился до тестов с
`ci_evidence_status=ambiguous`, `reason=dirty_worktree`. Это **не PASS**:
общий gate требует зафиксированного SHA. Обход проверки не применялся,
самостоятельный коммит без согласования не выполнялся.

## Исправления и очистка

- Удалены `CaptureHealthProjection`, `CustodyStatusProjection`,
  `AccessibilityState`, `WebRuntimeStateProjection`, `CaptureFaultRecovery`,
  `WindowsPermissionRecovery`: не было рабочих потребителей.
- Проверка констант доступности заменена честно названным тестом состояния
  индикатора. Доступность настоящих окон остаётся отдельной проверкой.
- Удалены синхронная отправка, прежние двоичные настройки автозаписи и
  неиспользуемые команды моста. Нет совместимых псевдонимов без потребителя.
- Сохранены записи, очередь, совместимость v5/v2 и проверки безопасности.
  Исходники удалённых модулей восстанавливаются из Git.
- Восстановлены lifecycle WebView, nonce/источник документа, переходы внутри
  страницы, повтор после ошибки, диалоги и загрузки без вложенного цикла событий.
- Локальные строки используют существующий интерфейс кабинета и непрозрачные
  идентификаторы. Нативные действия повторно проверяют состояние и хранилище.
- Удалены неиспользуемый `DesktopLocalPurgeService::purge`, три фиктивных вида
  подтверждения серверной очистки и `registerLocalPurge` с его маркером. Рабочая
  Корзина, подтверждение пользователем, проверка пути и журнал удаления сохранены.
- Удалены два пустых тестовых проекта `SmokeMain` и ссылки на них из решения.
  Настоящие 20 CMake-тестов сохранены; прежний успешный выход пустого проекта не
  считается проверкой продукта.
- По независимой проверке исправлены показ индикатора до начала захвата,
  неблокирующие запуск/отмена устройств, отложенное закрытие приложения до
  завершения записи и сохранения очереди. Кодирование WAV/AAC и проверка
  файлов выполняются вне потока интерфейса; окончательное изменение очереди —
  в её единственном потоке-владельце. Проверка потоков использует настоящие
  потоки выполнения с синтетическим телом устройства, не реальный микрофон.
- Исправлена ошибка размера буфера в обоих преобразованиях идентификаторов
  аудиоустройств: завершающий нулевой символ больше не делает вызов Windows API
  неуспешным. Проверяются и некорректные последовательности UTF-8/UTF-16.
- Обновление кабинета повторно проверяет локальные файлы после временной ошибки.
  Результат локального действия больше не скрывается старой ошибкой записи.
- Безопасная сводка получает реальные счётчики обработанных/записанных блоков
  только после завершения потоков. Префикс `ep_` больше не позволяет обойти
  обезличивание; фиктивные нулевые счётчики и неиспользуемые поля удалены.
  Кнопка копирования использует локальный буфер обмена, без отправки на сервер.

## Нерешённые условия

1. Визуальная проверка каждого окна не завершена: после прежней блокировки
   Mac содержимое кабинета снова видно, но прямой автоматический ввод в госте
   не даёт подтверждённых переходов. Причина не установлена. Успешное создание
   окна не выдаётся за визуальную проверку.
2. Нужны сценарии входа/выхода, OAuth-цепочки, списка/детали/экспорта, локальных
   действий и отправки на окончательной сборке с разрешёнными тестовыми данными.
3. Принадлежность записи фиксируется в момент Record, хранится в очереди и
   повторно проверяется перед отправкой; старая неизвестная запись требует
   явного назначения, а запись другого владельца не переназначается. Проверки
   очереди/декодирования/смены владельца пройдены. Первоначальное пространство
   теперь получается из существующего `/desktop/settings/spaces`, затем
   подтверждается через `/auth/me`; прежний вывод о необходимости изменения
   сервера снят. Алгоритм проверен синтетически, но сквозная отправка в
   настоящем кабинете ещё не подтверждена. Сервер не изменён и не развёрнут.
4. Для серверной очистки нет подтверждённой авторизации конкретной установки.
   Общая браузерная сессия не является основанием подтверждать такую очистку.
5. В Windows-каталоге нет утверждённых целей. Установленный подписанный Teams
   является наблюдением, а не разрешением автоматической записи. Нужны
   положительные и отрицательные проверки владения WASAPI-сессиями.
6. Общие условия автоматической записи не подменяются константой `true`.
7. T070/T071: подпись/чистая установка/обновление, реальное аудиооборудование,
   смена устройства, sleep/wake, системный звук, пауза/возобновление/Stop,
   физический x64-компьютер, клавиатура/Narrator/High Contrast/200% DPI.

## Справочная причина исправления WinUI

Приложение написано без App.xaml и автоматически создаваемого провайдера
метаданных. По официальному примеру Microsoft оно должно реализовать
`IXamlMetadataProvider`, делегируя три метода
`XamlControlsXamlMetaDataProvider`, до создания `XamlControlsResources`.

- [Microsoft: поддержка Microsoft.UI.Xaml.Controls.dll](https://github.com/microsoft/microsoft-ui-xaml/blob/aa3207e6ae8fce6f84baae35f965241913fe55ef/docs/design-notes/desktop-app-walkthrough.md#supporting-microsoftuixamlcontrolsdll)
- [Windows App SDK: пример провайдера](https://github.com/microsoft/WindowsAppSDK-Samples/blob/0057c23a44157b4cc8a539d52282a3d1f3559de8/Samples/Islands/cs-winforms-unpackaged/XamlApp.cs)

## Продолжение после автоматической очистки рабочей копии

- При возобновлении отсутствовала рабочая папка Feature 200; ветка и зеркало
  Windows сохранились. Найден автоматический снимок Codex `startup-cleanup`
  `d9c886e04df9f50af373c8b9b43a3030b0fbcda8`, родитель — `bb4fc1fac`.
- Снимок восстановлен в исходную рабочую папку. Ветка остаётся на `bb4fc1fac`,
  изменения снова незакоммиченные; сохранены новые файлы и прежние удаления.
  Нового implementation-коммита и push нет, другие рабочие копии не изменены.
- Сводная контрольная сумма Windows совпала с указанной выше
  `cd699056…4299`; каждый Windows-исходник также сверён с Git-снимком.
  SHA-256 EXE совпал с `423bf3dc…5df`. Повторная сборка неизменённого кода
  и повторное создание тестовых записей не выполнялись.
- Повторный fetch подтвердил `origin/master = 6ff8db3ee`.
- `speckit-bootstrap . --doctor --frozen`,
  `python3 scripts/check_spec_kit_governance.py`, `git diff --check` и
  `python3 scripts/validate-changelog-fragments.py` — PASS.
  Это проверка целостности рабочей копии, не GitHub Actions на SHA PR.
- VM Windows 11 запущена, один ранее запущенный процесс GRAF отвечает.
  Через CUA видна содержательная страница кабинета с авторизованной сессией;
  белого экрана и сообщения об отсутствии WebView2 на этом экране нет.
  Частное содержимое страницы не перенесено в доказательства.
- Прямые автоматические щелчки и клавиши не дали подтверждённых переходов,
  в том числе в интерфейсе Windows вне GRAF. Штатная команда поиска из меню
  Parallels изменила состояние гостя. Причина ограничения прямого ввода
  не установлена; работоспособность кнопок GRAF и сквозной вход не заявлены.
  Полная проверка каждого окна, консоли WebView, auth/upload/capture остаётся
  открытой. Настройки безопасности, VM и пользовательские записи не менялись.
- В документах моста удалены устаревшие обещания уже отклоняемых команд;
  действуют `request_app_quit` и `local_recording`, а в обратном направлении —
  `native_ready` и отображаемые `local_recordings`. Quickstart приведён к
  действующим правилам PR gate и changelog-фрагмента. Поведение кода не менялось.

## Продолжение T083: первоначальная проверка аккаунта

- Исправлен клиентский порядок запросов: `GET /desktop/settings/spaces`
  без workspace/device → строгое единственное активное пространство →
  `GET /api/v1/auth/me` с подтверждением workspace/user/active session.
  Оба запроса используют один снимок `X-Auth-Session`; WinHTTP не принимает
  cookies и не следует редиректам. Существующий серверный путь подтверждён
  исследованием обработчика spaces, session principal и tenant scope.
- Расширен существующий ограниченный JSON reader, а не добавлен второй:
  строгие типы, повторные ключи/UUID, глубина, 2 MiB и 1024 строки. Дополнительные
  поля не используются как полномочия. Владелец записи не переназначается.
- Смена сессии и закрытие приложения отменяют запрос аккаунта; проверка
  поколения входа сохранена. HTTP не выполняется в потоке интерфейса.
- TDD: новый тест сначала завершился ошибкой связывания отсутствовавших
  методов; после реализации `DesktopUploadRecoveryTests` PASS. Проверяются
  именно оба запроса, их порядок и поля, ошибки первого/второго запроса,
  отмена до/между/после запросов, неоднозначные ответы, несовпадение `/me` и
  отсутствие действующей сессии. Авторизационные ответы и токены не записывались.
- Переносимый CMake: **20/20 PASS, 7.12 с**.
- Windows CMake, x64 **Release**: **20/20 PASS, 3.71 с**, полный существующий
  набор без исключений. Предупреждение MSB8064 общей папки Parallels осталось.
  Синтетический тест Корзины переместил ещё одну небольшую тестовую папку:
  её можно восстановить; пользовательские данные и содержимое Корзины не читались.
- Проверка реального экрана снова показала полноценный список встреч без
  сообщения об отсутствии WebView2. Щелчок по «Настройкам» не дал перехода;
  отдельная поверхность приложения Parallels завершилась тайм-аутом. Полнота
  окон, сквозной auth/upload и новый запущенный EXE этим не доказаны.
- T083/T084 не закрыты. Серверная очистка по-прежнему требует подтверждённых
  полномочий конкретной установки; успешная проверка аккаунта их не выдаёт.
- Независимый review нового среза не нашёл блокирующих ошибок; отдельный
  Ponytail-review не нашёл безопасно удаляемой сложности. По замечанию review
  добавлены синтетические полные формы spaces/MeResponse: кириллица,
  providers, consent, registered devices, billing и посторонний вложенный
  workspace. Эти поля не подменяют проверяемого владельца.
- После добавления полных форм повторный `DesktopUploadRecoveryTests`:
  portable **PASS, 0.94 с**, native Release **PASS, 0.66 с**, переносимый
  AddressSanitizer/UndefinedBehaviorSanitizer **PASS, 0.58 с**. Изменялись только
  тесты; производственный код соответствует указанному выше полному набору.
- MSBuild **Release x64 PASS**, новый EXE собран отдельно:
  `C:\Temp\graf-f200-bootstrap\GrafWindowsApp.exe`.
  SHA-256: `159798e73e1079622cac22ecc025f0b01dc366c35d00c73e7cd4a05bddd2ba08`.
  Рядом проверено наличие `WebView2Loader.dll`, WebView2 projection, PRI и
  Windows App Runtime bootstrap DLL. Это проверка комплекта, не его запуска.
- Последний сводный SHA-256 Windows-исходников с дополнительными тестами:
  `9dc45f920b774f478b616c58dd735c15ffb0520eaff31a36e1d9568f753de4e7`.
  Зеркало синхронизировано без удаления пользовательских файлов.
- Продолжает работать прежний EXE из `out\x64\Release`, процесс отвечает.
  Принудительного завершения/замены запущенного приложения не было. Новый
  EXE не запускался, чтобы singleton не подменил проверку возвратом к старому.
- Настоящие HTTP-заголовки и A→B/выход с запоздалым ответом в `NativeCapture`
  проверены по исходникам, но ещё не в работающем приложении. Полный CI,
  GitHub Actions на точном SHA PR, аппаратная приёмка и выпуск не выполнялись.
  `git diff --check`, changelog и Spec Kit governance — PASS; hooks commit
  выключены. Коммита, push, deploy и публикации нет.

## Запуск после подтверждённого закрытия прежнего GRAF

- Пользователь подтвердил закрытие приложения. Проверка процесса перед
  запуском не нашла `GrafWindowsApp`; SHA-256 нового EXE совпал с
  `159798e7…ba08`. Запущен именно
  `C:\Temp\graf-f200-bootstrap\GrafWindowsApp.exe`, новый процесс `11840`,
  `Responding=true`, главное окно `GRAF`. Прежний EXE не запускался.
- Реальный экран Parallels: содержательная страница входа GRAF, не пустое окно
  и не локальный режим отсутствующего WebView2. Страница показывает
  «Сессия не найдена. Войдите снова». Авторизованный кабинет в этом запуске
  ещё не проверен; причина недействительной сессии не установлена.
- По коду обе сборки используют одну user-scoped папку WebView2. Путь
  штатного закрытия приложения не удаляет cookies и не вызывает logout.
  Профиль браузера, сессии и пользовательские записи не очищались.
- Щелчок по видимой нативной стрелке раскрытия панели не дал наблюдаемого
  перехода. Parallels сообщает Smart mouse/Keyboard optimization `auto`,
  захват кликов включён; эти настройки только прочитаны, не изменены.
  Причина ограничения автоматического ввода не установлена; это не
  доказательство поломки обработчика кнопки GRAF.
- Для продолжения запрошен ручной вход в Windows GRAF и проверка раскрытия
  панели. Коды подтверждения и другие учётные данные не запрашивались в чат.
  Browser plugin отсутствует; для настоящего нативного Windows-окна
  использован CUA/Parallels. Консоль WebView2, переходы кабинета, нативные
  настройки, auth bootstrap и отправка в работающем приложении остаются
  непроверенными. Аудиозахват не запускался.

## Подтверждённый вход и раскрытие панели

- Пользователь выполнил вход и нажал стрелку раскрытия панели. Свежий снимок
  CUA подтверждает список встреч и раскрытую правую нативную панель в той же
  новой сборке: процесс `11840`, контрольная сумма EXE не изменилась.
- Наблюдаемы «Начать запись», готовность микрофона и системного звука, выбор
  микрофона, карточка локальной сохранности и сообщение об отсутствии
  проверенного реестра приложений. Нажатие Record не выполнялось; готовность
  устройств не является проверкой качества записи или отправки.
- Автоматическое нажатие шестерёнки после явной активации окна Parallels
  также не дало перехода. Ручное раскрытие работает: ошибка обработчика
  раскрытия не воспроизведена. Запрошено отдельное разрешение пользователя
  на Windows UI Automation через PowerShell только для проверки интерфейса;
  до ответа этот способ не использован.
- Установленная Mac-версия `2026.09.06.1` проверена как эталон: нативная
  панель записи, отдельное окно «Автозапись», список приложений и три состояния.
  Окно настроек открыто и закрыто без изменения предпочтений. Исходники и
  пользовательские настройки Mac не менялись.
- Вход в веб-кабинет подтверждён; успешная нативная проверка аккаунта и
  сквозная отправка этим не доказаны. Автозапись Windows, все остальные окна,
  консоль WebView2 и доступность остаются открытыми условиями T082–T084.
  Частное содержимое экранов не сохранялось в репозиторий или этот отчёт.

## Проверка нативных окон через Windows UI Automation

- Пользователь явно разрешил управление интерфейсом внутри Windows. UAC,
  ExecutionPolicy и другие системные защиты не менялись. Неиспользованный
  временный PowerShell-файл удалён; рабочая проверка использует системные
  UI Automation API и временный C++-проверяющий вне репозитория, без новых
  зависимостей приложения.
- Managed UI Automation перечислял только семь элементов главного окна.
  Нативный `CUIAutomation8` с ограниченным обходом Raw View, пропускающим
  дочерние элементы WebView/Chrome, достигает нативной панели после WebView.
  Обычное перечисление WebView давало повторяющиеся ветви классов; цикл
  идентичностей элементов и ошибка самого GRAF этим не доказаны.
- Через настоящий InvokePattern подтверждены раскрытие панели, открытие
  нативных настроек и возврат из них. Сворачивание панели подтверждено
  нативным вводом Windows. В настройках проверены прокрутка к нижней кнопке
  и переход в серверную страницу «Аккаунт и безопасность». После каждого
  действия просмотрен новый снимок. Настройки аккаунта не сохранялись.
- Нативный список микрофонов раскрывается и содержит Windows default и
  доступный микрофон VM. При выборе конкретного микрофона исходный процесс
  `11840` завершился: Event 1000, `GrafWindowsApp.exe`, модуль `combase.dll`,
  код `c000027b`. Выбор сохранился и был виден после перезапуска старого EXE
  (процесс `2664`). Сам код события не раскрывает первичную WinRT-ошибку.
- Найден опасный синхронный путь: `SelectionChanged → refreshNativeState →
  updateMicrophoneChoices → Items.Clear`. Он пересоздавал выбранный элемент
  во время обработки выбора WinUI/UIA. Удалён непосредственный refresh из
  обработчика; существующий UI-таймер обновляет список после возврата.
  Сохранение выбора, readiness и запрет смены во время записи не изменены.
- Независимый review этого точечного исправления не нашёл блокирующих
  замечаний. Четыре смежных переносимых теста — PASS (0.05 с); они не
  исполняют WinUI и не заменяют повторение исходного сценария на новой сборке.
  MSBuild Release x64 — PASS, новый EXE
  `C:\Temp\graf-f200-micfix\GrafWindowsApp.exe`, SHA-256
  `29e8f94111635f46a39135978112bf7cc06ac414ae521e2f96349ff8d566089e`.
  Сводный SHA-256 Windows-исходников:
  `ab33819d04f67d10318d38e2649e4cb2bc9d00dab5fc2e4efa7ca09e96dd0178`.
  Сохранены прежние предупреждения стороннего WebRTC и переопределения /W1→/W4.
- Исходная сборка пережила обратный выбор default при повторном запуске:
  исходный crash не объявляется детерминированным на каждом переключении.
  Она штатно закрыта; новая сборка запущена отдельно, PID `9432`.
  Пять последовательных UIA-переключений default↔микрофон завершились с
  успешным HRESULT и живым процессом через 1.5 секунды после каждого Select.
  Процесс отвечает. После штатного перезапуска новый снимок подтвердил
  сохранённый конкретный микрофон и авторизованный кабинет без повторного входа.
  Проверка не использовала микрофонный аудиопоток. Шестое переключение вернуло
  исходное «По умолчанию Windows»: успешный Select, живой процесс и свежий
  снимок подтверждены. Мышь/клавиатура, отказ сохранения и смена оборудования
  ещё не закрыты этим UIA-сценарием. Одна попытка запуска проверяющего вернула
  ошибку транспорта Parallels `PrlJob_GetResult: Invalid argument`; успешный
  повтор и снимок записаны отдельно, первая попытка не считалась PASS.
- Финальная проверка текущего запуска: PID `1272`, `Responding=true`, тот же
  SHA-256 `29e8f941…6089e`, событий Application Error для GRAF после этого
  перезапуска — 0. Исправленное приложение оставлено открытым. Это ограниченное
  наблюдение, а не доказательство отсутствия всех возможных сбоев.
- Запись аудио, отправка/удаление пользовательских данных и завершение
  пользовательских сессий не выполнялись. T081/T084 остаются открытыми;
  проверка нескольких окон не означает полноту экранов и доступности.

## Уточнение внешнего клиентского использования

- Пользователь подтвердил: первая Windows-версия нужна сразу внешним
  клиентам. Ответ внесён в spec.md, а не подменён внутренним MVP.
- Исключение Constitution II для внутренней команды не покрывает внешние
  пространства. `/auth/me.policy.consent` описывает данные входа через
  провайдера и не является правилом записи встречи.
- Первоначальное предложение отдельной схемы правил и подтверждений заменено
  последующим ответом владельца продукта: поведение должно быть как в Mac.
  Новая Windows-only политика не согласована. Оставшийся вопрос — противоречие
  общего требования внешнего выпуска фактическому Mac-пути, подробно ниже;
  это не основание включить `consentSatisfied` константой или заявить готовность.
- Встроенный spec-quality checklist после этого уточнения: 22/22 → 21/22.
  CHK010 возвращён в открытое состояние: условия допуска внешнего пространства
  к Record ещё не определены полностью. Reviewer-owned custom checklists
  этим уточнением не менялись; реализация внешней политики ждёт завершения
  указанного цикла Spec Kit.

## Уточнение «абсолютно так же, как в macOS»

- Ответ пользователя внесён в контекст и Clarifications spec.md; отдельная
  Windows-only схема разрешений отклонена как несогласованная. Новых вопросов
  clarify в этом ходе не задавалось, обработан один ответ. Изменены только
  spec.md, plan.md и этот отчёт; код приложения, сервер, Mac и настройки
  пользователя в этом ходе не менялись.
- Установленная Mac-версия повторно проверена: `2026.09.06.1`.
  Статическая проверка относится к исходникам текущего worktree; версия
  установленного приложения сама по себе не доказывает совпадение бинарного
  кода с ними. Запись аудио для исследования не запускалась.
- Mac `apps/macos/RecApp/App/TwoBrainRecApp.swift`:
  `startManualRecording` (в том числе с `meetingDetectionTarget`) вызывает
  `evaluatedMeetingDetectionRecordingPrerequisite`, который передаёт
  `policyAllowsRecording: true`. Проверки микрофона, системного аудио,
  хранилища, источника и индикатора сохраняются. Сам
  `Sources/Capture/RecordingPrerequisiteGate.swift` умеет отказать при false,
  но не выбирает политику пространства.
- Общие `cabinet/templates/cabinet/auth/signup.html` и
  `public/templates/public/{terms,privacy}.html` содержат принятие условий
  регистрации и обязанность пользователя уведомить участников и получить
  требуемое согласие. Это существующая общая юридическая модель, не новый
  Windows-сценарий. `auth/policy.py` / `/auth/me.policy.consent` относятся
  к данным входа, а не к разрешению записи.
- Constitution II и `docs/prd-voice-layer-final.md`, раздел Participant notice
  requirements, требуют отдельно выбрать политику внешнего пространства до
  включения записи. В проверенном пути Mac это условие не подключено;
  наличие этих общих текстов не доказывает выполнение этого требования.
  Это общий блокер требований, а не основание изобретать Windows-only UX.
- Windows `AppMain.cpp`: ручная readiness также получает
  `recordingPolicyAllowed = true`, но `automaticPrerequisites()` оставляет
  `consentSatisfied = false`. Поэтому автозапуск запрещён независимо от
  выбора пользователя и даже при добавлении проверенного target. Это
  подтверждённое несовпадение, пока не исправленное из-за общего блокера.
- Spec-quality re-check: **21/22 → 21/22**, CHK010 остаётся открыт;
  новых переключений checkbox нет. Custom checklists не изменялись.
  Условия `before_clarify` / `after_clarify` commit hooks отключены.
  Следующий шаг Spec Kit — явное решение по общему правилу; при его изменении
  сначала `speckit-constitution`, иначе согласование общей реализации,
  затем plan/checklist/analyze до возврата в implement.
- Проверки документов: `git diff --check`, `check_spec_kit_governance.py` и
  `validate-changelog-fragments.py` — PASS. Независимая параллельная проверка
  сохранения настроек остановлена без итогового отчёта; её результат не
  используется как evidence. Повторная сборка и тесты приложения не запускались.

| Область уточнения | Результат |
|---|---|
| Функциональные границы и UX | Resolved: фактический Mac, без отдельной Windows-only схемы |
| Безопасность / внешнее использование | Deferred: конфликт общего требования с Mac-путём, нужна отдельная санкция на изменение правила или общей реализации |
| Данные, интеграции, ошибки, ограничения, термины, критерии завершения | Clear в рамках этого уточнения; прежние аппаратные, упаковочные и сквозные проверки остаются открытыми |

## Constitution 7: удаление отменённых условий старта и новая сборка

Этот этап отменяет описанный выше блокер требований внешнего использования:
владелец продукта явно исключил правовую политику пространства, подтверждения
уведомления/согласия и отдельный серверный допуск из условий записи.
Соглашение остаётся отдельной задачей; опубликованные юридические страницы
не изменены. Исторические результаты выше не переписывались.

- Lane: `high-risk-feature`, текущий срез T081/T082; выпуск не разрешён.
- База: `bb4fc1facc2d82355d165f1b7300efd4a2ff0990` плюс текущие изменения.
  Повторного merge master в этом этапе не было. Серверный и macOS-код не менялся.
- Конституция 7, PRD, product-gates, spec, plan, contracts, data-model,
  quickstart и задачи согласованы. Удалено также старое поле recording_policy
  из примера PermissionSnapshot; исторический T056 снабжён уточнением.
- Независимый review новых требований CHK024/CHK025 — PASS. Все списки:
  requirements **22/22**, audio-capture **24/24**, advanced-routing **14/14**,
  security **25/25**, UX **23/23**. Это проверка требований, не приёмка всей фичи.
- Анализ текущего изменения: FR-004/008/015/016 покрыты T081/T082, новых
  противоречий конституции не найдено. Все исполняемые задачи имеют открытые
  GitHub issues; актуализирована существующая #6639, дубликаты не создавались.

### Реализация и проверки

- Из WindowsReadinessGate удалены recordingPolicyAllowed и его проверка,
  из AutomaticRecordingPrerequisites — consentSatisfied. Удалены неиспользуемая
  причина recording_policy_blocked, фиктивное присваивание true в AppMain и
  текст об ожидании общих разрешений. Поиск этих символов в Windows-коде не
  находит совпадений. Числовой сериализации удалённого ReasonCode не обнаружено.
- Пересмотрены все вызовы и позиционные инициализации ReadinessInputs.
  Тесты задают оставшиеся поля по имени. Отказ по каждому из семи технических
  условий проверяется отдельно, с WebView и без него; нет новой зависимости.
- TDD: до исправления ContractFixtures и AutomaticRecordingSmokeTests
  воспроизвели лишний блокер и невозможность технически готового автостарта.
  После удаления проверок оба прошли. Для немедленного действия добавлен
  отдельный живой countdown, чтобы ранее заблокированный объект не создавал
  ложноположительную проверку. Индикатор, Stop, активная сессия, подавление,
  неизвестная/устаревшая идентичность и локальные настройки остаются проверяемыми.
- Переносимая сборка AppleClang/C++17 с warnings-as-errors:
  **20/20 PASS, 7.50 с**. Полный существующий CMake-набор Windows Release x64:
  **20/20 PASS, 4.05 с**, без исключений. Синтетический тест Корзины работает
  только с собственными временными файлами; пользовательские записи не удалены.
- MSBuild Release x64: **PASS**. Сохранены предупреждения стороннего WebRTC,
  /W1 → /W4 и MSB8064 общей папки Parallels; отсутствие предупреждений не заявлено.
- SHA-256 нового GrafWindowsApp.exe:
  `e2358828907b53f6250847aef164fcdbed51cdacba527d102b5851d70d7c4ff3`.
  Проверены WebView2Loader, projection/core DLL, PRI и Windows App Runtime bootstrap.
- Сводный SHA-256 Windows-исходников (отсортированный `rg --files apps/windows`,
  SHA-256 каждого файла, затем SHA-256 списка):
  `56f6a445969d68e271f82ace74a365510dea6fcce1fa21ee8c381c7a7db166d7`.
- Независимый correctness-review и Ponytail-review текущего изменения:
  **замечаний нет**. Это не обзор всех прежних изменений Feature 200.

### Работающее приложение

- Перед сменой сборки подтверждена неактивная запись: UI Automation находит
  видимую и доступную кнопку Start. Старая сборка закрыта штатно, без
  принудительного завершения; только после её выхода запущен новый EXE.
- Проверен новый процесс **1440**, точный путь новой сборки, **Responding=true**.
  Singleton не вернул проверку к старому EXE.
- На реальном экране новый WebView показывает авторизованный кабинет,
  а не локальный режим отсутствующего Runtime. Вход сохранился без очистки
  cookies/профиля. UIA раскрывает панель; видимая кнопка Start имеет
  `enabled=1, offscreen=0`. Открытие нативных настроек проверено снимком.
- Сохранено честное сообщение о пустом каталоге проверенных приложений.
  Настройки не выдают юридическое подтверждение за условие запуска.
  Запись звука, отправка и удаление пользовательских данных не выполнялись.
  Снимки и частное содержимое кабинета в репозиторий не сохранялись.
- Управление выполнялось существующим ограниченным UIA-проверяющим; обычные
  CUA-щелчки в Parallels не дали видимого изменения. Системные защиты не менялись.

### Границы результата

- `git diff --check`, governance и changelog validation — **PASS**.
- `infra/scripts/ci-local.sh --fast` остановился **до тестов** с
  `ambiguous / dirty_worktree`. Это не успешный общий CI. Проверка не обходилась,
  изменения не сбрасывались и не коммитились ради чистого каталога.
- Повторная сверка текущего среза не обнаружила оставшихся юридических
  блокировок. Вся Feature 200 **не завершена**: открыты T080–T084/T063/T070/T071.
  В частности, T082 включает настоящий Windows-каталог, работу в фоне,
  сохранение Never при обновлении идентичности и реальные сценарии встреч;
  T083 — сквозной auth/upload и доказательство серверной очистки.
- Публичная подпись MSIX, чистая установка/обновление, физическое оборудование
  x64, доступность и все страницы кабинета не приняты этой проверкой.
  Windows ARM64 Parallels с x64-приложением не заменяет аппаратную приёмку.
- Новых дублирующих задач при сверке не добавлено; существующие задачи не
  отмечены выполненными за частичный результат. Commit hooks отключены,
  converge hooks отсутствуют; необязательное обновление контекста AGENTS
  после plan не запускалось, корневой маршрутизатор остаётся стабильным.
- Коммита, push, публикации, deployment и Full CI нет.

## Фоновая автозапись: готовность и завершение встречи

Lane: `high-risk-feature`, продолжаются T081/T082, без новой фичи и дублей
issues. Перед реализацией уточнены US5/AC4–6, Phase 5, data-model, quickstart
и текущая область задач. Решение о 600 секундах взято из существующего
macOS `stopStaleMeetingDetectionRecordingIfNeeded`, дополнительных решений
пользователя не требуется. Constitution 7 не изменялась в этом этапе.

Независимый review CHK024/CHK025 (UX) — PASS: requirements **22/22**, UX
**25/25**, audio-capture **24/24**, security **25/25**, advanced-routing
**14/14**. Историческое CHK013 снабжено явной ссылкой на заменившее правило
Constitution 7; прежний маркер не переписан. Анализ текущих US5/AC4–6 и
Phase 5 не обнаружил блокирующих противоречий, покрытие — T081/T082/T084;
неполнота Windows-каталога и аппаратной приёмки остаётся в открытых задачах.
Необязательное обновление AGENTS после plan пропущено; commit hooks отключены,
before/after checklist и analyze не запускают запись, converge hooks отсутствуют.

### Реализация

- Главный кабинет/панель больше не определяют готовность автостарта.
  Существующий CompactOverlay создаётся скрытым один раз при запуске GRAF;
  перед началом захвата проверяются его фактическая видимость и доступная
  кнопка Stop. Ошибка создания/показа не разрешает захват.
- `AutomaticRecordingPolicy` отслеживает запись сразу после принятого
  `starting`. Подтверждение от другого PID допускается только для того же
  проверенного EXE/сертификата/версии реестра и подтверждённых render/capture
  потоков. Дополнительного класса/таймера/зависимости нет.
- Полные свежие снимки с новыми timestamp подтверждают 15 секунд отсутствия.
  Неудачные, устаревшие, будущие/переставленные снимки и разрывы свыше 2 секунд
  прерывают этот отсчёт; повтор не продлевает положительное подтверждение.
  После 600 секунд без подтверждения вызывается штатный Stop/финализация.
- Ручные Record/Stop/Exit и терминальное состояние сбрасывают слежение.
  Сохранённые настройки не меняются. Удалены четыре дублирующих поля AppMain
  и перенос состояния pending → active, который терял этап запуска.

### Промежуточные проверки

- TDD: новые проверки сначала остановили сборку ожидаемыми ошибками
  отсутствующих `captureAccepted`/`shouldStopCapture`; затем реализация
  прошла весь переносимый CMake-набор **20/20, 10.30 с** и Windows Release x64
  **20/20, 3.64 с**. Проверены границы 14/15 и 599/600 секунд, смена PID,
  чужая/неподтверждённая идентичность, отсутствие потока, разрывы, повторные
  снимки, обновление срока, ручная запись/Stop и завершённая сессия.
- До/после сборки сверен `origin/master`: остаётся один невлитый серверный
  коммит `a389657e6` (#6661), изменений Windows/macOS в нём нет. Merge не
  выполнялся; чужие изменения не сбрасывались.
- Независимое исследование Windows-каталога подтверждает, что общий JSON
  содержит только macOS/browser/cross-platform записи, а не утверждённые
  Windows-пары EXE/сертификата. Подробности — research.md. Это открытая
  часть T082, а не основание автоматически доверять имени процесса.

### Сборка и проверка работающего приложения

- MSBuild Release x64 — **PASS**. Предупреждения стороннего WebRTC,
  /W1 → /W4 и MSB8064 общей папки Parallels остаются; новых зависимостей нет.
- SHA-256 GrafWindowsApp.exe:
  `de488ede66359db81a80430da33e5c17c0d0cea3b3ca9700e9f9348394a3635e`.
  Сводный SHA-256 Windows-исходников тем же способом, что в предыдущем этапе:
  `812442b5cc6c96b97d5b191b4c0bb5a76b7da1a37686dc053789de7cd96e2918`.
- Независимые correctness/Ponytail проверки текущего изменения — **PASS**,
  замечаний нет. Не выдаются за повторную проверку всего ранее изменённого кода.
- Перед заменой сборки UIA подтвердил видимый Start, отсутствие активного Stop.
  Первая штатная команда закрыла окно настроек, поэтому ожидание выхода
  остановилось без принудительного завершения. После проверки точного
  главного окна GRAF повторное штатное закрытие завершило старый процесс.
- Новая сборка запущена отдельно, проверены точный путь и процесс **3916**:
  `Responding=true`, `HasExited=false`. Старая сборка не перехватила singleton.
- Нативная проверка HWND нового процесса: **indicators=1,
  visible_indicators=0**. Это доказывает скрытую подготовку, а не реальный
  показ/доступность Stop во время захвата.
- Сворачивание: **main_minimized=1**; повторная независимая проверка позже
  также даёт **1**. После восстановления — **0**. Индикатор на всех этих
  шагах остаётся созданным и скрытым, без ложной записи.
- Панель раскрывается; Start имеет **enabled=1, offscreen=0** до и после
  сворачивания/восстановления. Открыты нативные настройки. Реальный экран
  подтверждает авторизованный кабинет, а не режим отсутствующего WebView2;
  профиль/cookies не очищались. Снимки и данные встреч не сохранялись в git.
- Не запускались запись микрофона/системного звука, отправка или удаление
  пользовательских данных. Не менялись UAC, ExecutionPolicy или права.

### Сверка и оставшаяся работа

`speckit-converge`: текущие три AC (US5/AC4–6), решения Phase 5 о существующем
индикаторе/состоянии и сохранённые технические ограничения сопоставлены с
кодом и тестами. Не обнаружено новой неучтённой работы. Полного convergence
Feature 200 нет: оставшиеся пробелы уже принадлежат открытым T080–T084,
T063/T070/T071, поэтому дублирующие задачи не добавлялись и `[X]` не ставились.

- Настоящий автостарт/остановка встречи, видимый Stop до первого аудиоблока,
  каталог Windows и миграция Never ещё не приняты; переносимые проверки
  времени и HWND-проверки этого не заменяют.
- Полный auth/upload/очистка, все окна/доступность, физический x64, длительный
  захват, signed MSIX/clean install/update и выпуск остаются открытыми.
- `git diff --check`, governance и changelog validator — PASS.
  Повторный `infra/scripts/ci-local.sh --fast` остановился до тестов:
  `ambiguous / dirty_worktree`, run `ci-fast-bb4fc1facc2d-37518707884f`.
  Это не успешный общий CI; ограничение не обходилось.
- macOS/server код и root CHANGELOG не изменены в этом этапе. Коммит, push,
  merge, Full CI, подпись и публикация не выполнялись.

## Контрольный коммит и постоянные настройки приложений

По прямому запросу владельца создан коммит
`470de44b10c2b932b72dce36ef71149257c507e8` — сохранение предыдущего этапа,
99 файлов. Перед ним проверены совпадение исходников с ранее испытанной
Windows-сборкой, переносимые тесты 20/20, governance, changelog, whitespace
и состав индекса. Рабочая копия после коммита была чистой. Push/merge не было.

### Новый срез T082, US5/AC7–9

- `targetKey` хранит режим продукта, `identityKey` сохраняет точное сопоставление
  EXE/сертификата/ревизии для детектора, отсчёта и срока записи. Membership
  сверяет также targetKey; чужой ключ с правильными хешами отклоняется.
- V2 сохраняет Always/Ask/Never при одобренном обновлении и временном удалении
  продукта из каталога. Новое приложение получает Ask; bulk не даёт правил
  будущим приложениям. Несколько версий дают одну строку настроек.
- Удалён V1 parser. Старые значения и глобальные флаги не читаются/не удаляются;
  отсутствие реальных V1 правил подтверждено пустым прежним рабочим реестром,
  проверками всех сохранений и read-only `V1RulesPresent=False` в Windows.
- Первая bundled-запись — Microsoft Teams 26213.1006.5014.9784 ARM64,
  `microsoft_teams_new`. Метаданные/точные хеши — в research.md.
  Native `--inspect-process` подтвердил те же EXE/DER хеши через WinVerifyTrust.
  Teams запущен обычным способом без входа и присоединения к встрече.
- Изменений Mac/server implementation, системной политики Windows и
  пользовательских режимов автозаписи нет. Новых служб/зависимостей нет.

### Проверки разработки

- Независимый requirements review: CHK026/CHK027 PASS, пять checklist
  14/24/22/27/25 без открытых пунктов. Анализ AC7–9: 3/3 покрыты T082;
  противоречий Constitution 7 и требований без задач не найдено.
- Test-first: новые проверки первоначально не компилировались из-за отсутствия
  targetKey/identityKey/bundled; затем реализация. Промежуточный строгий Clang
  указал пропущенное поле в отрицательной fixture; исправлено явно.
- Итоговый portable CTest: **20/20 PASS, 6.43 с**.
- Windows Release CMake/CTest: **20/20 PASS, 3.84 с**. В native deletion test
  одна созданная тестом синтетическая папка перемещена в Корзину с возможностью
  восстановления; пользовательские записи/Корзина не перечислялись и не удалялись.
- Независимые code-reviewer и ponytail-review: **PASS**, проверены восемь
  изменённых Windows-файлов и вызовы. Проверка кода не заменяет runtime.
- Дайджест исходников Windows (метод как выше):
  `42440557a9f40fd221906aebdb0b3bad1df9446fec1aa021697f7f7e1fb89e4d`.
  `rsync -ain` не показал расхождений с зеркалом Windows-сборки.

### Предварительная проверка процесса разработки

Сразу после контрольного коммита `ci-local.sh --fast` остановился на
Development process preflight: в игнорируемом `.specify/feature.json` был
только feature_directory, отсутствовали семь обязательных полей.
Receipt: `.dev/ci-evidence/ci-fast-470de44b10c2-745772f7ece9.json`, status failed,
coverage partial. Это не успешный CI и не ошибка Windows-кода.

После независимой диагностики восстановлен локальный контекст (реальная ветка,
текущий SHA, high-risk-product и ограниченные пути Feature200). Исторические
изменения ветки учтены для валидатора, но это не разрешение редактировать
release-owned CHANGELOG. У Feature200 добавлена требуемая текущим master
секция Legacy Impact с фактически выполняемым удалением. После этого
`check-development-process.py`: **PASS** (context, fragments, Legacy Impact).
Governance/changelog/whitespace также PASS. Полный fast CI после нового dirty
среза не объявляется пройденным; нужен повтор на следующем одобренном SHA.

### Новая сборка и реальное окно

- MSBuild Release x64: **PASS**, EXE
  `C:\Temp\graf-f200-catalog\GrafWindowsApp.exe`, SHA-256
  `9319704db7c81b33c00873ab60733550d737b11c785c7a0ed3f72dbb434170d4`.
  Предупреждения WebRTC, переопределения /W1→/W4 и CMake/Parallels MSB8064
  остались; сборка не называется сборкой без предупреждений.
- До замены native UIA подтвердил старый EXE и
  `capture_control=start enabled=1 offscreen=0`, без Stop. Старые настройки,
  затем главное окно закрыты штатно, процесс завершился; принудительного
  завершения не было. Новая сборка запущена только после выхода старой.
- Новый процесс **1156**, точный путь catalog, Responding=True; контрольный
  повтор подтвердил тот же процесс. WebView загрузил авторизованный кабинет.
  Панель первоначально свёрнута; «Показать панель управления» и «Настройки»
  выполнены через проверенный native UIA, оба Invoke S_OK.
- Визуально в настоящем окне «Автозапись» видны «Для всех приложений» и ровно
  одна строка Microsoft Teams. Оба выбора — «Спрашивать»; светлый текст на
  тёмном фоне читаем. Содержимое кабинета/скриншоты в репозиторий не сохранялись.
- Повторный UIA: Start enabled/visible, один подготовленный скрытый индикатор,
  visible_indicators=0. `--observe-target` с подтверждёнными Teams pins:
  native_snapshot_status=0 (ready), verified_active_targets=0.
  Обычный запуск Teams без встречи не вызвал запись.
- V1RulesPresent=False и V2RulesPresent=False после проверки. В пользовательском
  HKCU правила не создавались/не менялись, звук не захватывался и не отправлялся.
  Сохранение/перезапуск всех трёх режимов проверены внедряемым тестовым
  хранилищем; реальное сохранение через UI этой проверкой не заявляется.

### Сверка текущего среза

`speckit-converge`: проверены US5/AC7–9 (3), решения Phase5 о ключах,
membership/группировке, V2 и bundled Teams (4), сохранённые технические условия
Constitution7. Новых противоречий/неучтённых обязательных задач не выявлено.
Новых задач не добавлено: оставшиеся разрывы уже покрыты T080–T084 и
T063/T070/T071. Отметки выполнения не менялись, полного convergence Feature200 нет.

Срез после контрольного коммита остаётся локальным и не закоммичен повторно.
Нет push, merge, Full CI, подписи, clean-image/update, физической x64-приёмки
или выпуска. Положительная настоящая встреча, остальные приложения/версии,
auth/upload/очистка и весь UI-паритет остаются открытыми. ARM64 Windows
в Parallels с x64 GRAF не является доказательством физической x64-приёмки.

## Продолжение 2026-09-07: восстановление отправки и отказ реальной записи

Основание: тот же HEAD `470de44b10c2b932b72dce36ef71149257c507e8` плюс локальные
изменения, без нового коммита. Lane: `high-risk-product`. Срезы ниже — результаты
разработки, не приёмка Feature200 и не разрешение на выпуск.

### Отправка (T083)

- Явное «Отправить» возобновляет выбранную запись после исчерпания попыток;
  интерфейс объясняет остановку автоматических повторов. Подтверждённый
  владелец, идентичность записи и принятые диапазоны не меняются.
- Сетевой отказ и HTTP 408/429/5xx проверки аккаунта допускают ограниченный
  повтор; 401/403 по-прежнему требуют входа. Сохранённый `uploading` после
  перезапуска возобновляется только после полной проверки реестра.
- Ключ замены истёкшей upload session связан с её серверным ID, не со
  сбрасываемым счётчиком. Исчерпанные первые 32 строки не скрывают доступную
  запись дальше; счётчик насыщается без переполнения. Ошибка сохранения
  исчерпания прекращает подготовку отправки.
- Независимый code-reviewer/ponytail-review (Dirac): APPROVED после исправлений;
  самостоятельно тесты не запускал. Настоящая сквозная отправка не подтверждена.

### Узкое исправление WASAPI

`GetBuffer` уже возвращает QPC в 100-нс единицах; измеренная аппаратная частота
VM 24 MHz не используется как делитель. Удалены настраиваемая частота и
параметризованный конструктор ClockMapper; вход назван `qpc100ns`.
TIMESTAMP_ERROR передаётся в mapper и отвергается. Регрессия первоначально
не собиралась без новых полей, после реализации прошла.

Независимая проверка (Planck): APPROVED только для единиц/error flag.
Найдены три прежних связанных дефекта: независимые device origins выдаются
за общий PTS; оценка ±100 ppm по первому короткому интервалу не учитывает
округление; clockRecoveryFrames=48 не используется, а normalizer требует
совпадения PTS в пределах одного кадра. Это новая T085 (#6781), не закрытие
реального отказа. Все flags/пороги в узком исправлении сохранены.

### Сборка и проверки этого среза

- Portable CMake/CTest: **20/20 PASS, 7.51 с**.
- Windows Release CMake/CTest текущего среза: **20/20 PASS, 7.39 с**.
- MSBuild Release x64 приложения: **PASS**, build_exit=0.
- Дайджест Windows-исходников перед последующим T085:
  `58b51b93ad8be19fe842e09337ecad9a6b48f070eaac1c733176fbbd8c25d93f`.
  Метод: `rg --files apps/windows -0 | LC_ALL=C sort -z | xargs -0 shasum -a 256 | shasum -a 256`.
- Запущенный EXE recovery, SHA-256:
  `38bc22d3ee760363382768dbf5180d3c270327da1cf6fc432e343bb8c480fd30`.
  Проверенный PID9476, Responding=True, заголовок GRAF; это наблюдение данного
  запуска, не гарантия текущего PID. Cabinet авторизован, панель раскрыта.
- Предыдущий PID1156 закрыт штатно: Start доступен, активных аудиосессий 0,
  CloseMainWindow=True и WaitForExit=True. Принудительного завершения нет.
- MSB8064 из-за регистра C:/Mac и c:/mac в Parallels остался предупреждением;
  сборка не названа сборкой без предупреждений. Пользовательские записи,
  профиль WebView и настройки не очищались.

### Настоящая запись: FAIL

На catalog и recovery выполнен ранее разрешённый короткий сценарий через
ограниченный native UIA: проверка отсутствия других активных аудиосессий,
временное заглушение микрофона, только синтетический тон 440 Hz, настоящий
Record. Индикатор появился, приложение само завершило попытку до Pause.

Для обеих сборок: start_invoked=1, indicator_seen=1, paused=0, resumed=0,
web_reloaded=0, stop_invoked=0, idle=1, mute_restored=1, diagnostics_copied=1,
helper_exit=12, new_packages=0. Исходное состояние микрофона восстановлено
и подтверждено. Аудио и частное содержимое не включены в evidence/git.

Безопасная сводка recovery: state=failed, reason_code=clock_discontinuity,
processed_blocks=0, written_blocks=0, duration_ms=0,
trusted_prefix_retained=false. Этот код не определяет точную ступень отказа:
controller помечает им любой отказ batch sink.

Отдельное параллельное измерение 128 пакетов на каждый endpoint не читало
содержимое аудиобуфера: сохранялись только ограниченные числовые метаданные,
вывод выполнялся после Stop. Render: 48 kHz, пакеты 480 кадров, первые
device delta 0/480/960/1440, QPC delta100ns 0/95311/185249/274034;
2 discontinuities, 0 timestamp errors. Microphone: 48 kHz,
первые размеры 482/487/488/487, device delta 0/448/896/1344,
QPC delta100ns 0/101522/204643/306459; 3 discontinuities, 0 timestamp errors.
Разность origins: device=-480, QPC100ns=129708; mute_restored=1, exit=0.
Это подтверждает необходимость проверки связи count/device delta, не право
автоматически исправлять противоречивые данные VM.

T083/T084/T085/T071 остаются открыты. Pause/Resume/Stop, native upload E2E,
SC-003, подпись, clean image, physical x64 и полный выпуск не подтверждены.
Новых commit/push/merge/deploy нет. Check-development-process, Spec Kit
governance, changelog-fragments и whitespace PASS; Full CI/полный fast на
текущем dirty-срезе не пройдены и не заявляются.

## T085: общая шкала источников и повторная проверка

Основание — тот же HEAD плюс локальные изменения, lane `high-risk-product`.
Задача T085 связана с #6781; перед реализацией уточнён контракт §5 и выполнен
независимый анализ требований. Issue-canon ensure был проверенным no-op;
последующая валидация проверила 300 issues. Реальная запись и SC-003 остаются
отдельными условиями, не заменёнными проверкой требований.

### Реализация и test-first

- Mapper сохраняет исходный QPC100ns, проверяет постоянную частоту и точное
  продолжение device position по размеру предыдущего пакета. Накопленный
  допуск — 100 ppm + фиксированная 1 ms + один кадр округления, без сброса
  погрешности на каждом пакете. Независимые device origins не становятся PTS.
- Normalizer ведёт непрерывную фазу по фактически поданным/выданным семплам;
  PTS относится к первому выданному, включая задержанный семпл. Сохранение
  raw QPC до этой ступени устраняет раннее округление маленьких пакетов.
  Проверяется неизменность каналов. Пакеты 441 + 441 при 44.1 kHz после
  первых 479 выходных продолжаются с позиции 479, а не 480.
- Timeline проверяет непрерывный `normalizedFrameOffset`, взятый из уже
  существующего deliveredFrames. Повторы/пропуски/переполнение отклоняются
  независимо от округлённого PTS. Только новый диапазон может быть обрезан
  целиком при малом перекрытии; он потребляется один раз, последний сохранённый
  семпл и канонический конец не двигаются назад, AEC/writer не вызываются.
- Коррекция остаётся ограниченной 48 кадрами: интерполяция положительного
  расхождения либо обрезка перекрытия. Удаляется начальный непарный префикс,
  ограничены очереди источников и готовых блоков. Неиспользуемые
  `reorderWindowFrames`/`knownGapFrames` удалены; обещание несуществующего
  пятнадцатисекундного восстановления убрано из активного плана/data-model.
- Worker отвергает ошибочные метки и DATA_DISCONTINUITY до копирования
  звука; ошибка получает существующий `clock_discontinuity`. Проверяется
  результат штатного ReleaseBuffer. Startup discontinuity не игнорируется.
- Регрессии сначала воспроизвели неверный общий origin и зависание выхода
  при положительном разрыве. Полная цепочка затем выявила ошибочный отказ
  новому пакету размером один семпл при 44.1 kHz/−100 ppm; сохранены тест и
  ограничение коррекции, исправлено различение нового диапазона и повтора.
- В `AudioNormalizerTests` восстановлены assert и для Windows Release,
  где NDEBUG раньше мог отключать проверки. Скрипт аудиопроверки и quickstart
  теперь включают AudioNormalizer и CaptureFaultState в синтетический набор.

### Подтверждённые проверки окончательных исходников T085

- Portable CMake/CTest: **20/20 PASS, 1.19 с**.
- Windows Release CMake/CTest: **20/20 PASS, 17.95 с**.
- Дополнительные сборки RecordingAudioTimelineTests и AudioNormalizerTests
  с Clang AddressSanitizer + UndefinedBehaviorSanitizer: **2/2 PASS**,
  exit=0, диагностик нет. Это повтор после окончательных изменений, а не
  переиспользование более ранней проверки помощника.
- Проверены 44.1/48 kHz, разные начала и размеры пакетов, ±100 ppm,
  границы коррекции ±48/49, повтор/пропуск, полная обрезка малого диапазона,
  ограничения памяти и арифметики. При точных часах коррекция отключена в
  сквозном тесте, поэтому она не скрывает ошибки преобразования частоты.
- Часовая симуляция mapper использует только метаданные; аудиоцепочка
  проверяется коротким синтетическим сценарием. **Это не SC-003** — нет
  часовой записи/декодирования, измерений CPU, памяти, потерь и качества AEC.
- Независимый code-reviewer/ponytail-review (Schrodinger): **APPROVED** по
  окончательному контракту и цепочке mapper → normalizer → timeline → worker
  → controller. Раннее округление и смена каналов исправлены; проверяющий
  сам не запускал тесты/сборки/ВМ. Неблокирующая недостижимая альтернатива
  reason при отказе normalizer оставлена вне этого проверенного среза.
- Дайджест `apps/windows` тем же методом, что выше:
  `bed96ee01555e32d4e645512e5683080f469d014de366d60d37a390e3aa67c08`.

Эти результаты не подтверждают запуск нового EXE или работающую запись.
Сборка приложения и настоящий сценарий проверяются отдельно ниже;
T085/T084 остаются открытыми до полной приёмки.

### Приложение T085 и настоящее испытание: FAIL

- MSBuild Release x64: **PASS**, exit=0; Windows-исходники с указанным выше
  дайджестом. Новый EXE clock, SHA-256:
  `1489ea240d1808f6b1ef71a03c264720e4262d78f357bf3568f6970e19b5ed17`.
  Предупреждения прежних зависимостей/путей остаются; это не warning-free build.
- Recovery PID9476 проверен по точному пути и idle Start: аудиосессий 0,
  microphone_muted=0. CloseMainWindow=True, WaitForExit=True, без kill.
  Новый PID9320 отвечает, заголовок GRAF. Native UIA подтверждает кнопки
  навигации, раскрытие панели InvokePattern и доступный Start. Повторная
  визуальная/маршрутная приёмка авторизованного кабинета этим не выполнена.
- Настоящий контролируемый Record: start_invoked=1, indicator_seen=1,
  paused=0, resumed=0, web_reloaded=0, stop_invoked=0, idle=1,
  mute_restored=1, diagnostics_copied=1, helper_exit=12, new_packages=0.
  Безопасная сводка: state=failed, reason_code=clock_discontinuity,
  processed_blocks=0, written_blocks=0, duration_ms=0,
  trusted_prefix_retained=false. **Исправление T085 не устранило реальный
  отказ ВМ; Pause/Resume/Stop и upload не приняты.**
- После сценария отдельно подтверждены microphone_muted=0,
  active_audio_sessions=0 и доступный Start. Пользовательские профиль,
  настройки и записи не очищались, системные аудионастройки не менялись.

### Дополнительное измерение форматов без чтения аудио

Метаданные собраны вне приложения отдельным проверяющим с тем же способом
инициализации WASAPI. Это подтверждает условия воспроизведения, а не трассу
точного пакета, на котором остановился GRAF. Оба источника ограничены
128 пакетами/3 секундами. Микрофон заглушён, проигрывается синтетический тон;
аудиоданные не читаются, не копируются и не сохраняются.

| Источник | GetMixFormat | DeviceFormat | IAudioClock GetFrequency | Первые размеры | Первые device delta | Первый флаг |
|---|---|---|---|---|---|---|
| Render | 48000 | 48000 | 384000 | 480/480/480 | 0/480/960 | 0x1 |
| Microphone | 48000 | 44100 | 384000 | 482/487/488 | 0/448/896 | 0x1 |

Render QPC100ns delta: 0/97363/198138/285116/373302/462702/551231/652171;
microphone: 0/101955/203043/304813/406391/508077/609773/711228.
Разрывы: render1, microphone2; timestamp errors0. Все чтения свойств успешны.
Это поддерживает гипотезу преобразования 44.1 → 48 kHz: 448 × 48000/44100
≈ 487.62. Но свойство DeviceFormat не определяет единицы именно пакетного
GetBuffer devicePosition; автоматический пересчёт по нему не реализован.

Отдельная проверка открытия в DeviceFormat **не дала поддерживаемого пути
микрофона**: IsFormatSupported shared возвращает S_FALSE (1), проверяющий
не использует приблизительный формат и не вызывает Start; итоговый
AUDCLNT_E_UNSUPPORTED_FORMAT=0x88890008, пакетов0. Render48k принимается,
но имеет последующие колебания QPC/разрывы. Код выхода проверяющего0 в этом
измерении означает восстановленный микрофон, **не успешный аудиотест**.
При отсутствии второго потока напечатанная разность origins недействительна
и исключена из evidence. Вывод временного проверяющего исправлен, чтобы
впредь сообщать origins_unavailable вместо арифметики пустого источника;
повтор этого изменения проверяющего не использован как доказательство записи.
Проверяющий затем пересобран успешно: повтор прямого DeviceFormat снова
получил S_FALSE/0x88890008 для микрофона, пакетов0 и origins_unavailable=1;
восстановление микрофона и отсутствие активных аудиосессий подтверждены.

Независимый анализ Microsoft/Chromium подтвердил историческую оговорку о
неопределённом первом DATA_DISCONTINUITY, удалённую Microsoft в 2019 году;
актуальная универсальная гарантия Windows11 не подтверждена. Узкое
отбрасывание первого пакета можно исследовать только как явное изменение
контракта, без чтения звука, продвижения часов/нормализатора и повторных
послаблений. Это **не исправляет** последующие QPC и count/device mismatch.
Исследование со ссылками добавлено в research.md; текущий контракт и
приложение продолжают отвергать startup discontinuity.

T085, T084, SC-003 и внешняя поставка остаются открытыми. Новой проверки
master/merge, commit/push, Full CI, подписи, чистой установки, физического
x64, настоящего upload и проверки всех окон в этом срезе нет.
Статус T085 опубликован без закрытия задачи:
https://github.com/yshishenya/graf/issues/6781#issuecomment-5565438857.
Финальные check-development-process, Spec Kit governance,
changelog-fragments и whitespace — PASS. Дайджест Windows-исходников и
HEAD перепроверены, изменений Mac/server/root CHANGELOG в текущем diff нет.

## T085: ограниченная подготовка первого пакета (2026-09-07)

Risk/validation lane: `high-risk-product`, продолжение существующей T085
(#6781), не закрытие задачи. Перед кодом уточнены spec/plan/contract §5,
data-model, research, quickstart и связь с T081. Независимая проверка
требований: APPROVED, CRITICAL 0/HIGH 0, покрытие FR-009/018/021 — 3/3.
Checklist markers не изменялись; 112/112 обозначают качество требований,
а не готовность приложения.

### Код и проверки

- Test-first: старый mapper воспроизвёл отказ на проверке healthy после
  первого помеченного пакета; CaptureFaultStateTests завершился FAIL.
- Mapper принимает полные flags; только первый непустой пакет с точным
  0x1 отбрасывается. Чистый/SILENT первый пакет расходует это исключение.
  Частота фиксируется сразу, origin/предыдущие позиции — только на следующем
  пригодном пакете. Последующие разрывы, смешанные/неизвестные flags,
  timestamp error, rate change, count mismatch и drift остаются отказами.
- Worker использует один consumePacket в настоящем цикле и тестах:
  без чтения отброшенного звука, с проверенным ReleaseBuffer(count),
  фактическим счётчиком после успеха и готовностью после нормализации.
  Удалены старый bool timestampError, недостижимая альтернатива ошибки
  normalizer, повторная проверка finite samples и повторное присваивание.
- Точная причина и число стартовых кадров по двум источникам проходят через
  controller в итоговую сводку AppMain. Поля строго типизированы и ограничены;
  unknown enum безопасен, размер сводки <=1024 байт; v2/v5 не изменены.
- Регрессии проверяют полный набор первых/следующих flags, ошибку release,
  отсутствие чтения через недоступный указатель, неизменность фазы/offset,
  общий QPC, неготовность после одного отбрасывания, прежний deadline,
  сохранение причины после финализации, redaction/locale/границы сводки.
- Независимый code review: APPROVED, без блокирующих замечаний; проверяющий
  не запускал тесты/ВМ и не изменял файлы. Ponytail-review рекомендовал
  удалить одно повторное присваивание; оно удалено.

Окончательный HEAD остаётся `470de44b10c2b932b72dce36ef71149257c507e8` с
локальными изменениями. Дайджест `apps/windows` после удаления повторного
присваивания:
`b5bc0f45f863880c79b583866630a39998771b63c77883a28e8c2838f6f84e09`.
Метод прежний: отсортированные пути `rg --files`, SHA-256 каждого файла,
затем SHA-256 полученного списка; это не commit SHA и не release receipt.

- Portable CMake/CTest на этом дайджесте: **20/20 PASS**, 7.29 s.
- ASan+UBSan: CaptureFaultStateTests, AudioNormalizerTests,
  WindowsDiagnosticsRedactionTests, RecordingAudioTimelineTests —
  **4/4 PASS**, 10.27 s.
- До удаления повторного присваивания Windows Release CMake/CTest:
  **20/20 PASS**, 13.61 s; это промежуточная проверка, не доказательство
  окончательного дайджеста. MSBuild сообщил MSB8064 для регистра пути
  CMakeLists в общей папке Parallels; компиляция и все тесты завершились.
- Повтор CaptureFaultStateTests 20 раз на окончательном срезе: PASS, 0.07 s.

Настоящий Record, тихий старт, Pause/Resume/Stop, SC-003 и отправка не
принимаются по этим тестам. Сборка/реальное испытание фиксируются отдельно.

### Окончательная Windows-сборка и настоящее испытание

- После последней очистки повторены Windows Release CMake/CTest:
  **20/20 PASS**, отдельно пересчитаны 20 `Test Passed.` в LastTest.log.
  MSBuild Release x64 приложения — **PASS**; предупреждения D9025 и C4100
  из сторонних заголовков остаются, без ошибок компиляции.
- EXE SHA-256:
  `378d762819cdb71abf6ada07761a80c338172a426ccf14b93cbe4de8bc923ae5`.
  Проверяется x64-приложение под эмуляцией Windows ARM64 в Parallels,
  не физический x64 и не подписанный MSIX.
- После idle preflight (0 активных аудиосессий, микрофон не заглушён)
  предыдущий процесс штатно закрыт, без принудительного завершения.
  Новая startup-сборка запущена, Responding=true; native UIA нашёл
  навигацию и раскрыл панель через InvokePattern. Профиль и записи сохранены.
- Первая длинная команда удалённого запуска дважды не открыла сеанс
  Parallels Tools. Read-only `cmd /c ver` подтвердил работающую Windows;
  разделённые короткие команды затем прошли. Ошибочные вызовы не считаются
  проведёнными испытаниями и не потребовали перезапуска/изменения защиты ОС.

| Поле | Синтетический тон | Без проигрываемого звука |
|---|---:|---:|
| start_invoked / indicator_seen | 1 / 1 | 1 / 1 |
| elapsed_ms в проверяющем | 1593 | 1578 |
| render_startup_discarded_frames | 480 | 0 |
| microphone_startup_discarded_frames | 482 | 482 |
| render_clock_fault | clock_drift | none |
| microphone_clock_fault | sample_count_mismatch | sample_count_mismatch |
| processed_blocks / written_blocks / duration_ms | 0 / 0 / 0 | 0 / 0 / 0 |
| new_packages | 0 | 0 |
| paused / resumed / web_reloaded / stop_invoked | 0 / 0 / 0 / 0 | 0 / 0 / 0 / 0 |
| idle / mute_restored / diagnostics_copied | 1 / 1 / 1 | 1 / 1 / 1 |
| helper_exit | 12 (FAIL) | 12 (FAIL) |

Обе итоговые сводки: state=failed, reason_code=clock_discontinuity,
trusted_prefix_retained=false. Тихий сценарий не вызывает Pause/Resume/reload;
он отказал на микрофоне до deadline, поэтому сам по себе не проверяет
истечение 5 секунд без render-пакетов. Этот случай покрыт тестом контроллера,
но успешный тихий старт на настоящем устройстве не принят.

Новая диагностика **внутри приложения** подтверждает успешное стартовое
отбрасывание и следующие два отказа; это не доказательство единственной
первопричины драйвера или ошибки в аппаратуре. Прежняя внешняя probe-трасса
больше не используется для угадывания, где именно отказал GRAF.

### Уточнение предложенного Windows формата

Дополнительно проверен ранее освобождавшийся без анализа closest match.
Microsoft `IAudioClient::IsFormatSupported` различает S_OK для полного
запрошенного формата и S_FALSE для предложенного похожего формата.
Четыре сводных поля не заменяют полного дескриптора WAVEFORMATEX/EXTENSIBLE.

Запрос DeviceFormat микрофона 44100 снова вернул S_FALSE. Предложены
48000 Hz, 2 channels, 32 bits, formatTag=3 (IEEE float); повторный запрос
полного неизменённого дескриптора получил S_OK без нового closest.
Это тот же номинальный формат, который уже использует приложение,
а не подтверждённый способ открыть исходный DeviceFormat 44100.
Захват микрофона в предложенном формате этим probe не запускался;
0 его пакетов и AUDCLNT_E_UNSUPPORTED_FORMAT остаются ожидаемым отказом
первоначального запроса. Формат устройства/ОС не менялся.

Ссылки первичных источников:

- https://learn.microsoft.com/en-us/windows/win32/api/audioclient/nf-audioclient-iaudioclient-isformatsupported
- https://learn.microsoft.com/en-us/windows/win32/api/mmreg/ns-mmreg-waveformatextensible

После обоих сценариев и дополнительного probe отдельно подтверждены
microphone_muted=0, active_audio_sessions=0 и доступный Start.
T085/T084/SC-003 остаются открытыми: дальше нужно доказательно разобраться
с интерпретацией device/count и временных меток, не увеличивая допуски и
не меняя единицы счётчика по предположению. Новых дублирующих задач нет.
Полная сверка завершения Feature 200 не объявлена Converged.

Новых commit/push/merge, синхронизации master, server deploy, полного fast/CI,
проверки всех окон, подписи, чистой установки и физического x64 в этом
продолжении нет. Этот результат не разрешает внешний выпуск.

Статус T085 опубликован без закрытия issue:
https://github.com/yshishenya/graf/issues/6781#issuecomment-5566499886.
Финально перепроверены HEAD, дайджест и checksum-сравнение рабочей и
сборочной копий Windows-исходников: совпадают. Изменений apps/macos,
apps/server и root CHANGELOG в рабочем diff нет. Check-development-process,
Spec Kit governance, changelog-fragments и whitespace — PASS.

## T084/T085: bounded dispatch и повторная ручная приёмка (2026-09-07)

Risk/validation lane: `high-risk-product`, продолжение Feature 200. Этот
раздел добавлен после предыдущих неуспешных попыток и не переписывает их.
Последующие результаты ниже относятся к актуальному срезу исходников.

### Исправления аудиопотока

- WASAPI callback больше не выполняет тяжёлую цепочку `timeline → AEC3 →
  writer`. После нормализации пакет передаётся в отдельный worker через
  ограниченную очередь на 256 пакетов. При переполнении запись завершается с
  `queue_overflow`; при сбое worker — fail-closed. Принятый trusted prefix не
  превращается в обычную запись, а Stop дожидается обработки уже принятых
  пакетов перед финализацией.
- Event-driven worker использует `IAudioCaptureClient::GetNextPacketSize`, а не
  `IAudioClient::GetCurrentPadding`: размер проверяется после сигнала события,
  после каждого `ReleaseBuffer` и после нулевого пакета. Проверки
  `DATA_DISCONTINUITY`, `TIMESTAMP_ERROR`, неизвестных флагов, монотонности
  часов и результата `ReleaseBuffer` сохранены.
- Portable CMake/CTest и Windows Release CMake/CTest: **20/20 PASS** каждый.
  Последняя сборка приложения MSBuild Release x64: **PASS**, без ошибок;
  предупреждения D9025 и два C4100 во внешнем WebRTC-коде остаются.
  SHA-256 EXE: `746F0C0952A9FDC60B4B7770C7E2EBDF872075F3DACEB276B2CDC985F3CB9EC2`.
  Проверялся x64 EXE под эмуляцией Windows ARM64 в Parallels; это не доказательство
  физического x64 и не подписанный MSIX.

### Реальный контролируемый сценарий

Внешний помощник проверял только метаданные и безопасную сводку. Аудио,
расшифровки, cookies, токены и содержимое встреч не читались и не сохранялись.

| Сценарий | Старт/индикатор | Pause/Resume | WebView reload | Stop/idle | Итог |
|---|---:|---:|---:|---:|---|
| `--run-unmuted` | 1/1 | 1/1 | 1 | 1/1 | `saved_local`, `reason_code=none` |
| `--run` с временным Mute микрофона | 1/1 | 1/1 | 1 | 1/1 | `saved_local`, `reason_code=none` |

Сводка `--run-unmuted`: `processed_blocks=960`, `written_blocks=960`,
`duration_ms=9600`, `trusted_prefix_retained=false`,
`render_startup_discarded_frames=480`,
`microphone_startup_discarded_frames=482`, `render_clock_fault=none`,
`microphone_clock_fault=none`. Безопасный endpoint fingerprint был выдан в
сводке; имя устройства и содержимое записи не раскрывались.

Сводка `--run`: `processed_blocks=988`, `written_blocks=988`,
`duration_ms=9880`, `trusted_prefix_retained=false`,
`render_startup_discarded_frames=480`,
`microphone_startup_discarded_frames=482`, оба clock fault — `none`.
Состояние микрофона после временного выключения восстановлено.

Отдельный `--quiet` в этой ВМ ожидаемо завершился fail-closed:
`state=failed`, `reason_code=endpoint_invalidated`,
`processed_blocks=0`, `written_blocks=0`, `microphone_startup_discarded_frames=482`.
Это не считается успешным тихим стартом и не является регрессией: в режиме
без системного звука и с выключенным микрофоном не было непрерывного пригодного
источника до startup deadline.

Таким образом, T084 закрывает проверенный ручной путь
Record/Pause/Resume/WebView reload/Stop с локальным сохранением, а T085 закрывает
реализованный bounded dispatch и корректировку event-driven WASAPI worker.
Это не закрывает SC-003: 60-минутная запись, CPU/память, потери и качество AEC
по-прежнему требуют отдельного доказательства. Также остаются открытыми
физический x64, полный UI/accessibility matrix, authenticated upload E2E,
подписанный MSIX и clean-image install/update/rollback.

Последняя проверка не выполняла commit, push, merge, deploy или публикацию.
Все результаты metadata-safe; внешний выпуск не разрешён.

## Windows host quickstart re-check (2026-09-07)

После push повторно запущены проверки на Windows 11 Pro build `26200` в
Parallels. Приложение и CTest — x64 под эмуляцией Windows ARM64; это не
доказательство физического x64.

- `validate-audio-contract.ps1 -Synthetic`: **4/4 PASS** —
  `RecordingAudioTimelineTests`, `CaptureFaultStateTests`,
  `AudioNormalizerTests`, `V5LocalRecordingWriterTests`.
- `validate-webview-boundary.ps1 -Contract`: **4/4 PASS** — route policy,
  bridge envelope, native recording при offline WebView и cabinet parity.
- `validate-package-smoke.ps1 -UiMatrix` запущен без пути к MSIX и корректно
  вернул metadata-safe статус: clean-image install/update/rollback и WebView2
  repair остаются pending до подписанного пакета. Это не package PASS.
- Windows execution policy разрешено только для текущего PowerShell-процесса,
  чтобы выполнить проверочные скрипты; системная политика и UAC не менялись.

Проверка снова не читала и не сохраняла raw audio, расшифровки, cookies,
токены или private meeting data.
