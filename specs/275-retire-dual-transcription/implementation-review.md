# F275 — независимая проверка кода

Дата: 2026-09-24. Навык `code-reviewer` прочитан полностью и применён. Режим: high-risk-feature, независимый CODE review. **Окончательный проверенный source/test SHA: `d7a909ba0cffbf6b95b1340db2b80a98f46faad2`.** Адресное исправление pickup и тест повторного draft сверены с коммитом относительно `a178597fabccfae73b742784093d7e392b56ba2d`. Прежние решения и результаты ee9533/b1b3cc/f431e8 сохранены как история; актуальное заключение C5 находится в конце отчёта.

## Текущее решение

**PASS — независимая проверка кода для `d7a909ba0cffbf6b95b1340db2b80a98f46faad2`. C1–C5 CLOSED; открытых подтверждённых дефектов и ожидающих действий по code review нет.** Последнее исправление и тест повторного draft сверены с точным SHA; завершённый объединённый прогон подтверждает 124 passed. Рекомендация null-quota выполнена. Прежние PASS и полный/native/Dev прогоны остаются историческими и не присваиваются новому runtime. Это окончательное решение независимого code review, а не утверждение об успешном полном прогоне нового кандидата или разрешение обойти CI/merge/release gates.

Рецензент изменяет только этот файл. Код, задачи, спецификации, план, пользовательские списки, GitHub и коммиты не изменялись. Интеграционные тесты, сборка macOS и производственные операции рецензентом не запускались. Испытания исполнителей не считаются автоматически проверенными доказательствами. Журналы native/ContractValidation впоследствии прочитаны независимо; подтверждённые результаты и ограничения привязки к SHA указаны ниже.

## Находки

### C2 — P2: старый workflow в not_submitted не получает заявленное конечное состояние

**CLOSED в code SHA: исправление и регрессионное покрытие проверены, полный прогон PASS. Владелец T003.** Место новой логики: `apps/server/src/twobrain_rec_server/processing/store.py:1754`, `retire_unsupported_processing()`. Ниже описано состояние на момент обнаружения, не текущий дефект; промежуточные ограничения доказательств сняты итоговой проверкой ниже.

Сценарий: сохранённый workflow старой `initial_recording` имеет `status=not_submitted`, внешнего ID нет. Это допустимое сохранённое значение и значение по умолчанию `ProcessingWorkflow.status` в `db/models/processing.py:120`. `processing_source_is_retired()` возвращает True; helper вызывает `set_workflow_status(...FAILED_TERMINAL, terminal=True)` и без проверки результата возвращает True.

Однако `processing/lifecycle.py`, `ALLOWED_PROCESSING_TRANSITIONS[NOT_SUBMITTED]`, разрешает только STARTING/BLOCKED/CANCELED. В `set_workflow_status()` запрещённый переход возвращает прежний workflow до установки причины, времени завершения и освобождения квоты. Поэтому вызывающий worker может сообщить `failed_terminal`, хотя в БД осталось `not_submitted`; pickup повторно обнаруживает тот же незавершённый источник. Если существовало резервирование, этот вызов его также не освобождает. Новая dual-отправка не происходит, но FR-005 о конечном понятном состоянии нарушен.

Доказательство: независимо выполнены фактические enum, таблица переходов и `can_transition()`, выделенные через AST без импорта приложения и без записи файлов. Результат: `not_submitted -> failed_terminal: False`, контрольный `workflow_started -> failed_terminal: True`. Полный сценарий с БД пока не запускался. Прочитанная матрица `test_historical_pending_stops_without_quota_claim_or_egress` охватывает только workflow_started/waiting_retry/blocked_unknown.

Исправление: обеспечить допустимое и долговечное завершение старого pending workflow, проверять фактический результат перехода; сохранить ограничения удаления/tenant и терминальность завершённых записей. Добавить not_submitted в проверку с повторным чтением БД: failed_terminal, unsupported_recording_source, отсутствие следующего повтора, освобождённое существующее резервирование, ноль новых provider POST/claims/reservations. Не ослаблять общую таблицу состояний без необходимости.

При последнем чтении после сообщения находки в `ALLOWED_PROCESSING_TRANSITIONS[NOT_SUBMITTED]` добавлен FAILED_TERMINAL, а в матрицу теста — not_submitted. Это устраняет исходный запрещённый переход; существующая проверка терминальных состояний сохранена. Результат запуска расширенного теста и освобождение уже существующего резервирования рецензентом ещё не подтверждены. Первоначальный результат False выше относится к версии до исправления.

### C1 — P2: известный ID ошибочно разрешал новую попытку без источника

**CLOSED в code SHA: исправление и регрессионное покрытие проверены, полный прогон PASS. Владелец T003.** Место: `processing/store.py`, `create_processing_attempt()`, первоначальное вычисление `known_provider_job` около строки 1275. Далее — история обнаружения, не текущий дефект; промежуточные ограничения доказательств сняты итоговой проверкой ниже.

В первоначально прочитанной версии любой `previous_job.external_job_id` обходил обе проверки отсутствующего источника. При этом перенос старого job к новой попытке происходил только при `resumable_provider_job is not None`. Для завершённого/окончательно неуспешного старого dual job можно было пройти ручной retry/reprocess, зарезервировать квоту, создать workflow с этапом submit без переноса ID и получить немедленный отказ unsupported в worker. Это также расширяло допуск single-file повторов после удаления источника.

После сообщения находки код изменён: `known_provider_job` требует `_RESUMABLE_PROVIDER_JOB_REASONS` и исключает FAILED/BLOCKED/DELETING. Это закрывает первоначальный сценарий чтением кода. Проверить отдельно: завершённый/failed dual с известным ID и отсутствующим single source отклоняется до новой попытки/квоты; допустимое восстановление известного задания продолжает poll/import без POST и без зависимости от уже удалённого источника. Результаты этих тестов рецензент пока не подтверждает.

## Проверенные области и границы вывода

| Область | Наблюдение в прочитанном коде |
| --- | --- |
| Отсутствие dual sender/egress | Из `mediascribe/client.py` удалён `submit_dual_track`; единственная ветка submit формирует multipart `file`. В `processing/submit.py` удалены paired staging/ветка отправки; `load_processing_source()` отвергает INITIAL_RECORDING; `upsert_mediascribe_job()` принимает только single_track. Поиск `submit_dual_track`, `/dual-track`, mic_file/incoming_file в runtime/scripts не обнаружил активного отправителя. Остатки включают безопасную фильтрацию журналов, исторические миграции/декодирование, импорт старых результатов. |
| Старые сессии до записи | `ingest/parts.py:211` выполняет проверку источника после tenant/owner/device проверки, до idempotent return, журнала хранения и PUT. `ingest/finalize.py:467` проверяет до materialization, затем повторяет проверку при окончательной блокировке сессии. `load_upload_session_record()` читает тип из ревизии самой сессии; отсутствие её идентичности не превращается в новый v5. |
| Новый ingest | Изменены defaults встречи/сессии; исторические kind/roles отвергаются в meetings/sessions. Enum, историческое чтение, abort и данные сохранены. `test_ingest_source_admission.py` содержит restart, старые/новые роли, более новую ревизию встречи, replay manifest и отсутствие изменений object storage. Тесты прочитаны, не запускались рецензентом. |
| Очередь macOS | Client проверяет `isUploadEligible` до сети; старые descriptors/payload удалены. Очередь нормализует сохранённые старые элементы при чтении/записи, исключает их из расписания и processDueItems, не возобновляет ручным retry. `currentUploadItem()` повторно проверяет состояние после приостановок. Исторические пути/ID/tombstones сохраняются; физический capture не удаляется. |
| Тесты очереди | Прочитан `DesktopUploadRetirementTests.swift`: v3/v4/unknown до сети, queue v1/v2/v3 и разные сохранённые состояния, два перезапуска, ручной retry, смешанная очередь v5/legacy, удаление локальной исторической копии. В независимо прочитанном native-журнале все пять тестов этой группы прошли; привязка к окончательному SHA ещё ожидается. |
| Известный provider ID | Ранняя ветка `submit_to_mediascribe()` возвращает сохранённый job без staging/POST; существующие lifecycle fences предшествуют ей. Poll/result/import исторических source_mode сохранены; методы DELETE/get_deletion по ID не удалены. Прочитаны known dual poll/import и новый тест локального удаления с последующими прямыми DELETE/get_deletion по сохранённому ID. Это не доказательство автоматического удаления у провайдера: тест прямо сохраняет локальный статус зависимости unknown. Запуск рецензентом не выполнялся. |
| Неизвестный старый POST | Новый retirement check стоит перед reconciliation в worker, перед повтором submit/резервированием/claim, в pickup и manual/closed recovery. Расширенная матрица теперь включает not_submitted/workflow_started/waiting_retry/blocked_unknown и повторное чтение конечного состояния из БД; первоначальный пробел C2 исправлен в коде и матрице. |
| Точный single fingerprint | Независимо сравнены функции из HEAD и рабочего дерева на 24 сочетаниях WAV/M4A, diarize/summarize, speaker mode/count: **24/24 точных совпадений**. Старой функции переданы mic_artifact=None/incoming_artifact=None. Проверка использовала реальные тела функций, выделенные через AST; приложения, БД и сеть не запускались, файлы не записывались. Отдельный frozen-digest тест также прочитан. |
| Tenant/deletion/reservations | Сохранены проверки workspace/owner/device для сессий, Meeting fence и deletion epoch до provider egress, повторная проверка после отправки/при импорте. Терминальные переходы освобождают reservation; новый retirement расположен до нового резервирования. C1/C2 относятся к дополнительным обходам/непроизошедшему переходу, а не к доказанному межтенантному доступу. Гонки требуют регрессионных испытаний. |
| Миграции и сохранность | Новые 0098 и 0099 последовательно меняют только server defaults request_mode/source_kind; UPDATE/DELETE старых данных и DROP старых полей отсутствуют. Исторические mic/incoming FK остаются в модели для сохранённых записей. Миграции ещё не применялись рецензентом; сохранность строк после upgrade и соответствие схемы должны подтверждаться тестами исполнителей. |
| Служебные сценарии | Прочитанные генератор/uploader создают/принимают v5 WAV/M4A, uploader явно объявляет initial_mixed_recording/single_wav_v1. Seed выбирает media конкретной ревизии с workspace/meeting фильтрами; новые фиктивные jobs используют single_track. Генерация вынесена в media worker с ffmpeg, передача ограничена тремя файлами. Это чтение кода, не запуск production smoke. |

## История: повторная проверка новых правок, 2026-09-24

Новых подтверждённых дефектов в повторно прочитанных правках не обнаружено. Это ограниченный просмотр изменений, не окончательный PASS и не утверждение об отсутствии регрессий во всём рабочем дереве.

- C1: дополнительно прочитан отказ `source is None and resumable_provider_job is None` после блокировки и выбора фактически возобновляемого задания. Он расположен до создания новой попытки и резервирования. В прочитанном `test_retired_processing_source.py` есть положительное восстановление известного ID из failed_terminal; отдельного отрицательного сценария C1 для невозобновляемого известного ID без источника и результатов его запуска пока не подтверждено.
- C2: тестовая матрица содержит not_submitted и проверяет сохранённые status/reason/ended_at/next_attempt/retry_class, отсутствие новых claims/reservations/POST. Проверку освобождения уже существующего резервирования эта прочитанная матрица пока не показывает; запрет создания нового резервирования не равнозначен освобождению старого.
- Worker создаёт клиент провайдера и storage после retirement; ранний возврат не требует настроенного провайдера, освобождение собственного engine в finally сохранено. В parts повторная проверка под блокировкой очищает уже загруженный объект при отказе; исходная проверка до PUT сохранена.
- macOS: дополнительные onProgress между сетевыми этапами сохраняют возможность остановить устаревшую отправку; отправляемые descriptors только manifest/media/playback. Новое значение metadata-only для неиспользуемых mic/system путей v5 исключено из удаления файлов; старые реальные пути остаются читаемыми, включая очистку короткой записи. Нового обхода запрета dual в прочитанной правке нет.
- Общие тестовые данные теперь создают поддерживаемую запись; duration_seconds/archive_audio передаются дальше, байтовые range/checksum данные отдельно помечены как не настоящий WAV. В `test_source_retention_purge.py` исторические microphone/system явно создаются в БД и остаются отдельными вариантами обоих тестов. Проверяются точные удалённые байты, журнал, отсутствие объектов и сохранение playback при очистке источников; смена общего helper не удалила эти исторические проверки.
- Новый `test_historical_known_id_preserves_local_and_provider_deletion_without_post` проверяет локальную очистку, отмену workflow, сохранность request_mode/ID/idempotency key/fingerprint, затем точные DELETE и GET deletion без submission. Провайдерские вызовы выполняются тестом явно, а не автоматически службой локального удаления.

Сообщённые основным агентом результаты: T006 — 101 pass; cabinet/normalization — 132/291/150 pass и один повторный запуск с пересечениями; strict — 69 pass; performance — 2 pass. Рецензент эти запуски не выполнял и журналов не проверял; числа не суммируются. Параллельный прогон на сообщённых ~63% имел шесть ошибок, traceback ещё не предоставлен: причина не установлена, ошибки не признаны ожидаемыми и не закрыты. Два дополнительных исправления current-media тестов ещё выполнялись. Финальный SHA не получен.

## История: проверка после остановки общих изменений

По сообщению основного агента все исполнители завершили работу, кроме адресных регрессий C1/C2 у T003. Навык code-reviewer повторно прочитан полностью; выполнены чтение поздних изменений и независимая проверка предоставленных журналов. Новых подтверждённых дефектов в проверенных изменениях нет. `git diff --check` завершился с кодом 0, без замечаний.

Подтверждено чтением `/tmp/f275-native-verified.log`: итог **1052 теста, 2 пропуска, 0 ошибок**, All tests passed; завершение в журнале — 2026-09-24 00:33:57.282 (часовой пояс журналом не указан). Все пять `DesktopUploadRetirementTests` прошли, включая повторную проверку допуска после поздних ответов, сохранение чтения/удаления, отказ до сети, смешанную очередь и перезапуск. Два пропуска — `DesktopNotificationCardTests.testCardSurfacesCanBeCapturedForReferenceComparison` (снимок не запрашивался) и `SystemAudioPermissionUXTests.testRenderSyntheticPermissionStates` (не задан GRAF_PERMISSION_PREVIEW_DIR); это отдельно включаемая генерация снимков, а не пропуск тестов запрета старой отправки. В `/tmp/f275-contract-validation.log` прочитано `ContractValidation: PASS`.

SHA-256 прочитанных журналов:

- native: `ce17076b2494cf8bf879f1bb4f45064e407d64090e7ca41b8ab5b08ed1a51ff0`;
- ContractValidation: `17189bd7192c0e37e850670a49ed6602566a183b4d772c368024c2674e389c87`.

Это независимая проверка содержимого журналов, не повторный запуск и не доказанная привязка к ещё не созданному финальному коммиту. SHA-256 вывода `git diff -- apps/macos/RecApp/Sources apps/macos/Shared/Sources apps/server/src apps/server/scripts infra/scripts/run-production-smoke.sh` на момент чтения: `9fbd4637589b0e260181bef6d814117bfe713551661fdbd1ba4ce9f4f154226b`. Это контрольный отпечаток только отслеживаемых изменений в указанных путях; он не включает новые неотслеживаемые файлы и не заменяет commit SHA.

Повторно прочитаны поздние поправки тестов аудита, retention/deletion, media-tools и transcription benchmark: переход system → media, ожидание очищенного playback candidate, переименование исторического media-теста и проверка точного single multipart согласуются с новым форматом; условия проверки результата не заменены безусловным успехом. Дополнительные проверки ContractValidation запрещают исторические descriptors и сохраняют идентичность/пути. Повторное чтение guards сессий и миграций 0098/0099 не обнаружило восстановления dual-допуска либо переписывания исторических строк.

Сообщённые, но пока не проверенные по журналам результаты: main targeted — 102 pass (включая Postgres migrations), T003 — 339 pass и benchmark 1 pass, Kepler — 17 и 9 pass. Основной агент уточнил, что первый полный прогон на меняющемся дереве закончил с 14 ошибками и все они исправлены; прежние шесть ошибок были промежуточным состоянием этого прогона. Независимое подтверждение итогового устранения ошибок ожидается от полного прогона на зафиксированном дереве после C1/C2. Новых требований к производственной готовности не добавлено.

## Проверка точного SHA и Dev promote

`git rev-parse HEAD` подтвердил `ee9533b28166d142fd1186d24ad303b2e89cc7be`; перед разрешённой записью отчёта рабочее дерево оставалось чистым, кроме позднее добавленного основным агентом раздела спецификации. `git diff --check` для сравнения базы с code SHA проходит. Контрольная сумма runtime/server/macOS/scripts diff совпала с ранее записанной `9fbd4637589b0e260181bef6d814117bfe713551661fdbd1ba4ce9f4f154226b` при исключении двух миграций, которые при первом подсчёте были неотслеживаемыми файлами. Миграции отдельно прочитаны и включены в проверенный коммит. Новых изменений кода относительно просмотренной версии не обнаружено.

Из `git show <code SHA>:apps/server/tests/integration/test_retired_processing_source.py` повторно прочитаны окончательные дополнения:

- **C1**, строки 280–384: completed/provider-failed/already-imported; недоступный источник; два последовательных отказа source_unavailable; повторное чтение БД подтверждает одну исходную попытку, один job, сохранность статуса/привязки/ID/idempotency key/fingerprint и отсутствие UsageReservation. Вызовы резервирования и захвата отправки запрещены тестом. Пробел отрицательного покрытия закрыт.
- **C2**, строки 387–463: реальное активное резервирование 60 секунд для not_submitted; после отказа submit новая сессия БД подтверждает failed_terminal, причину, ended_at, отсутствие следующего повтора, terminal retry class; резервирование released, reserved_seconds и committed_seconds равны нулю, новых резервирований/claims/POST нет. Пробел existing-reservation покрытия закрыт.

В `/tmp/f275-server-frozen.log` независимо подтверждены завершённые этапы: **strict — 69 passed**, **performance — 2 passed**, оба status=pass. Сборка тестового набора сообщает 5512 тестов, postgres_test_mode=full, worker_count=4. На последнем чтении параллельная часть достигла 76%, итоговой строки ещё нет; прохождение полного набора пока не заявляется. Сообщённые 30 pass адресного файла не выдаются за отдельно прочитанный журнал.

В `/tmp/f275-dev-promote.log` независимо прочитаны operation=promote, status=active, manifest_id=`dev-ee9533b28166`, source_sha=`ee9533b28166d142fd1186d24ad303b2e89cc7be`, migration_head=`0099_single_source_revision`. Health result=pass; в том числе exact_source_sha, app_identity, app_presentation, backend/database/migration/worker/storage/temporal и representative_api. Это доказательство завершённого продвижения Dev-сборки данного SHA, не production release и не замена полного тестового прогона. Рецензент приложение не запускал и promote не выполнял.

После разрешения записи отдельно просмотрен docs-only diff раздела `Legacy Impact` в spec.md: classification=remove, удаление старых producers при сохранении исторического чтения/удаления/отпечатков ссылается на существующие FR-003/FR-006 и residue-inventory. Нового поведения, исключения для dual-отправки или изменения требований не добавлено. Правка остаётся собственностью основного агента; рецензент её не изменял.

## История: окончательная проверка первоначального code SHA

Независимо прочитан завершённый `/tmp/f275-server-frozen.log`:

| Этап | Результат | Продолжительность этапа |
| --- | --- | --- |
| strict | 69 passed, status=pass | 37 секунд |
| performance | 2 passed, status=pass | 15 секунд |
| parallel | 5440 passed, 1 skipped, status=pass | 537 секунд |

Итого: **5511 passed, 1 skipped при 5512 собранных тестах**. Итоговая строка — `postgres_test_result=pass mode=full collection_digest=00bca9ba64c85df116a80006a6a4201976c5befeacf0cf42103e4fa2529b2ee7`; после неё `postgres_test_cleanup=isolated_container_removed`. Основной агент дополнительно сообщил exit 0. В журнале нет итоговых failures/errors; предупреждения показаны отдельно и не выдаются за ошибки. Причина единственного server skip в данном кратком журнале не раскрыта: полное выполнение абсолютно каждого теста не заявляется. Адресный файл C1/C2 отдельно имеет сообщённые исполнителем 30 pass, а его содержимое независимо проверено в code SHA; его выполнение не подменяется предположением о причине skip.

SHA-256 завершённого server-журнала: `4709bab98d47236cd25495dcd4a8fa78470a4727224474f1ab3c8dec7938e4df`.
SHA-256 прочитанного Dev promote-журнала: `6b34b7666eff161aa9813c572294c64af86dc0406653c64f041cab56c5d10b0a`.

Результат старого полного прогона на меняющемся дереве не использован как доказательство. Итог относится к зафиксированному code SHA. На окончательном чтении HEAD по-прежнему `ee9533b28166d142fd1186d24ad303b2e89cc7be`; `git diff --name-only <code SHA>` перечислил только `spec.md`, `quickstart.md` и этот отчёт. Исходники и тесты после code SHA не изменились. Просмотрены дополнение Legacy Impact и описание Dev-установки: они не меняют намерение или поведение; ограничения живого MediaScribe и отсутствие production rollout явно сохранены. Сообщение об AX-проверке готовности приложения принадлежит основному агенту, рецензент интерфейс самостоятельно не проверял.

**C1 закрыта:** невозобновляемый известный job без источника не создаёт новую попытку/квоту; прежний job и его отпечатки сохраняются. **C2 закрыта:** старый not_submitted получает долговечное конечное состояние, существующее резервирование освобождается без нового POST/claim/reservation. Новых подтверждённых дефектов по согласованным направлениям проверки нет. Повторная полная инвентаризация не требуется при последующем коммите только этих документов; любое изменение исходников или тестов после code SHA требует адресной повторной проверки.

По поручению пользователя основному агенту принадлежат docs/readiness/OpenAPI, T003 — processing, T004 — ingest, T005 — macOS, T006 — scripts; рецензент ни в один из этих файлов не писал. Режим проверки: high-risk-feature. Производственная готовность и аудит действующего хранения не входят в данный PASS. Независимый CODE review завершён; CI на актуальном PR SHA, merge и release gates остаются отдельными обязательными проверками.

## История дополнения: новые замечания перед выпуском, 2026-09-24

Область: две подтверждённые находки после первоначального PASS и пять изменённых файлов исходников/тестов поверх HEAD `7eee56b1cb223a4e0c387ba7126116d68b05ed20`. Навык code-reviewer перечитан полностью. Рецензент самостоятельно прочитал diff и завершённые RED/GREEN журналы; тесты сам не запускал. Изменён только этот отчёт, исходники/тесты/спецификации/задачи/GitHub не редактировались.

### C3 — P2: историческая finalization оставалась ready

На исходном HEAD `readiness/matrix.py` оставлял local-artifact-finalization в ready с единственным docs_only доказательством feature-020-finalization, хотя и stage notes, и описание самого доказательства отрицали его применимость к текущей single-source отправке. Это ложноположительный статус этапа, а не доказанный обход всех production gates.

Исправление просмотрено: status=degraded; добавлена ссылка на зарегистрированный LaunchGap single-source-finalization-evidence, severity=P2, owner_area=desktop, с требованием актуального metadata-only доказательства финализации. Историческое доказательство не повышено до live/local_runtime. Новый unit-тест проверяет degraded и связь этапа с пробелом. Исправление минимально, соответствует F275; новых замечаний нет. Отсутствующее доказательство остаётся честно обозначенным ограничением, а не объявляется полученным этим изменением.

### C4 — P2: null-revision workflow без известного ID обходил retirement

На исходном HEAD processing_source_is_retired() при media_revision_id=None и job=None возвращал False. Миграция 0039 сохраняет такие workflow, добавляя legacy fingerprint без создания ревизии. Путь без новой ревизии получал blocked_missing_artifacts вместо unsupported_recording_source; новой dual-отправки не доказано. Прежняя ветка missing-artifacts уже могла завершать обработку и освобождать резервирование, поэтому бесконечный повтор или гарантированная утечка квоты не заявляются.

Исправление просмотрено: `if media_revision_id is None: return True` стоит **после** раннего исключения для известного external_job_id. Poll/import/delete известного задания не блокируются новой классификацией; подстановка новой ревизии, переписывание миграций и изменение отпечатков не добавлены. Новые real-DB pickup тесты охватывают not_submitted/workflow_started/waiting_retry, повторно читают terminal status/reason/ended_at/next_attempt и проверяют отсутствие provider job, нового резервирования и Temporal dispatch. Unit-тест отдельно проверяет отказ без ID и разрешённое исключение для известного ID без запроса ревизии. Исправление и данное покрытие приняты; нового дефекта не обнаружено.

### Фактические результаты

- `/tmp/f275-review-red.log`: **5 failed, 47 passed**. Падают именно null-source unit, readiness unit и три состояния revisionless pickup; phase focused-parallel=fail. Это подтверждает чувствительность новых тестов к исходным дефектам.
- `/tmp/f275-review-green.log`: **81 passed, 10 warnings**, 41.76 секунды pytest; focused-parallel status=pass, duration_seconds=46; затем postgres_test_result=pass mode=focused partitioned=true shard=none и isolated_container_removed. Collection digest: `e121d0a997215dce009a3e9397f818f318df9cf54c24567d2d807d0d147dc37f`. Это завершённый расширенный целевой прогон, не новый full.
- `git diff --check` прошёл без замечаний. Привязка нового результата к неизменяемому code SHA ещё ожидается; результаты старого ee9533 не переименованы в результаты нового кода.

Контрольные суммы SHA-256: RED `c33a5f5d2165ec4e148d46d782405a3ed3166437cd965107a1264a65f00f30a7`; GREEN `a7d465480c59138b9f5529be6f0e861bc972fc99d22e01246dba5c07300c97fd`. Отпечаток `git diff` пяти просмотренных файлов исходников/тестов относительно HEAD: `af7f0efc7161ea39f372b2281ca5276ff91f04259f80aee8420b5d581db2c5e7` (без этого отчёта).

### Достаточность проверки существующего резервирования при null revision

Существующий C2 создаёт ключ `processing:<revision_id>`. При media_revision_id=None код `_processing_usage_reservation_key()` использует другой ключ — `processing:<meeting_id>`. В прочитанной цепочке set_workflow_status → release_processing_usage_reservation meeting_id передаётся правильно; доказанного дефекта освобождения нет. Однако C2 не является прямой проверкой именно этого варианта ключа.

Рекомендация: добавить **один** real-DB сценарий либо расширить один новый pickup-сценарий активным резервированием `processing:<meeting_id>` и повторным чтением released, reserved_seconds=0, committed_seconds=0, без нового резервирования. Полная повторная матрица состояний не нужна. Это узкая рекомендация по покрытию, не новая подтверждённая ошибка и не основание требовать аудит production-квот или изменение runtime сверх двухстрочного guard.

Итог дополнения: C3/C4 исправлены в просмотренном рабочем дереве и подтверждены целевым GREEN; нового блокирующего дефекта не найдено. Окончательное решение для нового коммита последует после его SHA и проверки соответствия diff. Все отдельные условия выпуска/production остаются у основного исполнителя.

## Окончательное дополнение для source/test SHA f431e8

`HEAD` независимо подтверждён как `b1b3ccdb2a0641e4f6bd4748eac386f7e394e8a8`. Прочитан полный diff от 7eee56b1: два runtime-файла, три файла тестов и quickstart. Исправления C3/C4 и тестовые условия совпадают с ранее просмотренными; две посторонние правки переноса строк в readiness-тестах убраны, логика не менялась. Quickstart корректно отличает новый целевой прогон от прежних полных результатов. `git diff --check` для коммита проходит. Журнал GREEN повторно прочитан: 81 passed, итог pass; SHA-256 остался `a7d465480c59138b9f5529be6f0e861bc972fc99d22e01246dba5c07300c97fd`. Прогон выполнен до фиксации; соответствие проверено сравнением кода и условий тестов, а не наличием SHA в самом журнале.

После фиксации b1b3cc исполнитель расширил только три новых параметризованных pickup-сценария: создаётся действительное резервирование 60 секунд по `processing:<meeting_id>`, сохраняются reservation/window ID, после retirement в новой сессии БД проверяются released, reserved_seconds=0, committed_seconds=0. Запрет нового резервирования и Temporal dispatch остаётся. Runtime не изменён. Это закрывает рекомендацию о другом ключе квоты; дополнительная матрица или изменение реализации больше не требуется.

Независимо прочитан `/tmp/f275-review-null-quota.log`: **3 passed, 10 warnings**, 10.33 секунды pytest; focused-parallel status=pass duration_seconds=13; затем postgres_test_result=pass mode=focused partitioned=true shard=none и isolated_container_removed. В списке выполненных сценариев видны not_submitted/workflow_started/waiting_retry. Collection digest: `43afc85cf1455cee0c7f9a6b6bb09ca6afb022c597f5987247ad247604d7a7de`. Эти три проверки пересекаются с набором 81; результаты не суммируются как 84 уникальных теста.

После сообщения основного агента повторно выполнен `git rev-parse HEAD`: **`f431e8076d0bd5381dcfcdff97ca3a1bc9d25449`**. Прочитан полный diff b1b3cc..f431e8: ровно один файл `test_retired_processing_source.py`, 16 добавленных строк, только описанное создание/повторное чтение резервирования. Runtime и остальные тесты не менялись. `git diff --check b1b3cc..f431e8` прошёл. Повторно прочитан окончательный null-quota footer; SHA-256 журнала `482627ed73933e95a7c8aa333f560206c32746e750a538b0008e7c56c72045d3`. Exit 0 отдельно сообщил основной агент; рецензент подтвердил итог pass по журналу. На момент проверки незакоммичены только этот отчёт и принадлежащее основному агенту дополнение quickstart; runtime/test изменений в рабочем дереве нет.

**Решение: code review PASS для f431e8076d0bd5381dcfcdff97ca3a1bc9d25449; C1/C2/C3/C4 CLOSED; рекомендация null-quota выполнена и проверена.** Полный аудит повторно не проводился: дополнительное решение основано на ранее проверенном F275 и адресной проверке последующих изменений. Все прежние сведения о полном/native/Dev прогоне остаются историческими. Полная проверка нового кандидата выпуска и обязательные CI на актуальном PR SHA остаются отдельными условиями, этот отчёт их успеха не утверждает. Последующий коммит только документации не меняет проверенный runtime/test SHA. Рецензент изменил только implementation-review.md; коммит и push не выполнял.

## Дополнение C5 — граница retirement в pickup для нового draft

После прежнего решения дополнительный pickup/restart прогон воспроизвёл регрессию: `/tmp/f275-review-pickup-regression.log`, **1 failed, 42 passed**. `test_processing_pickup_blocks_invalid_meeting_states` обнаружил unsupported_recording_source вместо blocked_invalid_meeting_state для нового draft без ревизии и workflow. Причина — безусловное применение null-revision retirement до существующей проверки допустимости состояния встречи. Предыдущий PASS ограничен прежними проверками и не используется как разрешение выпуска с этой известной регрессией.

Проверен окончательный diff `processing/pickup.py`: retirement вызывается при наличии media_revision_id, либо historical_workflow с причиной, отличной от BLOCKED_INVALID_MEETING_STATE, либо состоянии INGESTED_PENDING_PROCESSING. Новый draft без истории и повторный draft после отказа проходят к прежней проверке состояния. Положительные ветви revision/state независимы от прежней причины: допустимое состояние или существующая ревизия не обходят retirement из-за сохранённого invalid-state отказа. Исторические workflow без ревизии, включая существующее резервирование, продолжают завершаться до отправки/dispatch. Общий helper и исключение для известного provider ID не изменены; новых допустимых состояний встречи не добавлено.

В первоначальном варианте should_check_retirement сохранность причины повторного draft следовала из терминальности BLOCKED и раннего возврата set_workflow_status. Окончательное уточнение дополнительно исключает саму классификацию invalid-state отказа как исторической обработки. В existing test_processing_pickup_blocks_invalid_meeting_states добавлен второй API-вызов: оба ответа 202/blocked_count=1, повторное чтение сохраняет draft и blocked_invalid_meeting_state. Тест входит в окончательный успешный объединённый прогон; ограничений повторного pickup, требующих ещё одной правки, не найдено.

Первый объединённый прогон `/tmp/f275-review-final-regression.log` дал 124 passed за 40.60 секунды до уточнения повторного draft. Для окончательной версии независимо прочитан **`/tmp/f275-review-final-draft-repeat.log`: 124 passed, 10 warnings, 40.91 секунды pytest**; focused-parallel status=pass duration_seconds=44; далее postgres_test_result=pass mode=focused partitioned=true shard=none и isolated_container_removed. Exit 0 отдельно сообщил основной агент. Объединённый набор включает retirement/null-quota, pickup/blockers, restart и readiness. Collection digest: `8e6c9e762770c49da4e9fad22d52b09bce4dd315161addc18944fcdb090b0e2a`; SHA-256 окончательного журнала: `ddfe56613ffe6d144f6aa7a37699f6978aa98a74b540510ba5a45f3fd79b9502`. Это целевой прогон, не full; повторные и пересекающиеся результаты не суммируются.

Финальная привязка: `git rev-parse HEAD` подтвердил **`d7a909ba0cffbf6b95b1340db2b80a98f46faad2`**. Полный diff a178597..d7a909 содержит ровно два просмотренных файла — pickup.py и test_processing_pickup_blockers.py, 19 добавлений и одно удаление. Он совпадает с проверенной окончательной логикой; иных runtime/test изменений коммит не содержит. `git diff --check a178597..d7a909` прошёл. В рабочем дереве остаются только этот отчёт и принаджащее основному агенту дополнение quickstart; исходники и тесты чистые. Целевой прогон выполнен перед фиксацией; соответствие коммиту подтверждено чтением diff, а не вымышленной SHA-меткой внутри журнала.

**Окончательное решение: PASS для d7a909ba0cffbf6b95b1340db2b80a98f46faad2, C1–C5 закрыты, null-quota рекомендация выполнена.** Ожидающих действий независимого рецензента нет. Последующий коммит только отчёта/quickstart не меняет проверенный source/test SHA; обязательные CI должны относиться к актуальному PR SHA. Полная проверка кандидата, production и release gates остаются отдельными условиями и не объявляются пройденными этим заключением. Рецензент изменил только implementation-review.md, не создавал коммитов и не выполнял push или выпуск.
