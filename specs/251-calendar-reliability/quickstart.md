# Validation / user journey
1. Use disposable PostgreSQL via apps/server/scripts/run_local_postgres_tests.sh --focused with calendar/caldav/google tests; never a production database URL.
2. Provider fixtures: PRIVATE with long title/email/URL, HTML escaping, TZID/DST, RRULE/RDATE/EXDATE, moved/cancelled override, id-only Google delete; titles/participants and protected description roundtrip equal provider input.
3. Worker fixtures: both providers become due at 60s; queued claim, cancellation, five-minute recovery; old result cannot commit after reclaim/selection/disconnect; refresh catalog and remove missing entries; 410 full resync.
4. Browser local synthetic app: connect (including invalid/cancel), select zero/one/multiple, manual sync, open empty main, inject future provider event and observe automatic appearance; rename/move/delete; keep dirty settings and dialog through delayed sync; one source fails while another works; explicit safe link; disconnect/reconnect. Check keyboard labels/focus and narrow layout. Embedded shares web code; check installed production only as unchanged reference until release.
5. swift test --package-path apps/macos --disable-swift-testing --filter 'DesktopCalendarReminderTests|CalendarAutoContextMatchTests|DesktopUploadClientTests'; node --check cabinet JS; browser JS functional tests.
6. Record exact commands/results in validation.md, run infra/scripts/ci-local.sh --fast, convergence and independent review. No production deploy/credentials grants as a side effect. Dedicated real provider end-to-end after approved release remains a separately identified gate.
