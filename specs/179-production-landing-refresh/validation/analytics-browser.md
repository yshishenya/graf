# Public funnel analytics validation

The integrated pages render the approved counter configuration only on `/` and `/download`. Browser interaction exercised product tabs, both pricing cycles and a FAQ item. Source and contract tests verify nine allowlisted goal names, one-time initialization, per-key de-duplication, safe campaign normalization and disabled Webvisor/click-map/automatic-link features.

The in-app browser executes page scripts in a separate page world from its read-only inspection API, so `window.ym` cannot be read directly from that inspector. No claim of a received production goal is made from this local pass.

Production receipt remains pending until the same nine goals are configured in the authenticated Yandex counter and verified through the provider interface after deployment.
