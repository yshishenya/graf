# T031: независимая проверка требований и согласованности

Проверяющий: audit_capture, 2026-09-09. Прочитаны krisp-prompt-reference.md,
дополнения spec/plan/tasks/quickstart, прежние UX/audio checklists и настоящий
путь presentMeetingDetectionPrompt/геометрии/решений. Реализация не менялась.
Новые отметки reviewer добавлены отдельно; прежние отметки сохранены.

## Трактовка запроса владельца

«Реализуй также» после разрешённого осмотра Krisp достаточно для адаптации
наблюдённой компактной формы существующего временного запроса GRAF. Согласованное
F214-поведение сохраняется, поэтому новый вопрос владельцу о8с/remember не нужен.
Наблюдавшиеся202×82pt — собственный индикатор уже начавшейся записи Krisp,
не запрос решения и не Notification Center.320×192pt — самостоятельная
адаптация GRAF, не измерение эталона. Это различие корректно отражено в reference.
Разрешение не добавляет постоянный индикатор после Start и не отменяет
проверки разрешений/источника/актуальности и явный выбор Never/Always.

## Замечания перед реализацией

### R031-1 HIGH: область действия клавиатуры при отсутствии захвата фокуса

Reference требует «без захвата фокуса», SC009 — Enter/Escape. Пока не определено,
когда эти клавиши принадлежат карточке. Нельзя перехватывать Enter фонового
Zoom и тем самым разрешать запись либо красть клавиатуру при появлении.
Предлагаемое точное уточнение: появление не активирует GRAF и не меняет key
window; после явного фокусирования карточки пользователем доступны Tab/Space
для remember и Enter/Escape для Record/Skip. В фоне клавиши другой программы
не действуют на запрос. При истечении8с действует прежний F214 timeout;
внешний фокус/наведение/перемещение карточки не перезапускает таймер.
Проверить и отсутствие перехвата внешнего Enter/Escape, и локальную клавиатуру.
Это уточнение можно принять в рамках текущего запроса, без нового изменения
режима автозаписи или глобальных сочетаний клавиш.

### R031-2 HIGH: сжатие рамки не гарантирует доступность действий

SC009 запрещает обрезку обязательных действий; reference ограничивает размер
экраном. Но не определены нижняя граница поддерживаемой рабочей области и
поведение содержимого, когда320×192 не помещается. Прежний код уменьшает
NSWindow, сохраняя fixed-size SwiftUI/minSize/maxSize: простое сохранение
такой схемы может обрезать Skip/Record/remember. До кода нужно зафиксировать
поддерживаемый минимум и согласованный reflow/прокрутку либо иной доступный
путь для тесной области. Нельзя считать PASS только прямоугольник внутри экрана
при скрытых действиях или уменьшать шрифт до нечитаемого. Особо проверить
«Запись пока недоступна» в горизонтальной кнопке и длинное имя приложения.

### R031-3 MEDIUM: точный выбор якоря и экрана

«Текущий экран» допускает разные трактовки. Безопасное уточнение: сохранить
существующий выбор экрана по курсору с прежними fallback; использовать frame
собственного status item только если он актуален, действительно доступен и
принадлежит выбранному экрану. При скрытом меню, другом экране или отсутствии
frame — верхний правый край visibleFrame выбранного экрана. Координаты другого
монитора не переносятся на этот экран. Проверить отрицательные origin,
границы visibleFrame, скрытую строку меню и повторный layout без скачка таймера.

## Анализ покрытия и границ

- FR015/SC009 покрыты T031 и дополнением quickstart. Кодовые пути указаны:
  TwoBrainRecApp.swift, существующий CalendarTrayController и один тестовый файл.
  Новые модели/таймеры/OS delivery не нужны. Якорь меню должен быть read-only
  геометрией; он не открывает меню и не активирует приложение.
- FR003/SC001/SC002 сохраняются: один countdown, один итог решения, ручные
  действия и remember; после решения/конца встречи0 постоянных окон. При
  startDisabled кнопка и timeout сохраняют прежний fail-closed путь.
- Наблюдённый красный индикатор Krisp относится к recording, поэтому до
  подтверждённого старта GRAF не должен выдавать карточку за «Идёт запись».
- Проверки ширины/тем/клавиатуры относятся к новому UI: прежняя F214 приёмка
  подтверждает логику исходника, но не утверждает работоспособность нового
  расположения кнопок. После нового SHA нужна повторная установленная матрица
  Ask/Skip/Record/remember и focused регрессии прежних Always/Never/lifecycle.
- Риски компактности — действия справа, перевод/длинные имена, текст disabled,
  режимы контраста, края экрана и fullscreen; они входят в SC009/FR010.
  Недоступный физический второй монитор можно честно отделить от исполняемой
  геометрии. Unit с подставными CGRect не заменяет проверку реального текста.
- Нет новой необходимости измерять аппаратные60мин/dropout из-за изменения
  карточки. Прежние пределы/неизмеренные показатели и открытый sleep gate
  сохраняются; это не разрешение понизить их или считать выполненными.

## Решение

Reference/provenance и сохранение F214 приняты. Перед кодом устранить R031-1
и R031-2 в спецификации/плане/quickstart; R031-3 уточнить там же. До повторной
сверки качество новых UX-требований частично принято, готовность реализации
не заявляется. Issue sync и свежая task/PR identity остаются задачей оператора.

## Повторная проверка после уточнений R031-1—3

Повторно прочитаны spec/plan/quickstart/reference до изменения кода.
Все три замечания закрыты по качеству требований:

- R031-1: показ сохраняет активное приложение/key window; явный фокус разрешает
  локальные Enter/Escape/Tab/Space. Nonactivating panel с возможностью стать key
  по действию пользователя не требует глобального перехвата. Негативные внешние
  клавиши и отсутствие перезапуска таймера включены в quickstart.
- R031-2: поддерживаемый минимум visibleFrame240×160pt задан. Удаление fixed
  width и повторных setContentSize, перенос/ScrollView, вертикальные кнопки
  при фактической ширине<300pt и двухстрочный disabled-текст согласованы с
  доступностью действий без уменьшения шрифта. Более тесные области не получают
  ложного обещания полной доступности. Quickstart требует реальное содержимое,
  а не только CGRect; при реализации учитывать, что отступы уменьшают доступную
  ширину/высоту относительно240×160, а прокрутка должна достигать обеих кнопок.
- R031-3: сохранён существующий выбор экрана по курсору/fallback; только
  видимый собственный status item на том же экране и нескрытая строка меню
  дают якорь. Hidden/no/foreign anchor, отрицательные origin и повторный layout
  включены в исполняемую проверку; physical multi-display отделён от неё.

Повторный analyze: FR015/SC009 и связанный FR003/FR010 имеют реализацию и
приёмку в T031, противоречий spec/plan/quickstart/reference в уточнённом объёме
не найдено. Новые UX CHK013–017 и audio CHK008–011 приняты reviewer;
старые отметки не изменены. Открытых HIGH/CRITICAL по требованиям нет.
Требования достаточны для перехода к реализации после штатного issue sync.
Это не приёмка нового UI: реальные фокус/клавиши, читаемость/прокрутка,
таймер и installed Dev должны пройти на новом SHA. Прежний sleep gate
и release gates этим решением не изменяются.

## Независимый code/Ponytail review T031

Проверен diff TwoBrainRecApp.swift, CalendarTray.swift и нового исполняемого
расчёта геометрии в AppLifecycleWindowRegressionTests.swift. Реализацию reviewer
не менял. Подтверждённых замечаний P0/P1/P2, требующих исправления кода, не найдено.
`git diff --check` выполнен независимо: PASS. Сборка и focused F214 tests
выполняются оператором; их текущий результат этим code review не подменяется.

- Фокус: панель создаётся сразу с nonactivatingPanel, canBecomeKey=true и
  canBecomeMain=false. Показ через orderFrontRegardless не вызывает makeKey
  или NSApp.activate. Глобальных обработчиков клавиатуры не добавлено;
  SwiftUI keyboardShortcut остаются у панели. Это соответствует выбранной
  модели, но фактическое получение клавиатуры borderless NSPanel после
  явного клика и отсутствие её перехвата до клика требует installed проверки.
- Решения: onStart/onDismiss, countdown, guards и task cancellation сохранены.
  Перерасчёт frame не пересоздаёт view, не меняет appearedAt и не заводит
  новый таймер. Изменение layout через AnyLayout не выносит countdown-state
  в новую модель. После решения действует прежнее закрытие единственной панели.
- Тесная область: удалены фиксированная ширина SwiftUI, min/maxSize и
  последующие принудительные setContentSize. ScrollView сохраняет доступ к
  содержимому большей высоты; при ширине<300 используются вертикальные кнопки.
  Например, visibleFrame240×160 после прежних отступов даёт рамку196×116,
  поэтому принятие требует реальной прокрутки до обеих кнопок/remember.
  Это предусмотрено spec/quickstart; CGRect-тест в одиночку этого не доказывает.
  Disabled-текст получает две строки/44pt, уменьшение шрифта убрано.
- Якорь: NSMenu.menuBarVisible, statusItem/button/window visibility и совпадение
  screen проверяются до конвертации frame. Дополнительный contains ограничивает
  результат экраном; helper только читает геометрию и не открывает меню.
  Отсутствующий якорь использует top-right; отрицательные origin/clamp
  обрабатываются прежним чистым расчётом без изменения захвата.
- Ponytail: использованы штатные NSPanel/ScrollView/AnyLayout и существующий
  status item. Маленький subclass нужен для key-window поведения и не является
  новой продуктовой системой. Удалены лишние принудительные layout-повторы;
  новых зависимостей, таймеров, presenter или постоянных окон нет.

### Границы проверок перед convergence

Новый testPromptGeometryUsesRealLayout исполняет настоящие frame/clamp методы,
проверяет нормальный размер, top-right, якорь, края, отрицательные origin,
маленькую область и детерминизм. Он не вызывает visibleStatusItemFrame:
hidden/foreign status item должны быть подтверждены отдельно на границе
этого helper или установленным опытом; clamp чужого CGRect не заменяет
проверку его отбрасывания. Никакого нового вычисления для этого не требуется.

До приёмки T031 остаются объявленные в quickstart реальные фокус/Enter/Escape/
Tab/Space и отсутствие реакции на внешнюю клавиатуру, themes, текст и
прокрутка тесной области, позиция/размер, прежние Ask/Skip/Record/remember
и отсутствие постоянного окна. Review кода принят; installed UI и новый
exact-SHA CI пока не объявляются PASS.

SHA-256 проверенных файлов:

- TwoBrainRecApp.swift: `0cd1fe551b52928bb455c211f07beb18a243836d9d8b74b35523aa0b5fb966a2`.
- CalendarTray.swift: `599b62b48ac201feed128e70661cb106cd8359f6d481b675d661e8b537f3fa4c`.
- AppLifecycleWindowRegressionTests.swift: `2e8c48d111705f0d13d256d520e70e17198c4f776737dc142a39c72e5fac1c40`.

## Проверка исправления локальной клавиатуры2098c5020

Проверен коммит `2098c5020cb52270b236b3bb3eab87038c9d1393`: три локальных
focusable/onKeyPress для checkbox и двух действий. Реализация reviewer не менялась.

**P2 K031-1: Space повторяет переключение remember при удержании.**
Сокращённый onKeyPress(.space) принимает key-down и key-repeat. Это прямо
подтверждено актуальной документацией Apple:
https://developer.apple.com/documentation/swiftui/view/onkeypress(_:action:)
(«SwiftUI performs the action for key-down and key-repeat events»).
Повтор autoRecordOptIn.toggle() может вернуть checkbox в исходное состояние,
хотя пользователь выполнил одно удержанное нажатие. Это существенно для
явного сохранения Always/Never. Минимальное исправление — использовать
`onKeyPress(.space, phases: .down) { _ in ... }` на трёх контролах.
У Record/Skip прежние countdown.resolveStart/cancel уже защищают от второго
исхода, но одна фаза для всех трёх соответствует семантике кнопок и не требует
нового состояния или собственного обработчика клавиатуры.

Остальная форма минимальна: .edit делает фокус достижимым независимо от
системной навигации, не превращает SwiftUI Toggle/Button в текстовые поля;
onKeyPress локален сфокусированному view, .handled прекращает дальнейшую
доставку события. У disabled Record явно запрещён фокус, resolveStart
дополнительно сохраняет startIsTemporarilyDisabled. Ни глобального монитора,
ни изменения key-window при показе не добавлено.

С включённой системной навигацией установленный опыт должен проверить один
последовательный Tab-маршрут checkbox→Skip→Record, обратный Shift-Tab и ровно
одно изменение checkbox от короткого/удержанного Space. По source нельзя
объявить PASS взаимодействия штатных и добавленных focus targets. До явного
фокусирования карточки внешние Tab/Space/Enter/Escape не должны действовать
на неё; после снятия фокуса клавиатура снова принадлежит внешней программе.
Enter/Escape и countdown сохраняют отдельные прежние действия.

Прочитан t031-extra-checks.json: actual-view rendering и anchor helper
сообщают PASS на bf9fdc964, узкая область имеет достижимые через прокрутку
кнопки/remember. Это устраняет прежний пробел проверки содержимого/anchor
в ограниченном автоматическом объёме. Контролируемый native Tab/Space на
bf9fdc964 дал FAIL при AppleKeyboardUIMode0; его нельзя отменить сборкой и
48 focused tests. Новый2098 native ещё не проверен этим reviewer.

Решение: исправить K031-1 до итогового принятия нового keyboard diff;
остальное code/Ponytail замечаний не вызывает. Необходим установленный
повтор после исправления, без фиктивного native PASS.

## Итоговый повтор keyboard/acceptance403ce829

Повторно проверен HEAD `403ce8291452df73db07eda835840ba24fb60d92`.
GitHub API непосредственно подтвердил completed/success exact-SHA запуска
https://github.com/yshishenya/graf/actions/runs/34351174310 . Изменений
реализации reviewer не выполнял; `git diff --check` PASS.

K031-1 закрыт в собственном обработчике: Space принимает только .down.
Общий MeetingPromptKeyboardNavigation при NSApp.isFullKeyboardAccessEnabled
возвращает исходный контрол, иначе добавляет локальный .edit/Space. Это
устраняет найденные на fea0a8ecd duplicate focus targets и двойной dispatch
native+wrapper, сохраняя штатную семантику при включённой навигации.

Непосредственно прочитаны t031-final-nav-on.log и t031-final-nav-off.log:
после явного клика focusedWindow=prompt, keyboard owner=Dev; в обоих режимах
Tab проходит checkbox→Skip→Record→checkbox, ShiftTab возвращает Record.
В режиме0 heldSpace даёт1, следующий короткий Space0; в режиме2 heldSpace
даёт0 после четырёх native переключений, короткий Space1. Короткое нажатие
обрабатывается один раз, собственный wrapper в режиме2 отсутствует.

Прежняя рекомендация reviewer «одно переключение при удержании» относилась
к нашему введённому onKeyPress. Spec/quickstart/checklists не требуют
переопределять штатный autorepeat AppKit. Он честно остаётся наблюдённой
особенностью режима2, а не held-once PASS и не открытым дефектом T031.
Новый нестандартный перехват ради его подавления не нужен. Escape закрывает
карточку в обоих опытах:2,409с и2,230с от начала соответствующего наблюдения.

Дополнительная установленная матрица:

- t031-final-remember-skip-repeat.log: checkbox1, Skip AXPress0,
  закрытие0,135с. Оператор отдельно подтвердил Never, новую встречу без
  автостарта и рабочий ручной Start/Stop; эти отдельные наблюдения не выводятся
  из одного только лога нажатия.
- t031-final-record-light-fullscreen-repeat.log: checkbox1, Record AXPress0,
  закрытие0,390с. Светлая тема и выбранный Always подтверждены оператором.
  Имя этого файла само по себе не используется как доказательство fullscreen.
- t031-final-always.log содержит только timeout наблюдателя. Автостарт новой
  Always принят по отдельному переданному оператором AX-состоянию recording;
  timeout в одиночку доказывает только отсутствие наблюдённой карточки.
- t031-final-fullscreen.log:320×192, явный фокус и закрытие Escape0,5485с.
  Оператор отдельно подтвердил Zoom AXFullScreen1 до появления и отсутствие
  capture после отказа. Проверка полноценного fullscreen отделена от
  предыдущего неудачно названного опыта светлой темы.
- Zoom=Ask и тёмная тема восстановлены по сообщению оператора.

Native bf9fdc964 ранее подтвердил внешний Enter/Escape,8,924с timeout и
локальные Enter/Escape. Между ним и текущим HEAD изменена только локальная
keyboard-навигация; прежние размеры/геометрия/таймер/рендеринг не изменены.
Actual-view rendering/прокрутка маленькой области и inert anchor helper из
t031-extra-checks.json остаются ограниченными доказательствами этих путей,
а не новым установленным физическим multi-display опытом.

### Convergence-решение в рассмотренном объёме

T031 принят по требованиям, коду и описанной установленной матрице; открытых
подтверждённых P0/P1/P2 в этом изменении нет. Для документального завершения
оператору осталось перенести итоговые403ce829 наблюдения в validation/task/PR,
сохранить связи с отдельными AX-результатами Never/Always/темы/fullscreen и
не переписывать исторические FAIL в PASS. Сам повтор всей матрицы для этого
не требуется. Исполнитель обновляет tasks, reviewer старые отметки не меняет.

T014 по-прежнему зависит от выполняемой реальной sleep/wake-проверки либо
прямого документированного исключения владельца. Пока результата нет, sleep
не объявляется PASS. T015 — итоговый convergence/metadata и эти зависимости.
Dropout%/новый60мин baseline не измерены и не становятся новым требованием
из-за keyboard/UI diff. Общий frozen release-full/CD/publication gate отдельно;
текущее решение не является blanket release-go.

## Дополнение: фактический sleep/wake и итог F6788

Прочитаны новый итоговый раздел validation.md,
`.dev/release/native-sleep-keyboard.json` и `t031-final-acceptance.json`.
Оба JSON привязаны к `403ce8291452df73db07eda835840ba24fb60d92`.
Новых действий над Dev или изменений реализации reviewer не выполнял.

Прежний блокер «реальный сон не подтверждён» закрыт: оператор записал
фактические pmset Sleep12:47:31UTC→DarkWake12:50:00 (149с) и последующую
Maintenance Sleep12:50:45→keyboard Wake12:51:03 (18с). Это подтверждение
сна ОС, а не только принятой команды. Неудачный wake-schedule returncode1
сохранён; он не опровергает последующее реальное пробуждение клавиатурой.
ended_at12:50:43 в JSON относится к завершению помощника и восстановлению
inhibitor; последующие user_wake/new_reminder поля добавлены по наблюдениям
оператора после его завершения, а не получены внутри завершившегося помощника.

После wake восстановилась календарная карточка, ранее доставленный стабильный
occurrence не повторился. Для нового occurrence отдельно подтверждены
usernoted Delivering12:54:08.141 и NotificationCenter banner12:54:08.145,
по сообщению оператора на точном Dev bundle. Это независимые проверки dedup
старого и доставки нового события; старая reservation не выдана за новую
доставку. Cleanup двух synthetic наборов и восстановление разрешённого
caffeinate записаны. Sleep-исключение владельца для этого опыта не требуется.

Границы сохраняются: результат доказывает реальный sleep/wake и восстановление
нативного календарного пути; отдельное сквозное появление нового summary
в момент wake этим JSON не измерялось. Прежние проверки продолжения опроса,
сети и >5мин ожидания остаются отдельными доказательствами своего объёма.
Часовой аудиопуть/dropout, VoiceOver и физический второй экран не становятся
PASS. Нового требования повторить весь продуктовый baseline из этого
клавиатурного/UI-изменения не возникает.

Итог независимого review рассмотренного объёма F6788: T031 принят; T014 можно
завершать по накопленной установленной матрице с указанными ограничениями.
Открытых подтверждённых P0/P1/P2 или прежнего sleep-блокера в этом объёме нет.
T015 остаётся процедурным завершением: сверка tasks/issue/PR/фрагмента и
convergence, точные SHA CI/Dev и документирование границ. Этот reviewer не
закрывает задачи/issue и не заменяет проверки остальных фич общего релиза.
После документационного коммита прежние native результаты сохраняют свой
SHA; новый GitHub exact-SHA gate нужен согласно release policy, а не путём
переименования старых evidence. Далее штатный frozen candidate, один
authoritative release-full, CD dry-run и публикационные проверки.
