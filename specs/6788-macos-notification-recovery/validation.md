# Проверки объединённой F6788 — рабочий журнал

Дата: 2026-09-08. Ветка codex/6788-macos-notification-recovery, база
 e81b412dff920cc29ed37de60be13dd8fc4d1c43. Это результаты незакоммиченного
объединённого дерева; точный кандидат/CI/Dev добавляются после фиксации.
Lane: high-risk-product. Merge, release и production не выполнялись.

## Выполнено

- Независимые требования: affected-analysis.md, 22 FR/SC и 21 задача,
  38/38 требований UX/capture/security/infra, CRITICAL/HIGH/MEDIUM0.
- Обязательные ensure/validate после объединения: 279 Spec Kit issues PASS;
  #6825 владеет T001–T021; источники F258/#6824 остаются Refs.
- Issue-canon upstream: 21 unittest PASS, GRAF full-ID regression3 PASS.
  Закреплён источник adf2b1b6d7f04ec97099b6716d2f971350262920;
  отдельный PR зависимости spec-kit-ext-github-issue-canon#11, без публикации.
- Frozen governance PASS с изолированным specify1.0.1. Extension установлен
  штатным CLI из immutable archive; lock хранит реальный source/archive/tree,
  а не скрывает drift. Глобальный specify не изменён.
- Нативные focused: 289 PASS (capture, calendar, route, controls, notifications,
  upload/client/queue/custody). Включены реальные async gates для смены owner,
  snapshot/calendar во время permission/submit, отсутствие recap при12 готовых
  встречах, свежие/исторические/чужие incidents, переход к своей записи внутри
  общей группы и повтор команды. ОС этими тестами не вызывается.
- TwoBrainRecApp compile PASS после итогового переноса/targeting.
- Серверный объединённый набор: 197 tests, первоначально191 PASS/1 SKIP/5 FAIL.
  Все5 FAIL — удалённые по решению владельца поля UUID/time native recap в
  старых ожиданиях F258. После адаптации весь изменённый summary-slot набор:
  23 PASS. Таким образом функциональные проверки выбранных197 прошли,
  кроме явно пропущенного bootstrap ниже; исходные F2581734 не засчитываются.
- В серверном наборе выполнены actual restricted media-role/RLS/DML checks,
  processing/sync/audio source fences, selected summary format, Node behaviour,
  contract/static assets/settings. PostgreSQL запускался в одноразовом
  контейнере, удалён штатно. Общий GRAF Dev и его БД не использовались.
- Общий Swift набор820: первоначально818 PASS/1 SKIP/1 FAIL. FAIL был старым
  source-string ожиданием группировки custody; исправлено на адресный выбор.
  Повтор всей соответствующей группы: 17 PASS. Итоговый общий прогон: 820 tests, 819 PASS / 1 SKIP / 0 FAIL (38.28с).
- git diff --check и changelog validator PASS.

## Не засчитано как PASS

- Swift permission preview без GRAF_PERMISSION_PREVIEW_DIR — штатный SKIP.
  DesktopCabinetWorkspaceTests и EmbeddedCabinetJavaScriptConfirmTests исключены
  из CLI: создают отдельные окна вместо единственного установленного GRAF Dev.
- Full runtime-role bootstrap idempotence — SKIP в shared fixture namespace;
  отдельная actual media-role проверка прошла. Не выдавать её за полный bootstrap.
- Установленная приёмка, реальная synthetic запись и настоящий OS banner/actions
  объединённого кандидата — NOT RUN. VoiceOver исключён владельцем.
- На момент проверки общего стенда active F256/257, schema0090. F256 согласована
  короткая повторная приёмка. Наша база0089; штатный harness запрещает
  schema rollback после допуска новых writers. Данные/manifest не подменялись;
  требуется совместимый путь приёмки, иначе этот gate остаётся открытым.
- Exact-SHA GitHub governance-fast — ещё не запускался для продуктового PR.
- Release-full/CD/notarization/appcast/production — не выполнялись: эта задача
  готовит PR, публичный релиз имеет отдельный frozen candidate и разрешение.

## Legacy и объём F258

Убраны DesktopRecordingWidget/его View/placement и четыре private tray events;
общий dispatcher, F214, очередь, decoder и данные сохранены. Не перенесены
native recap, отдельные UUID/time и helper без оставшегося потребителя.
Сохранены звук/progress/обновление/узкие grants, owner/auth epoch/incidents.
Результаты обработки остаются в кабинете. Старые настройки false читаются.
Дедупликация календаря использует stable occurrence ID и синхронизирует прежний
ключ с временем при его обнаружении. Claim означает попытку, не доказанный
показ ОС. Никакой fallback-overlay при Focus или отказе нет.

## Дополнение T022 и финальные проверки до коммита

- Независимый bounded review Reload: code-review.md, подтверждённых замечаний нет.
- На объединённом дереве настоящий WebKit/loopback HTTP воспроизвёл прежний
  отказ: второй GET не пришёл за5с. После guard исправления весь набор:
  29 PASS (18route +10request-policy +1reload),0.338с. Тестовое окно не показывалось.
  Логи reload-red.log/reload-green.log в ignored .dev/imported-postrelease.
- Серверный ruff check: PASS. Issue canon после T022:279 PASS.
- PR зависимости issue-canon#11: все обязательные CI/Security checks PASS
  на adf2b1b6d7f04ec97099b6716d2f971350262920; не merged/released.
- 2026-09-08: F256 освободил Dev после14smoke, активный dev-74ceabab096b
  со schema0090; следующий согласованный слот взял F257. Эти результаты
  не засчитываются F6788. Installed часть T014/T021 и финальный T015 открыты.

- Финальный общий серверный набор всех изменённых test-файлов:173 PASS/1 SKIP,
 101.37с; actual media-role/RLS/DML, summary-slot/format, sync/context, Node,
 шаблоны и настройки. Лог merged-server-final.log. Одноразовый контейнер удалён
 штатно. Это174 теста; ранний197-набор включал дополнительные неизменённые
 проверки, его пройденные результаты остаются историей этого дерева.
- Общий code reviewer после длительного чтения был прерван родителем и
 продолжен в той же read-only session для конечного заключения. Прерванный
 проход сам по себе не засчитывается как review PASS.

## Итоговый review и T023

Независимый общий review:combined-code-review.md. C1–C3 закрыты, P0/P1=0,
один P2 о неверной подписи partial. Отдельных Ponytail findings нет. T023
согласовал серверную строку/метку и реальный JavaScript списка/этапов.
Optional summary_status в MeetingListItem берётся из прежней проверенной
проекции; новых данных/прав/миграций нет.

Регрессии до исправления:2 FAIL (сервер и Node); после:132 PASS/0 SKIP/0 FAIL,
125.57с, включая все summary-slot/Node/static-assets/meeting-list. Логи
partial-red.log/partial-green.log. Ruff, whitespace и changelog PASS.
Независимая повторная проверка T023 выполняется отдельно.

## Сверка перед первым PR

T023 независимо принят:t023-review.md, P1/P2=0; код пяти файлов после
132PASS неизменен. Общий review вместе с повторной проверкой закрывает
C1–C3 и последний P2. Продуктовый объём23 задач/14 FR/8 SC: реализация
T001–T013,T016–T020,T022–T023 завершена. Остались существующие T014/T021
(установленный путь и измерения) и T015 (Dev/CI/готовый PR). Дополнительной
непокрытой работы по коду при повторном converge не выявлено; открытые gates
не продублированы новыми задачами и не названы полным завершением фичи.

Перед коммитом:git diff --check, changelog, frozen governance PASS; bounded
credential-pattern scan88 изменённых/новых файлов — совпадений нет. Разрешение
на проверенный commit/push/PR уже дано владельцем. Ни исходный F258PR, ни
старые задачи не закрываются этим черновиком.

## T024: достоверная проверка конкурирующей установки Dev

F257 передал воспроизведённый в общем CI нестабильный результат прежнего
test_concurrent_promote_is_serialized. Независимый reviewer подтвердил
границы SC006: один изменяемый manifest не моделирует двух конкурирующих
кандидатов. Второй процесс может прочитать уже опубликованный active и
законно получить idempotent PASS. Если оба прочитали исходный manifest,
второй получает identity-changed, а не заявленный старым комментарием
stale-parent. В изолированном последовательном воспроизведении получены
active/active и second_idempotent=true. Старый тест на Mac в данном запуске
прошёл; это не red→green доказательство постоянного отказа.

T024 меняет только tests/governance/test_dev_harness.py: общий активный
родитель, два разных кандидата, один победитель, точный stale-parent отказ
второму и сверка active ID/SHA/parent с победителем. Отдельная проверка
повторного запуска active сохранена. scripts/dev-harness.py, блокировка,
приложение, runtime, данные и manifest настоящего Dev не менялись.

После исправления: весь test_dev_harness.py —17 PASS,0.51с (macOS spawn);
целевой тест с multiprocessing fork —1 PASS,0.08с; весь tests/governance —
299 PASS/1 SKIP,36.82с. Проверки используют временные метаданные без --live.
Успех одного запуска не означает перебор всех вариантов планирования.
Независимая проверка требований T024 принята до изменения; итоговая проверка
diff: t024-review.md, PASS/P1/P2=0, собственный запуск reviewer1 PASS.
Convergence T024 закрыт; дополнительных задач в этом изменении не найдено.
Установленная приёмка T014/T021 и итоговая T015 открыты.

## Перенос поверх F256 и T025 (2026-09-09)

Опубликованный F6788 SHA8497ae99b7adba3e6ecea19b7863f25659242ad4 прошёл
GitHub governance-fast34264520246 (6мин51с). Скачанные CI evidence и receipt
прошли оба валидатора; requested/start/end/component SHA совпали.
Это fast, не release-full; после следующего commit/rebase результат исторический.

Для совместимости с действующей схемой0090 выполнен чистый rebase поверх
F25609e03ffec2b2ef29a39394be7c892bb2eef1c89f. Локальная база проверки
73b9d814231b9ea61186e411c1d834445dd54a5a, tree
d96e65851986ddb9b58152b19ae2b378aea5c595. Оба продуктовых коммита F6788
сохранили range-diff `=`. F256 остаётся отдельной зависимостью PR6828.
На этом дереве Swift focused148 PASS; совместный серверный набор194 PASS /
2 SKIP (181.95с). Один SKIP был browser без GRAF_NODE_MODULES, второй —
прежний full runtime-role bootstrap; они не названы PASS.

Независимый review совместимости нашёл P2: первое появление расшифровки
сохраняло оболочку аудио без новых дорожек, аватаров и контекста комментариев.
T025 обновляет только изменяемое содержимое сохранённого плеера, использует
один набор обработчиков и динамически читает спикеров. Звук, черновик, фокус
и состояние раскрытия сохраняются; другой media revision останавливает
и заменяет прежний плеер. playback-comments.js не изменён.

- Новый реальный browser regression сначала воспроизвёл FAIL (0 вместо2
  дорожек), затем PASS в Chromium и WebKit. Используются синтетический WAV,
  реальные DOM/медиасобытия и существующий playback-comments.js.
- Проверены непрерывность time, прежний audio node, отсутствие pause/loadstart,
  textarea/draft/focus, повторный refresh, collapse, новый «К реплике»,
  Next/track Home/lane/avatar/listen и смена media revision.
- Второй независимый review выявил P2: recovery signature не учитывала
  media/processing revision при одинаковом тексте. Дополнена существующая
  сигнатура, без нового механизма обновления. Добавлен вызов настоящего
  refreshPlaybackRecovery: RED на старом processingResultId, затем
  Chromium PASS и WebKit PASS, включая остановку другого media revision.
- Существующие playback-comments browser tests: Chromium PASS / WebKit PASS.
- Затронутый server набор104 PASS /0 SKIP,41.53с, включая настоящий
  HTTP/PostgreSQL comments browser test. Одноразовый PostgreSQL удалён штатно.
- Pytest wrapper нового browser regression с GRAF_NODE_MODULES вместе
  с progress tests:5 PASS,1.47с после исправления recovery signature.
- Ruff изменённого Python test, node --check, git diff --check, changelog
  validator и frozen Spec Kit governance:PASS. Issue canon269 PASS после
  синхронизации единственного владельца #6825 для T001–T025.

Повторное независимое review T025:t025-review.md, P1/P2=0. Reviewer сам
выполнил Chromium PASS и WebKit PASS; зафиксировал SHA-256 обоих файлов.
T025 закрыта, избыточной сложности не найдено. Код после review не менялся.
Повторный bounded converge по FR012/SC007 не выявил новых задач;
остаются ранее заведённые T014/T021/T015, без дублирования приёмки.
Установленная приёмка F6788 всё ещё NOT RUN: активный Dev041cd8541e9d
используется согласованной работой F256/F257. F256 сообщила о готовящейся
следующей миграции; окончательный SHA зависимости ещё не зафиксирован.
Стенд, схема, подпись и разрешения не менялись. T014/T021/T015 остаются
открыты; чужая установленная приёмка100%/200% им не присваивается.

## Исправление устаревшего ожидания CI после T025

GitHub34269176917 на17d6cbde451feb55e6ea9454d667700f73fa5f23 обнаружил
1 FAIL /1564 PASS /1 SKIP в unit: старый test_cabinet_web_shell искал
буквальный track.addEventListener. Делегирование теперь принадлежит оболочке,
а фактический click/Home и обновлённые дорожки проверяет исполняемый
playback-refresh.test.cjs. Удалено только это устаревшее source-string
ожидание; остальные проверки HTML/CSS сохранены. Рабочий код не менялся.
Весь test_cabinet_web_shell.py + test_meeting_progress_ui.py с browser
зависимостью:96 PASS,2.07с; Ruff и whitespace PASS. Новый CI остаётся обязателен.
Первый PR event34269015795 был запущен ещё с base master и предыдущим
описанием: отказал по F256/F6788 identity и оформлению evidence. PR теперь
имеет base F256, а описание заново прочитано с GitHub и прошло валидатор
с настоящим SHA. Ни один из этих отказов не объявлен PASS.

## Изоляция проверки миграций T021

GitHub34269776829 на5f2ae23076557f2197c7daff2c3ff4a87cfea583:
unit1565 PASS/1 SKIP; changed-server194 PASS/2 SKIP/1 FAIL. Отказ
test_slot_migration_head_is_idempotent обращался к исходному run-prefix DB,
хотя fixture передала созданную worker DB. Причина: Alembic env.py читает
кэшированный get_settings(), а тест изменял только переменную окружения.

В тесте повторно используется существующий prepare_schema из общей fixture:
он очищает кэш до миграции, восстанавливает URL и снова очищает кэш после.
Удалены дублированная настройка Alembic и неиспользуемые импорты. Два
последовательных upgrade сохранены; migrations/env.py и рабочий код не менялись.
Полный test_meeting_summary_slots.py:23 PASS,27.45с; Ruff/whitespace PASS.
Изолированный PostgreSQL удалён штатно, данные Dev не затрагивались.

## Проверка версии 533a25b6 и исправления T026/T027 (2026-09-09)

Проверенный SHA: `533a25b65946ad76cb3f82687e2eff15a5b3362e`.
GitHub governance-fast [34272212021](https://github.com/yshishenya/graf/actions/runs/34272212021)
прошёл за 630 с: governance — 299 PASS / 1 SKIP, server unit — 1565 PASS /
1 SKIP, changed server — 203 PASS / 2 SKIP, CI contracts — 66 PASS.
Скачанные evidence и receipt прошли оба валидатора. SHA запроса, начала,
завершения, цели и компонентов совпали с 533a25b6; рабочее дерево чистое,
контрольная сумма совпала. Swift в Linux workflow пропущен. Отдельно на Mac:
149 focused tests PASS, Chromium/WebKit playback-refresh PASS. Это проверка
PR, не release-full. После нового коммита эти результаты станут историческими.

Единственный GRAF Dev установлен штатным build/promote/smoke на этом SHA:
13/13 PASS, схема 0091. Bundle ID, сертификат, designated requirement и
entitlements сохранены. После приёмки стенд передан F257; чистая рабочая копия
прежнего SHA сохранена для штатного возврата. Вторых приложений не создавали.

Метод проверки: CUA getApp подтвердил страницу синтетической встречи, затем
инструмент перестал быть доступен для действий. Дальнейшие действия выполнены
через публичный macOS Accessibility API, привязанный к точному пути
`/Applications/GRAF Dev.app`; снимки — штатным screencapture. Это автоматизация
интерфейса, не проверка VoiceOver. Разрешения ОС и Focus не менялись.

- Основной исполнитель прошёл оба общих входа «Настройки» → «Уведомления» →
  «Настроить на этом Mac». Открывается выбранная нативная вкладка.
  Сохранённые включённые reminders/title/sound оставлены без изменения.
  Начальные значения не проверялись сбросом настроек. Интерфейс показывает,
  что macOS разрешает уведомления.
- «Проверить уведомление» нажато ровно один раз. Появился текст «Тест передан
  macOS. Показ зависит от системных настроек и Фокусирования». Баннер и действие
  на нём не наблюдались. Это подтверждение передачи ОС, не показа баннера.
- При закрытом кабинете выполнен один цикл Start/Mute/Unmute/Stop с заранее
  подготовленным синтетическим звуком. Свежие события одной сессии: started,
  pause_requested (silenced), resume_requested (capturing), local_recording.saved,
  recording.stopped. Затем меню вернулось к Start; доступных окон управления нет.
- Монотонное время от внешнего AXPress до наблюдения свежего AppLog:
  завершённый Start — 398 мс, Mute — 65 мс, Unmute — 49 мс, Stop до saved —
  229 мс. Первое число измеряет завершение старта, а не появление starting.
  Это четыре наблюдения, не p95 каждой команды. Stop <5 с подтверждён;
  p95 ответа ≤200 мс пока не измерен.
- Пакет длительностью 9,308 с: saved/none; WAV — 16000 Hz, mono, 148932 кадров;
  M4A — 48000 Hz, mono, 446795 кадров. Оба timelineAligned=true,
  durationDifferenceSeconds=0. Счётчики ptsGap, hostUnderrun, hostOverrun,
  processError, nonFinite, clipped и reset равны нулю. Доля dropout не измерена;
  эти показатели не заменяют её или аппаратную проверку длительностью 60 минут.
- Прежний media-validator отверг корректный AAC из-за пробелов и отсутствия
  кавычек в afinfo. Отдельный afinfo подтвердил AAC mono 48000 Hz. T027 меняет
  только разбор текста; исправленный валидатор принял тот же неизменённый пакет.
- На первом входе воспроизведено постоянное состояние загрузки списка.
  Независимая диагностика установила цикл processed/not_requested →
  summaryPending → requestSubmit → сброс интервала → afterSwap/init.
  Четыре итерации при неподвижных часах дали четыре запроса и обновления.
  T026 устраняет причину.
- Владелец F256 проверил источник и дорожки на 533a25b6 через public AX:
  переход к источнику, выполнение rAF, видимость первого источника, исходные
  раскрытые дорожки и сворачивание — PASS. Повторное раскрытие не засчитано:
  последующий снимок оказался на Home без соответствующего действия основного
  исполнителя. Это отдельное наблюдение до исправления T026.

Требования T026/T027 независимо проверены: новых неоднозначностей
HIGH/CRITICAL нет. Для T026 два теста воспроизвели отказ: not_requested и
четыре обновления при неподвижных часах. После исправления целевой набор:
173 PASS / 1 SKIP. Регрессия проверяет queued/generating/blocked_dependency,
первое появление и уже видимую расшифровку, затем available/partial/failed/
unavailable. Повтор swap/init не создаёт цикл. Прежние ограничения источника,
владельца и поколения запросов сохранены.

В T027 старый manual harness делегирует существующему отказу run-local-app.sh.
Устаревшая Swift-проверка текста cleanup удалена; её заменяет исполнение
отключённого входа с ловушками для build/open/kill в governance. Проверки
Dev identity: 5 PASS. Самопроверка метаданных: PASS, два положительных и семь
отрицательных примеров. ContractValidation, синтаксис shell, Ruff и
check-development-process: PASS. После изменений также пройдены
Chromium/WebKit playback-refresh.

T014/T021/T015 открыты до окончательной приёмки нового SHA. Непройденные
сценарии F214, календарных действий ОС, восстановления, черновика/фокуса,
работы без сети и последовательного появления результатов не объявлены
выполненными.

## Независимая проверка T026/T027 — 9 сентября

Агент F257, не автор изменений, выполнил read-only code/Ponytail review.
Замечаний P0/P1/P2 нет; собственный набор проверяющего — 3 PASS, включая
24 сочетания состояний списка. Хеши пяти исполняемых файлов перед коммитом
сверены с отчётом `t026-t027-review.md` и совпали. T026/T027 завершены по
реализации и автоматическим проверкам. Это не заменяет установленную приёмку
нового SHA: T014/T021/T015 остаются открыты.

## Перенос на окончательную основу F256

Основа F256: `b05d3f3899a03a3e81b0c7a160f45141b3ddb96d`. Семь собственных
коммитов F6788 перенесены без конфликтов; range-diff для каждого равен `=`.
Повтор: server173PASS/1SKIP, Swift149PASS/0FAIL. Browser wrapper пропущен
в pytest и выполнен отдельно: Chromium/WebKitPASS. После переноса изолированный
T025 browser fixture первоначально не включал новый общий helper прокрутки;
добавлена одна строка с настоящим source section, без заглушки и изменений
продуктового кода. Оба первоначальных отказа и повторы сохранены отдельно.
Этот повтор не заменяет новый GitHub CI и установленную приёмку.


## Подготовка общего выпуска 2026-09-09

F256/F257/F6788/F259 объединены локально. F254 пока не включена: её
незавершённый объём и состав выпуска требуют отдельного решения владельца.
Это подготовка, не доказательство готового публичного релиза.

- Общий код до исправления проекции: Swift 140/140 PASS; playback-refresh
  Chromium и WebKit PASS. F256 WebKit: первоначально timeout при seek;
  диагностический повтор без изменения продукта — 56 вариантов геометрии PASS.
- Набор F257 с изолированной PostgreSQL: 106 PASS, 1 FAIL. Падение выявило,
  что reported available без сохранённых итогов ошибочно становится
  not_requested. Общий summary_progress теперь возвращает unavailable;
  явный ещё не заказанный формат сохраняет not_requested. Регрессия проверяет
  оба состояния и отсутствие stored_output. Первый адресный повтор: 1 PASS.
  Полный набор summary slots вместе с регрессией: 24 PASS, изолированная
  PostgreSQL удалена штатным runner после завершения.
- Проигрыватель не переписывался при объединении: сохранены открытие вкладки
  recording и общий scrollTranscriptTurnIntoView; в новой компоновке F257
  оставлен один toolbar. Подсказка незапрошенных итогов требует выбора формата.
- Ponytail review разрешения конфликтов и исправления проекции: новых
  зависимостей и абстракций нет, использованы общий расчёт и существующий тест.
- T014/T015/T021 остаются открытыми: установленная приёмка общего SHA,
  независимая оценка финального кандидата, точный GitHub gate и выпуск
  не заменяются перечисленными адресными проверками.

## Приёмка release-prep SHA63b9b58, 2026-09-09

Точный SHA: 63b9b58b0192d79c20dec81389e1acb751ff337c. Штатные Dev build/promote
и smoke13/13 PASS, схема0091. GitHub governance-fast34329125322 PASS.
Lane release-deploy. Master/production/публичный выпуск не изменялись.

- 20 циклов public AXPress → доступное состояние значка: p95 Start60.65мс,
  Mute25.29мс, Unmute26.42мс, Stop43.57мс. Stop→stopped максимум364.81мс.
  25 сохранённых пакетов с дополнительным повтором: все tracks aligned/saved,
  максимальная разница длительности1мс при пороге100мс; ptsGap/overrun/underrun/
  processError/nonFinite0. Это короткий опыт, не измерение процента dropout и
  не новый аппаратный60мин baseline.
- В первом циклическом опыте кабинет оказался открыт после Mute цикла11,
  видим в циклах12–20. Журнал: became_active_recovery, источник активации
  неизвестен. Пять повторов после закрытия — PASS, но первоначальный оконный
  критерий остаётся FAIL/UNRESOLVED, а не отменяется повтором.
- Оба входа в настройки, нативная вкладка уведомлений, сохранение черновика
  после перехода в другую вкладку и повторного входа: PASS. Черновик отменён.
- Тест macOS принят. Usernoted сообщил Presenting as banner для теста и
  синтетического календарного reminder; фактическое наблюдение баннера и его
  действия пока не приняты. Разрешения/Focus/системные настройки не менялись.
- Локальное синтетическое календарное событие появилось в меню без capture;
  источник/календарь/событие удалены после опыта. Внешних provider calls0.
- 20с паузы только локального Dev API: проигрывание и черновик/фокус сохранялись,
  API восстановлен. Это bounded недоступность API, не весь offline lifecycle.
- T028/#6839: pending с08:52:22UTC, восстановление синтетического опубликованного
  результата08:57:42UTC. За20.09с итоги не появились, хотя аудио и активный
  черновик/фокус сохранились. Root cause — titleEditorActive guard откладывает
  всё содержимое. Тестовый результат восстановлен, временная попытка удалена.
  T014/T021/T015 остаются открытыми. VoiceOver исключён владельцем.

T029/#6840: в09:05:26UTC настоящая доставленная карточка graf.local.test
найдена в Notification Center и нажата штатным AX действием. macOS usernoted
зафиксировал contents response и foreground; приложение сначала показало
настройки (reason=notifications), затем main (reason=reopen_visible).
Это подтверждённый конфликт собственного повторного открытия, а не отказ ОС.
Исправление использует стандартный hasVisibleWindows: при уже видимом окне
reopen не меняет выбор; без окон явный reopen открывает кабинет. Удалено
автоматическое открытие main при любой активации; launch/tray/settings/error
и обычный reopen сохраняют явные входы. Нужна повторная проверка нового Dev.

### Исправления T028/T029 перед повторной установленной проверкой

- T028: вместо откладывания всего обновления сохраняются подключённые форма
  названия и её предки; соседнее содержимое обновляется. Реальные Chromium и
  WebKit проверяют черновик, фокус, выделение, непрерывное audio, отсутствие
  автоматического POST, последующие Enter/Escape и начало редактирования во
  время запроса. Независимый повтор этих двух browser-проверок: PASS.
- T029: удалён автоматический показ кабинета при любой активации;
  applicationShouldHandleReopen открывает кабинет, только когда нет видимых
  окон. Swift build TwoBrainRecApp: PASS. Повтор установленного действия
  уведомления и скрытого кабинета требуется после штатного обновления Dev.
- Ponytail review T028: лишних зависимостей/абстракций не найдено; перенос одной
  формы отсоединением недопустим, поскольку blur сохраняет черновик.
- Эти проверки относятся к изменениям после 63b9b58; прежний governance-fast
  не используется как доказательство исправленного SHA. T028/T029 и общая
  приёмка пока остаются открытыми.

- Независимый review T029 нашёл аналогичную ветку activation_recovery в
  отложенной проверке launch. Она удалена; восстановление при отсутствии
  видимых окон оставлено. Повторная сборка TwoBrainRecApp: PASS.
- Канон GitHub: PASS, 263 задачи. Development-process: PASS.
- Первоначальная frozen-проверка отказала: глобальный specify обновился до1.0.4
  при закреплённой1.0.1. Повтор с существующим изолированным CLI1.0.1: PASS;
  lockfile и глобальная установка не менялись.

- AppLifecycleWindowRegressionTests: 1 PASS; исполняются настоящие тела
  lifecycle-методов с управляемыми оконными зависимостями. Каждый из трёх
  прежних методов поочерёдно возвращён только во временном тестовом файле:
  все три отрицательные проверки воспроизвели соответствующий отказ.
  Тест не подменяет фактическую проверку AppKit через AX.

### Повтор установленной приёмки на 343f5e099dd312d0a2c82cf36438805761789db0

- Штатные build/promote/smoke: PASS13/13, schema0091, идентичность Dev сохранена.
- T029: пять циклов Start/Mute/Unmute/Stop без открытых окон PASS; p95
  93.75/29.62/29.21/36.33мс. Простая активация сохраняет скрытый кабинет;
  явный reopen открывает закрытый и разворачивает свёрнутый кабинет: PASS.
- Реальная карточка graf.local.test нажата в центре уведомлений09:30:07UTC.
  Вкладка нативных уведомлений сохранилась впереди, повторный visible reopen
  её не перекрыл: PASS. Временный баннер отдельно не засчитан как увиденный.
- При20с паузе только Dev API audio и черновик сохранились. Первый измеритель
  не видел root AXFocusedUIElement у фонового приложения; поле сохраняло
  AXFocused=true. Дополнительный прогон подтвердил фокус/черновик, но ошибочно
  переключил playback через совпадение Help. Эти неполные прогоны сохранены.
- После>5мин ожидания результат появился за546.44мс, однако общий тест FAIL:
  тест нажал Play после чтения черновика; blur вызвал сохранение названия.
  Найден самостоятельный отказ POST403: multipart FormData не передавал
  X-CSRF-Token, а сервер читает form token только у urlencoded.
- В T028 добавлен существующий общий csrfToken в заголовок сохранения;
  защита сервера не меняется. Browser-регрессия проверяет заголовок;
  Chromium/WebKit PASS. Установленный повтор Enter и автоматического появления
  с черновиком требует нового SHA. T028 остаётся открытой.

### Итог T028/T021 на bc76098240028958fe27c201809e416f2b1f81cb

- Build/promote/smoke: PASS13/13; GitHub governance-fast34335444153 PASS.
- Реальный Enter в установленном Dev: POST title HTTP200 и «Название сохранено».
  Тестовое название возвращено тем же UI и HTTP200.
- При>5мин pending результат появился за9710.21мс (предел15000), audio продолжилось,
  черновик и AXFocused сохранились. После публикации Enter снова дал HTTP200;
  следующий черновик отменён Escape без сохранения. Исходное название восстановлено.
- Повтор20с offline с исправленным измерителем: audio, черновик и фокус PASS.
  Dev API восстановлен в finally; черновик отменён. Открытие нативной вкладки
  уведомлений из загруженного кабинета при остановленном API отдельно PASS.
- Предыдущий native-summary-clean остановлен до публикации из-за несоблюдённых
  предусловий инструмента; backup результата восстановлен. Ошибки ранних
  измерений не выданы за PASS. Из AX helper убрана лишняя установка AXFocused;
  кнопки выбираются по точному имени без совпадения с Help.
- Календарь: будущее напоминание восстановилось после штатного перезапуска,
  появилось09:38:38.979UTC; default открыл меню, запись не началась. Для отдельного
  синтетического события macOS выполнила graf.join09:44:02.742UTC; в Zen
  подтверждён документ https://example.com/graf-release-check, запись не началась.
  Оба синтетических источника/календаря/события удалены. Это ещё не полная матрица
  disable/enable/move/cancel/sleep/account. F214 ждёт разрешения на пустую Zoom-встречу.
- T028 и T021 реализованы и проверены; общий T014 и зависимый T015 пока открыты.

### Продолжение установленной матрицы на bc7609824 — 2026-09-09

- Независимый UI reviewer повторно принял T028/CSRF и T021; GitHub fast на
  точном SHA проверен непосредственно. Новых блокирующих замечаний нет.
- Показанное синтетическое напоминание удалено при disable09:58:57UTC;
  enable не повторил его. Будущее событие выключено/включено и перенесено:
  вместо10:07:32 начало10:04:40UTC, единственная доставка10:03:40UTC.
- Выход из тестового Dev аккаунта10:01:41UTC, входа10:01:55UTC. OS cleanup и восстановление будущего
  напоминания подтверждены. Другой пользователь не проверялся этим сценарием.
- Действие календарной карточки открыло нативную вкладку уведомлений; запись
  не началась. Настройки возвращены: reminders/titles/sound включены, offset1мин.
- Корректно отменённое будущее событие удалено из OS10:09:22UTC, до due10:10:09UTC; доставки нет до10:10:21UTC.
  Ранний probe менял только source_status и пропустил source_deleted_at,
  устанавливаемый штатной синхронизацией; он не засчитан как приёмка отмены.
- Внешний полный экран: Example Domain в Zen, AXFullScreen=true. После
  штатного раскрытия строки меню курсором меню GRAF доступно. Один цикл
  Start/Mute/Unmute/Stop PASS без окон кабинета;82.53/31.79/27.30/37.98мс,
  Stop→stopped316.29мс. Это один образец, не оценка p95. Полный экран проверен
  до и после, затем выключен; созданная тестовая вкладка закрыта.
- Сон не подтверждён: первая pmset sleepnow принята, но экран включился
  по событию клавиатуры; предсуществующий caffeinate -is удерживает assertions.
  Повтор отклонён macOS: error0xe00002e2. Настройки сна и чужой процесс не менялись.
- Все четыре синтетических набора календаря удалены. API восстановлен,
  тестовый аккаунт вошёл, захват остановлен. Evidence хранит только метаданные.
- T014/T015 остаются открыты: F214 ждёт разрешения на пустую Zoom-встречу,
  настоящий sleep/wake не доказан. Dropout% не измерен; прежние нулевые
  counters и одна синтетическая запись не подменяют этот показатель.

### T030 — контекстный вход в автозапись

Независимый reviewer подтвердил неверное соединение onMeetingDetectionSettings
с общими openSettings(nil). На bc7609824 AX/CUA-кнопка не открыла автозапись.
Существующий callback заменён на openLocalRecordingSettings без нового окна
или контроллера; общие настройки сохраняют свой маршрут. Исполняемая регрессия
извлекает настоящее тело callback: прежний код воспроизвёл FAIL1/1.
T030/#6841 остаётся открытой до новой сборки, установленного перехода и exact CI.

T030: после исправления AppLifecycleWindowRegressionTests1 PASS/0 failures,
Swift build PASS; независимый review не нашёл P0/P1/P2. Старый callback ранее
дал1 FAIL, так что проверка обнаруживает неверное соединение компонентов.


### Финальная F214 и T030 на 29ec541b4454bedeb9e0d56f1103fd899a466237

- Штатный Dev build/promote/smoke13/13 PASS; GitHub governance-fast
  [34340109214](https://github.com/yshishenya/graf/actions/runs/34340109214) PASS
  на этом же SHA. Подпись, bundle ID, разрешения и schema0091 сохранены.
- T030: AX-нажатие «Автоопределение встреч» открыло нативную «Автозапись».
  Повтор с открытой вкладкой уведомлений и несохранённым выключением звука
  также открыл автозапись. При возврате черновик сохранился; «Отменить» вернула
  звук. Переход и сохранение черновика PASS; T030 завершена.
- F214 проверена в разрешённых владельцем тестовых Zoom-встречах. Ask показал
  8-секундный запрос; исчезновение измерено через8.779с и Dev начал запись.
  Явные «Не записывать» и «Записать» без remember выполнили выбор, сохранив Ask.
- «Запомнить выбор» подтверждён AXValue1: отказ сохранил Never, запись Always.
  В новом сеансе Never не было запроса и автозаписи; ручной Start/Stop работал.
  В новом сеансе Always запись началась без запроса. Исходное правило Zoom=Ask
  восстановлено; тестовые конференции завершены, Dev-захват остановлен.
- Первый AX-helper искал «Не записывать» только в AXTitle и пропустил кнопку
  с AXDescription; его повторный countdown не засчитан за отказ. После точного
  поиска Title/Description отказ подтверждён. В одном raw log возникло
  пересечение stdout/append; подтверждение состояния взято из отдельных
  AX-результатов. Общий журнал содержит также production GRAF и не используется
  как доказательство количества Dev-стартов.
- Сон пока не доказан. Владелец остановил калибровку AlDente, SleepDisabled
  сменился1→0. Разрешённый caffeinate остановлен и восстановлен после попыток.
  pmset sleepnow теперь принят, однако Logitech задерживает sleep acknowledgement
  на30000мс, а событие MX Master включает экран через18с; фактической пары
  system Sleep/Wake нет. Ожидается решение о повторе/явном исключении.
- Напоминания/названия/звук включены, offset1мин; новый временный календарный
  набор для sleep-проверки удалён после повторов. T014/T015 пока открыты.

## T031 — подготовка установленной приёмки, 2026-09-09

Компактная карточка по black-box наблюдению Krisp; reference и R031-1—3
приняты независимым reviewer. Issue #6842, FR015/SC009. Swift build PASS,
48 focused tests PASS (Countdown/Policy/RecordingLifecycle и настоящий расчёт
геометрии/lifecycle callback), Spec Kit governance PASS. Code/Ponytail review
без P0/P1/P2. T031 остаётся открытой до новой установленной приёмки тем, фокуса,
клавиатуры, сжатого содержимого и матрицы F214. Предыдущий Dev29ec не является
проверкой новой поверхности. Sleep/wake по-прежнему NOT PROVEN.

### T031: установленная проверка bf9fdc964

Dev build/promote и smoke13/13 PASS. Собственная карточка320×192 под значком;
внешний Enter/Escape не закрывает её, timeout8.924с до закрытия/запуска. Явный
клик даёт фокус карточке, Escape0.499с отклоняет, Enter0.999с начинает запись.
Проверенные тестовые встречи завершены, записи остановлены.
Первый Tab-опыт прерывался внешним фокусом, не засчитан. Повтор при удержании
фокуса владельцем: keyboard owner=Dev, focused window=prompt, но Tab пропускает
контролы при AppleKeyboardUIMode=0. Escape после этого отклоняет за1.322с.
Для нового повтора добавлены native SwiftUI focusable(interactions:.edit) и
локальные Space-действия; системная настройка пользователя не меняется.
Swift build/48focusedtests PASS; установленный повтор остаётся обязательным.

### T031: установленная проверка fea0a8ecd

Dev build/promote/smoke PASS. При выключенной системной клавиатурной навигации:
Tab checkbox→Skip→Record→checkbox, ShiftTab→Record; удержанный Space переключает
checkbox один раз, короткий возвращает исходный выбор. В этом опыте синтетический
ShiftTab оставил модификатор Shift в драйвере теста, поэтому последующий Escape
не засчитан; исправлен именно тестовый драйвер явным отпусканием Shift.
При включённой через системный UI навигации (AppleKeyboardUIMode=2) обнаружены
лишние цели фокуса и двойная обработка Space native+wrapper. После опыта
системная настройка восстановлена0, Zoom=Ask, встречи завершены, запись остановлена.
Исправление: общий локальный ViewModifier трёх контролов добавляет поддержку
только при выключенной системной навигации; в штатном режиме возвращает исходный
контрол. Swift build/48focusedtests PASS; оба native-повтора ещё ожидаются.

Независимый audit_capture повторно принял этот diff без P0/P1/P2: единый
ViewModifier оправдан тремя потребителями; установленный PASS ещё не заявлен.


## Финальная приёмка T031 и sleep/wake — 403ce8291452df73db07eda835840ba24fb60d92

Установлен единственный GRAF Dev через штатный build/promote, smoke13/13 PASS.
GitHub governance-fast [34351174310](https://github.com/yshishenya/graf/actions/runs/34351174310)
PASS на этом SHA. Независимый итог: t031-requirements-review.md, P0/P1/P2=0.

- Клавиатурная навигация macOS OFF и ON: один Tab-маршрут checkbox →
  «Не записывать» → «Записать» → checkbox; Shift-Tab возвращает к Record.
  Проверен фактический владелец клавиатуры Dev. Короткий Space переключает один раз.
  При OFF собственный обработчик принимает только down: удержание даёт одно
  переключение. При ON оставлен штатный checkbox и его autorepeat; это не
  held-once PASS и не дополнительное требование подавлять native repeat.
- Skip+remember закрыл запрос за0.135с и сохранил Zoom=Never; следующая встреча
  без запроса/автостарта, ручной Start/Stop работает. Record+remember закрыл
  за0.390с, начал запись и сохранил Always; новая встреча автоматически записалась
  без карточки. Auto-start подтверждён отдельным AX-состоянием записи, не одним
  timeout наблюдателя.
- Светлая тема: снимок установленной карточки320×192 проверен, текст и действия
  читаемы. Тёмная тема проверена установленными клавиатурными сценариями.
  Реальное SwiftUI-содержимое повторно отрисовано на этом SHA: обе темы,
  маленький viewport196×116 и длинное имя; прокрутка достигает checkbox/обеих кнопок.
- Полноэкранный Zoom подтверждён AXFullScreen=1 до появления карточки.
  Frontmost при показе us.zoom.xos; после явного клика Escape закрыл запрос
  за0.549с без записи. Физический второй экран NOT RUN, реальный helper геометрии
  и граница видимости/принадлежности status item проверены исполняемыми тестами.
- Прежняя проверка bf9fdc964 остаётся доказательством неизменённого countdown:
  внешние Enter/Escape не действуют на карточку, timeout8.924с, локальные
  Enter/Escape выполняют решение. Финальные логи имеют собственные SHA и не
  переименовываются в результаты более поздней сборки.
- После проверки восстановлены Zoom=Ask, тёмная тема, keyboard navigation OFF.
  Все тестовые Zoom-встречи завершены, Dev-захват остановлен.

Фактический sleep/wake подтверждён pmset: Sleep12:47:31UTC → DarkWake12:50:00
(149с), затем Maintenance Sleep12:50:45 → keyboard Wake12:51:03 (18с).
SleepDisabled=0, прежний разрешённый caffeinate временно остановлен и
восстановлен с теми же аргументами. Автоматическое wake не назначено из-за
требования sudo-пароля; пробуждение подтверждено клавиатурой владельца.
После wake календарная карточка восстановилась; повторный баннер старого
occurrence не появился. Новый синтетический occurrence после wake принят ОС,
доставлен один раз12:54:08.141UTC и показан NotificationCenter как banner
12:54:08.145UTC. Старый occurrence не используется как доказательство новой
доставки: его стабильный ID уже имел прошлую запись dedup. Оба синтетических
набора удалены, поставщики не вызывались. Настройки напоминаний/названий/звука
сохранены включёнными, offset1мин.

Метаданные: t031-final-acceptance.json, native-sleep-keyboard.json и отдельные
AX/action logs в локальном каталоге проверки; приватное содержимое не включено.
Ограничения прежнего объёма сохранены: dropout% NOT MEASURED, новый60мин
hardware baseline NOT RUN, VoiceOver исключён владельцем. Эти пункты не PASS.
Release-full, production и публикация выполняются отдельно после freeze.


### Итоговая сверка Spec Kit

Повторная сверка FR001–015, SC001–009 и31 задач с текущей реализацией,
накопленными независимыми review и установленной матрицей не обнаружила новой
недостающей/частичной/противоречащей реализации. Проверены прежние архитектурные
границы: общий обработчик захвата, маршруты настроек, owner/epoch уведомлений,
независимые статусы результатов, обновление без потери ввода, узкая роль worker,
собственная временная NSPanel без новых таймеров/моделей. Capture/consent,
privacy, deletion, release, Spec Kit и reference-fidelity ограничения сохранены.
Converged: новых задач не добавлено; T014/T031 завершены после acceptance,
T015 — после сверки документов/review/фрагмента. Новый документационный SHA
требует свежего governance-fast и финального Dev smoke; прежние UI-результаты
остаются привязаны к403ce829. Это PR-gate перед merge, а не выполненный release-full.
