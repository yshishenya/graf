# Проверка реализации F249

Дата: 2026-09-06. Lane: high-risk UX / capture-adjacent / reference fidelity.
Это evidence разработки, не разрешение выпуска или утверждение идеального интерфейса.

## Реализовано и проверено

- Сервер: одна карточка на источник и получателя, revision/read_revision,
  транзакционные producers processing/summary/sharing, текущие попытки и форматы,
  RLS, CSRF, повторная проверка доступа/удаления, срок истории 30 дней,
  подписанные cursor, HTML fallback. Mandatory billing outbox сохранён.
- Веб: important/history, точка только для нового важного, одиночный просмотр,
  обновление 30 секунд с сохранением focus, очистка при ошибке/смене области,
  отсутствие системной доставки. Отдельная форма настроек с CAS-конфликтом.
- Mac: общая панель CalendarTray, отдельный виджет с существующими capture
  действиями, локальные проблемы, нативный presenter с владельцем сессии,
  настройками и дедупликацией. Запросы напоминаний сериализованы, чтобы старое
  завершение add не отменяло новую версию. Контракт автозаписи 214 сохранён.
- Legacy: удалён всегда пустой rowsNeedingNativeVisibility и недостижимая
  compact queue; автономный recovery renderer и decoder сохранены.

## Автоматические результаты

Не суммируются как уникальные тесты: наборы частично пересекаются.

| Проверка | Результат |
|---|---|
| Swift полный набор | 805 passed; после последнего уточнения текста паузы 23 targeted passed |
| Swift notification/calendar/Dev packaging после правок | 32 passed |
| Server processing/settings/attempts | 58 passed |
| Server inbox/sharing/RLS/summary CAS/dispatch | 47 passed |
| Server domain после учёта нескольких форматов | 56 passed |
| Последние HTTP/RLS/миграция/preferences/contracts | 38 passed |
| HTTP сохранение и конфликт настроек | 2 passed |
| Workflow/CI regressions: Dev identity, отказ launchers, auth, account merge inventory, schema head | 22 passed; после включения preview в отказ 4 passed |
| JS syntax, touched Python lint, git diff --check | pass |

Локальный check_spec_kit_governance.py: FAIL — установленный github-issue-canon
не соответствует bootstrap lock. Это отдельный tooling gate, не passing check.
Глобальная проверка issue canon ранее не прошла на чужом #6684; scoped F249 23/23 PASS.

## Живой браузер

Настоящие маршруты, templates, JavaScript и PostgreSQL с искусственными карточками:
вход; сохранение настроек; конфликт двух вкладок и явное сохранение своего выбора;
отмена после сохранения; просмотр не решает проблему; тихая готовность; сохранение
focus после 30 секунд; Escape; 360 px без горизонтального overflow и с доступным
footer; offline очищает карточки; retry восстанавливает историю. Все 12 проверок PASS,
pageErrors пуст. Light и dark screenshots осмотрены, данные синтетические.
Это проверка UI на подготовленных карточках, не реальная MediaScribe-транскрибация.
Транзакционные producers отдельно проверены интеграционными тестами.

## Единый Dev-процесс

По указанию пользователя все последующие установленные macOS-проверки выполняются
только в GRAF Dev через harness. AGENTS.md и scoped guidance задают этот маршрут.
Два старых launchers и сборщик native-preview теперь отказывают до сборки/запуска;
10 старых инструкций обновлены, README упаковки отделён от Dev-проверок.
Удалены только две временные app-копии, созданные этой задачей. Данные приложений
и TCC не удалялись. Имя действующего сертификата, внутренний loopback-флаг,
старый decoder канала и исторические release facts сохранены ради совместимости.

## Сверка реализации и упрощение

Converge: проверены 20 FR, пять пользовательских сценариев, семь архитектурных
решений плана и применимые ограничения семи принципов конституции. Первичный вывод об отсутствии новых
расхождений дополнен реальным испытанием: T023 исправляет неверную семантику
паузы микрофона и таймер. Открытые аппаратные/пользовательские
критерии остаются в T007/T018/T020/T021; полная приёмка не заявляется.
Ponytail review: повторно использованы capture actions, CalendarTray, maintenance,
RLS/access helper и текущие CSS/JS. Новых зависимостей/очередей нет; убрана
недостижимая ветвь stale после явной очистки календаря. Защитные проверки сохранены.

## Открытые испытания

GRAF Dev: подтверждённый видимый OS banner, закрытое окно/fullscreen,
второй монитор, запрет Focus, VoiceOver/200%, disk full, upgrade/rollback и p95.
SC-004 требует пяти людей и не может быть подтверждён агентом самостоятельно.
Commit и PR авторизованы пользователем. Merge/deploy/release не выполняются.

## Реальный GRAF Dev — первый проход

На SHA `2cafd6479bc6b514c540d80e38e020f0ddfff6b4` штатные build/promote и
13/13 smoke прошли. Путь и identity прежние, разрешения микрофона/системного
звука сохранены. Вход есть, календарь не требуется для настроек уведомлений.
Настройки Mac: напоминания включены, названия и звук выключены. Разрешение
уведомлений в macOS включено; одна явная тестовая отправка принята API ОС.
Видимый баннер не подтверждён: последующее чтение NotificationCenter не удалось.
Повторные тестовые уведомления не отправлялись.

Start → Pause → Resume → Stop: запись 44 секунды, «Сохранено на Mac», затем
«Отправлено» в списке. Синтетический тон, содержимое не исследовалось и не
сохранялось в evidence. Закрытие главного окна этим проходом не доказано.
Обнаружены и исправлены пауза обоих источников/замороженный таймер в UI и
необновляемый статус разрешения после возвращения из системных настроек.
Повтор этих сценариев на новом SHA — следующий обязательный шаг T023.

CI первого SHA выявил два устаревших тестовых контракта: FK inventory новой
истории и migration head. Исправления проверены целевым набором; прохождение
GitHub governance-fast на следующем точном SHA пока не заявляется.

## Повторный GRAF Dev на 84d485c66

Точный SHA `84d485c66de4959ae18712be3bf6530b79f402c2`: build/promote и smoke
13/13 PASS; GitHub governance-fast PASS:
https://github.com/yshishenya/graf/actions/runs/34041584279.

В реальной записи: Start → пауза микрофона → закрыть главное окно кнопкой
закрытия → виджет → продолжить микрофон → Stop. Таймер на паузе 0:04 → 0:09 →
0:22; системный источник отдельно «Поступают аудиоданные». Виджет сохранился
после закрытия окна, Stop завершил запись одним нажатием; 48 секунд,
«Сохранено на Mac» и «Отправлено». Содержимое записи не исследовалось.
Открытие нативных настроек после обновления показало «Уведомления macOS
разрешены»; звук и названия по-прежнему выключены. Повторного OS уведомления нет.

T023 подтверждён автоматическими тестами, независимым review и реальным Dev.
Новый T024: на 290 px снимок показал обрезание кнопок и статуса источника.
Исправление использует вертикальный существующий контейнер кнопок и перенос
текста. Следующий Dev-проход должен подтвердить исправленную геометрию.
Полноэкранный режим/второй монитор/VoiceOver/Focus/200% ещё не подтверждены.

14 полностью проверенных задач T001–006/T008–009/T011–016 закрыты в GitHub с
подробными комментариями и ссылкой на PR/SHA. T010/T017/T019 оставлены открытыми
до их остающихся критериев; checkbox state приведён к этому состоянию.

## Завершение T024 — 3de3eec50

Код SHA `3de3eec50430f99b4540822518bf3ed5dc7337b3`. После правки вёрстки
40 целевых Swift проверок PASS; validator макета 25 checks и git diff PASS.
Первый запуск целевого набора обнаружил прежнее требование `.lineLimit(1)`
в source-contract тесте: оно заменено проверкой отсутствия обрезания и переноса.
Повторный набор прошёл полностью.

Штатные Dev build/promote и smoke 13/13 PASS. В настоящем виджете шириной
290 px осмотрены запись и пауза: «Пауза микрофона», «Продолжить», «Остановить»
и обе строки источников полностью видны. Главного окна нет; Stop завершил
запись, виджет исчез. 20 секунд: «Сохранено на Mac» → «Отправлено».
Настоящее содержимое не исследовалось. Факт наличия аудиоданных не является
проверкой качества сигнала или внешней транскрибации.

Разрешение уведомлений дополнительно проверено выключением и восстановлением
системного переключателя: GRAF показал «выключены», затем после повторного
открытия раздела «разрешены». Исходное разрешение включено; остальные настройки
не менялись. Автоматическое обновление обратно во включённое состояние без
переоткрытия раздела этим проходом не подтверждено. Новых баннеров не отправляли.

T023 и T024 закрыты по их ограниченным критериям. Общая F249 остаётся неполной:
T007/T010/T017/T018/T019/T020/T021/T022 содержат ещё не пройденную приёмку.
PR остаётся черновиком. Финальная запись результатов меняет только Markdown;
она не требует ещё одной установки того же исполняемого кода GRAF Dev.

## Исправление измерения identity — T025

Финальное сравнение обнаружило, что старый harness хешировал объединённые
stdout/stderr codesign: строка Executable включала путь артефакта, давая ложную
разницу entitlements_digest. Plist двух артефактов фактически совпадает;
сертификат и designated requirement также одинаковы. Installer уже исключал
stderr и не ослаблен этой правкой.

Harness теперь читает только stdout, разбирает plist стандартной библиотекой,
сортирует ключи при сериализации и отклоняет нечитаемый документ. Регрессия
проверяет независимость от пути/порядка ключей и изменение суммы при изменении
разрешения. Наборы dev_harness/graf_local_adapter/dev_runtime_identity:
49 PASS; Ruff и diff PASS.

Read-only измерение старого артефакта 2cafd6479, нового 3de3eec50 и установленного
`/Applications/GRAF Dev.app`: все три identity совпали. Manifest history не
переписана, сертификат и разрешения не менялись; новой установки нет. Последующие
изменения после runtime SHA 3de3eec50 относятся к отчётам и инструменту измерения,
а не к коду приложения/сервера. T025 закрывает дефект metadata workflow.


## Заключительная приёмка: T026–T029

Независимые экспертные разборы (пять разных субагентов) сведены в agent-study.md.
Исправлены будущие резервы напоминаний (атомарный ledger), актуальный URL/expiry,
обновление разрешения при фокусе окна, правдивое время позднего напоминания и
проверка сохранённого звука. При видимом активном виджете локальная проблема
не создаёт второй системный баннер.

Stop теперь публикует переход остановки; виджет остаётся видимым до окончания
очистки даже при failed-сессии. После подтверждённой остановки восемь секунд
показывает фактическую существующую проекцию локального хранения/отправки.

809 Swift tests PASS (44.704 s). Регрессии охватывают будущий/истёкший резерв,
замену ссылки/запрет/expiry, позднее напоминание и индикатор до конца cleanup.
Результат исполняемого кода ещё должен быть проверен установленным GRAF Dev.

Веб: непринятая попытка не скрывает failure; подтверждение shown revision только
после реального перехода; исходный CSRF связывает просмотр с прежней сессией.
Отказ read очищает содержимое сразу, новое открытие всегда ведёт во «Важное».
Показана тихая подпись решения; порядок/дата отражают значимое обновление,
а повтор той же ошибки не меняет время. Новый cursor binding не принимает
старые курсоры с прежним порядком. Обновлённые страницы перечитываются при polling;
изменение порядка между страницами отражается при следующем обновлении с начала.

После проверки rollback-совместимости T026 старый числовой `.attempts` ledger
сохранён. Новые сроки/резервы записываются атомарно в отдельный ключ после
старого deny marker. Прерывание до записи резерва остаётся fail-closed; старый
клиент при откате читает прежний формат и не возвращает повторную доставку.
9 целевых Swift tests PASS; новый тест проверяет читаемость ledger старой версией.
Финальный серверный повтор: 9 tests PASS (16.88 s), изолированный PostgreSQL
удалён штатно. JS read отказов и открытие Important исполнялись, не только
проверялись строковым поиском. Ruff и syntax PASS.

## Следующая проверка готовности, 2026-09-06

- T030: добавлен явный previous-checkout/target-checkout, проверка одинакового
  репозитория, полного SHA, чистоты и байтов Git до исполнения; pycache не
  используется. Сохраняются общий live lock и исходный runtime digest.
- Переход ограничен одинаковой схемой и образами PostgreSQL/MinIO/Temporal.
  Проверяются все 13 smoke-пунктов кандидата и компенсации. Отказ публикации
  активного указателя даёт rollback_required. Исходная рабочая копия хранится,
  пока runtime её использует; второй установленной копии приложения нет.
- 64 governance/adapter/identity/transaction tests PASS; 812 Swift tests PASS,
  41.296с. Scoped Ruff E9/F63/F7/F82 и git diff --check PASS. Новых зависимостей нет.
- T031: пятый отдельный UX-агент устранил два сигнала одной сессии после Stop,
  повтор после скрытия результата и неточную подпись перехода в общий список.
  Совместимость ledger в обе стороны проверена targeted Swift тестами.
- Ponytail review: использован существующий адаптер и прежние скрипты; повтор
  публикации manifest/pointer сведён в общий helper. Миграционный движок,
  отдельный installer и новые приложения не добавлены.

На момент этого коммита живой переход ещё не выполнялся; runtime evidence
фиксируется после build/promote/status/smoke точного SHA.

## Живой переход на eb9a8d517adce49a87f9e701acaafdaeb9adee1a

Build и promote --previous-checkout прежнего 3de3eec50 прошли штатно, 13/13 smoke
PASS. Источник и приложение обновлены без новой установки продукта и без TCC
reset. После первого запуска показано stale system-audio; кнопка штатного
перезапуска восстановила «Готово к записи» без изменения разрешений.
GitHub governance-fast: run 34047215263 PASS на eb9a8d517. Предыдущая попытка
остановилась на старом SHA в описании PR; описание исправлено до повторного запуска.

В GRAF Dev проверена запись 47 секунд с синтетическим тоном: пауза микрофона,
непрерывный системный звук и таймер, закрытие основного окна, видимый виджет
во время полноэкранного Krisp, Stop одним действием, состояние «Останавливаем
запись…», затем «Сохранено на Mac» и «Отправлено». Точное 8-секундное окно
результата не поймано снимком; его нельзя считать визуально принятым.

Настройки macOS: исходно разрешены, desktop/notification center включены;
временный баннер, звук в GRAF выключен. Два тестовых запроса приняты OS;
видимый баннер пока не подтверждён. OS off отражается при фактическом фокусе,
но повторный on иногда оставляет старый текст при уже разрешённом test().
T032 устраняет этот живой дефект проверкой статуса в активном разделе каждые
2с и перед тестом. Targeted Swift 12/12 PASS, 0.020с; живая проверка новой версии
выполняется отдельно. Системное разрешение восстановлено в on.

Веб на реальном Dev8081: вход, открытие тихого пустого колокольчика, переход
в настройки аккаунта, отмена черновика, сохранение и восстановление исходного
переключателя. Старые встречи не заполнили уведомления задним числом.

Повторно открыт установленный Krisp: верхнее меню содержит About Krisp,
Settings, Check for update, Quit и Quit Krisp Completely. В кабинете отдельно
есть Activity и Settings. Команда Settings в этом проходе не дала наблюдаемого
перехода; поведение содержимого её панели не выдумывается.

## Приёмка 57db9ba345a794aeebc5833801ae4e34eb5d9aed

- Exact GitHub governance-fast PASS: run34047732822.
- Build/promote13/13; штатный rollback на3de3eec50 через target-checkout13/13,
  возврат57db9ba34 через previous-checkout13/13. Новая запись47с и исходные
  reminders/on, offset1, titles/off, sound/off читаемы старым и новым клиентом.
  Схема0086 и stateful image digests не менялись. Временный checkout удалён
  только после возврата; никакая вторая копия GRAF не устанавливалась.
- T032 live: OS off виден как «выключены», on самостоятельно обновился до
  «разрешены» в том же открытом разделе. Старый дефект воспроизведён и устранён.
- T028 live: отдельная короткая синтетическая запись с паузой микрофона,
  закрытием главного окна и Stop показала сначала остановку, затем фактический
  результат с сохранённой локальной копией и отправкой. Снимок виджета осмотрен.
- Системная доставка: usernoted для graf.local.test от GRAF Dev сообщает
  Delivering to alert/lockScreen/notificationCenter и Presenting as banner.
  Поля содержимого в журнале отсутствуют; текст реального баннера снимком не получен.
- Веб Dev8081: save/cancel/restore, default important после повторного открытия,
  Escape,360px,200% CSS-масштаб и light/dark проходят. Снимки осмотрены.
  p95 открытия панели33.7мс, max33.9мс,n40; измерение через два кадра rAF
  после UI-команды на стабильном runtime. Нативное время этим не заменяется.
- При попытке VoiceOver Mac заблокировался. Инструмент вернул locked и
  не смог автоматически разблокировать. VoiceOver, полная двухэкранная/Focus/
  fault-матрица и нативныйp95 остаются непроверенными. PR сохраняется draft.

Финальный отчёт/статусы tasks относятся к проверенному продуктовому коду57db9ba34;
последующий коммит меняет только отчёты. Для совпадения всех identity harness
будет собран и проверен также на этом окончательном SHA; результат фиксируется
в PR после выполнения, без переноса старого CI на новый коммит.


## Продолжение после разблокировки Mac

На d36074bfd8c482abf14d138ed29714560f9b5da8 статус Dev active и разрешение
уведомлений on. В системных настройках наблюдался включённый VoiceOver;
в GRAF клавиатура Tab/Space прошла переключатели → Save → Cancel,
отмена вернула reminders/on без сохранения. Полное озвучивание и весь путь
записи этим не подтверждены. Системные окна параллельно переключались,
поэтому запрошена координация исключительного управления интерфейсом.
Расписание Focus не создано, системные разрешения не менялись.

Аппаратная конфигурация: два активных немиррорированных дисплея Color LCD
и MateView. Наличие двух дисплеев не означает прохождение multi-display gate.

Живое AX-дерево обнаружило T033: три действия строки автозаписи имеют
одинаковые имя и value. Исправлены только containment группы и собственные
label кнопок. Независимый ux_pass_5 подтвердил подход; выбранность и disabled
сохранены. AppControlAccessibilityTests, DesktopNotificationControlTests,
MeetingDetectionCountdownTests, MeetingDetectionRecordingLifecycleTests:
40 PASS, 0.142с. Проверка исходника не заменяет последующую живую AX-приёмку.
GitHub issue canon: 300 issues PASS. Предыдущий финальный governance-fast
на d36074bfd8c482abf14d138ed29714560f9b5da8 — run34048862966 PASS;
исправление T033 требует нового exact-SHA gate и canonical build/promote.


## Приёмка T033 и продолжение календарного сценария, 2026-09-07

Установленный GRAF Dev: eb4f5808292dde362b0fa9d89aa65845d06875ee,
manifest dev-eb4f5808292d. Build/promote и отдельный smoke13/13 PASS.
Обязательный governance-fast34051940484 PASS на eb4.
Живая проверка AX: группа 8x8 Work и три собственных имени «Всегда»,
«Спрашивать», «Никогда». Каждая кнопка меняет правило и выбранность;
проверка закончена с исходным «Спрашивать». T033 закрывает этот дефект
доступности, но не заменяет полный VoiceOver и аппаратную матрицу T020.

После ночного перерыва tray показывал «Нужен вход». Переход к настройкам
календаря подтвердил истёкшую сессию и в кабинете; штатный Dev-вход восстановил
нативную авторизацию. Дефект cookie bridge этим не подтверждён.
Первый синтетический календарь находился во внутреннем bootstrap workspace,
а обычная авторизация выбирает личное пространство. Сервер правильно
изолировал события. Временные данные во внутреннем пространстве удалены
проверенным скриптом с контролем владения; исправляется тестовая постановка.

Пробная Animation Hitches trace завершилась и экспортируется, но не содержит
согласованной серии пользовательских действий; результат не считается p95.

В личном пространстве синтетическая встреча появилась в живом меню GRAF Dev.
На eb4 в 09:14:52 местного времени usernoted подтвердил Delivering и Presenting
as banner для graf.local.reminder от pro.2brain.graf.dev. Это настоящее
календарное напоминание, не команда «Проверить уведомление».
Снимок самого баннера пока не получен; настройки названий/звука и Focus
по одной записи журнала не принимаются.

T034: исправление перехода без ссылки; 59 targeted Swift PASS перед последним
уточнением единого now, независимый review выполнен. Повторная проверка
последнего diff и установка канонического Dev следуют перед live acceptance.

Диагностическая безопасность: исходная Instruments trace содержала окружение
процесса и была удалена вместе с TOC; в репозиторий они не добавлялись.
В служебном результате review ошибочно появились секретные значения;
пользователь уведомлён. Публикуются только сведения о проверках без значений.

T034/T035 перед установкой: 59 тестов DesktopNotificationControlTests,
DesktopCalendarReminderTests, AppControlAccessibilityTests PASS; отдельные
45 CaptureControlTests PASS, включая новую регрессию верхнего меню.
Первый filter по имени файла CaptureControlV5Tests не выбирал класс;
корректный повтор по имени CaptureControlTests выполнен и принят.
Исходные календарные тестовые данные удалены с проверкой владения.

## T036/T037 — корректировка панели и места уведомлений (2026-09-07)

- Основание: пользовательский снимок обрезанного popover и отказ от колокольчика рядом с заголовком; повторное UI-наблюдение Krisp: отдельный Activity в боковой навигации. Приватное содержимое референса не сохранено.
- Реализация: bounded popover, закреплённое управление, прокрутка подробностей; вычисление размера и положения отдельного виджета; наблюдение за состоянием записи в подробностях. Ошибки действий и источников остаются видимыми. Веб использует постоянный пункт меню и существующий inbox, без новых каналов доставки.
- Независимые проверки inspect_graf и ux_pass_5 выявили скрытую ошибку Pause/Resume, неподтверждённую формулировку паузы, отсутствие наблюдения details, рост popup после загрузки и сжатие мобильного пункта. Все замечания исправлены; аппаратная приёмка новой сборки остаётся отдельным этапом.
- XCTest: 105 проверок notification/calendar/accessibility/capture прошли. Дополнены проверки границ экрана и направления восстановления после failed session.
- pytest: 4 исполняемые проверки навигации/прочитанности/геометрии и положения элемента в Jinja shell прошли. JavaScript syntax и git diff --check прошли.
- Браузерный синтетический прогон текущих шаблонов/CSS/JS: web + embedded на 360/980/1440px, 30 карточек после загрузки, границы панели, отсутствие перекрытия профиля, внутренняя прокрутка, Escape/focus, сворачивание меню и resize — 6 сценариев PASS, ошибок JavaScript нет. Это проверка реального движка Chrome с синтетическим HTTP, не установленного GRAF Dev и не серверного producer.
- Ponytail Review: переиспользованы sidebar, профильный маршрут адаптации, текущий inbox и нативные capture-команды; новых зависимостей/каналов/очередей нет. Удалены старые координаты bell, отступ шапки, дублированный calendar statusText и пояснения штатных разрешений.
- Локальный governance checker после удаления созданного Python-кеша расширения: PASS; lock и исходники расширения не изменены. GitHub exact-SHA проверка необходима после коммита.

Дополнительный прогон 200% выявил закрытие inbox вслед за автоматической прокруткой меню и необходимость учитывать масштаб координат. Исправлено: прокрутка пересчитывает положение, размеры/отступы учитывают фактический масштаб. 6 дополнительных сценариев web/embedded на 360/980/1440px при CSS zoom 200% прошли в Chrome с синтетическими данными. Это не проверка системного размера текста macOS.

Перед передачей стенда F255 подтверждено новое предпочтение владельца: сохранить HTML sidebar/profile и компактные кнопки автозаписи без заглушки app.dashed, 104×28, radius6. В F249 сохранены эти размеры и удалена заглушка; логика и отдельные доступные названия T033 не меняются. Новый hover-путь меню профиля остаётся ответственностью F255 и этим изменением не затрагивается.


## Живой проход и совмещение с релизом 2026-09-07

Проверенный до совмещения источник: `8a81d5af67cd3933088c5e7c858b54655b35dd54`.
Штатные build/promote/status/smoke единственного GRAF Dev: 13/13 PASS,
схема `0086_notification_inbox`. В живом приложении пройдено: открытие панели,
раскрытие источников, старт, пауза только микрофона, продолжение, закрытие
главного окна и остановка из оставшегося виджета. Синтетическая запись
завершилась за 79 секунд: «Сохранено на Mac» и «Отправлено»; готовность
обработки внешним сервисом этим не подтверждена. Важное/История,
свёрнутое/развёрнутое меню, возврат фокуса по Escape и переход к настройкам
уведомлений проверены во встроенном кабинете. Полная матрица дисплеев,
Focus, VoiceOver и p95 этим проходом не закрыта.

При совмещении с `48faecb41` независимая проверка обнаружила пересечения
с выпущенными настройками и обновлениями. Исправления T036/T022: единый
размер окна настроек с адаптивным вложенным содержимым; рабочий обработчик
обновления в панели; приоритет индикатора записи над обновлением; прямой
переход шестерёнки к локальным настройкам; доступ к защищённой настройке
разрешений при недоступном старте. Предупреждение о микрофоне ограничено
активной записью и её остановкой, исторический manifest не меняется.
Две независимые ветви схемы соединены добавочной миграцией
`0088_merge_notifications`; уже применённые миграции не переписаны.
Это новая версия исходников и требует собственных проверок.

Краткое «Нужна проверка» после Stop не заменено ложным «Сохраняем»:
по коду оно может означать проблему обработки, а «Отправлено» подтверждает
лишь серверный приём. Причина конкретного краткого состояния не доказана;
следующий живой проход должен проверять безопасные коды состояния очереди.


Проверки исправлений совмещения: Swift126 PASS; PostgreSQL65 PASS из66,
единственное падение — формат `revision: str` в миграции. Объявления приведены
к принятому формату без изменения идентификаторов/DDL; повтор именно этой
проверки PASS. Браузер12 сценариев100/200% PASS, JavaScript syntax PASS,
`git diff --check` PASS. Это целевые проверки, не authoritative release-full.
Восстановление при отсутствии разрешений проверено отдельным тестом на
завершённой сессии: причина предупреждения имеет приоритет над историческим
состоянием; ошибки сохранения продолжают вести к записи.


На `3c383346838202cf13cb576c94b7a3847352f8c6` governance-fast
[34089159159](https://github.com/yshishenya/graf/actions/runs/34089159159) PASS,
штатная сборка Dev завершена. Promote отклонён до мутации: требуется одинаковая
схема и образы хранилищ при смене управляющего кода. Проверка status подтвердила
прежний active `51fd80f6f069fb54347042678bec7d2f9847b552`, schema0086.
T038 фиксирует недостающий безопасный путь; новая сборка не выдаётся за установленную.
После дополнительного rebase на master8e18 и коррекции перехода указателя через
зазор меню профиля: Swift94 PASS, включая WebKit и контекст часового пояса.
Инвентаризация macOS обнаружила только встроенный дисплей; второй физический
дисплей не проверен.


Независимое рассмотрение проекта T038 выявило пробелы в границе первого
писателя, долговечном журнале, холодной точке и полном восстановлении пары
томов. Требования уточнены в `dev-schema-transition.md`; checklist остаётся
открытым до повторного принятия. Реализация перехода схемы не начата,
никакие данные Dev не мигрированы и резервные снимки не применялись.

## T038 — автоматическая проверка перехода схемы, 2026-09-07

Реализация в существующем dev-harness: доказанный ancestor, обе ветви merge,
проверка действительных image IDs/томов, долговечные фазы, холодная пара полных
томов, полная замена при восстановлении и запрет старого снимка после допуска
writers. Прерванная операция блокирует обычные команды; recover-schema управляет
прежним проверенным адаптером либо восстанавливает целевой стенд вперёд.

- 64 адресные регрессии harness прошли до последнего уточнения поиска
  контейнеров; итоговый повтор записывается отдельно. 25 из них покрывают новый
  переход, аварийную смерть процесса, пары снимков и недопустимые tar-пути.
- Настоящая изолированная проверка PostgreSQL/Temporal/MinIO: PASS, 30.72 s.
  Исходный image соответствует e2e218affa0544fd29a1b24855691fa7924c966d,
  целевой — 3c383346838202cf13cb576c94b7a3847352f8c6. Проверены обычный Alembic
  upgrade0086→0088 и возврат0086, исходная контрольная строка, отсутствие
  добавленной строки, содержимое и metadata объекта, отсутствие добавленного
  объекта, прежний настоящий namespace Temporal. Применены те же helpers пары
  снимков, что и в harness. Синтетические ресурсы удалены с проверкой владения.
- Первый запуск фикстуры выявил отсутствовавший BIND_ON_IP у её Temporal;
  настройка исправлена по существующему Compose. Это не сбой рабочего Dev.
- Исходные данные, архивы, имена записей и частное содержание в git не добавлялись.

Это ещё не evidence установленного нового GRAF Dev: его переход и окончательная
приёмка интерфейса выполняются после проверки кода и фиксации точного SHA.

### Повторная проверка T038 и интеграции F255

- Независимый read-only review выявил другой фактический том, альтернативный
  каталог журнала и окно запуска shell до записи PID. Исправлены обязательная
  проверка mounts, canonical state-root и pipe-допуск после durable runtime.json.
  Новым методом запуска управляется также замороженный предыдущий адаптер.
- Повторный независимый review подтвердил эти исправления и оставил один
  конкретный случай volume-subpath. Добавлены запрет Subpath, сравнение Source
  с Mountpoint тома и тесты обоих вариантов, а также положительный контроль.
  Рецензент разрешил переход к установленной проверке после этого исправления
  и регрессии; безусловный PASS всего PR этим не объявляется.
- 130 адресных тестов schema/harness/adapter/runtime PASS. Настоящий повтор
  холодной пары PostgreSQL/Temporal/MinIO PASS,27.43s; ресурсы фикстуры удалены.
- Удалена независимо найденная недостижимая повторная ветвь metadata rollback.
- Перенесены3проверенных integration commit F255:434f24e68,8a2b00953,f1a216584.
  Удалён повторный минимум содержимого главного окна; системный минимум
  сохранён. При конфликте декоративного scope-badge сохранён прозрачный фон
  актуальной ветки и спокойная типографика. Swift87 PASS после переноса.
- Для T021 добавлено Dev-only измерение от команды открытия до popoverDidShow;
  журнал содержит только milliseconds, без содержания или идентификаторов встреч.
  Численный результат будет принят только после установленного запуска.

### Реальная компенсация обновления Dev — 2026-09-07

- Кандидат aa031ac95bb247bd17e5d9ddf20e4ec0f5025a51 собран штатно. Promote
  остановился на команде установщика до допуска новых writers. Журнал достиг
  recovered; прежний f1a21658405bd2f681363f0f8d998caa63ca4206 восстановлен
  со схемой 0086_notification_inbox, все 13 smoke-проверок прошли.
- До и после совпали состав встреч, число и digest объектов MinIO, digest
  настроек уведомлений. Содержимое данных в evidence не сохранялось.
- Точная причина отказа ещё устанавливается: harness теперь показывает только
  фиксированные сообщения доверенного установщика; произвольный stderr и
  подставленные значения не выводит. Регрессия проверяет это ограничение.
- GitHub governance-fast 34096172047 отказал на устаревшей обвязке проверки
  mutable image: ей добавлен обязательный state root, защита startup сохранена.
  Это не успешная установленная проверка кандидата и не закрытие T038.

### Переход схемы и проверка длительной работы — 2026-09-07

- b0fd763e870533ad5609e45289fcc4e04a6a9391: штатные build/promote прошли,
  схема 0088_merge_notifications установлена, журнал complete; отдельный smoke
  13/13 PASS. Совпали прежние встречи, объекты MinIO и настройки уведомлений.
  GitHub governance-fast 34097703411 PASS на этом SHA.
- После успешного smoke серверные компоненты остановились; факт первоначального
  запуска не закрывает приёмку длительной работы. Независимо воспроизведён
  дефект startup: неуспешный опрос Docker считался пустым списком служб и
  запускал cleanup. Теперь ошибка повторяется без остановки данных, пустой
  ответ подтверждается повторно; причина завершения и exit code фиксируются.
  Источник конкретной наблюдавшейся остановки ещё уточняется.
- Повторный promote неизменного активного manifest восстанавливает остановленный
  runtime, сохраняет прежнего parent и отвергает изменённый manifest того же ID.
  118 адресных тестов harness/Compose/schema/adapter PASS, включая ошибку
  наблюдения Docker, временный пустой ответ и однократную остановку по SIGTERM.
  Новая установленная проверка остаётся обязательной.
- Независимый ponytail-review нашёл только два остатка: недостижимое stale
  календаря и повторное присваивание requests. Удалены9строк, Swift87 PASS.

## Финальная проверка нативного отклика и Dev-стенда, 2026-09-07

- Случайный календарный фикстурный объект текущего прохода удалён штатным
  loopback-скриптом с проверкой владельца. Повторный `status` вернул
  `{"result":"absent"}`; данные других встреч Dev не менялись.
- На коммите `6fefaeb5b68adcd4fd30ca0d5f447b9114891398` изменена только анимация
  `NSPopover`: `popover.animates = false`. Журнал предыдущей сборки показывал
  13 открытий за 523–825 мс; календарное обновление уже запускалось после
  показа. Поэтому устранён измеряемый блокирующий переход без изменения
  размеров, положения, действий записи или сетевого пути.
- Для этого SHA выполнены штатные `build --live`, `promote --live`,
  `status --json` и `smoke --json --live`. Manifest `dev-6fefaeb5b68a` активен,
  все 13 health-проверок и smoke-пунктов — `pass`; bundle ID, подпись,
  designated requirement, entitlements и разрешения сохранены.
- На предшествующей установленной сборке `6fefaeb5b68adcd4fd30ca0d5f447b9114891398`
  реальное открытие панели дало значения `134.666500, 7.646792, 6.011500,
  9.403750, 4.386125, 2.099583, 2.906042, 3.084833` мс (n=8).
  Nearest-rank p95 = `134.666500` мс, максимум = `134.666500` мс. На
  финальной exact-SHA сборке `14ff8188a032c78b14d9ea01515f547cef2ea8f8`
  контрольное открытие дало `73.598750` мс (n=1); оба результата проходят
  SC-006 (≤200 мс). Значения получены из категории `panel-performance`,
  журнал содержит только время.
- После серии открытий панель визуально и в AX-дереве целиком помещается в
  рабочую область: доступны «Начать запись», «Нужна проверка», источники,
  «Открыть GRAF» и «Настройки календаря». Активной записи после проверки нет.
- `system_profiler SPDisplaysDataType` в этом сеансе показывает только один
  встроенный дисплей (`Main Display: Yes`, `Mirror: Off`, `Online: Yes`).
  Проверка второго физического экрана не выполнена и не подменяется этим
  фактом. Focus, disk-full/no-speech и снимок видимого баннера также не
  заявляются как PASS; ранее подтверждённые OS delivery записи остаются
  журнальным evidence без текста уведомления.
- Целевые проверки после правки: `swift test --filter
  DesktopNotificationControlTests` — 15/15 PASS; `validate_prototype.py` —
  25 правил, 95 уникальных id; `check_spec_kit_governance.py` — PASS;
  `git diff --check` — PASS. Полный CI и публичный release gate ещё не
  запускались; PR остаётся draft до authoritative `governance-fast` на
  финальном SHA.

## Замечания общего релизного PR #6790

T082 / F249 T040: переход к источнику проверяет точную пару редакция/сегмент показанной расшифровки; ошибочные ответы, отказы поставщика/публикации и повтор после сбоя финализации создают summary_failed через существующий обработчик. Напоминания подавляются только для явно связанной календарной встречи, остальные планируются тихо во время записи. HTML-формы обычного и встроенного кабинета показывают понятное объяснение при 409/422.

Проверки: совместный Swift-набор — 115 PASS. PostgreSQL: test_meeting_protocol_generation.py, test_notification_inbox_routes.py, test_meeting_protocol_rendering.py — 44 PASS. Первый запуск этого набора: 41 PASS и один отказ в новом тесте из-за неверного ожидания 422 для конфликта версии; после корректировки входных данных и проверки обоих путей весь набор повторён успешно. Ruff, governance, changelog-fragments и diff-check — PASS. Новый GitHub governance-fast и установленный GRAF Dev привязываются к следующему коммиту; прошлое подтверждение d2c8 не переносится на новый SHA.

## Исправление Swift 6.0.3 при подготовке релиза

Полный CI `34183886878` на `0e7ebb7153bf6f1f00961f64f0f49d0ac4310f1f` выявил три ошибки компиляции: два переноса `UNNotificationSettings` через границу главного актора и инициализацию экрана без `@MainActor`. Обе проверки разрешения теперь используют callback macOS и передают только `UNAuthorizationStatus`; экран явно принадлежит главному актору. Поведение разрешений, доставки, дизайна и уведомлений не меняется.

`swift test --package-path apps/macos --parallel --num-workers 1 --filter 'DesktopNotificationControl|DesktopCalendarReminder'`: 48 PASS. Это локальная проверка; Swift 6.0.3 подтверждается отдельным `macos-diagnostic`, а выпуск — новым полным CI после слияния. Старый кандидат не разрешён к публикации.
