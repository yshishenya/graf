# Анализ согласованности 249

2026-09-06. Проверены spec/plan/tasks/data-model/contracts/constitution. До исправления: HIGH 3 (источники N17/N19, очистка отозванного доступа, ownership Mac preferences), MEDIUM 3 (терминология, остатки read-all, связь тестового локального баннера с server read). Исправления включены в producer-contract.md, experience §12 и действующие задачи; независимая оценка — review-evidence.md.

## Покрытие

| Требование | Задачи |
|---|---|
| FR001–003 | T005–T007, T020 |
| FR004 | T007, T009, T017 |
| FR005–006 | T003, T010–T011 |
| FR007–009 | T003–T004, T011–T014 |
| FR010–012 | T010, T012, T015–T017 |
| FR013–014 | T005–T007, T013, T019–T020 |
| FR015–018 | T002, T004, T008, T011, T018–T022 |
| FR019–020 | T010, T014, T017, T020 |
| SC001–003, SC005 | T020 |
| SC006 | T020–T021 |
| SC004 | T021; пользовательское исследование, не заменяется автоматическими тестами |

20 FR, 5 технически проверяемых SC, 22 задачи, покрытие 25/25. SC004 отдельно требует участия людей. Несвязанных задач нет. Дублирование требований не создаёт отдельного поведения. После уточнений открытых critical/high противоречий в нормативных документах не найдено; конституция 214/видимый Stop/независимая реализация сохранены. Старые отчёты макетов — исторические, не runtime acceptance.

Reviewer-owned проверка и issue mapping завершены; реализация проверяется по tasks и implementation-evidence.md. Публичные референсные PNG с идентификаторами исключены; исходные снимки сохранены вне git. Выбран high-risk lane; этот анализ не доказывает работоспособность продукта.

## Уточнение по реальному Dev

T023 закрыл найденное противоречие US1/AC4: пауза только микрофона,
самостоятельное состояние системного источника и непрерывное время.
Независимый review PASS. T024 покрывает FR013/017, SC006: обрезанные кнопки
и статус на 290 px. Это исправление существующего требования, новый
пользовательский контракт и изменение capture не вводятся.

## T030, проверка уточнения 2026-09-06

HIGH partial: T022 не мог обновить единственный GRAF Dev при изменении самого
harness. T030 добавляет проверенный previous/target checkout в существующую
транзакцию. Независимый inspect_graf проверил план: общий live lock, проверка
байтов против Git, без pycache, одинаковые schema/stateful images, полный smoke
кандидата и компенсации, rollback_required при отказе публикации указателя.
Это ограниченный переход управляющего кода; миграционный движок и новая
установка приложения не добавляются. Требования не меняют capture/privacy;
реализация и live evidence закрываются отдельно, reviewer markers не изменены.


## T033, живая проверка доступности 2026-09-06

HIGH contradicts FR-013/017: три кнопки AutomaticRecordingRulePicker наследуют
имя приложения и текущее значение, скрывая собственное действие в AX-дереве.
Независимый ux_pass_5 подтвердил минимальное решение: явная группа .contain,
собственная label кнопок, сохранение value группы, selected и disabled.
Это исправление существующего требования; правила и таймеры автозаписи не меняются.
Source regression проверяет семантические модификаторы; живая проверка после
canonical build/promote обязательна и не заменяется чтением исходника.

## T034 — напоминание без ссылки

Convergence 2026-09-07: событие без ссылки допустимо, но openResponse завершался
без действия. Добавлена отдельная проверка текущего события и переход в
существующий CalendarTrayController; owner/expiry/policy guards сохранены.
Независимый inspect_graf подтвердил root cause и diff. Его замечание о двух
моментах Date() устранено: решение использует один now. Новых зависимостей нет.
Issue #6779, FR-012/019, high-risk UX; живая проверка остаётся обязательной.

T035: живое начало синтетической встречи отключало Start верхнего меню при
обоих granted. Независимый review подтвердил ошибочное использование
правила скрытия дубликата главного окна. Snapshot теперь проверяет возможность
ручной записи; разрешения и переходы неизменны. Issue #6780, FR-002/003/019.

## T036/T037 — аудит по снимкам пользователя

T036 HIGH: ограничена только календарная часть maxHeight280; высота всего
popover не управляется. widget ограничивает координаты до завершения раскладки,
не размер. Independent ux_pass_5 подтвердил причины; верхний блок должен
оставаться неподвижным, переменные сведения — прокручиваться.
T037 HIGH: notification-entry находится вне app-shell и имеет fixed top38/12
на разных порогах. Independent inspect_graf подтвердил разрыв привязки и
жизненный цикл замен main. Перенос в постоянный sidebar сохраняет root при
замене main и не требует нового общего компонента заголовка.

Krisp исследован только через доступный интерфейс: пункт Activity левого меню,
Updates, Only show unread, Mark all as read; настройки Notifications отдельно.
Копирование закрытого кода или ресурсов не выполнялось. Содержимое встреч
не переносится в evidence. Пользовательские снимки — источник визуальных
замечаний; чтение AX не объявляется полным визуальным сравнением Krisp.

## T038 — анализ согласованности перед реализацией, 2026-09-07

FR-021 → T038/#6786 → dev-schema-transition.md. Сверены границы хранения,
журнал фаз, полный merge-граф, независимый checklist CHK001–CHK005 и проверки
прерывания/настоящего восстановления. CRITICAL 0, HIGH 0: новая задача покрывает
обнаруженный отказ единственного Dev, не расширяет production-процедуру.
Уточнение границы writers перед любым обычным startup имеет приоритет над
кратким описанием допуска API. before_analyze/before_implement hooks отключены.
VoiceOver исключён владельцем; остальные требования доступности сохранены.
