# Plan

1. Apply pure CSS updates to `cabinet.css` (bento cards, glow effects, transitions).
2. Apply pure Swift updates to macOS native shell (`DesktopMeetingShellView.swift` and `CaptureControlViewCore.swift`) for color and corner matching.
3. Ensure no external fonts are requested in HTML.
