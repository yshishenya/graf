# Приёмка исходной F225 в общем PR с F259

Дата: 2026-09-09. Lane: Active Spec Kit slice; продукт и production не меняются.
Сверены исходные body/comments всех #6189–#6196 и сохранённые документы F225.
Документы исходной рабочей копии перенесены без её изменения; исторический
checklist сохранён без изменения отметок. Он не выдан за новый независимый review.

| Задача / issue | Текущая проверка |
| --- | --- |
| T001 / #6190 | Граница служебных namespace записана в research/spec; F259 review и независимый numbering checklist сохраняют обычные F1000+ и все коллизии. Исторические числа помечены датой. |
| T002 / #6191 | F225.yaml восстановлен и дополнен обязательным known_limitations; продуктовый changelog не редактируется. |
| T003 / #6192 | Общий `_ids_from_refs` отбрасывает служебные namespace и internal refs; реальные локальные/удалённые ветки остаются занятыми. |
| T004 / #6193 | `--self-test` содержит искусственные служебные refs, timestamp и обычную F225; временная копия с удалённым namespace-фильтром падает с AssertionError. |
| T005 / #6194 | Целевые тесты проверяют spec/ref/GitHub collision, strict без umbrella, offline draft, неполный ответ/таймаут и конкуренцию. |
| T006 / #6195 | Quickstart запрещает угадывать номер, описывает старый checkout, online/strict/offline и повторную проверку при резервировании. |
| T007 / #6196 | Локальная проверка ниже; обязательный GitHub governance-fast на окончательном SHA и живое включение PR в master остаются gates перед закрытием. |
| T008 / #6189 | Четыре исходных критерия сопоставлены T003–T007; umbrella получает реальный task ID перед закрытием после детей. |

`python -m pytest tests/governance/test_feature_allocator.py tests/governance/test_validator_safety.py -q`: **108 passed, 2 skipped**, 9.42 s; оба пропуска — PowerShell без pwsh. `python3 scripts/claim-feature.py --self-test`: PASS. Отрицательная проверка удаления namespace-фильтра: PASS (изменённая временная копия ожидаемо падает).

Число 225/226 из старого snapshot не является ожиданием текущего репозитория.
Действующая F259 policy исключает историческую F6788 только из начала поиска;
занятость сохраняется. Точное текущее предложение проверяется read-only, не
резервирует новый номер и не меняет существующие ветки.

Авторский analyze/converge: исходные FR-001–FR-007 покрыты существующим общим
allocator, тестами и документацией; новых требований к продукту нет. Независимый
review реализации не заявляется; принятые требования общей US3/F259 сохранены.
Доказательства GitHub и факт merge фиксируются в PR/closure comments на точном SHA.
Full CI, запуск приложения, публикация и deployment не выполняются.
