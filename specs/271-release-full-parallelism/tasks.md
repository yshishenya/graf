# Задачи 271: Полное использование параллелизма в релизном прогоне

Требования: FR-001 — FR-005, SC-001 — SC-004 из `spec.md`.

## Фаза 1. Замеры и подготовка

- [X] T001 Записать базовые длительности релизных прогонов до и после F270
      (прогоны 35153978174 и 35183409199). Требования: FR-004.
- [X] T002 Записать длительности фаз набора из отчётов CI обоих прогонов и
      распределение времени по случаям. Требования: FR-004, SC-004.

## Фаза 2. Согласованность артефактов (до реализации)

- [X] T014 Прогнать `$speckit-analyze` по `spec.md`, `plan.md` и `tasks.md` и
      записать замечания. Выполнено до завершения реализации: найдены
      несоответствие критерия SC-003 (сравнение числа случаев и длительностей
      не может сойтись при добавленной проверке и смене числа потоков),
      невозможное сравнение в сценарии 6 `quickstart.md` и порядок ворот
      анализа. Замечания устранены. Требования: SC-003, SC-004.
- [X] T015 Сверить задачи с внешним трекером и не создавать дублей: работа
      связана с зонтичной задачей #7127 и #7152, отдельные задачи не
      создавались. Требования: SC-004.

## Фаза 3. Реализация (FR-001)

- [X] T003 Поднять `GRAF_TEST_WORKERS` в задаче `server-full` до 8.
      Требования: FR-001, FR-003.
- [X] T004 Добавить контрактную проверку: значение релизной задачи в
      допущенном диапазоне и не вырождается в один поток. Требования: FR-003.

## Фаза 4. Локальная проверка (FR-002)

- [X] T005 Запустить контрактные и управляющие проверки локально.
      Требования: FR-002.
- [X] T006 Проверить, что состав проверок не изменился, а статические
      команды по-прежнему выполняются один раз. Требования: FR-002.

## Фаза 5. Проверка результата (FR-004, FR-005)

- [X] T007 Прогнать `release-full` на новом дереве и записать длительности
      задач. Прогон 35189322951 на `7cba8b257`: компонент Ubuntu 17 мин 26 с,
      весь релиз 17 мин 54 с. Требования: FR-004, SC-001, SC-002.
- [X] T008 Сравнить опознавательные признаки случаев и итог с прошлым
      прогоном: снятых случаев нет, добавленные относятся к новой
      контрактной проверке, отказов нет. Сравнивать число случаев и
      суммарную длительность нельзя. Результат: снято 0, добавлен 1 случай в
      `test_ci_cd_contract.py`, отказов 0; строгая фаза 204 и фаза
      производительности 3 без изменений. Требования: FR-002, SC-003.
- [X] T009 Если релиз идёт медленнее, вернуть значение 4. Требования: FR-005.
      Выполнено: 26 мин 21 с → 17 мин 26 с, откат не нужен.
- [X] T010 Записать результат в `docs/current-product-status.md` и во фрагмент
      журнала. Требования: SC-004.

## Фаза 6. Закрытие

- [X] T011 Прогнать `$speckit-converge` и убедиться, что незакрытых работ по
      спеке не осталось. Результат: сведено, незакрытых работ нет. Проверены
      FR-001—FR-005 и SC-001—SC-004 против дерева, решений плана и конституции;
      расхождений не найдено, поэтому раздел сведения в `tasks.md` не
      добавлялся. Требования: SC-004.
- [X] T012 Получить обязательные GitHub-проверки `governance-fast`,
      `macos-pr`, `pr-metadata` на точном SHA пул-реквеста. Подтверждено на
      каждом пул-реквесте этой доработки: #7189, #7190, #7192, #7193, #7194,
      #7195, #7197, #7198, #7200, #7201, #7203 — все три проверки пройдены на
      точном SHA. Проверка `pr-metadata` дополнительно проверена в обе стороны:
      она отклонила устаревший SHA, запись `Feature ID: scoped` там, где нужен
      конкретный номер, и приняла исправленное описание. Требования: SC-001.

## Фаза 7. Разбиение набора и ускорение церемонии (продолжение F271)

Замер узкого места: компонент Ubuntu длится 25 минут 18 секунд, и всё это время
занимает фаза `parallel` — 1179 секунд при 8 потоках. Статические проверки и
сбор списка случаев добавляют ещё около 4,5 минуты последовательно. Остальные
фазы укладываются в 53 и 24 секунды.

- [X] T016 Вынести статические проверки Ubuntu в отдельную задачу релизного
      прогона, чтобы они шли параллельно набору тестов. Требования: FR-001,
      FR-005.
- [X] T017 Разбить фазу `parallel` на четыре части, выполняемые отдельными
      задачами на разных раннерах, и доказать полноту покрытия в агрегирующей
      задаче: объединение частей обязано точно совпадать с полным списком
      случаев, пропуск или повтор случая обязан проваливать проверку.
      Требования: FR-001, FR-005, SC-002.
- [X] T018 Читать записанный итог поезда релиза из поля `decision`, а не только
      из `status`: из-за этого подтверждённый поезд считался непроверенным, и
      подтверждение, решение и выкатка заново сверяли одни и те же квитанции
      пул-реквестов. Требования: FR-004.
- [X] T019 Повторять проверку квитанций пул-реквестов при временном сетевом
      сбое вместо остановки релиза и ручного повтора. Требования: FR-004.
- [X] T020 Заменить двенадцать ручных команд выпуска одной командой
      `scripts/release.sh`, которая выводит все аргументы из репозитория и
      останавливается только на двух решениях оператора: вливание пул-реквеста
      подготовки релиза и выкатка в прод. Требования: FR-004, SC-004.
- [X] T021 Замерить длительности шагов выкатки в прод: каждый шаг печатает
      `step_duration_seconds=<имя>=<секунды>`, добавлен итог `deploy_total`.
      Повторную проверку готовности после дымового теста проверяющий попытался
      убрать как дубль, но доказал обратное и сохранил: она смотрит на
      контейнеры уже после настоящей работы дымового теста, и контрактная
      проверка `test_remote_deploy_rechecks_temporal_and_processing_worker_before_success`
      этого требует. Проверка восстановления из резервной копии вынесена из
      релиза в недельный прогон, резервная копия в релизе сохранена.
      Требования: FR-004.
- [X] T022 Перекрыть выпуск обновления macOS-приложения с проверками сервера.
      Замер показал, что ожидание нотаризации Apple занимает около минуты, а
      критический путь — локальная сборка двух архитектур (9 минут 41 секунда),
      поэтому фазы приложения сделаны независимыми от серверного прогона:
      подготовка требует только чистый коммит, публикация — только тег.
      Выпуск приложения, который был полностью ручным, сведён к одной команде
      `release-app-update.sh`. Требования: FR-001.
- [X] T024 Упростить машину состояний подготовки образов и заменить общие
      сообщения о сбоях выпуска на конкретные. Проверок стало больше (39 → 41,
      отказов 58 → 71), ни одна защита не снята. Требования: FR-004.
- [X] T023 Записать сплошной замер всего цикла. Выпуск v2026.09.18.8:
      **1520 секунд (25 минут 20 секунд)** при необычно большом поезде из 14
      пул-реквестов. Разбивка: подготовка 20, вливание 119, синхронизация 4,
      поезд **710**, полная проверка **404**, решение 13, выкатка **231**,
      публикация 10. Ожидание подписи на публикации — **0 секунд**: подпись
      прошла параллельно выкатке. Поезд в этом прогоне вбирал накопившееся от
      провалившихся попыток; при обычном поезде (190 секунд вместо 710) тот же
      цикл даёт **около 1000 секунд — 16 минут 40 секунд**. Требования: SC-003,
      SC-004.

- [X] T030 Прикладывать входные файлы обновления приложения до подписи и
      создавать каталог для них. Настоящий прогон вскрыл два отказа подряд:
      подписывающая часть читает образец приложения и заметки из самого выпуска,
      а выпуск их не прикладывал; затем копирование входных файлов падало с
      «файл не найден», потому что каталог назначения никто не создавал.
      Требования: SC-003.

- [X] T031 Переживать временный сетевой сбой и не пересобирать готовое
      приложение. Живой прогон вскрыл два отказа: скачивание доказательств
      упало на `TLS handshake timeout` уже после того, как все проверки прошли,
      и повторный заход зря пересобрал приложение, хотя сборка для нужного
      источника была готова, — нотаризация отвергла вторую попытку для той же
      версии. Теперь загрузка повторяется с растущей паузой, а готовая сборка
      переиспользуется. Требования: SC-003.

- [X] T032 Убрать подпись и загрузку обновления приложения с критического пути.
      Сплошной замер дал 21 минуту 14 секунд: подпись и загрузка (3 минуты
      54 секунды) шли после выкатки. Теперь черновик выпуска создаётся сразу
      после подготовки, а подпись и загрузка идут в фоне параллельно поезду,
      полной проверке и выкатке. Тег по-прежнему создаётся только после всех
      проверок: черновик тега не создаёт, поэтому неудачный выпуск не оставляет
      лишний тег. Требования: SC-003, SC-004.

- [X] T033 Объединить сборку и подпись приложения в одну фоновую работу. Живой
      прогон вскрыл отказ: оболочка не может ждать процесс, запущенный другой
      оболочкой, — фоновая доводка не смогла дождаться сборки и выпуск
      остановился. Теперь одна фоновая работа делает всё: сборку, прикладывание
      входных файлов и подпись; основной поток ждёт только её. Требования:
      SC-003, SC-004.

- [X] T034 Создавать тег вместе с черновиком и убирать его при провале. Живой
      прогон вскрыл ограничение целостности: подписывающая часть требует, чтобы
      тег уже указывал на собираемый коммит, поэтому подпись не могла идти
      параллельно. Теперь тег создаётся вместе с черновиком сразу после
      подготовки, а провалившийся выпуск убирает его сам — лишний тег не
      остаётся. Требования: SC-003, SC-004.

- [X] T035 Объединять разделы журнала после провалившегося выпуска. Каждый
      провал после подготовки оставляет неопубликованный раздел и архивную
      запись. Два провала подряд дали две записи про одну доработку, и
      подготовка остановилась: «conflicting unpublished changelog entries for
      Feature 271; consolidate them explicitly». Записи объединены в одну.
      Требования: SC-003.

- [X] T036 Разделить сборку и подпись приложения по шагу решения. Живой прогон
      вскрыл противоречие: проверка поезда требует, чтобы тега ещё не было, а
      подписывающая часть — чтобы он был. Теперь сборка идёт с самого начала, а
      тег и подпись создаются после решения и идут параллельно выкатке в прод.
      Требования: SC-003, SC-004.

- [X] T037 Сводить фрагменты и записи доработки, пережившей провал выпуска.
      Провалившийся выпуск оставлял неопубликованный раздел журнала и архивный
      фрагмент; новая работа по той же доработке добавляла второй фрагмент, и
      подготовка следующего выпуска останавливалась. За один день это
      потребовало ручной расчистки четыре раза подряд. Теперь подготовка сводит
      фрагменты в один файл, объединяет задачи и ограничения, соединяет описания
      без потери текста и печатает явный отчёт. Проверка на испорченный архив
      сохранена и стала строже: одинаковое имя файла при разных номерах
      доработки по-прежнему останавливает подготовку до изменений. Требования:
      SC-003, SC-004, SC-005.








## Зависимости (продолжение)

- T017 зависит от T016: без выноса статических проверок части набора всё ещё
  делят задачу с ними.
- T018 и T019 независимы и могут выполняться первыми.
- T020 зависит от T018 и T019: одна команда выпуска должна опираться на
  исправленную церемонию.
- T021, T022 и T023 идут после T020.

## Зависимости

- T014 идёт до реализации: без него высокорисковое изменение релизного пути
  не имеет ворот согласованности.
- T003 независима от T001 и T002 и может выполняться первой после T014.
- T004 зависит от T003.
- T005 и T006 зависят от T003 и T004.
- T007 зависит от T003—T006 и от вливания изменения в master: workflow
  `release-full` требует точного совпадения запрошенного SHA с вершиной master.
- T008 зависит от T007.
- T009 зависит от T007 и T008.
- T010 зависит от T007—T009.
- T011 и T012 зависят от T010.
