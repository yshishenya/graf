# Проверка изменений F261 перед PR

Проверено агентом 2026-09-09 после синхронизации с master.

- FR-001–FR-003: все действия интерфейса используют один `requestExport`, который
  передаёт `include_evidence: false`; флажок и его обработчики удалены.
- FR-004–FR-006: серверные формирователи, хранение, генерация, контроль доступа
  и переходы к источникам не изменены. Обычный и встроенный HTML проверены.
- Изменение рабочего кода: одно значение параметра и удаление трёх строк.
  Новых зависимостей, обходных путей и дублирующих формирователей нет.
- Quickstart: 35 passed; Ruff, синтаксис JavaScript, проверка пробелов passed.
- Существенных замечаний к исправлению нет. Набор тестов подтверждает запрос
  и серверное формирование документа; ручная проверка установленного приложения
  не заявляется.

Это проверка реализации, не изменение состояния reviewer-owned checklists.
GitHub `governance-fast` на SHA PR остаётся обязательной проверкой перед merge.
Слияние, полный CI кандидата, развёртывание и проверка production не выполнены.
