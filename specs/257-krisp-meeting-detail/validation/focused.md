# F257 — первоначальные проверки перед коммитом

Дата: 2026-09-08. Lane: high-risk-product / reference-fidelity. Проверены незакоммиченные изменения F257 поверх `e81b412dff920cc29ed37de60be13dd8fc4d1c43`; этот SHA не содержит реализацию. Синтетические данные, без production и сторонних ресурсов Krisp.

## Исполняемые проверки

| Набор | Результат |
|---|---|
| `test_meeting_detail_reference_contract.py`, `test_meeting_detail_copy_runtime.py`, `test_cabinet_web_shell.py`, `test_cabinet_static_assets_contract.py` | 179 passed, 2.37 s на финальном продуктовом diff |
| Изолированная PostgreSQL: `test_summary_template_ui_contract.py`, `test_cabinet_playback_contract.py`, `test_transcript_export_contract.py`, `test_meeting_sharing_contract.py`, `test_meeting_protocol_rendering.py`, `test_cabinet_meeting_detail.py` | 92 passed, 180.99 s; временный контейнер удалён |
| Изолированная PostgreSQL: `test_speaker_names.py` | 8 passed, 41.50 s; установка, reload, сброс подписи, ограничения доступа и аудит; контейнер удалён |
| Ruff для изменённых Python-файлов и новых тестов | PASS |
| `node --check` для `cabinet.js`; `git diff --check` | PASS |
| Полный `check_spec_kit_governance.py` в отдельной копии F257 с настоящим CLI/commit и расширениями из lock | PASS: bootstrap integrity + GRAF invariants; без обхода doctor и изменения глобального CLI |

Итого 279 разных тестов в перечисленных наборах. Общие предупреждения pytest о rewrite импортированного fixture и Starlette/httpx не менялись. После последнего изменения обработки источников повторены затронутые unit/contract/runtime проверки и сценарий HTMX; PostgreSQL не повторялась, поскольку серверные операции не менялись.

Новые проверки фиксируют границу `#cabinet-main`, семантику вкладок/формата, положение возврата в расшифровке, существующие owner/shared скачивания без JavaScript и ограничения текущей проекции. Shared-читатель сохраняет разрешённый сервером опубликованный артефакт; owner replacement скрывает прежний состав по существующему контракту.

Runtime запускает настоящий код копирования и обновления доступности: захваченный состав, CSRF, идентификаторы редакций, no-store, единый pending, отсутствие записи после отсоединения/запрета/replacement, ошибка clipboard, потеря прав, повторный клик и смена вкладки. Отдельно проверен возврат после входа из списка и замены main, без новых глобальных подписок.

## Браузер

Playwright CLI, Chromium, синтетическая FastAPI-поверхность с реальными шаблонами/CSS/JS. Название и подписи сохранялись в памяти тестового процесса, проверялись перезагрузкой и восстанавливались. Долговечность и права подтверждаются отдельными PostgreSQL-тестами выше. Положительные ответы мутаций экспорта/генерации не подменялись; их успешный пользовательский цикл остаётся для точного SHA в T009.

Проверены 16 сочетаний: web/embedded × dark/light × 390/768/1024/1440. Видимые новые действия внутри viewport, горизонтального переполнения страницы и main нет. Проверены picker/стрелки/Escape, справка/список без совместного открытия, каталог/закрытие/фокус, вкладки, источник/возврат, сохранение/отмена названия, сохранение/reload/восстановление подписи, no-JS. Подробные измерения и ограничения — [ui-matrix.md](ui-matrix.md).

## Открытые условия на момент первоначальной проверки

- `infra/scripts/ci-local.sh --fast` выполнен, но остановлен до тестов: `status=ambiguous`, `reason=dirty_worktree`, exit 2. Это не PASS и не результат тестирования исходного SHA. Повтор — после разрешённого коммита в чистой рабочей копии.
- Текущий `check_spec_kit_governance.py` остановлен из-за несогласованного состояния инструментов: установлены Spec Kit 1.0.4 и issue-canon 0.3.3, lock закрепляет 1.0.1 и 0.3.2; doctor сообщает дрейф трёх skills, extension/workflow state и версии CLI. Изменения общих инструментов уже присутствовали при возобновлении реализации и сохранены; они не входят в F257. Причина изолирована: отдельная рабочая копия с теми же файлами F257, расширениями из исходного HEAD и настоящим CLI1.0.1 из закреплённого Git commit прошла полный doctor. Глобальный CLI и исходные изменения инструментов сохранены. Этот PASS относится к согласованному набору F257, а не к дрейфующей исходной рабочей копии.
- GRAF Dev не обновлялся. `dev-harness status --json` показывает активный `dev-e81b412dff92`, не F257. Проверки подписи/стенда/точного SHA после коммита — T009.
- Нативный масштаб браузера 200%, VoiceOver, реальная WKWebView, current-SHA governance-fast и Full CI не выполнены. CSS zoom=2 — только предварительная проверка переразмещения.


## Последующая проверка

Этот отчёт сохраняет результаты этапа до первого коммита; актуальное состояние установок, GitHub и приёмки — в [closeout.md](closeout.md) и [ui-matrix.md](ui-matrix.md). Набор279 выполнялся несколькими запусками: после последнего изменения возврата источника PostgreSQL-набор тогда не повторялся. На финальной приёмке повтор выявил одно устаревшее assertion прежнего прямого обработчика вкладок; оно исправлено без изменения обработчика ради теста. Результат повторного PostgreSQL:48 PASS/1FAIL, затем адресные contract/runtime:15 PASS. Поэтому первоначальные279 не следует представлять как единый новый прогон окончательного SHA.

Повтор на продуктовом HEAD `8cf943ec450bcaa6d2b843143e56ff878b088d2a`: существующий `run_local_postgres_tests.sh --focused` для `test_meeting_detail_reference_contract.py` и `test_summary_template_ui_contract.py` — **49 PASS**,52.69с (полная фаза57с),2 прежних предупреждения, временный контейнер удалён. Это окончательный повтор набора, ранее давшего48 PASS/1FAIL; журнал `/tmp/f257-current-focused.log`. Он не заменяет GitHub governance-fast финального документационного SHA.


## Исправление T011 перед выпуском — 2026-09-09

Состояние замены и видимость расшифровки обновляются до события доступности
экспорта, включая страницу без блока восстановления. Регрессия проверяет
переход processing→failed_terminal и значения, видимые синхронному слушателю.
Проверки static assets и копирования:77PASS; окончательная адресная проверка
storage_failure_recovery PASS, Ruff и git diff --check PASS.
Новый GitHub exact-SHA gate, совместная приёмка и release-full ещё не пройдены;
T011 остаётся открытым до согласования evidence. Refs #6836.
