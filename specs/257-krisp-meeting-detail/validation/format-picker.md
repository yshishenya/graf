# F257: проверка компактного выбора формата

Дата: 2026-09-09. Lane: active Spec Kit slice / high-risk reference-fidelity. Объём: дополнение FR-020–024, US2. Проверки относятся к рабочему дереву; новый commit SHA и установленная приёмка пока отсутствуют.

## Независимый review и повторная проверка

Отдельный агент требований прочитал три продуктовых файла и существующие вызывающие пути picker: open/close/All/Back, ввод/фокус, загрузку личных форматов, current/busy, смену вкладки/replacement, ARIA, ширину/прокрутку. Проверены неизменные обработчики генерации и серверный маршрут списка личных форматов: он возвращает только активные шаблоны текущего владельца и рабочей области. Reviewer не запускал повторно browser или PostgreSQL.

Первый проход выявил P2: общая подсказка назначения находилась после длинного списка внутри общего скролла и исчезала из видимой области. Основной агент изменил панель на flex-column, сохранил служебные элементы без сжатия, а список сделал сжимаемым с собственной прокруткой. Повторное чтение подтверждает устранение причины: назначение не входит в прокручиваемый список. Геометрическую проверку при всех ширинах выполняет отдельный браузерный проход; статический review её не заменяет.

Ponytail: удалён дублирующий шестистрочный обработчик Escape в listbox; единый обработчик controls получает всплывающее событие и возвращает фокус trigger. После исправлений новых блокеров корректности и замечаний по переусложнению в прочитанном объёме не найдено. Проверка focusout отложена на следующий цикл события и использует итоговый document.activeElement: фокус в панели/trigger сохраняет её, выход закрывает без перехвата цели.

## Локальные проверки: сообщённые результаты

Результаты ниже переданы основным агентом после его запусков; reviewer проверил соответствующие изменения исходников, но не повторял команды.

| Проверка | Результат | Граница доказательства |
|---|---|---|
| Первоначальный focused набор182 | 178 PASS / 3 FAIL / 1 SKIP | Три FAIL — прежние строковые assertions структуры; browser пропущен. Полный набор182 не объявляется PASS |
| Повтор трёх исправленных assertions с PostgreSQL | 3 PASS | Обновлены именно устаревшие ожидания исходников после замены модалки; не повтор всего182 |
| Базовые проверки | 15 PASS | Отдельный сообщённый набор |
| Текущий scoped без браузера | 16 PASS / 1 SKIP | Browser пока пропущен |
| Chromium / WebKit | PASS / PASS | Полный сценарий нового меню, реальные шаблон/CSS/JS, синтетические сетевые ответы; не установленный GRAF Dev |
| git diff --check / changelog validator | PASS в подготовке | Не заменяют runtime или exact-SHA gate |

Нет суммы, объединяющей разные запуски в один искусственный PASS. Приватные данные, снимки встреч и локальные секреты не сохраняются.

## Сопоставление текущей реализации с требованиями

| Требование | Наблюдаемая реализация | Что ещё подтвердить |
|---|---|---|
| FR-020 | ≤4 быстрых option, собственный значок/название/галочка; повторяемые описания убраны | Browser и GRAF Dev |
| FR-021 | Один немодальный popover, All/Back, дополнительные встроенные и личные строки; существующая ссылка управления | Browser и GRAF Dev |
| FR-022 | Одна область назначения focus/hover вне скролла строк; прежние submit/current/busy/error, отсутствие генерации в навигационных обработчиках | Browser проверяет видимость и число запросов |
| FR-023 | Видимые option, стрелки/Home/End, естественные Enter/Space кнопок, Tab, единый Escape, закрытие при tab/replacement/busy и внешнем фокусе | Browser и GRAF Dev |
| FR-024 | Ограничения viewport, перенос названий, listbox scroll, coarse targets и focus-visible | Полная browser матрица и GRAF Dev |

Предварительное converge в пределах дополнения: недостающей продуктовой реализации по FR-020–024 после устранения P2 не найдено; новые исполняемые задачи не требуются. Окончательная сходимость всей приёмки не заявляется: существующие T014/T015 уже покрывают незавершённые проверки. tasks.md при этом проходе не менялся, пустая фаза Convergence не добавлялась. Hooks before/after_converge не настроены.

## Открытые гейты

- T012–T014 выполнены локально; tracker closeout ожидает PR/merge и подтверждённый gate.
- T015 остаётся открытой: одобрение коммита, штатный build/promote единственного GRAF Dev на точном SHA, установленная приёмка, current-SHA governance-fast и tracker closeout.
- Глобальный issue-canon validate ранее FAIL на посторонней F6790 #6852; scoped текущих четырёх issues4/4 PASS. Чужая задача не исправлялась.
- Локальный governance ранее FAIL из-за несовпадения установленного specify/extension с frozen lock; исправление/повтор ещё не подтверждены.
- Release-full, выпуск и production не выполнялись. Исторические PASS F257 не подтверждают новое меню.

## Итог локальной проверки

После последней поправки pointerdown отдельный исполнитель проверок запустил Chromium (1,99с) и WebKit (3,14с): оба PASS. Основной агент просмотрел синтетические снимки быстрого/полного меню и узкой страницы Chromium, полного меню WebKit. Матрица:390/768/1024/1440, light/dark, CSS zoom200%,24 личных формата, прокрутка списка при доступных служебных действиях, safe textContent, ошибка списка и повтор, late response без кражи фокуса, quick/all/back, текущий пятый формат, Escape/outside/keyboard exit,0 POST при текущем и1 при личном выборе. WebKit использует нативный macOS Option+Tab для прохода всех кнопок; обычный Tab сохраняет системное поведение. Это проверка CSS-масштаба, а не ручного системного zoom или VoiceOver.

Исполняемый тест выявил и защитил исправления: задержка проверки focusout до завершения нативного перехода; явный pointerdown focus в WebKit, где клик не фокусирует кнопки; деление доступной высоты на фактический CSS scale. Обработчики генерации не заменялись. По итоговой проверке новых обязательных пробелов реализации FR-020–024 не найдено; остающаяся приёмка уже представлена T015, новая дублирующая фаза converge не добавлялась.

Дополнительно основной агент:94 PASS (static assets/GRAF UX/accessibility/copy/source suites), Ruff/node --check/diff/changelog PASS. Отдельный исполнитель:104 PASS контрактов,1 условный browser skip с последующими отдельными запусками Chromium и WebKit. Эти пересекающиеся наборы не складываются. T012–T014 отмечены выполненными локально; #6854–6857 не закрыты: нет нового PR/merge/current-SHA governance или установленной приёмки.

Воспроизведение из apps/server:

```sh
GRAF_NODE_MODULES=<installed-node-modules> uv run pytest tests/contract/test_meeting_detail_reference_contract.py -k summary_picker_keyboard -q
GRAF_NODE_MODULES=<installed-node-modules> GRAF_BROWSER=webkit uv run pytest tests/contract/test_meeting_detail_reference_contract.py -k summary_picker_keyboard -q
```

Рабочее дерево не закоммичено. SHA-256 проверенных продуктовых файлов:

- `apps/server/src/twobrain_rec_server/cabinet/static/cabinet/cabinet.css`: `6311919a2dbe8298985ec0471f8550375abed95a78036d60a5e7c4ecd62c6873`
- `apps/server/src/twobrain_rec_server/cabinet/static/cabinet/cabinet.js`: `6287ecf69c0bf893a30a524c6d387179793e32d2a8c57908055dd070b3e785d4`
- `apps/server/src/twobrain_rec_server/cabinet/templates/cabinet/pages/meeting_detail_content.html`: `dda6c5114acca12e398a295749bd27b79aedb6b62a4fb0ae990616753f6667b0`

## Подготовка PR

Пользователь 2026-09-09 поручил довести изменение до готового к релизу PR: коммит, push/PR и штатная установленная проверка разрешены. Локальный specify 1.0.1 установлен из закреплённого ref в отдельное окружение. Несовпадение дерева github-issue-canon вызвал только сгенерированный Python `__pycache__`; кеш удалён, исходники расширения и lock не менялись. Последующие проверки расширения выполняются с `PYTHONDONTWRITEBYTECODE=1`.
