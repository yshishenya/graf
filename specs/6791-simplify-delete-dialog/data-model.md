# Data model

Изменений сущностей, данных, схемы, retention и состояний нет. MeetingReviewResponse.governance.delete.state по-прежнему определяет доступность. Форма передаёт прежние _csrf и confirmation_boundary; MeetingDeletionReport создаётся существующим запросом удаления.
