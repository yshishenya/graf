# Data Model

No migration. Reuse seeded Organization, Workspace, UserIdentity, RegisteredDevice,
WorkspaceMembership and one verified email ExternalIdentity. EmailLoginState and
AuthSession are created by the existing flow. Local volumes are disposable state.
