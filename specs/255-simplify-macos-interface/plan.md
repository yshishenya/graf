# F255: действующий план после проверки владельцем

## Решение владельца от 2026-09-08: проверка macOS 14

Владелец сообщил: «У меня нет макос 14. Давай без него если невозможно так протестировать. Доводи до готовности к мерджу и релизу».
Для F255 отсутствие ручной проверки на macOS 14.5 принимается как явное ограничение выпуска, а не блокирующий пункт и не PASS. Минимальная поддерживаемая версия не повышается; сохраняются deployment target и проверки совместимости API при сборке. Матрица на доступной macOS 26.5, доступность, совместимость и остальные проверки остаются обязательными. Это решение не отменяет Full CI, подписывание, нотариализацию или контроль точного SHA.


## Решение владельца от 2026-09-07 — действующий приоритет

Владелец отверг замену левой панели системным списком и замену меню профиля: вернуть исходные навигацию, состав, порядок и подменю по нажатию на пользователя из `c6bbcf376`. Это явно отменяет прежнее требование удаления десяти disabled-команд и передачи управления в native. Такие команды возвращаются в исходном недоступном состоянии; подключение новых действий не входит в откат. Не превращать основное окно в системные настройки.

T019 выполняет точечный возврат, T020 — повторное исследование реального Krisp и согласованной геометрии всех областей. Компактная автозапись T018 сохраняется. Liquid Glass и матовый вариант остаются предметом проектирования; новая композиция не считается реализованной или принятой. Старые требования native bridge и его runtime-проверки ниже являются историей отвергнутого варианта, а не основанием повторно его внедрять. Reviewer-owned отметки прежнего варианта не означают приёмку нового решения.

## Область и проверки

Active Spec Kit slice; high-risk UX/reference fidelity. Сохраняются constitution/product-gates, независимое управление записью/Stop, текущие формы/CSRF/история и семантика доступа. Миграций и новых зависимостей нет.

1. Вернуть `DesktopCabinetWorkspaceView.swift`, sidebar/profile `sections.html`, CSS и JS к прежнему владельцу HTML. Удалить `EmbeddedCabinetShellBridge.swift`, его регистрацию и отдельные тесты. Сохранить исправление detach lifecycle и semantic background в `EmbeddedCabinetWebView.swift`.
2. Восстановить прежние серверные контракты меню и навигации. Проверить все потребители удалённого bridge. Сохранить компактные controls автозаписи, доступность и доказанную чистку недостижимой native queue.
3. Исследовать установленный Krisp по открытому UI и официальные Apple HIG: sidebar, windows, materials, menus. Не извлекать исходники/ресурсы конкурента. Привязать наблюдения к безопасным свежим снимкам; указать непроверенное.
4. Профильные Swift/server проверки; согласованный чистый интеграционный SHA с F249. Штатные build → promote dry-run → promote → status → smoke для одной `/Applications/GRAF Dev.app` с текущей схемой0086. Проверить возврат меню в самом приложении.
5. Точный SHA PR + governance-fast. Выпуск и полный CI отдельно, когда готовы матрица приёмки и frozen candidate. PR остаётся черновиком, незакрытые задачи не выдаются за готовность.

## Геометрия для следующего решения

Сначала сохранить привычную структуру и пути. Предпочтительное направление — один общий внутренний контейнер для навигации и рабочей области с согласованным оформлением правой панели. Внешняя рамка и системные кнопки окна принадлежат macOS. Радиус, отступы и материал проверяются на всей композиции, а не на одной sidebar. Это направление исследования, не новая самовольная переделка.

## Сохранённая правка T018

Без одинаковых `app.dashed`; высота переключателей автозаписи28 pt, радиус6 pt. Сохраняются три правила, выбранное состояние, клавиатура, сохранение и исправления F249. Никакого нового поиска приложений/иконок и дополнительных пользовательских действий.

## T023: общая матовая композиция, 2026-09-07

Продолжение FR-003/FR-006 и T020 по запросу владельца: один внешний отступ8 pt и непрерывный радиус12 pt вокруг всей HStack (WebView с левой навигацией + правая панель), без отдельных карточек-панелей. Системный titlebar и отдельный RecordingTitlebarAccessory остаются снаружи декоративного clip. Цвет правой панели — существующая непрозрачная системная controlBackground; её карточки и доступность не перестраиваются.

В CSS заменить яркую декоративную границу sidebar на line-soft, убрать градиент upcoming, использовать surface и мягкий контур карточек обзора настроек. Служебная подпись области настроек получает спокойный фон и обычный шрифт вместо рамки/моноширинного начертания. Input borders, focus-visible, status/error и контрастный режим остаются отчётливыми. Меню, маршруты, hit targets и формы не меняются. Новые настройки, зависимости, JS bridge и миграции не нужны.

Проверки: существующие Swift boundary/accessibility/WK submenu tests и server shell/settings contracts; production fixture в светлой/тёмной теме, 200%, компактном окне и контрастном режиме; установленный совместимый GRAF Dev с полной композицией и раскрытой/свёрнутой правой панелью. Скриншоты содержимого остаются вне git. T023 не закрывает всю T020/T016 и не доказывает системный Liquid Glass. Владелец задачи — существующий #6755.

## T024: спокойная поверхность экрана встречи, 2026-09-07

После проверки цельного экрана встречи продолжить ту же матовую систему: убрать тяжёлую тень закреплённого плеера и использовать `--line-soft` для декоративных границ закреплённого заголовка, вкладок и панели формата. Предупреждения, фокус, активная вкладка, цвета ошибок и независимое управление записью не изменять. Проверка должна включать синтетический экран в обеих темах и сохранённые снимки; это адресное упрощение поверхности, а не изменение сценария встречи.

## T025: тема без ухода со страницы, 2026-09-07

В меню профиля выбор темы остаётся быстрым действием: JavaScript отправляет существующую CSRF-защищённую форму через `fetch`, поэтому текущий экран и положение пользователя не заменяются страницей аккаунта. Сервер принимает необязательный локальный `return_to` только для обычного HTML/no-JS пути и использует прежний `/settings/account` fallback. При сетевой или серверной ошибке локальный preview откатывается к последней сохранённой теме, элементы выбора снова доступны, а меню не закрывается. Новых API, хранения или зависимостей нет.

Проверки: серверные unit/web-shell контракты, `node --check`, существующий `CabinetSidebarRuntimeTests` с подменой `fetch`; установленный совместный GRAF Dev и полная матрица доступности остаются отдельными гейтами F249/F255.

Дополнение T010 от 2026-09-08: удалить из `scripts/validate-dev-runtime.py` проверку номера F229 и её owned_paths, уже принадлежащую `validate-agent-context.py`. Сохранить существующие ограничения Compose и evidence; заменить пропускаемый тест в `tests/governance/test_dev_runtime.py` исполняемыми регрессиями для отсутствующего/другого контекста и нарушенных ограничений. Без новых зависимостей и изменения запуска Dev; действующий high-risk-product lane F255 сохраняется.

Ручной VoiceOver пропускается по дополнительному решению владельца от 2026-09-08; это не PASS. T016 продолжает владеть остальными native-проверками. T017 проверяет включённые через #6762 изменения; открытость исходных PR сама по себе не блокирует F255.

## T026: целочисленная длительность короткой записи

Причина подтверждена в установленном Dev: исходники и контрольные суммы сохранны, а ceil-декларация сравнивается с миллисекундами по допуску 250 мс. В существующем `_validate_authoritative_source_duration` учесть совпадение ceil фактических миллисекунд с целочисленной декларацией только для пакета `initial_mixed_recording`, через явный флаг `rounded_up`; прежний строгий вызов по умолчанию остаётся. Отдельный тест обрезанного MP3 обязан продолжать отклонять расхождение. Остальные проверки и manual-upload policy не меняются. Все вызовы общего helper просмотрены. Регрессия в существующем media matrix: 4382/4437/4001 мс → declared 5 допустимы; 4000/5251 мс → 5 остаются mismatch; прежние длительные и ручные случаи сохраняются. Реальная проверка после обновления Dev обязательна. Новых зависимостей/миграций нет.

## T027: единая тема кабинета и нативной оболочки

Фактическое сохранение light показало светлый кабинет внутри тёмного нативного окна. Для FR-005 существующий Coordinator WebView принимает только light/dark/system от текущего разрешённого главного документа и устанавливает NSApplication.appearance. WKUserScript наблюдает существующий data-theme, поэтому preview, rollback, загрузка сохранённого предпочтения и system используют один источник. Никакого нового хранения/пункта меню, навигационного bridge или сетевого запроса. Неверное значение, iframe, чужой origin и отсоединённый документ не меняют оформление. Проверить реальный WKWebView и установленную правую панель/настройки.

## T028: закрытие меню строки macOS и акцент

В установленном Dev меню осталось после клика в другом окне; кнопка использует системный синий. NSPopover.transient по документации AppKit не гарантирует закрытие для всех внешних взаимодействий. На время показа использовать штатные local/global NSEvent mouseDown monitors; внутреннее окно и кнопка status item исключены, события не поглощаются, при popoverDidClose оба monitor удаляются. Клавиатурное transient-поведение остаётся. Использовать существующий фиолетовый акцент без новой палитры/зависимости. Белый текст на #8c73ff имеет контраст 3,49:1: у выбранных кнопок автозаписи вернуть чёрный (6,01:1), основную кнопку меню проверить отдельно.

Дополнение владельца: верхнее меню также обязано учитывать светлую тему. Установленный fd8353 показал тёмный popover при светлом главном окне. Popover получает NSApplication.appearance при открытии и текущем обновлении; nil сохраняет системный режим. Иконка NSStatusItem остаётся template image: контраст относительно самой строки меню выбирает macOS, независимо от светлой темы GRAF.

## T029: компактное меню GRAF в строке macOS

Фирменный вход использует уже лицензированную собственную AppIcon.icns из ресурса приложения, без нового изображения/генерации. Цветной знак с собственной тёмной подложкой сохраняет контраст на светлой и тёмной строке; размер изображения равен высоте строки минус 2 pt. При обновлении знак не заменяется стрелкой: небольшой системный маркер и доступная подпись сохраняют уведомление.

Содержимое: всегда «Открыть GRAF» и «Настройки календаря»; раздел «Ближайшие встречи» с периодом и ссылками только при наличии реальных событий. Успешный пустой результат — только компактный ряд действий, без заголовка, дат, пустой прокрутки и объяснений. По уточнению владельца загрузка, ошибка, требование входа и кнопка повтора не выводятся в этом вспомогательном меню. Фоновый запрос не блокирует действия; после неуспешного запроса список убирается, чтобы не выдавать устаревшие сведения за актуальные и не сохранять события после отказа авторизации. Следующий успешный запрос возвращает список. Состояние подключения, вход и восстановление остаются в существующих настройках календаря. Ручная кнопка обновления удаляется: данные обновляются при открытии и фоновым таймером. Обновление приложения — существующая AppUpdateNotice с достаточной высотой. Все клики снаружи/Escape закрывают меню; закрытие не блокирует фоновую синхронизацию. Запись по календарю не запускается; новые режимы, второй набор правил записи и настройки этого меню не добавляются.

## T030: собственный template-знак и состояние записи

В CalendarTray.swift построить NSImage нативными векторными путями в сетке 22 pt: прозрачное кольцо с выступами и тремя полосами, без уменьшения цветной AppIcon. isTemplate=true передаёт цвет macOS. Постоянная ширина 22 pt во всех состояниях: круг/пауза в верхнем углу с прозрачным зазором, без анимации/таймера; обновление приложения использует отдельный знак стрелки и не затирает запись. Переиспользовать существующие captureSession.state/localRecordingActive/recordingStopInProgress из ContentView, передать вычисленное состояние через существующий AppLifecycleDelegate к CalendarTrayController, без опроса и вмешательства в capture pipeline. Сохранить последнее состояние в delegate до старта tray. Короткая строка в меню выводится только при записи/паузе/завершении. Проверить переходы, отказ запуска, деградацию, Stop и сочетание с обновлением; короткий тест записи ранее разрешён владельцем.

## T031: системное меню быстрого старта

Active Spec Kit slice, high-risk-product. Переиспользовать CalendarTrayModel, existing NotificationCenter callbacks и startManualRecording/stopManualRecording. Заменить CalendarTrayView/NSPopover на NSMenu без custom views: удалить ручные размеры/мониторы внешнего клика и контейнер SwiftUI. NSMenuDelegate строит содержимое при открытии; фоновый календарь не переставляет строки под указателем. Выбранная ссылка повторно разрешается из текущей проекции. Смена capture закрывает tracking; Start/Stop проверяют состояние повторно. AppLifecycle открывает существующее окно перед командой, чтобы permission/error feedback оставался видимым даже после сворачивания. Состояние starting берётся из существующего recordingStartInProgress. Новый опрос/ресурсы/зависимости не нужны.

Перекрывает геометрию и popover реализацию T028/T029, сохраняя их пользовательские результаты. T030: одинаковая активная отметка для recording/paused, поскольку system audio продолжает записываться. Удаление popover также исключает отдельное фокусируемое окно; ранее не наблюдавшееся меню во время записи остаётся регрессией для установленного Dev, причина не считается доказанной.

До кода: уточнения пользователя отражены в spec; проверены требования сохранности, ручного Start/Stop, privacy projection и отсутствия новых действий по фоновым событиям. Противоречие «пауза = запись остановлена» устранено. Анализ T031: CRITICAL 0, HIGH 0; старые reviewer-owned отметки не редактируются и не считаются приёмкой нового runtime. Issue #6755 — действующий владелец T030/T031.

## T032: корректировка меню по пяти пунктам владельца

Та же active high-risk-product slice. CalendarTrayController остаётся единственным владельцем NSMenu: локальный NSEvent monitor закрывает меню при mouseDown в обычном окне ниже системного menu level, глобальный — при клике в другом приложении; didResignActive также отменяет tracking. Window level — публичный AppKit API; геометрию и внутренние окна NSMenu не считывать через private API. menuDidClose удаляет оба monitor. App-menu entry ставит открытие в следующую очередь main, чтобы не вкладывать один tracking loop в другой. Нативный выбор/размер сохраняются.

Переиспользовать openSettings для общих настроек и NSApp.terminate для выхода. Notification календарного меню удалить вместе с единственными потребителями. Удалить Combine/@Published/ObservableObject и CalendarTrayState/lastUpdatedAt, поскольку native NSMenu читает модель при открытии и UI не наблюдает эти значения; тесты должны проверять полезную проекцию и восстановление, не мёртвые фазы. Start/Stop/settings/quit callbacks обязательны, пустые defaults и convenience не нужны. Проверки: outside window levels и lifetime monitors, NSMenu settings/quit callbacks, bitmap outline/filled без боковой отметки, прежние capture/HTTPS/privacy/state checks; затем официальный Dev, реальные внешний клик в своём/чужом окне, настройки, безопасный выход и повторный запуск.

Уточнение дано самим владельцем; новый scope не меняет разрешения, данные, область capture или quit pipeline. Проверка требований T032: CRITICAL 0/HIGH 0; пользовательский вопрос уточняет только место воспроизведения внешнего клика и не блокирует остальные изменения. Reviewer-owned отметки не изменяются. Владелец задач T030–T032 — #6755.

## T033: встроенный индикатор и Mute

Активный high-risk-product slice; подтверждение владельца сохраняет mic-only privacy pause. Переиспользовать pauseManualRecording/resumeManualRecording и shared SystemAudioStatusLabels; никаких изменений writer/mixer. Обязательные отдельные callbacks Mute/Unmute проверяют ожидаемое состояние; доставляются через существующее главное окно/NotificationCenter с повторной проверкой в capture. Native template-логотип плюс неинтерактивный прозрачный NSView для красной точки, CABasicAnimation вместо таймера; NSWorkspace accessibility notification отключает движение. Проверить графику, hit-testing, анимацию/Reduce Motion, переходы и устаревшие команды; затем короткий цикл Start → Mute → Unmute → Stop в штатном Dev.

Анализ T033: подтверждённая семантика не меняет формат, захват, хранение и privacy boundary. FR-005/006/009/012 сохранены; reviewer checklist не меняется. Новая задача принадлежит открытому Issue #6755.

## T034: устранить задержку главной очереди

Установленный 9d4e6248a выявил зависание Quit. sample подтвердил CalendarTrayController.showMenu → DispatchQueue.main callback → NSMenu tracking → NSApplication.terminate/terminateLater; cleanup Task и timeout той же очереди не исполняются. Заменить только планирование showMenu на NSObject.perform(afterDelay: 0), вызываемый циклом событий после завершения исходного меню. Все потребители showMenu найдены; прямой statusItem.menu сохраняется. Проверить native Quit, внешнее закрытие, переходы и Reduce Motion. Это root-cause correction текущего active high-risk-product slice, без изменения termination/writer.
