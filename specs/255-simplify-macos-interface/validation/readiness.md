# F255: допуск по качеству требований

Дата: 2026-09-06. Рецензент: отдельный агент `requirements_review`; автор реализации не выставлял эти отметки.
Проверяемая документация: `c6bbcf3765ff7a23221ced2aca64dd809dc420f8` (`c6bbcf376`).
Основание: владелец после предложения отдельного рецензента поручил «коммит и начинай реализацию»; отдельному рецензенту передана оценка требований до изменения продуктового кода.

## Результат

**PASS: требования готовы к началу реализации по предусмотренной последовательности.** Блокирующих пробелов, противоречий или неразрешённых вопросов к владельцу в проверенной области не найдено.

| Reviewer-owned список | Оценено | Удовлетворено | Открыто |
|---|---:|---:|---:|
| [ux.md](../checklists/ux.md) | 11 | 11 | 0 |
| [security.md](../checklists/security.md) | 8 | 8 | 0 |

По каждому пункту добавлено конкретное основание. Встроенный `requirements.md` не изменялся. Предыдущий [planning.md](planning.md) описывает состояние до этого review; его исторические открытые 19 пунктов разрешены настоящей оценкой.

## Основания оценки

Прочитаны spec, plan, contracts/interface, journeys, inventory, research, data-model, quickstart, tasks и planning. Анализ связывает все 16 FR и 6 SC с задачами; незакрытых CRITICAL/HIGH в нём нет. Отдельно сверены действующее меню `sections.html` (3 + 4 + 3 заглушки и рабочие формы), main-frame/route guards `EmbeddedCabinetWebView.swift` и различие недостижимой compact queue от действующего local mode в `DesktopMeetingShellView.swift`.

Применены правила корневого AGENTS, guidance index, spec-kit-flow/product-gates и reviewer semantics `speckit-checklist`. Проверены применимые ограничения constitution: видимое native capture/Stop, сохранность действующего сценария, отсутствие новых секретов и соблюдение полного процесса.

Упрощение измеримо: прямой переход из настроек к общим встречам 2 → 1, удаление 10 нерабочих вариантов/2 пустых подменю и отсутствие новой настройки материала. Остальные работающие пути имеют предел не хуже baseline. Контракт не размывает границы между системным стеклом, matte и HTML совместимостью; runtime проверка остаётся обязательной.

## Границы допуска

Это review полноты, ясности, согласованности и проверяемости требований, **не приёмка реализации** и не разрешение выпуска. Продуктовый код рецензент не менял, приложение не запускал, runtime тесты не выдаёт за выполненные.

- T002 должен подтвердить реальную системную sidebar с текущим titlebar и стабильным WebView до расширения интеграции. При неудаче план пересматривается по фактам.
- T003/T004/T007 и профильные проверки остаются обязательными; передача управления должна доказать отказ недоверенных/устаревших сообщений и восстановление HTML.
- T011/T012 должны повторно проверить фактические объединённые ревизии F245–F253. Состояния открытых PR в inventory — исходная точка, не доказательство совместной приёмки.
- macOS 14.5, macOS 26, темы/доступность, 200%, VoiceOver, запись и mixed versions ещё требуют фактического evidence. Нет среды или результата — строка приёмки остаётся открытой.
- T013/T014, exact-SHA CI, разрешение на коммит реализации после проверки и release/notarization gates сохраняются.

Изменение протокола доверия, пользовательских путей или иных оценённых требований требует повторной оценки затронутых пунктов. Уточнение внутренних деталей в рамках контракта не подменяет требования и не освобождает от тестов.

## Exact-SHA runtime readiness 2026-09-07

`GRAF Dev` manifest `dev-46d693849973` на SHA `46d6938499730af3712e31e52abd227268cdf19c` успешно прошёл build/promote/status/smoke в свежем Dev namespace с подтверждённой migration head `0087_merge_calendar_timezone`; 13/13 live checks PASS. Исправлен проброс `GRAF_DEV_EXPECTED_MIGRATION_HEAD` в `docker-compose.dev.yml`, регрессия — `22 passed`. Это подтверждает готовность среды для следующего визуального прохода, но не заменяет его.
