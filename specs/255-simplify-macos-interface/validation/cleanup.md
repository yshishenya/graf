# F255: адресная чистка

Legacy Impact: **remove** в перечисленной области. Новых зависимостей, legacy aliases, опроса и постоянного хранилища оформления нет.

| Удаление | Круг потребителей и доказательство | Проверка |
|---|---|---|
| 10 постоянных disabled-команд и 2 пустых подменю | Единственный шаблон `components/sections.html`; действия не имели рабочей реализации. Аккаунт, тема, настройки, выход, quit сохранены | `test_cabinet_web_shell.py`, `test_settings_ui_contract.py`; в браузерном меню 0 disabled на пяти ширинах |
| `.sidebar-profile-menu__item--disabled` и условия `:not(...)` у quit | Поиск по всему server src/tests: после удаления заглушек остаются лишь CSS и старые тестовые ожидания. Ожидания заменены проверкой отсутствия | 146 профильных server tests |
| Ранний selected background, повторный nav gap, перекрытые свойства `.list-card.cabinet-card` | Найдены все совпадающие селекторы в cabinet.css; окончательные правила оставлены, gap=4px сохранён. Не удалены shared .sidebar/.nav/.brand | Браузер light/dark, web/embedded, 320–1440; отсутствие переполнения. Admin отдельной строкой ещё открыт |
| Приватный `localQueueCompactPanel` и его условие | Его единственный вызов получал `rowsNeedingNativeVisibility`, всегда возвращающий []. После удаления вызовов panel нет. Публичная policy и её контрактные тесты оставлены | Swift boundary/queue/upload-link suites; рабочий `allRowsForLocalMode` сохранён |
| Фиксированные тёмные заливки оболочки/WebView | Все потребители цветов находятся в native shell/WebView; заменены семантическими NSColor. Декоративная граница HTML использует line-soft, контраст контролов не ослаблен | Swift build/runtime и визуальная матовая диагностика; обе ОС и измеренный контраст ещё открыты |

`.card`, локальный режим, маршруты, формы CSRF, no-JS, функции записи, отправки и хранения не удалены. Исторические публичные hex-константы с живыми контрактными тестами не объявлены мёртвыми только из-за перехода текущей отрисовки на NSColor.

Ponytail review: общий повтор проверки текущего документа свёрнут в `currentWebView`; материал предоставляет стандартный split view. Новых UI-библиотек, ручного blur/glass-движка или второго HTTP-клиента в native нет. Обработка формы использует существующий серверный POST; число запросов не возрастает относительно POST/303.
