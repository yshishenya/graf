# План реализации: выход платных сервисов в боевой режим

**Фича**: 272-billing-production-launch
**Ветка**: `codex/272-billing-release-readiness`
**Полоса риска**: high-risk-product (деньги, вход, развёртывание)
**Дата плана**: 2026-09-18

## 1. Что уже есть и что мешает

Исходная проверка 18 сентября обнаружила включённую оплату в тестовом магазине
и препятствия ниже. Это историческая отправная точка, а не доказательство
готовности текущей боевой среды.

При чтении 20 сентября настройки файла и работающих служб уже указывали на
`production` / магазин `1430118`, но оформление оплаты было выключено.
Наблюдение за провайдером включено в файле настроек и его рендере Compose,
а в работающих `rec-api`, `rec-processing-worker` и `rec-maintenance` выключено.
Причина расхождения не установлена. Одного изменения файла недостаточно:
нужно одобренное развёртывание проверенного SHA с проверкой настроек всех трёх
работающих служб. Подробный снимок и незакрытые условия — в `quickstart.md`.

Четыре препятствия из исходной проверки, которые устраняет фича:

1. **Потерянный ответ провайдера = деньги без доступа.** `provider_id`
   записывается только вторым коммитом (`cabinet/web_routes/billing.py`, после
   POST к провайдеру). Если ответ не дошёл, платёж у провайдера создан, а у нас
   связи с операцией нет. Вебхук и выдача доступа ищут операцию строго по
   `provider_id`, поэтому платёж «не находится», деньги списаны, доступ не выдан,
   а операция блокирует новую оплату.
2. **Бессрочная блокировка.** Операция без `provider_id` остаётся в списке
   блокирующих навсегда: 30-минутный проход обслуживания переводит её в
   `unknown`, чем дополнительно убирает кнопку «Продолжить платёж».
3. **Резкое отключение при продлении.** Списание делается одной попыткой в
   последний день; отказ карты сразу отзывает автопродление и переводит на
   бесплатный тариф без предупреждения.
4. **Нельзя заплатить заранее.** Активная подписка запрещает новую оплату, а
   выдача доступа считает срок от даты платежа, поэтому остаток периода сгорел бы.

## 2. Подход

Каждое препятствие закрывается наименьшей правкой в существующем механизме; новых
сущностей и фоновых служб не вводится.

### 2.1 Связь платежа с операцией (FR-010, FR-012, SC-003)

Метаданные платежа уже содержат наш `operation_id`, но разбор его выбрасывает.

- `billing/provider_events.py`: `ProviderEvent` получает поле `operation_id`,
  разбираемое из `object.metadata`; испорченное значение не считается ошибкой —
  это подсказка, а не признак подлинности.
- `billing/reconciliation.py`: `PaymentObservation` получает `operation_id`
  через `extract_payment_observation`.
- `billing/webhook_reconciliation.py`: новый помощник `_locked_operation` сначала
  ищет операцию по `provider_id`, а при промахе — по `operation_id` среди
  операций без сохранённого платежа, и связывает их. Выдача доступа остаётся
  однократной: она по-прежнему идёт через `grant_confirmed_payment` по счёту.

### 2.2 Гарантированный выход из зависшего состояния (FR-011, FR-013, FR-027, SC-002)

- `billing/maintenance.py`: после истечения срока ключа идемпотентности
  операции `initial_checkout` без `provider_id` отменяются вместе со счётом.
  Это локальное завершение ожидания, а не утверждение об отмене у провайдера:
  поздний подтверждённый платёж остаётся доступен сверке по метаданным.
- Если `provider_id` известен, истечение окна даёт `observation_expired`:
  блокировка новой оплаты снимается, но связь с платежом и счёт сохраняются.
  Поздний вебхук или явное обновление статуса могут завершить сверку;
  общий фоновый проход не опрашивает истёкшую операцию бессрочно.
- Операции продления не блокируют новое оформление; неопределённый результат
  предыдущего продления при этом не разрешает планировщику второе списание
  за тот же период с новым ключом.
- Тот же файл: 30-минутный перевод в `unknown` больше не применяется к
  операциям без `provider_id` — у них ещё есть рабочий путь «Продолжить платёж»,
  и их выходом владеет новый проход.

### 2.3 Продление: три попытки заранее и письмо (FR-014, FR-015, FR-016, FR-017, FR-018, SC-004)

- Окна попыток открываются за 72, 48 и 24 часа до окончания периода.
  Неопределённый исход предыдущего платежа блокирует новую попытку продления.
- Ключ идемпотентности получает номер попытки, но сохраняет признак периода,
  поэтому повторный запуск той же попытки не создаёт второй платёж.
- Отказ первых двух попыток не отзывает автопродление. Третий подтверждённый
  отказ отключает автопродление, но оплаченный доступ сохраняется до конца
  периода. Перевод на бесплатный тариф происходит только по окончании периода,
  если успешная оплата не продлила его.
- Новый вид письма сообщает о неудаче и о дате отключения доступа; в список
  разрешённых полей письма добавляется поле с датой.

### 2.4 Досрочная оплата и переход на год (FR-019, FR-020, FR-021, SC-001)

- `cabinet/web_routes/billing.py`: снимается запрет новой оплаты при активной
  подписке.
- `billing/entitlements.py`: срок нового периода считается от
  `max(сейчас, текущая дата окончания)`, поэтому остаток оплаченного периода
  сохраняется, а переход «месяц → год» продлевает подписку, а не обнуляет её.
- Повторное нажатие «оплатить» по-прежнему переиспользует существующую операцию
  по ключу идемпотентности.

### 2.5 Вход как регистрация (FR-001…FR-006, SC-001)

- `cabinet/web_routes/auth.py`: неизвестный адрес больше не получает отказ на
  шаге выдачи кода; подтверждение кода создаёт аккаунт
  (`allow_registration=True`).
- Отдельная страница регистрации сохраняется: на неё ведут реферальные ссылки, и
  её поведение защищено контрактами фич 058, 065, 066 и 159. Она перестаёт быть
  обязательным шагом.
- Копия на странице входа приводится в соответствие: уходит утверждение, что
  обычный вход не создаёт аккаунт, и совет искать отдельную ссылку.
- Ограничение частоты, срок жизни кода, привязка к браузеру и аудит не меняются.

### 2.6 Воспроизводимый каталог (FR-022, FR-023, FR-024, SC-005)

- Миграция `0093_billing_catalog_seed` вставляет одобренные строки каталога
  идемпотентно (`on conflict do nothing`) и не перезаписывает настроенные
  значения; откат удаляет только неизменённые сеяные строки.
- Кабинет и публичная страница используют общую проверку
  `matches_approved_public_catalog()`: обе цены, валюта, права тарифа и редакция
  оферты должны совпадать с одобренным каталогом.

### 2.7 Проверка защиты вебхука (FR-026)

- `infra/scripts/install-billing-webhook-edge.sh` после перезагрузки nginx
  отправляет секрет, записанный в конфигурацию границы, напрямую в приложение.
  Для специального уведомления без метаданных рабочего пространства требуется
  строго `503` хотя бы на одном из двух путей окружения: секрет принят,
  обработка уведомления отложена. Любой другой результат на обоих путях —
  провал с откатом. Для боевой приёмки дополнительно нужны фактическое
  окружение `production` и `secret_probe_production=503`; успех тестового пути
  этого не заменяет. Секрет передаётся через файл настроек curl и не попадает
  в список процессов.

### 2.8 Сквозной тест денежного пути (FR-025, SC-006)

- Новый файл `apps/server/tests/unit/test_billing_money_path_e2e.py` проходит
  путь «оформление → ответ провайдера → вебхук → сверка → выдача доступа» на
  настоящей базе и настоящем приложении; подменяется только транспорт ЮKassa
  (`YooKassaClient` уже принимает `transport`).
- Файл попадает в обычную проверку на pull request без правки сборочных
  скриптов: тесты в `tests/unit`, использующие общую фикстуру `client`,
  автоматически получают маркер `postgres` и ресурсную фазу быстрого прогона.

### 2.9 Встроенная цепочка оплаты macOS (FR-028, SC-008)

- `DesktopCabinetRoutePolicy.swift` оставляет первый внешний переход из
  биллинга только на точные домены ЮKassa. Банк и 3-D Secure разрешаются после
  этого только в рамках активной платёжной цепочки и по HTTPS.
- `EmbeddedCabinetWebView.swift` обрабатывает обычную навигацию, iframe и
  `target="_blank"` в том же `WKWebView`, без отдельного окна или второго
  приложения. Ошибка или завершение popup не оставляет навигацию в состоянии
  загрузки.
- Состояние цепочки очищается при её завершении, истечении 15 минут, отмене
  основного запроса, возврате `nil` из `WKWebView.load` и завершении процесса
  содержимого WebKit. После очистки следующий внешний переход блокируется до
  новой разрешённой передачи в ЮKassa.
- `DesktopCabinetPaymentNavigation.swift`, `DesktopCabinetBillingHandoffTests.swift`,
  `DesktopCabinetPaymentPopupTests.swift` и `DesktopCabinetRoutePolicyTests.swift`
  фиксируют эти переходы и отрицательные сценарии в focused macOS suite.

## 3. Изменяемые файлы

| Область | Файлы |
| --- | --- |
| Вход | `cabinet/web_routes/auth.py`, `cabinet/templates/cabinet/auth/login.html`, `tests/contract/test_account_routes.py`, `tests/integration/test_web_owner_session_context.py` |
| Связь платежа | `billing/provider_events.py`, `billing/reconciliation.py`, `billing/webhook_reconciliation.py`, `tests/unit/test_billing_observation.py` |
| Зависшие операции | `billing/maintenance.py`, `tests/unit/test_billing_maintenance.py` |
| Продление и досрочная оплата | `billing/renewal_charge.py`, `billing/entitlements.py`, `billing/notifications.py`, `cabinet/web_routes/billing.py` |
| Аварийная остановка | `config.py`, `billing/operations.py`, `readiness/checks.py`, `api/health.py`, `infra/env/rec.production.env.example`, `docs/runbooks/billing-launch.md` |
| Каталог | `db/migrations/versions/0093_billing_catalog_seed.py` |
| Граница вебхука | `infra/scripts/install-billing-webhook-edge.sh` |
| Сквозной тест | `apps/server/tests/unit/test_billing_money_path_e2e.py` |
| Встроенная оплата macOS | `apps/macos/RecApp/Sources/Cabinet/DesktopCabinetPaymentNavigation.swift`, `apps/macos/RecApp/Sources/Cabinet/DesktopCabinetRoutePolicy.swift`, `apps/macos/RecApp/Sources/Cabinet/EmbeddedCabinetWebView.swift`, `apps/macos/Shared/Tests/DesktopCabinetBillingHandoffTests.swift`, `apps/macos/Shared/Tests/DesktopCabinetPaymentPopupTests.swift`, `apps/macos/Shared/Tests/DesktopCabinetRoutePolicyTests.swift` |

## 4. Порядок и зависимости

1. Каталог (FR-022…FR-024) — от него зависят остальные проверки.
2. Вход как регистрация (FR-001…FR-006).
3. Связь платежа и выход из зависшего состояния (FR-010…FR-013, FR-027).
4. Продление и досрочная оплата (FR-014…FR-021).
5. Удаление аварийной остановки (FR-007, FR-008).
6. Граница вебхука (FR-026).
7. Сквозной тест (FR-025).
8. Встроенная цепочка оплаты macOS (FR-028).
9. Согласование боевых настроек работающих служб (FR-009), одобренное
   развёртывание и отдельная проверка живой оплатой.

## 5. Проверка

| Что | Как |
| --- | --- |
| Каталог на чистой базе | `alembic upgrade head`, чтение `billing_plan_versions`, повторный прогон и откат |
| Серверные тесты | `apps/server/scripts/run_local_postgres_tests.sh --full -q` |
| Полная местная диагностика | `infra/scripts/ci-local.sh --full`; не заменяет GitHub-проверки точного SHA |
| Денежный путь на PR | новый файл в `tests/unit` внутри быстрой ресурсной фазы |
| Встроенная оплата macOS | `cd apps/macos && swift test --filter DesktopCabinet`; переходы, popup и очистка цепочки проходят в focused suite |
| Отсутствие аварийной остановки | поиск по `apps/server/src`, `infra`, `apps/server/tests` пуст |
| Граница вебхука | `install-billing-webhook-edge.sh --dry-run`; `--execute` только после отдельного разрешения и с результатом `503` для боевого пути |
| Развёртывание | `infra/scripts/cd-remote.sh --dry-run` |
| Живая оплата | ручная оплата в боевом магазине владельцем продукта |

Чек-лист качества требований `checklists/release-readiness.md` принадлежит
рецензенту; создание файла и локальный коммит не закрывают его пункты.
Для слияния обязательны GitHub `governance-fast`, `macos-pr` и `pr-metadata`
на точном SHA и проверенной базе; для выпуска — `release-full` на замороженном
кандидате и отдельное разрешение согласно `docs/agent-guidance/release-and-validation.md`.

## 6. Риски и как их снимаем

- **Отмена операции, по которой всё же пришли деньги.** Отменяются только
  операции без сохранённого платежа с истёкшим ключом. Если провайдер позже
  подтвердит платёж, вебхук свяжет его по метаданным и доступ будет выдан.
- **Переименование исключения аварийной остановки.** Обработчики и разбор отказа
  обновляются в одном изменении, иначе штатный отказ превратится в ошибку 500.
- **Рассинхрон состояния готовности.** Ветка `emergency_stop` убирается из
  проверки готовности и из списка некритичных состояний одновременно, иначе
  `/ready` начнёт отдавать 503 и сломает развёртывание.
- **Ложная зелёность сквозного теста.** Подмена транспорта делается только через
  `monkeypatch`, чтобы не протекла в соседние тесты; проверяется и повторная
  доставка того же события.
- **Хрупкие тесты по тексту исходников.** Перед правкой денежных маршрутов
  прогоняются контрактные тесты, читающие исходники как текст.

## 7. Что вне области

- Реферальные начисления и промокоды: механизм уже работает и не меняется.
- Возвраты: граница возврата описана отдельным контрактом и не меняется.
- Остальные функции приложения macOS — запись, оболочка, разрешения и системные
  интеграции — в эту фичу не входят; встроенный путь оплаты входит и проверяется
  focused macOS suite и вручную владельцем продукта.
