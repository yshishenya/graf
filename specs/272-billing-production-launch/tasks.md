# Задачи: выход платных сервисов в боевой режим

**Фича**: 272-billing-production-launch
**Источник правды по реализации.** Отмечай `[X]` только после проверки.

Легенда: `[P]` — можно делать параллельно, `[US#]` — сценарий из spec.md.

## Этап 1. Каталог цен (FR-022, FR-023, FR-024, SC-005)

- [X] T001 Миграция `0093_billing_catalog_seed`: идемпотентная вставка двух
      одобренных строк каталога (`personal` месяц 100 000 копеек, год
      1 000 000 копеек) с редакцией оферты `personal-2026-08-21`.
- [X] T002 Проверка миграции на чистой базе: `upgrade head` даёт ровно две
      строки, повторный прогон не дублирует, `downgrade -1` удаляет только
      сеяные строки, публичная страница сообщает `catalog_ready = True`.
- [X] T003 [US6] Свести редакцию оферты в кабинете с публичной страницей, чтобы
      проверка каталога в кабинете не была слабее (FR-024).
- [X] T004 [US6] Тест-страж: литерал редакции оферты в миграции совпадает с
      `PUBLIC_APPROVED_OFFER_VERSION` (файл
      `apps/server/tests/contract/test_billing_catalog_seed_contract.py`).

## Этап 2. Вход как регистрация (FR-001…FR-006, US1, SC-001)

- [X] T005 [US1] Неизвестный адрес больше не получает отказ на шаге выдачи кода
      входа.
- [X] T006 [US1] Подтверждение кода входа создаёт аккаунт
      (`allow_registration=True`).
- [X] T007 [US1] Копия на странице входа: убраны утверждение «обычный вход не
      создаёт аккаунт» и совет искать отдельную ссылку регистрации.
- [X] T008 [US1] Отдельная страница регистрации сохранена по контрактам фич
      058, 065, 066 и 159 (на неё ведут реферальные ссылки).
- [X] T009 [US1] Тесты, фиксировавшие прежнее поведение, переписаны на новое:
      выдача кода новому адресу, создание аккаунта только после подтверждения,
      сохранение ограничения частоты.
- [ ] T010 [US1] Проверка входа на живой среде: новый адрес получает код и
      доходит до страницы оформления оплаты без обращения к оператору.

## Этап 3. Зависшие операции (FR-010…FR-013, FR-027, US3, SC-002, SC-003)

- [X] T011 [US3] `ProviderEvent` и `PaymentObservation` несут `operation_id` из
      метаданных платежа.
- [X] T012 [US3] `_locked_operation` связывает платёж с операцией по
      метаданным, когда поиск по `provider_id` не дал результата.
- [X] T013 [US3] Проход обслуживания отменяет операции без `provider_id` с
      истёкшим ключом идемпотентности и закрывает их счёт. При известном
      `provider_id` снимает блокировку через `observation_expired`, сохраняя
      позднюю сверку без бессрочного фонового опроса (FR-011).
- [X] T014 [US3] 30-минутный перевод в `unknown` больше не применяется к
      операциям без `provider_id`, поэтому кнопка «Продолжить платёж» живёт весь
      срок ключа.
- [X] T015 [US3] Тесты: связывание по метаданным, приоритет `provider_id`,
      отсутствие связывания без метаданных, отмена зависшей операции.
- [X] T016 [US3] Прогон сценариев сбоя: ни один сценарий не оставляет операцию,
      которую нельзя завершить без оператора (SC-002): проверено на настоящей
      базе в `apps/server/tests/integration/test_billing_stuck_operation_recovery.py`
      с негативным контролем на версии кода до исправления (2 падения из 4).

## Этап 4. Продление и досрочная оплата (FR-014…FR-021, US4, US5)

- [X] T017 [US4] Автопродление включено по умолчанию при первой покупке
      (FR-014): отдельного переключателя нет, согласие на автоплатёж —
      обязательная часть оформления покупки.
- [X] T018 [US4] Первая попытка списания за 72 часа до окончания периода, всего
      три окна за 72, 48 и 24 часа; неопределённый предыдущий платёж не
      разрешает второе списание с новым ключом (FR-015, FR-016).
- [X] T019 [US4] Ключ идемпотентности учитывает номер попытки и сохраняет
      признак периода.
- [X] T020 [US4] Первые два отказа сохраняют автопродление; третий отказ
      отключает автопродление, но не сокращает оплаченный доступ. Перевод
      на бесплатный тариф — только по окончании периода без успешного
      продления (FR-018).
- [X] T021 [US4] Новый вид письма: неудача списания и дата отключения доступа;
      поле с датой добавлено в список разрешённых (FR-017).
- [X] T022 [US5] Снят запрет новой оплаты при активной подписке (FR-019).
- [X] T023 [US5] Срок нового периода считается от текущей даты окончания, а не
      от даты платежа (FR-020).
- [X] T024 [US5] Тесты: досрочная оплата продлевает период, переход
      «месяц → год» сохраняет остаток, повторное нажатие не создаёт второй
      платёж (FR-021).
- [ ] T025 [US4] Проверка на живой среде: письмо о неудаче приходит, доступ
      отключается в дату окончания (SC-004).

## Этап 5. Аварийная остановка (FR-007, FR-008, SC-007)

- [X] T026 Поле `billing_emergency_stop` и все его чтения удалены.
- [X] T027 Исключение переименовано в `BillingCheckoutDisabled`, проверка
      выключенного флага оплаты сохранена во всех трёх денежных путях (FR-008);
      доказано дифференциальным прогоном: при выключенном флаге оформление
      возвращает 303 и не создаёт ни одного платежа у провайдера.
- [X] T028 Состояние готовности и список некритичных состояний обновлены
      одновременно, семантика `/ready` численно совпадает с прежней.
- [X] T029 Переменная удалена из примера окружения.
- [X] T030 Тесты и документация вычищены; поиск по коду, инфраструктуре и
      тестам не находит упоминаний (SC-007).

## Этап 6. Граница вебхука (FR-026)

- [X] T031 Установщик границы после перезагрузки nginx отправляет секрет
      границы напрямую в приложение и требует строго `503` хотя бы на одном
      пути окружения; любой другой результат на обоих путях — провал с откатом.
- [ ] T032 Проверка на сервере: `install-billing-webhook-edge.sh --execute`
      сообщает `secret_probe_production=503`, а работающие службы используют
      `production` / `1430118`; тестовый путь не заменяет боевую приёмку.

## Этап 7. Сквозной тест денежного пути (FR-025, SC-006)

- [X] T033 Файл `apps/server/tests/unit/test_billing_money_path_e2e.py`: путь
      «оформление → ответ провайдера → вебхук → сверка → выдача доступа» на
      настоящей базе, подменён только транспорт провайдера.
- [X] T034 Проверка идемпотентности: повтор вебхука и повтор оформления не
      создают второй платёж и вторую выдачу доступа.
- [X] T035 Подтвердить, что файл попадает в обычную проверку на pull request.

## Этап 8. Перевод на боевой магазин (FR-009, US2)

- [ ] T036 По отдельному решению владельца отключить автопродление тестовых
      подписок, способы оплаты которых принадлежат тестовому магазину,
      без сокращения уже оплаченного доступа (quickstart.md §3.3).
- [ ] T037 Подтвердить согласованные боевые окружение, магазин и секреты всех
      трёх работающих служб через одобренное развёртывание; устранить обнаруженное
      20 сентября расхождение наблюдения между файлом и службами. Уже прочитанные
      `production` / `1430118` сами по себе не закрывают задачу.
- [ ] T038 Зарегистрировать адрес вебхука боевого окружения в кабинете ЮKassa.
- [ ] T039 Установить границу вебхука и убедиться в совпадении секрета (T032).
- [X] T040 Публичная страница перестаёт обещать оплату, пока магазин тестовый
      (защита от показа цены без возможности заплатить).
- [ ] T041 Живая оплата владельцем продукта: реальный платёж проходит, доступ
      выдан, чек зарегистрирован.

## Этап 9. Закрытие

- [X] T042 Полная местная проверка: `run_local_postgres_tests.sh --full`,
      `ci-local.sh --full`, `cd-remote.sh --dry-run`. Это сохранённая локальная
      диагностика изменений поверх `77139df6afecdb827bb6d44ca70e4349e146d335`,
      а не GitHub-доказательство будущего SHA или готовности production.
- [ ] T043 Развёртывание одобренного кандидата после GitHub `governance-fast`,
      `macos-pr`, `pr-metadata` и `release-full` на соответствующих точных SHA;
      локальные прогоны не заменяют условия выпуска.
- [X] T044 Обновить `docs/runbooks/billing-launch.md`: убрать остановку, добавить
      провижининг каталога и шаг проверки совпадения секрета.
- [X] T045 Фрагмент changelog в `changes/unreleased/`.

## Этап 10. Уточнения после проверки пути пользователя

- [X] T046 Подтверждение банка, открываемое новым окном или ссылкой с
      `target="_blank"`, загружается в текущем представлении приложения, а не
      пропадает. Адрес проходит ту же проверку маршрута, что и обычный переход;
      вне оплаты внешние адреса по-прежнему не загружаются. Проверено двумя
      тестами, отрицательный контроль пройден.
- [X] T047 Кабинет называет днём следующего списания момент первой попытки
      (за 72 часа до конца периода), а не конец оплаченного периода. Иначе
      надпись обещала бы дату, в которую деньги уже списаны. Проверено сквозным
      тестом на настоящей базе, отрицательный контроль пройден.

## Этап 11. Безопасность перехода в оплату (FR-028, SC-008)

- [X] T048 Первый внешний переход из биллинга разрешает только точные домены
      ЮKassa; произвольные HTTPS-адреса до передачи провайдеру блокируются.
      После разрешённой передачи активная цепочка допускает банк/3-D Secure.
      Проверено в `DesktopCabinetRoutePolicyTests` (FR-028, SC-008).
- [X] T049 Для `target="_blank"` действуют те же ограничения: прямой переход
      из биллинга на банк или неизвестный домен блокируется, а переход
      `ЮKassa → банк` остаётся в текущем WKWebView. Просроченная 15-минутная
      цепочка блокирует следующий внешний переход до загрузки страницы; отказ,
      завершение popup, `nil`-загрузка, отмена и завершение процесса WebKit
      очищают состояние. Проверено в focused suite (FR-028, SC-008).

## Этап 12. Ревью требований перед выпуском

- [X] T050 Рецензент оценивает все пункты
      `checklists/release-readiness.md`. Генерация чек-листа и локальный коммит
      не закрывают ревью, подписи ответственных или живую приёмку.

## Phase 13: Convergence

Проверка 20 сентября 2026 года на
`7b2bc14a7b0dea01760bf2695b32208d76b121c1` выявила два воспроизводимых
пробела в продлении. Обычный набор `test_renewal_charge.py`,
`test_billing_observation.py` и `test_renewal_resolution.py` прошёл
(`45 passed`), но изолированные проверки ниже не прошли. Проверки использовали
только синтетические объекты в памяти, без обращения к провайдеру или production.
Это уточняет границы прежних отметок T018–T021, а не подтверждает готовность.

- [X] T051 [US4] HIGH: в
      `apps/server/src/twobrain_rec_server/billing/renewal_charge.py` разрешить
      следующую попытку после подтверждённой отмены предыдущего платежа,
      сохранив его `provider_id`, но продолжать блокировать новую попытку при
      неопределённом исходе. Добавить проверку в
      `apps/server/tests/unit/test_renewal_charge.py`: на границе −48 часов
      отменённая первая попытка с ID провайдера должна создать вторую;
      сейчас создаёт 0, тогда как без ID создаёт 1. Подтвердить предел трёх
      попыток и повторный запуск без дублирования по FR-015, FR-016,
      US4/AC2 и plan §2.3 (contradicts).
- [X] T052 [US4] HIGH: согласовать обработку подтверждённого отказа продления
      в `apps/server/src/twobrain_rec_server/billing/renewal_charge.py`,
      `apps/server/src/twobrain_rec_server/billing/webhook_reconciliation.py`
      и `apps/server/src/twobrain_rec_server/workflows/worker.py`, используя
      существующую очередь писем: HTTP-отказ, отмена из ответа провайдера,
      авторитетного GET и вебхука должны давать одно предупреждение на попытку,
      сохранять оплаченный доступ и применять правила первых двух/третьего
      отказа одинаково. Зафиксировать регрессию в
      `apps/server/tests/unit/test_billing_renewal_workflow.py` и
      `apps/server/tests/unit/test_billing_observation.py`: сейчас
      `run_billing_renewal_activity` при GET со статусом `canceled` отменяет
      операцию и счёт, но ставит в очередь 0 писем. Проверить повторную
      доставку без повторного письма по FR-017, FR-018, SC-004 и plan §2.3
      (partial).

## Этап 14. Сохранение доступности и защиты перед выпуском

После T050 и повторного analyze; не подменяет живые T010/T025/T041/T043.

- [X] T053 [US2] Проверить отрицательную матрицу FR-030/SC-010 на настоящем
      приложении и PostgreSQL: гость, не-владелец, чужая операция/пространство,
      неверный/отсутствующий CSRF, каждое пропущенное согласие, старая оферта.
      Дополнить существующие проверки в
      `apps/server/tests/unit/test_billing_money_path_e2e.py` и
      `apps/server/tests/integration/test_billing_rls.py` только недостающими
      сценариями; сохранить `tests/contract/test_billing_security.py`.
      Провайдер не вызывается, финансовые записи/права не создаются, чужие
      строки недоступны; evidence только синтетический или metadata-only.
- [ ] T054 [US2] Проверить FR-029/SC-009 по quickstart §2.1: семантика в
      `apps/server/tests/contract/test_billing_accessibility.py`, клавиатура,
      фокус, VoiceOver, темы/масштаб и синтетические возврат/ошибка в браузере
      и единственном `/Applications/GRAF Dev.app`. При дефекте исправлять
      существующие `cabinet/templates/cabinet/pages/billing_*` и обработку
      фокуса кабинета/`EmbeddedCabinetWebView.swift`, не подменять страницы
      провайдера. Записать отдельно автоматический, ручной и ещё не проведённый
      внешний прогон; T054 нельзя закрыть только контрактными тестами.

## Этап 15. Follow-up после ревью объединённого PR

- [X] T055 [US3] Сохранить единый порядок блокировок при фоновой сверке
      начального платежа: advisory lock workspace до `BillingOperation
      FOR UPDATE`; добавить регрессию на порядок и повторную выборку операции
      по FR-010–FR-013 и plan §2.1 (review P1, исправлено).
- [X] T056 [US4] При отсутствии сохранённого способа оплаты завершать текущую
      попытку как подтверждённо неуспешную с письмом и `attempt_failed`, не
      переводить её в `manual_resolution`, чтобы планировщик мог открыть
      следующее окно; ошибки ключа, версии и расшифровки оставить
      `manual_resolution` по FR-015–FR-018 и plan §2.3 (review P1, исправлено).
- [X] T057 Включить `observation_expired` в административную очередь
      неизвестных исходов и проверить численное значение метрики на PostgreSQL,
      чтобы закрытое состояние не исчезало из наблюдаемости по FR-011 и
      runbook §1 (review P2, исправлено).
