# 090 follow-up hotfix validation

Date: 2026-07-20

## Scope

- Strict production-smoke `run_id` validation before shell/path use.
- Per-run host receipts and container artifact/token paths.
- Row-level serialization for immutable media-revision acceptance.
- Direct stale-download, range/playback, and stream-reader failure coverage.
- Review follow-up: collision-resistant default run IDs, direct-child `/tmp`
  path enforcement without traversal or symlink parents, required container
  cleanup verification, and a disposable two-transaction lock receipt.

## Receipt

- Focused PostgreSQL suite: 49 passed, 2 warnings; the follow-up lock and
  immutable-fingerprint selection passed 2/2 in an isolated PostgreSQL run.
- Smoke boundary suite: 20 passed, 2 warnings.
- Ruff focused check: pass.
- Python compile: pass.
- Shell syntax and `git diff --check`: pass.
- Invalid run IDs fail closed; bounded valid IDs pass.
- No credential, media, transcript, summary, object key, or private path is
  included in this receipt.

## Remaining boundary

The external `test-rec` transcript/speaker/summary acceptance and browser
focus/AX/zoom runtime evidence remain separate open gates.
