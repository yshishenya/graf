# Проверки F256

Дата: 2026-09-08. Ветка `codex/256-krisp-playback-panel`.
Исходный HEAD: `e81b412dff920cc29ed37de60be13dd8fc4d1c43`.
Режим: `high-risk-product`, US1–US3. Изменения пока не закоммичены;
исходный HEAD не является SHA реализации. Ножницы и диапазоны отложены владельцем.

## Подготовка

- Независимая проверка требований: CRITICAL 0 / HIGH 0 / MEDIUM 0;
  reviewer-owned checklists: UX 5/5, security 5/5, infra 4/4.
- Общий `validate_issue_canon.py`: PASS, 276 Spec Kit issues.
- Все 12 задач привязаны к GitHub, [карта](../issue-map.md).
- До реализации: 14 focused playback/speaker checks PASS, 151 deselected.
  Новый сценарий реального аудио остановился на отсутствии меню пяти скоростей;
  два новых контракта панели также сначала падали, затем прошли после реализации.

## Подтверждённые проверки

| Проверка | Результат |
|---|---|
| Renderer, статические контракты и CSRF contract | 171 PASS |
| Сервер: комментарии, независимые границы доступа, share links, notifications, account merge, deletion | 73 PASS |
| Последующие исправления канонических источников и ролей | 15 PASS; отдельные четыре RLS/source проверки PASS |
| Существующие проверки переименования спикеров | 8 PASS |
| Итоговый PostgreSQL: comments, boundaries, browser HTTP, playback contract, CSRF | 36 PASS |
| Настоящий Chromium → HTTP → PostgreSQL | PASS: роли viewer/commenter/editor, момент/реплика, счётчик, сохранение, перезагрузка, ответ, реакция, редактирование, resolve/reopen/delete |
| Настоящий WebKit → HTTP → PostgreSQL | PASS: тот же сценарий, входит в итоговые 36 |
| Независимый строгий RLS: проекция внешнего адресата и историческое уведомление | Два отдельных сценария PASS |
| Chromium и WebKit: настоящий звук, геометрия, открытые меню 200%, resize мышью/клавиатурой | PASS: по 56 вариантов геометрии, все сценарии обоих surfaces |
| Comments UI: обычный/200% масштаб, форма и список | PASS в Chromium и WebKit |
| Ruff, node syntax, diff whitespace, changelog fragment | PASS |

Числа разных наборов пересекаются и не суммируются. Все PostgreSQL проверки
выполнялись в изолированных временных контейнерах с автоматической очисткой.
Браузерный HTTP-мост использует настоящие маршруты/renderer/базу и синтетическую
тестовую личность; он не доказывает вход через настоящий пользовательский сеанс.
CSRF проверен отдельно контрактом и интеграционными сценариями.

`playback-panel-runtime-check.cjs` воспроизводит настоящий синтетический WAV,
создаваемый в памяти: play/pause, пять скоростей, ±15, next/previous, один шаг
native range, Listen/перекрытия/паузы/конец, аватары, подсветка, collapse/resize,
ошибка play и сохранение audio после замены расшифровки. Матрица для каждого
движка: web/embedded × light/dark × 100/200% × семь ширин = 56 вариантов.
Проверены допуски геометрии основных элементов 2 px и seek 100 ms.
Браузерная имитация embedded не заменяет установленный GRAF Dev.

UI comments использует настоящие DOM/CSS и подмену HTTP для ошибок/повторов:
Unicode, безопасный plain text, сохранение draft/request_id, конфликты версии,
реакции, replies pagination, source filter, badge после замены DOM и прямую
ссылку. Полный aggregate проверяется на 53 обсуждениях при одном загруженном
корне. Дополнительная проверка охватывает форму/список при 200%.

## Исправления по независимой проверке

- Выбор принятой неизменяемой media revision переиспользован из текущего
  проигрывателя. Pending/rejected upload не скрывает действующие обсуждения.
- Упоминания внешней организации восстанавливают ограниченное доказательство
  доступа и повторно проходят ACL. HTTP не раскрывает адреса.
- Удаление корня очищает уведомления чужих ответов, включая упоминание,
  ранее удалённое редактированием. Producer INSERT/UPDATE не расширен.
- Якорь принимает реальный TranscriptSegment или DiarizationSegment только
  своей встречи/области/результата/медиа; несовместимость найдена реальным HTTP.
- Устранена гонка resolve → filter → reopen → delete; прежние GET не заменяют
  актуальные карточки. Подлинный 409 имеет явное обновление перед повтором.
- Права внешнего редактора проходят общий workspace query в share/rename UI.
- Ручка resize получила правильный порядок отображения поверх toolbar; реальная
  мышь теперь меняет высоту. Узкие открытые меню и форма комментария помещаются
  при 200%; высота списка ограничивается доступной областью окна.
- Убраны четыре ненужных ветви/аргумента по Ponytail review; новых зависимостей нет.

## Отдельные непройденные и ожидающие gates

- F205 Chromium/WebKit: две ошибки вне новой панели — `web-without-playback:
  bottomDelta=57`, `web-no-js-narrow: horizontalOverflow=51`. На исходных HEAD
  renderer/JS/CSS Chromium воспроизводит ровно те же ошибки; это установленный
  baseline, а не PASS всего F205. Случаи с проигрывателем проходят.
- `infra/scripts/ci-local.sh --fast`: штатный отказ `dirty_worktree`,
  `ci_evidence_status=ambiguous`; общая CI не исполнялась. Повторить на чистом
  SHA после разрешённого коммита.
- `check_spec_kit_governance.py`: BLOCKED отдельными, уже существовавшими
  изменениями Spec Kit skills/extension/workflow и несовпадением установленного
  specify v1.0.4 с lock v1.0.1. Эти чужие изменения сохранены. Для итогового
  gate нужен чистый checkout кандидата и соответствующая закреплённая среда.
- GRAF Dev: status подтвердил активный исходный `dev-e81b412dff92`,
  bundle ID `pro.2brain.graf.dev`. Новая реализация ещё не собрана/продвинута.
  Нужны разрешённый коммит, clean checkout, build/promote/status/smoke,
  установленное приложение, VoiceOver и отдельная проверка длинной записи.
- PR/exact-SHA governance-fast и Full CI не запускались. Production и публичный
  выпуск вне этапа. Issues не закрыты до завершения соответствующих gates.

Рабочие изменения других задач в `.agents/skills/` и `.specify/` сохранены.
Реальные встречи, записи и секреты в проверочных данных не используются.

## Сверка реализации и требований

Проверены 16 требований первого этапа (FR-001–011, FR-015–018, FR-020),
6 критериев успеха, 9 сценариев US1–US3, пять шагов архитектуры и шесть
конституционных границ из plan.md. Новых непокрытых работ по коду не выявлено.
Полная convergence/приёмка не объявляется завершённой: оставшиеся проверки
уже принадлежат T011/T012, включая чистый SHA, установленное приложение,
VoiceOver и длинную запись. Дублирующие задачи не добавлялись.
T001–T010 отмечены по evidence; T011/T012 открыты. Все issues остаются открыты
до проверки их критериев закрытия и связи с PR.

## Приёмка штатного Dev после разрешения владельца

Владелец явно разрешил отдельный коммит F256, общие проверки чистой копии и
обновление единственного GRAF Dev. Промежуточный SHA
`737b859f55f80118b6f989d34689a1cddc17a391` прошёл clean `ci-local --fast`:
296 governance, 1560 server unit, 111 changed server, 66 CI-contract tests.
По одному пропуску в governance и changed-server suites отражены в отчёте;
браузерный HTTP/DB тест запущен отдельно с установленным Playwright.
Feature preflight, lint, compile, whitespace и clean checkout — PASS.
Использован закреплённый Spec Kit v1.0.1 в изолированной среде.

Штатные build/promote/status/smoke промежуточного SHA — PASS, схема
`0090_playback_comments`; подпись, bundle ID и единый SHA компонентов сохранены.
На настоящем Dev через обычный вход Chromium/WebKit прошли по 15 проверок:
play/pause, пять скоростей, ±15, next/previous, Listen/паузы/конец,
collapse, полный цикл комментария и двухчасовая запись с 720 репликами.
Это синтетические данные, без обращения к MediaScribe; доказательством
распознавания через провайдера эти проверки не являются.

Computer Use в установленном GRAF Dev подтвердил play/pause, сдвиг15 секунд,
следующую реплику, скорость1.5, выбор спикера и остановку на конце его речи,
сворачивание, resize мышью и клавишей End, сохранение ответа и переименования.
Ответ, созданный в приложении, прочитан в браузере после перезагрузки.

Ручная проверка выявила и привела к исправлению трёх дефектов:
- при перекрытии речи комментарий выбирал первую активную реплику, игнорируя
  явно выбранный фрагмент; проигрыватель сохраняет source IDs выбранной реплики;
- обсуждение в узком embedded-окне перекрывалось боковой навигацией;
  ширина/отступ теперь учитывают закреплённую панель и узкую полосу навигации;
- после переименования доступные названия отдельных цветных фрагментов оставались
  прежними; обновляются вместе с остальными обозначениями без замены audio.

Добавлены проверки явного выбора перекрывающейся реплики, геометрии обсуждений
в настоящем embedded HTML при390/720px и переименования с сохранением audio.
PostgreSQL/HTTP browser check — PASS в Chromium и WebKit; static contracts —
167 PASS. Эти исправления требуют нового чистого SHA, общего прогона и
повторного штатного обновления Dev. Промежуточная приёмка не закрывает T012.

## Проверка владельцем и уточнение иконок

Владелец сообщил, что самостоятельно проверил VoiceOver: всё работает отлично.
VoiceOver gate принят по ручной проверке владельца установленного Dev; это
не результат автоматического Computer Use. Предыдущее ограничение инструмента
сохранено выше как исторический результат.

По замечанию владельца исправлена пара иконок перемотки15: окружность
центрирована в viewBox, направление второй стрелки зеркально отражено,
цифры построены векторными линиями и не зависят от системного шрифта.
Это визуальное уточнение T003/T010 внутри существующей F256; поведение не меняется.

Адресные playback/speaker suites:16 PASS. Chromium/WebKit: проверены обе темы,
обычные и увеличенные иконки; центр12/12 (допуск0.01), равная толщина2,
отсутствие зависимости цифр от шрифта — PASS. git diff --check — PASS.
Иконки пока проверены из рабочей копии; установленный Dev этим не обновлялся.

### Повторное уточнение перемотки вперёд

Владелец отклонил предыдущую боковую стрелку. В окончательном рабочем варианте
наконечник перенесён наверх окружности, между ним и цифрами оставлен просвет.
Парная стрелка назад зеркальна; число15 не отражается. Окружность/стрелка имеют
единый штрих2; цифры облегчены до1.35 для читаемости в20px. Это осознанная
оптическая разница между контуром и типографикой, заменяющая предыдущий вариант
с тяжёлыми цифрами. SVG целиком центрирован12/12, линии не обрезаются.
Повторные Chromium/WebKit (обе темы,20/56px): PASS. Адресные suites16 PASS.
Изменения остаются в рабочей копии, проверка установленного Dev этой правки
не заявляется. Поведение перемотки не изменено.

## Итог реализации и независимой проверки перед PR

Этот раздел уточняет исторические ограничения выше. Реализация
`4ca70eaac4a51c63c582c0dd0263a0be885cb7c0` прошла чистый fast за296 секунд:
296 governance PASS/1SKIP,1560 server unit PASS,111 changed-server PASS/1SKIP,
66 CI-contract PASS. Штатный Dev dev-4ca70eaac4a5 со схемой0090 прошёл
build/promote/status/smoke и отдельную приёмку установленного приложения.
Chromium и WebKit на реальном Dev: по16 функциональных сценариев PASS;
ошибка сетевой загрузки аудио в обоих движках PASS. Длинная запись7205сек,
720реплик: native переход119:22→119:32, play/pause; реальный seek60:02,
Right→60:17, Shift+Right→60:22. Это синтетические данные без MediaScribe.

Независимый reviewer f256_final_review проверил весь код первого этапа и
окончательные SVG: обязательных P0/P1/P2 нет. Отдельный повтор PostgreSQL
comments/boundaries:19 PASS,89.12сек. Проверены ACL/CSRF/RLS, роли, версии,
повторные запросы, уведомления, удаление/слияние и состояние проигрывателя.
Ponytail review: новых зависимостей и лишних слоёв нет; используются общий
HTML audio, существующие grants/уведомления и штатный PostgreSQL. Обязательных
упрощений нет. Проверки безопасности и доступности сохраняются.

Сверка текущей реализации:16 FR первого этапа,6 SC,9 сценариев US1–US3,
5 решений плана,6 конституционных границ. FR001–004→renderer/общий проигрыватель;
FR005–009→дорожки/Listen/навигация; FR010/011→comments/ACL/CSRF/RLS;
FR015→удаление обсуждений/встречи; FR016→ошибки/версии; FR017/018/020→общий
web/embedded UI, независимые SVG и доступные controls. SC001–006 покрываются
runtime/HTTP/PostgreSQL/Dev/owner VoiceOver и матрицей эталона с утверждёнными
исключениями. Недостающих работ по коду не найдено; новые задачи convergence
не добавлены, оставшиеся итоговые gates уже определены T011/T012.

Настоящая mergePlaybackIntervals выполнена для10000 перекрывающихся интервалов
(один результат),10000 раздельных интервалов и некорректных границ:PASS,16ms.
Это проверка алгоритма, а не утверждение о10000 DOM-элементах.
Окончательные SVG утверждены владельцем; обе темы,20/56px,Chromium/WebKit,
центр12/12±0.01, отсутствие обрезания:PASS; focused16 PASS.

Окончательный SHA после фиксации SVG требует своих CI и Dev результатов;
они будут привязаны в PR. Старые receipts не заменяют эту проверку.
VoiceOver принят по проверке владельца. Два baseline-дефекта F205 вне панели
сохранены. Первоначальная пустая область кабинета после promote требует
повторной проверки; причина не доказана. Full CI относится к будущему
замороженному кандидату выпуска, production/публичный выпуск вне текущей задачи.
