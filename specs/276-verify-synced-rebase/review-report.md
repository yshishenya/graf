# F276 — независимая проверка требований

Дата: 2026-09-24. Проверяющий: независимый агент требований.
Ветка: `276-verify-synced-rebase`.
Базовый HEAD при проверке: `a057e29f502dd09cb33118f329ece30d1ea2a40e`.
Документы F276 проверены в рабочем дереве; на момент проверки они ещё не
зафиксированы коммитом. Указанный HEAD не является SHA реализации F276.

## Решение и границы

**PASS первоначальной проверки требований и outline T001–T004.**
В `checklists/infra.md` подтверждены **6 пунктов, неподтверждённых 0**.
Блокирующих пробелов в рассмотренных требованиях нет. Неподтверждённые
пункты и причины отказа отсутствуют: основания для каждого маркера приведены ниже.

Разрешена генерация `tasks.md`. Это не разрешение начать реализацию:
после генерации обязательна независимая сверка задач до кода, затем соблюдение
остальной последовательности Spec Kit. Отсутствие `tasks.md` сейчас не дефект:
первичная проверка требований предшествует его генерации.

Режим: `significant-feature`, повышенный риск проверки происхождения изменений
для выпуска. Проверены требования и проектируемые ограничения, не работающий
алгоритм. Тесты, проверки GitHub и выпуск в рамках этого ревью не выполнялись.
Исторический пример из `research.md` рассматривается как описание воспроизводимого
случая, а не как независимо подтверждённый здесь результат общего валидатора.

## Прочитанные основания

- Полностью: `spec.md`, `plan.md`, `research.md`, `data-model.md`, `quickstart.md`,
  `checklists/infra.md`, `checklists/requirements.md` этой функции.
- Конституция `.specify/memory/constitution.md`, правила `AGENTS.md`, индекс
  руководств, `codex-worktrees.md`, `spec-kit-flow.md`, `tracker-policy.md`,
  `release-and-validation.md`, `product-gates.md`.
- Относящиеся к границам продукта и выпуска разделы
  `docs/prd-voice-layer-final.md` (§1/1A, §11, §34) и
  `docs/current-product-status.md` (F211 и F271).
- Ветка, `.specify/feature.json` и результат `check-prerequisites.sh --json
  --paths-only` согласованы с `specs/276-verify-synced-rebase/`.

## Основания для пунктов infra

| Пункт | Решение | Конкретное основание и граница вывода |
| --- | --- | --- |
| CHK001 | PASS | `spec.md`: Clarifications, US1, FR-001 и Out of Scope ограничивают новую форму линейными изменениями и одним завершающим слиянием синхронизации. `plan.md`: Architecture, шаги 1–4, задаёт двух родителей, точную базу, полный исходный диапазон и линейный итоговый диапазон. Минимум два переносимых изменения; одиночный перенос остаётся в прежней проверке squash. Произвольные графы не разрешены. |
| CHK002 | PASS | FR-002 и Edge Cases требуют полного состава, порядка и каждого промежуточного результата, включая подмену, исправленную следующим коммитом. Architecture, шаги 3–6, сопоставляет диапазоны, чистый результат синхронизации и каждый перенос по порядку; одного равенства конечных деревьев недостаточно. `data-model.md` сохраняет упорядоченные шаги и их tree SHA. Корректность реализации этой схемы ещё предстоит проверить кодом и отрицательными тестами. |
| CHK003 | PASS | US2, Edge Cases и FR-003 требуют отказа при неполноте, неоднозначности, конфликте, лишних изменениях и чужой базе, запрещают исключения по номеру PR и разрешающие флаги. Architecture, шаги 1–7, и Validation Plan включают неверный count/base, пропуск/добавление/перестановку, промежуточную подмену, ручные изменения sync, лишнее слияние и отсутствующие объекты. Ошибка Git или неподдержанная версия не превращается в разрешение. Восстановленная база затем обязательно связывается с доказательствами общего валидатора. |
| CHK004 | PASS | FR-004, SC-003, Edge Cases и Architecture сохраняют прежние squash/rebase/поддержанное merge-слияние и общие проверки источника, базы, попытки, актуальности, описания и артефактов CI. `data-model.md` не меняет форматы run/receipt/attempt/currentness. Validation Plan требует сохранить ожидания прежних потребителей; `quickstart.md` запрещает считать ошибку API или незавершённый CI успешным результатом. Это соответствует точной привязке SHA/базы и актуальности попыток в руководстве по выпуску. |
| CHK005 | PASS | FR-005 и SC-004 запрещают изменения веток, индекса, рабочих файлов, настроек защиты, удалённые записи и чтение записей встреч. Technical Context явно допускает только побочные недостижимые Git tree/blob objects от `merge-tree`, не изменение пользовательского рабочего состояния. Validation Plan отдельно требует проверить refs/index/files. Новые БД, зависимости, данные встреч и производственные операции отсутствуют в области работ. |
| CHK006 | PASS | SC-001–004 задают воспроизводимый положительный случай, общий валидатор для исторического PR, отказ во всех заданных отрицательных случаях, сохранение старых ожиданий и нулевые изменения защищённого состояния. Validation Plan и `quickstart.md` называют конкретные команды и случаи, включая начальный FAIL положительной регрессии. Release Gate отдельно сохраняет обязательные проверки точного SHA и один `release-full` неизменного кандидата; подпись, нотариализация и развёртывание не подменяются локальным результатом. |

## Конституция и outline

Конституционные требования V/VI соблюдены на уровне требований: правила
публичного распространения и проверки выпуска не ослаблены, новая форма истории
не получает обхода обязательного CI, выбран полный процесс Spec Kit с независимым
ревью. Изменений правил захвата, хранения, AI, удаления и пользовательского
интерфейса не заявлено; соответствующие продуктовые ограничения сохраняются.
Поправка конституции для указанной области не требуется.

Outline достаточен для генерации задач:

- T001 задаёт воспроизводимую положительную регрессию до исправления.
- T002 покрывает отрицательные сценарии US2; их ожидания нельзя ослаблять ради
  положительного результата T001.
- T003 ограничен описанной проверкой истории и не меняет политику CI.
- T004 включает потребителей, независимую проверку, сверку реализации с
  требованиями, принадлежащий функции changelog-фрагмент и доказательства точного SHA.
- `plan.md` прямо запрещает параллельные правки общего файла тестов.

На завершающей сверке `tasks.md` необходимо подтвердить конкретные зависимости
T001/T002 → T003 → T004, точные пути и одного владельца общего файла тестов.
Упоминание review в T004 не передаёт исполнителю право редактировать независимый
отчёт или маркеры checklist: это остаётся областью проверяющего по конституции.
После задач сохраняются обязательные analyze и связь с GitHub issues до кода;
параллельная подготовка связи с issues не заменяет эти шаги.

## Итоговая проверка и ограничения

Повторно сверены все шесть формулировок checklist с приведёнными основаниями.
Итог первоначального этапа: **checked 6 / unchecked 0 / blockers 0**.
Общий `checklists/requirements.md` содержит ещё 5 отмеченных пунктов, но не
редактировался и не включён в счётчик независимого infra review.

Изменения проверяющего ограничены `checklists/infra.md` и этим отчётом.
Код, спецификация, план, задачи, GitHub, коммиты и выпуск не изменялись.
PASS реализации, фактических CI-проверок и готовности к выпуску здесь не заявлен.

## Дополнение 2026-09-24 — завершающая сверка tasks.md до кода

**PASS требований с учётом сгенерированного `tasks.md`; блокеров ревью нет.**
Это завершает ранее отложенную сверку задач, а не проверку реализации.
Полностью прочитаны `tasks.md`, принятый `plan.md` и `quickstart.md`, повторно
сверены infra-маркеры. Форматирование строки `Classification: untouched` в
Legacy Impact не меняет согласованной классификации или требований.

- Зависимости явно заданы как T001 → T002 → T003 → T004. Положительная
  регрессия сначала должна завершиться FAIL; отрицательные сценарии добавляются
  до исправления. Общий файл `tests/governance/test_pr_metadata_event.py`
  принадлежит основному агенту, параллельные правки запрещены.
- T001 покрывает FR-001/FR-005 и SC-001/SC-004: допустимую историю, движение
  API base и сохранность refs/index/files. T002 покрывает FR-002/FR-003 и
  SC-002: состав, порядок, промежуточную подмену, базу, конфликты и неполную
  историю; прежние ожидания сохраняются.
- T003 ограничен `scripts/validate-pr-metadata.py`, FR-001–005 и прежним
  пределом count 1..1000. Хотя метка задачи — US1, текст явно охватывает
  защитные требования US2; пробела покрытия здесь нет.
- T004 через конкретный `quickstart.md` включает прежние проверки и общий
  валидатор, SC-001–004, независимое ревью реализации и converge. Записи
  исполнителя ограничены `validation.md` и `changes/unreleased/F276.yaml`;
  владение независимым отчётом и checklist остаётся у проверяющего.
- Обязательные проверки точного SHA до слияния и отдельный `release-full`
  до выпуска сохранены. Пройденные локальные тесты их не заменяют.

Итог после повторного чтения: **infra checked 6 / unchecked 0 / blockers 0**.
В `tasks.md` все четыре задачи пока открыты — это ожидаемое состояние до кода,
а не незакрытые пункты требований. Независимое условие допуска по требованиям
выполнено; перед реализацией основной агент отдельно завершает analyze и
связь задач с GitHub issues. Их фактическое выполнение этим отчётом не подтверждается.

В этой сверке изменён только данный отчёт; задачи, маркеры checklist, код и
GitHub не изменялись, коммиты не создавались.

## Дополнение 2026-09-24 — уточнение требований после C1

Уточнение FR-004 до «прежние однозначные формы» и отказ при неоднозначности
согласуются с US2 и конституционным запретом обхода доказательств. Оно не
разрешает отменить проверки CI или принять неправильное число переносов.
CHK004 подтверждён именно в этой уточнённой области совместимости, а не как
обещание принимать всякий ранее проходивший неоднозначный граф.

Однако новый абзац Architecture описывает защиту лишь при точном совпадении
границы count-1 со вторым родителем sync. Независимое воспроизведение показало:
при том же правильном source count=4, но двух либо четырёх итоговых переносах
вместо трёх и сохранённом постороннем файле общий squash-путь выдаёт PASS.
Различие длины диапазона не доказывает, что это однозначное продвижение target,
а не пропуск/добавление переноса. Подробности — дополнение C1 в
`implementation-review.md`.

**CHK003 открыт повторно: checked 5 / unchecked 1 / blockers 1.**
Причина изменения маркера теперь не только ошибка реализации: обновлённый plan
явно закрепляет неполный критерий отказа, конфликтующий с FR-003 и отрицательными
случаями dropped/extra. Принцип уточнения FR-004 принят, но доказательство
достаточности описанного выбора ветки отсутствует.

Для закрытия требуется согласованный критерий, исключающий неоднозначные
переносы до разрешающего squash-пути, и проверки сочетаний retained-extra
с dropped/extra. Поддержка действительно доказанных прежних форм сохраняется;
новые продуктовые возможности или обходы CI не требуются. Данный результат
заменяет прежний 6/6 только для обновлённых документов после уточнения C1.

## Дополнение 2026-09-24 — строгий проект устранения C1 до кода

**PASS требований и проекта; CHK003 закрыт повторно. Актуальный итог:
checked 6 / unchecked 0 / blockers требований 0.** Прочитаны обновлённые
spec/plan, задачи, quickstart, research, data-model и infra checklist.
Код и тесты не изменены относительно предыдущей проверки: их SHA-256 остаются
`bdb27f1f0d124068f97850f2eb61a91d4b89ed0b284cca7ed39f27df28e6b555` и
`4d37a7267adeb2b4aabe2029fdb78026365f6c3b64343cd4eaf52ef281b95207`.

Основание CHK003: новый Architecture безусловно исключает aggregate-squash
при несовпадении конечных деревьев и нескольких родителях source head.
Количество итоговых переносов больше не служит условием доверия. Тем самым
на уровне проекта закрыты исходный retained-extra и его сочетания с dropped
и extra: ни одно из них не может перейти в разрешающую ветку по изменению длины.
Проверка равных деревьев остаётся пошаговой, а не только итоговой.

CHK004 подтверждён с явным ограничением совместимости из Clarifications и
FR-004: aggregate-only squash синхронизированного head с отличающимся деревом
теперь намеренно не поддерживается. Это не заявляется сохранённым поведением;
предусмотрены синхронизация ветки и повторная проверка вместо обхода.
Фраза Edge Cases о сохранении прежних форм читается в рамках этой прямо
названной оговорки, а не как отменяющая её. Advanced-target squash линейного
source, прежние однозначные формы и все доказательства CI остаются обязательными.
Такое консервативное ограничение явно разрешено пользователем и согласуется
с US2/FR-003; поправка конституции не требуется.

Новые задачи не требуются: отрицательные сочетания и осознанный отказ для
синхронизированного advanced-target squash относятся к T002; замена защиты —
к T003; повторные проверки и независимое ревью — к T004. Положительный тест
синхронизированного advanced-target squash был добавлен в рамках F276 и меняется
в соответствии с согласованным ограничением; ожидания тестов, существовавших
до F276, менять нельзя. Владение файлами и зависимости задач сохраняются.

Разрешён переход к описанному исправлению. Это **не PASS текущего кода**:
он пока содержит ранее воспроизведённый обход. C1 реализации закрывается
только после изменения, проверок retained-extra/dropped-extra/extra-extra,
подтверждения сохранённых положительных случаев и независимой сверки версии.
