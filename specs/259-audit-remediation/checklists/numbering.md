# Требования восстановления последовательности

Дата:2026-09-09. Владелец отметок: независимый рецензент. Scope: US3,
FR-023–FR-026, SC-006/007; не завершение общего бэклога и не проверка кода.

- [x] Определено, почему F6788 не задаёт начало, но сохраняет занятость и историю.
- [x] Обычные F1000+ и отсутствие политики не меняют прежний контракт.
- [x] Формат политики, ошибки и запрет молчаливого fallback однозначны.
- [x] Все входы сохраняют одну политику, блокировку, повторный выбор и отказ при неполных данных.
- [x] Новая работа не доверяет старому spec_next и не переключает чужой checkout.
- [x] Задачи/проверки покрывают требования, границы commit/CI/release и новый этап процесса разделены.

## Независимая проверка требований — 2026-09-09

Рецензент: Codex, отдельный агент `graf_requirements_review`, не автор
реализации. Основа рабочей копии: `495cefab3d4a712a3e34bf922d315cc243817de8`,
ветка `codex/259-sequence-policy`. Вердикт: **PASS, 6/6; CRITICAL 0, HIGH 0**
для требований текущего ремонта B019, а не для реализации или всего F259.

Основания по порядку пунктов:

1. [spec.md](../spec.md), FR-023/SC-006, и [research.md](../research.md):
   F6788 — явно согласованное историческое исключение только из начала
   последовательности; все источники занятости и история сохраняются.
2. [contracts/allocator.md](../contracts/allocator.md): точная формула начала,
   пустое множество, отсутствие политики, допустимость F1000+ и пропуск
   занятого исключённого ID не зависят от размера числа.
3. [spec.md](../spec.md), FR-024, [plan.md](../plan.md),
   [data-model.md](../data-model.md), [quickstart.md](../quickstart.md):
   однополевой формат, повтор ключа, неизвестный ключ, дубликат ID,
   bool/string/zero/negative и ошибка чтения имеют явный отказ без подмены.
4. FR-014–FR-017/FR-025 и контракт: общий выбор предложения и allocation,
   повтор под общей блокировкой, отказы при неполных источниках сохраняются.
   Read-only относится к предложению без `--feature-id`/`--allocate` и dry-run;
   explicit claim сохраняет свои записи, занятость, повтор и upgrade draft.
   Общая блокировка относится к рабочим копиям с общим git-каталогом;
   новая распределённая гарантия для независимых клонов не заявляется.
5. FR-026 и план: свежая проверенная основа, отсутствие доверия к старому
   `spec_next`, повторная проверка предложения; чужие рабочие копии не меняются.
6. [tasks.md](../tasks.md), T014–T017 → T006, и quickstart: регрессии до
   реализации, отдельные checklist/analyze/issue-sync gates, ограниченные
   проверки и явные оставшиеся commit/CI/merge/release условия. Следующее
   улучшение контекста и тестового процесса не включено в ремонт.

Устранены при проверке: противоречие FR-025 с действующим записывающим режимом
`--feature-id` и неоднозначность повторённого JSON-ключа. Прочитаны реальные
ветви `main`/`claim`/`_next_feature_id`, потребители в трёх оболочках и
существующие сценарии конкуренции/занятости; исполнение кода не заявляется.

Отметки не закрывают исторический [requirements.md](requirements.md), задачи
реализации, 279 исходных issues, T006 или их приёмку. Перед кодом сохраняются
все применимые high-risk gates, включая независимые открытые пункты других
списков и актуальный analyze. Продуктовые тесты, Full CI, GitHub mutations,
коммиты и выпуск этим рецензентом не выполнялись.
