# Проверка готовности PR F259 к слиянию

Дата: 2026-09-08. Проверяющий: Codex, авторская проверка в отдельном этапе review; независимое одобрение человека или другого агента не заявляется. Правила sole-owner repository допускают записанный owner/agent review при required approval count 0. Проверено через GitHub: обязательный governance-fast, strict актуальность основной ветки, конфликтов нет.

## Границы приёмки

Принимается ограниченный этап US3 / FR-012–FR-022: allocator, оболочки, проверка завершения issues и воспроизводимость обновления bootstrap. Бэклог FR-001–FR-011 и продуктовые US1/US2 — план последующих работ. Незавершённая приёмка исходного F225 и общий reviewer-owned requirements.md не объявляются завершёнными и не используются как доказательство реализации. Состояние существующего checklist не изменяется.

## Проверка кода и требований

- FR-012/013: последовательность начинается после specs; реальные ветки и claims остаются занятыми, служебные refs и timestamp не поднимают номер. Числа больше 999 допустимы. Проверены все входы claim/suggestion/allocation и три оболочки.
- FR-014/015: повторная проверка под общей блокировкой, отказ при устаревшем предложении, ошибке GitHub и неполной выдаче. Чистота ветки проверяется до записи; тест конкуренции допускает только одно резервирование.
- FR-016/017/018: предложение номера делегировано локальному allocator, generic режим сохранён. Метка создаётся до umbrella и не перезаписывается. Действующие номера, чужие изменения и разрешения выпуска сохранены.
- FR-019/020: полный граф workflow и отдельный closeout после приёмки. Новый live режим использует существующую verify_feature_runs; проверяет repository, merged PR, связь issue, workflow/event и точный SHA. Валидатор не изменяет GitHub.
- FR-021/022: локальный [X] не заменяет приёмку; незавершённые merge/release/manual критерии и ограничения GitHub сохраняют открытый остаток. Положительные и отрицательные сценарии проверены тестами. Полностью закрываемых исходных issues в PR нет.
- Ponytail review: дополнительных зависимостей, сервисов или общих абстракций не введено; переиспользованы действующие механизмы выбора номера, проверки GitHub и миграции известных вариантов текста.

## Проверка обновления существующего GRAF

Исходник bootstrap из PR #39, SHA ab6da355e77eb59810ce4bfc7e7998c9ca1b7e1f. На отдельном клоне GRAF ec06ef80a578c1c1624c2fdf50d60eeb7edd8ddd выполнен весь штатный bootstrap с CLI 1.0.1 и issue-canon 0.3.2, затем повторный bootstrap и doctor --frozen. Глобальный CLI и продукт не менялись; Ponytail явно отключён только в тестовом клоне.

В source исправлены переходы двух известных ранних инструкций коммита; сохранены два точных проверенных GRAF файла, уже исключивших выбор фичи по времени. Неизвестные варианты/изменённые файлы по-прежнему блокируются. Обновление сохраняет существующий context_file и отключённые документальные автокоммиты; after_implement остаётся отключённым. Дополнительные изменения стандартных шаблонов bootstrap остались в изолированном клоне и не внесены в GRAF PR.

Результат: первый и повторный запуск PASS; сравнение всех файлов .agents/.specify после повторного запуска — без изменений; frozen doctor PASS. Сохранены настройки проекта, оба этапа allocator (suggestion и reservation), полный workflow и closeout mode. Governance-тесты после обновления: 323 passed, 3 skipped, 41.75 s. Bootstrap unit: 31/31, Bash syntax и ShellCheck PASS. Пропуски — PowerShell без pwsh и отдельный Docker restore rehearsal без manifest.

## Вердикт

Блокирующих замечаний в ограниченном объёме PR не осталось. Рекомендация: ready for review / merge после зелёных обязательных проверок на окончательных SHA обоих PR. Слияние, релиз, применение bootstrap к основной рабочей копии и закрытие общего бэклога не выполнялись.
