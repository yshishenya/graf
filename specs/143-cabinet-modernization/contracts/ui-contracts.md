# UI Contracts

- We must not break any existing `cabinet-*` class names as they are tested by `test_cabinet_template_components.py`.
- Hover/focus states must be visually distinct but maintain the accessibility requirements.
