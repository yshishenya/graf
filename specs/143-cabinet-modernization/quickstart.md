# Quickstart: Modernize Web Cabinet UI

1. Run `uv run pytest tests/contract/test_cabinet_template_components.py` to ensure UI contracts are maintained.
2. Run `infra/scripts/ci-local.sh --fast` to verify build integrity.
